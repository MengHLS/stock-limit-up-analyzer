# 首页预览交互验收记录

本次使用独立Chromium实例访问开发预览首页，并通过真实浏览器调试协议执行控件点击，结果保存在 `preview-interaction-acceptance.json`。

| 操作 | 点击结果 | 页面行为 |
|---|---|---|
| 月份快速跳转 | 成功，初始 `2026-08` 切换为 `2024-12` | 下拉选择值发生变化，日历月份状态更新 |
| 当日首板 | 成功，按钮进入激活状态 | 股票列表从 `79` 只变为 `62` 只，说明筛选实际生效 |
| 按时间 | 成功，按钮文字变为 `时间↑` | 排序方向状态发生变化 |
| 创业板板块筛选 | 成功，按钮进入激活状态 | 股票列表从 `62` 只变为 `5` 只，说明筛选实际生效 |

初始页面同时验证了日期 `2026-08-18`、当日总量 `79`、历史日均 `72`、累计记录 `11888`、板块联动摘要、CSV导出按钮、编辑和删除图标均已渲染。由于本次首板判定已通过真实点击并改变列表数量，首页关键交互验收完成。


## 按日加载回归验收

在未登录状态下真实输入“金牛化工”，搜索结果区域出现且包含股票名称；随后清空搜索并点击日历上的 `2026-08-17`，页面保持在首页，没有跳转登录页，也没有进入错误边界。日期标题更新为 `2026-08-17 题材统计`，日历显示 `106` 条，列表标题更新为 `涨停股票 106 只`。本次回归还验证了月份快速跳转仍可用，详见 `search-date-interaction-acceptance.json`。
