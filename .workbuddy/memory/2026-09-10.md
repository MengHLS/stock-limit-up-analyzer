# 2026-09-10 工作日志

## STEP DATASET-001 — Dataset Registry + 独立物理表架构 + 全局命名规范（READY，全链路真实 TiDB 验证）

按规格将「数据集功能」从 `research_datasets` 内容指纹快照架构重构为 **Dataset Registry + 独立物理表 + 全局命名规范** 三层，作为新层落地、既有稳定模块零破坏。

### 落地内容
- **Migration** `drizzle/0028_dataset_registry.sql`：6 表（`dataset_definition`/`dataset_version`/`dataset_build_job` + `ds_first_limit_pullback_event/path/outcome`），数值列用 `double`，`lastCursor` 用 LONGTEXT。
- **幂等 applier** `scripts/applyDatasetRegistry.mjs`：真实 TiDB 已执行（17 语句、0 错误、6 唯一约束全在）。
- **领域层** `server/datasetRegistry/`：`naming`（ds_{code}_{role} 规范+校验）、`types`、`registry`（三实体生命周期+Service）、`detection`（复用 boardRules + PIT ST）、`path`、`builder`（两阶段：events 逐日 keyset+批量 IN 富集；paths 30 日分块范围下推+批插）、`db`（onDuplicateKeyUpdate 幂等）。
- **构建 CLI** `scripts/runDataset001Build.mts`（支持 --resume/--list）+ **校验 CLI** `scripts/verifyDataset001.mjs`。

### 验证结果
- `tsc --noEmit` exit 0；`vitest run server/datasetRegistry` **33/33 全过**。
- 真实构建（v2，2024 全年）：**10,240 事件 / 206,408 路径 / 30,720 结果 / 237,128 行，1381.6s（171.6 rows/s）**。
- 数据质量全 PASS：唯一性 0 重复、版本隔离、交易日历（周五→周一）、反策略绑定 0 列、涨停价 sanity。

### 关键 bug 修复（真实 DB 落地中暴露）
1. 手写 migration 注释块未按 `--> statement-breakpoint` 分隔，TiDB 禁 multi-statement → applier 改「剥注释 + 按分号切」。
2. `lastCursor` 原 TEXT(64KB) 被增长中的 checkpoint JSON（cumulative 涨停历史）撑爆 `ER_DATA_TOO_LONG` → 改 LONGTEXT。

### 性能优化（smoke 基线 4.6 rows/s → 171.6 rows/s，~37×）
- Phase 1 逐事件 fetchLiquidity/fetchIndustry → 批量 IN 查询。
- Phase 2 逐事件 fetchSymbolBars → 30 交易日分块 fetchBarsRange（消除 ~95% 重叠拉取）。

产出：`docs/STEP_DATASET_001_FINAL_REPORT.md`（A–M 节，状态 READY）。ROADMAP §47 append + §44.1 快照覆盖式更新。

---

## STEP DATASET-002 — Dataset 全量产品化审计（READY_FOR_IMPLEMENTATION，纯审计不改代码）

产出：`docs/step-dataset-002/DATASET-002-AUDIT-FINAL.md`（A–R 节）。

### 核心结论
- 项目并存两套语义不同、互不连通的 Dataset 架构：**旧 `server/researchDataset/`（STEP 12.6 宽行 PIT panel，内容指纹 version）** 全链路活跃（router `researchDataset.*` + 前端 `DatasetBuilder.tsx` + Research 消费 `research/datasetAccess/`→`signalEngine`）；**新 `server/datasetRegistry/`（DATASET-001 事件物理表，逻辑 version）** 已完成领域+DB+构建（真实 TiDB 237,128 行）但**零 API/零前端/零 Research 消费**。
- 两个「DatasetVersion」概念互斥：旧=内容指纹 `rd-1.0.0-1-<sha>`，新=逻辑字符串 `v1/v2`（(datasetId,version) 唯一 + DRAFT→BUILDING→READY→FAILED）。
- `first_limit_pullback` 逻辑两份实现：旧 `pullback.ts`+`tDayFilter.ts`（宽行 signal 窄化）vs 新 `detection.ts`+`path.ts`（event 物理表），都复用 `boardRules`。
- 唯一事实来源方案：事件/路径/结果=新 Registry（ds_*）；PIT panel+认证 gate=旧 ResearchDataset（research_datasets）；二者是上下游而非替代。

### 迁移路线（低→高风险）
002.1 审计固化（本步）→ 002.2 契约统一（新增 datasetRegistry router）→ 002.3 前端 MVP → 002.4 Build/Quality UI → 002.5 口径收敛（tDayFilter/pullback → detection/path）+ eventAccess→Research → 002.6 旧代码清理 → 002.7 E2E 验收。

---

## STEP DATASET-002.2 — Dataset Registry API / Shared Contract（COMPLETE）

为新 Dataset Registry 补齐 API 层。产出 `docs/step-dataset-002.2-report.md`。

### 落地
- **新增** `shared/datasetRegistryContracts.ts`（枚举字面量 + zod 入参 schema + wire 类型）、`server/datasetRegistry/query.ts`（keyset cursor 编解码纯函数 + `DatasetDataReader` 接口 + InMemory + `DbDatasetDataReader` + `DatasetQueryService`）、`server/datasetRegistry/router.ts`（`buildDatasetRegistryRouter` 工厂 + 默认实例）、3 个测试文件（32 用例）、`scripts/verifyDataset0022.mts`。
- **修改** `registry.ts`（Service 补 `updateDefinition`/`archiveDefinition`）、`index.ts`（export query/router）、`routers.ts`（注册 `datasetRegistry`，`researchDataset` 不动）。

### 关键设计
- **keyset 排序键**：Event `(tradeDate,eventId)`、Path `(eventId,relativeDay)`、Outcome `(eventId,horizon)`，各由唯一键保证全序（同天多 event 不重不漏）；cursor = base64url(JSON)，前端不透明；`limit+1` 探测 hasMore；无 OFFSET。
- **BigInt 安全**：沿用 `bigint(mode:"number")` + `safeIntId`（.max(MAX_SAFE_INTEGER)）防御。
- **Statistics**：4 并行 SQL 聚合（COUNT/MIN/MAX/DISTINCT），零全量加载；declared vs actual 分两层。

### 验证
- `tsc --noEmit` exit 0；新增 32 用例全过；相关既有 131 用例全过（researchContracts + datasetRegistry + researchDataset）。
- 全量 vitest：2443 passed / 16 failed（全为既有环境依赖失败：tushare token/网络、marketData 等，与本次无关）。
- **真实 TiDB 只读验证**（v2 id=90001）：Definition 1、Version smoke/v1/v2 全 READY、Statistics event=10240/path=206408/outcome=30720、Event 52 页 walk=10240 match/unique/ordered=true、Outcome 154 页 walk=30720 同上、Path 抽样 600 条 ordered=true；总 ~53.7s。

### 关键经验（跨会话复用）
- **跨境 TiDB 冷连接可达 ~2min**（暖连接 ~7ms），单次 `npx tsx` 脚本都付一次连接成本；分页单页查询仅 ~250ms。验证脚本应单进程内完成全部查询。
- **path 有序判定勿用字符串拼接 key 比较**（`relativeDay` 数字跨 9→10 时 `#10 < #9` 字符串序假阴性），用 `comparePathKeys` 数值比较。
- 全量 walk 用 reader 直连（绕开 tRPC 序列化），单发端点（definition/statistics）用 caller 证明 router 全链路。

### 遗留（后续步骤）
Quality service + 口径统一 + Version 状态机守卫 + Build 触发 API 化 → 002.4；前端消费 → 002.3；Research 消费 → 002.5。

---

## STEP DATASET-002.2 — Dataset Registry 只读契约/查询/路由层（CODE_READY，002.3 后端前置）

- 新增 `shared/datasetRegistryContracts.ts`（DTO + zod 契约）、`server/datasetRegistry/query.ts`（keyset 分页查询 + 统计聚合 + DatasetQueryService）、`server/datasetRegistry/router.ts`（只读 8 endpoint：listDefinitions/getDefinition/listVersions/getVersion/listJobs/getStatistics/listEvents/listPaths/listOutcomes），挂载 `server/routers.ts` `datasetRegistry` 节点。
- 纪律：keyset cursor 原样回传、禁 OFFSET、禁全量加载。56 例单测全过、tsc 干净。

## STEP DATASET-002.3 — Dataset Registry Frontend MVP（COMPLETE）

- 前端 4 页（`/datasets` 系列，wouter 路由）+ 5 组件（ListTable/VersionListTable/BuildJobTable/StatisticsGrid/PreviewTable）+ `datasetRegistryAdapter.ts` + `datasetPreviewState.ts` keyset 游标栈状态机（NEXT 压栈/PREV 弹栈/RESET 清残留，limit 20/50/100）。
- 旧 `/dataset-builder` 与 `researchDataset.*` 完全不动；复用 DataTable/StatusBadge/Skeleton/EmptyState/ErrorState/TechnicalDetails/Tabs/MetricCard 零重复组件；`status.ts` STATUS_TONE 增 6 映射。
- 23 前端单测全过；真实 API 验证（verifyDatasetRegistryRead.mts 走真实 TiDB）`first_limit_pullback`/v2：eventCount=10240/pathCount=206408/outcomeCount=30720/rowCount=247368/horizons=[5,10,20]；tsc + vite build 通过。
- 产出 `docs/step-dataset-002.3-report.md`（15 节）。状态 COMPLETE，不自行启动 002.4。

## STEP DATASET-002.4A — Dataset Registry Build Lifecycle & State Machine（COMPLETE）

- 新增 `server/datasetRegistry/lifecycle.ts`（纯函数状态机唯一来源）：VERSION_TRANSITIONS（DRAFT→BUILDING→READY；BUILDING→FAILED；FAILED/READY→BUILDING 重建，**不臆造 schema 未定义的 CANCELLED/ACTIVE/ARCHIVED**）、JOB_TRANSITIONS（5 态 terminal 无出边）、assert 函数、computeBuildProgress、DatasetLifecycleError 6 稳定错误码。
- 重构 `registry.ts` DatasetRegistryService：补 createJob（PENDING）/cancelJob/retryJob（历史保留），修正 startJob（§六 6 项校验）/completeJob/failJob/markBuilding/markReady/markFailed（加守卫）。
- 并发安全（最小保护）：repo 增 `transitionJob`（条件 UPDATE 原子转换）+ `getRunningJobForVersion`（版本级 RUNNING 守卫）。
- API：router 增 4 admin mutation（create/start/cancel/retry BuildJob），领域错误→TRPC 稳定映射；completeJob/failJob 不暴露 client（防伪完成）。
- 验证：tsc exit 0；target tests 112 全过；build PASS；真实 TiDB 22 步全 ✅（v2 只读 READY 未动 + 临时版本全生命周期 + 验证后清理无孤儿）；全量 vitest 2498 过/16 失败（均既有环境失败，零新增）。
- 关键决策：startJob 不自动改版本状态（版本→BUILDING 由构建服务显式 markBuilding，与作业状态机解耦）；cancelJob 取消 RUNNING 时版本 BUILDING→FAILED 解除卡态。
- 产出 `docs/step-dataset-002.4a-report.md`（14 节）。状态 COMPLETE，不自行启动 002.5。

## WORK DATASET-002.4B — 数据集构建整合（旧 /dataset-builder → 新 /datasets，COMPLETE）

- 用户决策：**完整执行构建**（服务端真跑 builder 写 ds_* 表）+ **删除旧页面并重定向**。
- 新增 `server/datasetRegistry/runner.ts`（DefaultDatasetBuildRunner）：让 RUNNING 作业真实执行（异步不阻塞、running Map 幂等、真实 totalChunks、chunk checkpoint 落库、协作式取消、失败落 job+version FAILED）。**这是 002.4A 缺口的关键补齐**——之前 startJob 之后没有真正构建。
- 后端：contracts + DATASET_VERSION_LABEL_PATTERN / DATASET_BUILD_CONFIG_DEFAULTS+LIMITS / createDatasetVersionInputSchema；naming + validateVersionLabel；lifecycle + resolveBuildConfig + 4 错误码；registry + createVersionWithBuildConfig；router + createDatasetVersion(admin) + buildRunner/buildIO 注入。
- 前端：新增 BuildVersionDialog（一键建版本+构建）、VersionBuildControls；BuildJobTable + onCancel/onRetry；VersionDetail 构建中 3s 轮询；DatasetDetail 加「构建新版本」入口。
- 旧簇删除：pages/DatasetBuilder.tsx + components/dataset/(10 文件) + adapters/datasetAdapter.ts + buildResultAdapter.ts(.test)；App.tsx 旧路由 Redirect → /datasets；AppShell 导航更名「数据集构建」。**legacy researchDataset 后端保留**；**migration 0028 零改动**。
- 关键教训：临时版本标签必须 ≤32 字符（标签正则 `/^[A-Za-z0-9][A-Za-z0-9._-]{0,31}$/`），验证脚本首次因 33 字符标签被正确拦截。
- 真实 TiDB 端到端 21/21 ✅（真实构建 98/280/196 行落库 + 真实取消 + retry 历史保留 + v2 零变化 + 清理无孤儿）；tsc exit 0；target tests 149 全过；build PASS（2966 模块）；全量 2518 过 / 16 既有环境失败（零新增）。
- 产出 `docs/step-dataset-002.4b-report.md`（14 节）。不自行启动 DATASET-002.5。

## WORK DATASET-003A — 多数据集架构 + 版本/数据集删除能力（COMPLETE，真实 TiDB 44/44）

- 用户需求：支持多个数据集（各自多版本）；补删除版本、删除数据集。澄清两条口径：**① 多数据集 = 每个数据集独立表结构与构建逻辑（拒绝模板复用）；② 删版本 = 删数据保留表结构；删数据集 = 删表结构 + 数据。**
- 插件化架构（核心，`server/datasetRegistry/plugins.ts`）：`DatasetPluginRegistry`（datasetCode→插件）+ `DatasetPlugin{physicalTables, createIO, createBuilder}` + `firstLimitPullbackPlugin`（DDL **自包含**，不用 `CREATE TABLE ... LIKE` 模板表，否则删表后无法重建）+ `resolvePluginTables`。**核心零硬编码 datasetCode**；未注册 → `BUILDER_NOT_REGISTERED`。
- 物理表层（`physicalTables.ts`）：`DatasetPhysicalStore` + `DbDatasetPhysicalStore`（ensureTables/purgeVersionRows(分批 DELETE LIMIT 5000)/dropTables(DROP IF EXISTS + SHOW TABLES 复核)）+ `InMemoryDatasetPhysicalStore` 测试替身 + `assertSafeTableName`（反解一致，防表名注入）+ `resolveDefinitionTables`。
- 删除顺序：删版本 守卫→清数据→删作业→删版本记录；删数据集 守卫→逐版本级联→DROP 表→删定义。守卫 = 存在 RUNNING 作业则 `VERSION_HAS_RUNNING_JOB` / `DEFINITION_HAS_RUNNING_JOB`。
- **零新增表、零 migration/schema.ts 改动**：复用 DATASET-001 三实体；一个数据集 = 一组固定物理表，多版本靠 datasetVersionId 隔离（**禁止一版一表**）。
- 关键设计：`runner.ts` 插件解析放在 `markBuilding` **之后**（保持「DRAFT→FAILED 不合法」状态机纪律，执行失败可落 FAILED 可重试）。
- 新增 4 端点：`listDatasetPlugins`(query) / `createDatasetDefinition` / `deleteDatasetVersion` / `deleteDatasetDefinition`（admin，删数据集必须回传 `confirmDatasetCode`）。
- 前端：`CreateDatasetDialog`（+`validateDatasetCodeInput`）/ `DeleteDatasetVersionDialog`（勾选确认）/ `DeleteDatasetDialog`（**手工输入 code** 确认 + `isDeleteDatasetConfirmed`）；`DatasetDetail` 构建能力门禁（buildable=false 隐藏构建入口 + 琥珀说明条）；`VersionListTable` 行级删版本；`DatasetListTable` 显示「待实现」徽标。
- 验证：tsc exit 0；目标套件 **15 文件 190/190 全过**；build PASS（2969 模块）；**真实 TiDB `scripts/verifyDataset003a.mts` 44/44 ✅ exit 0**（第二数据集 3 表真实建立 / 多版本各自真实构建（探针插件自有构建器写自有表，first_limit_pullback 表零写入）/ 删版本保留表结构且 v2 不变 / RUNNING 守卫双拒绝 / 删数据集 DROP 3 表 + 同 code 可重建再 DROP / 既有数据集零变化 / 零残留）。
- 教训：验证脚本断言版本 READY 时必须**同时等 job COMPLETED 且 version READY**——runner 收尾是 `completeJob → markReady` 两次跨境往返，只等 COMPLETED 会撞上 BUILDING 的时序竞态（首次运行 1 项假失败，已修正）。
- 产出 `docs/step-dataset-003a-report.md`（10 节）。不自行启动 DATASET-003B（第二生产数据集插件）。

## STEP DATASET-003B — 构建筛选能力完整化（板块 / ST / 事件相对日 / 前后窗口）COMPLETE

- 用户需求：构建新版本弹窗筛选过少 → 需支持 t 日/t-1 日等事件维度、板块选择、是否排除 ST、t 日前后数据天数范围；**配置完成才允许构建**；并要说明配置保存方案与落点评估。
- 用户拍板四口径：**板块=交易所板块**（主板/创业板/科创板/北交所，与 `classifyBoard` 同源，拒绝申万行业）；**事件维度=相对日×事件类型**（两控件组合，多行 OR 叠加）；**保存位置=新建独立配置表**（明确拒绝「版本级 JSON 列」）；**生效范围=全链路真实生效**。
- 两层筛选模型：Universe 层（boards / excludeSt）+ Signal 层（events: 相对日×事件类型 / preWindowDays / postWindowDays）+ 执行参数（outcomeHorizons / batchSize）。
- **配置三表**（`dataset_build_config` 主表，与版本 1:1 UNIQUE，标量入列可索引；`dataset_build_config_event` 多值相对日×事件类型；`dataset_build_config_board` 多值板块）。migration `drizzle/0029_dataset_build_config.sql` + `scripts/applyDatasetBuildConfig.mjs`。理由：标量入列可索引、多值入子表有唯一键约束，**拒绝纯 JSON 一把梭**。
- 语义权威层：新增 `server/datasetRegistry/filter.ts`（`BUILD_FILTER_DEFAULTS`/`BUILD_FILTER_LIMITS`/`matchesEventSpec`/`eventSpecKey`/`isBoardAllowed`/`isStExcluded`/`maxLookbackDays`）+ 前端纯逻辑层 `client/src/components/datasetRegistry/datasetFilterForm.ts`（`validateFilterForm` 即构建门禁）。相对日锚点 `relativeDay ≤ 0`（0=t 日、-1=t-1 日），**契约层+纯函数层双层禁止正锚点**（反未来泄漏）。
- 构建门禁：非法筛选 → `INVALID_BUILD_FILTER`（不静默夹取）；checkpoint 与筛选口径不匹配 → `CHECKPOINT_INCOMPATIBLE`（不静默续跑错误数据）。三级回退链：`dataset_build_config` 行 → `version.filterDefinition`(legacy 镜像) → 权威默认值。
- 🔴 **发现并修复涨停判定系统性漏判**：旧逻辑用未四舍五入的 `preClose*(1+ratio)` 浮点乘积比较，真实 DB 4,325 个封板样本漏判 1,645 = **38.03%**。新增权威函数 `exchangeLimitUpPrice(prevClose, ratio) = Math.round(prevClose*(1+ratio)*100)/100`（四舍五入到分），`isLimitUpClose` 与事件行事实列 `limitUpPrice` **统一走它**。**既有 ds_* 数据集须重建**。
- 左边界预热（warm-up）：构建窗口左边界需有界回扫 `max(|负锚点相对日|, 1)` 个交易日填入 `limitUpDays`，否则连板被误判首板（T-3 预热深度已有专门用例锁定）。
- 关键重构：`limitUpDays: Map<symbol, number[]>`（升序涨停交易日序号，二分查找）取代旧 `prevLimitUp + cumulative` 双标量；`tradingDayIndex` 基于完整交易日历，构建范围用 `[windowStartIdx, windowEndIdx]` 表达（索引基准与构建窗口解耦）；负锚点下须**逐 bar 全量判定**（事件日 T 当天可能不涨停，不能 `if (!isLimitUp) continue`）。
- 测试：`filter.test.ts` 20 例 + `datasetFilterForm.test.ts` 26 例 + builder/detection 回归组；目标套件 **17 文件 252 tests 全过**（基线 193 → 252）；`npx tsc --noEmit` exit 0；`npm run build` PASS。
- 真实 TiDB 端到端 `scripts/verifyDataset003b.mts`（10 阶段，生产同源装配）：板块筛选 / 排除 ST / T-1 日首板 / OR 叠加 / preWindowDays 前置行 / preWindowDays=0 六类口径**真实改变出数**，且逐事件按生产口径回查 **0 违规**。
- 脚本形态变更遗留修复：`runDataset001Build.mts` 不再手工拼装配置，改为 `service.resolveBuildConfigForVersion(version.id)` 解析已固化配置（否则 `--path-horizon` 改名 `--post` 后会**静默产出 0 条 path**）；verifyDataset0024a/0024b 的 `createVersion` 补 `filter` 参数。
- 用户即时提问两则已解决：①「已完成构建为何后台还在跑」→ 目标版本 job COMPLETED ≠ 验证脚本整体结束（串行 5 个版本 + 逐事件回查 + 级联删除 + 零残留）；②「path/outcome 数据一直重复」→ 真实 DB 三层取证（L1 表内重复 0 / L2 跨版本同源正确 / L3 行数关系严格成立），**不是重复写入**，观感来自临时验证版本与 v1/v2 窗口重叠且 v1 窗口是 v2 子集。
- 并行发现（未修复，属独立改造）：**执行器终态非原子**——`completeJob` → `markReady` 两次独立写库，中间窗口可观测「job=COMPLETED 而 version=BUILDING」。
- 产出 `docs/step-dataset-003b-report.md`（10 节）+ ROADMAP.md §44.1 备注。
- 教训：验证脚本必须加 `withRetry`（跨境 TiDB 只读瞬时 ECONNRESET）+ `emergencyCleanup`（`unhandledRejection`/`uncaughtException` 钩子）+ `safeDeleteVersion`（先 `cancelJob` 再删，否则撞 `VERSION_HAS_RUNNING_JOB`）；临时 datasetCode 勿以数字结尾（`INVALID_DATASET_CODE`）。

## WORK DATABASE-DESIGN-001 — 数据库设计文档 + 事件窗口四表划分方案（文档交付，改造待评审）

- 用户需求两则：① 认为数据集「还是缺两张表」，应有一张存 t 日及之前、一张存 t 日之后，不该只有 path/outcome；② 需要一份数据库设计 MD 文档，解释现在数据库所有表及字段。
- **实查基线（真实 TiDB 只读探针 `scripts/_schema_probe.mts` → `_schema_probe_out.json`）**：**39 张表 / 422 字段 / 185 索引项**。
- **重大修正（推翻我上一轮的部分结论）**：跨表确实存在 **t 日重复**——`ds_*_path` 的 `relativeDay=0` 行与 `ds_*_event` 的 t 日行情是同一份数据，行数**完全 1:1**（smoke 151=151 / v1 516=516 / v2 10,240=10,240 / 合计 10,907）。上一轮只证明了"表内不重复"，未指出跨表重复。同时实测三版本 `negRows` 全为 0，即 `preWindowDays` 的负相对日**尚未落库**（只在临时验证版本出现过）。
- **用户拍板三口径**：① 表划分 = **严格互补零重复**（event 只存事件身份、去掉逐日 OHLCV；行情行只存在于 prefix(≤0) 与 path(≥1)）；② 命名 = **`prefix` / `path`**（保留 path 表名，只收紧 relativeDay 语义为 ≥1）；③ 本轮范围 = **只出设计文档，改造待评审**（不动代码）。
- **交付物**：`docs/DATABASE_DESIGN.md`（811 行 / 6 节 + 2 附录）。内容 = 7 层分层总览 + 39 张表逐字段说明（类型/键/默认值/中文说明）+ 横向设计约定（命名/PIT/状态枚举/数值类型选择/身份与复权）+ **目标设计「事件窗口四表划分」** + 迁移影响分析 + 已知问题清单。
- **四表目标设计核心论证**：prefix 与 path 的分界落在 **t 日**，而这个分界恰好是「可用于特征（后视，PIT 安全）」与「仅可用于标签（前视）」的 **PIT 边界**。拆表后「误用未来信息」从"需要人工判断"变成"表选错就查不到"——这是该方案最大价值。定义 8 条不变量（I1~I8），其中 I8 = prefix 表**不含** `*FromEventClose`/`isBreakout` 列（结构级 PIT 防线）。
- **关键实现约束（改造时必踩的坑）**：`EventReference`（`eventClose`/`eventHigh`/`eventVolume`）当前取自 `event` 表行（`builder.ts` 的 `buildEventPathsAndOutcomes`）；event 删 8 个行情列后**必须改道从 `prefix(relativeDay=0)` 取**，否则 path/outcome 全部衍生列会算空。
- 迁移面：新增 `ds_*_prefix` 表 + `DatasetRole` 增 `prefix` + `dataset_definition` 增 `prefixTableName` 列（migration `0030`）；`ds_*_outcome` 完全不受影响；物理表操作层按插件 `physicalTables` 声明驱动，**天然覆盖新表、无需硬编码**。建议与 P1 涨停口径修复**一次性重建**，不做 ALTER 渐进迁移。
- 文档内记录 6 项已知问题：P1 涨停漏判 38.03%（代码已修，数据待重建）/ P2 跨表 t 日重复 / P3 `securityId` 普遍 NULL / P4 `industry_assignments.effectiveFrom` 单点 / P5 执行器终态非原子 / P6 双套数据集体系并存。

## WORK DATABASE-DESIGN-002 — path/outcome 血缘实证 + 列级冗余发现 + 五表方案（文档更新，决策待定）

- 用户追问：「path 与 outcome 是不是强相关？path 不是原生日线吧？是不是需要再加一张表存 t 日之后的原始交易数据？」
- **血缘实证（`server/datasetRegistry/path.ts#buildOutcomeRow`）**：`outcome(H) = f(path.filter(1 ≤ relativeDay ≤ H))` —— 是**函数依赖**，不是并列。实测 v1（id=30001）逐条比对 **1,509 条**：精确相等 1,506 + 双方同为 NULL 3 = **100% 可重算**，**max_abs_diff = 0**。3 条 NULL 样本（`300799.SZ@2024-01-23`、`600647.SH@2024-01-11` H=5/10）是窗口内无行情，双方一致属诚实不伪造。**方向性**：outcome 可由 path 重算，反之不可（极值聚合有损）。
- **`path` 非原生数据确认**：24 列 = 6 身份 + **7 原始** + **10 衍生** + 1 时间戳；10 个衍生列全部是 `ref=(eventClose,eventHigh,eventVolume)` 的函数 → 前视。
- **🔴 新发现：path 表列级冗余（用户"重复感"的真实来源之一）**——`_coldup_probe.mts` 实测：
  - `returnFromEventClose` ≡ `closeFromEventClose`：**213,536 / 213,536 行完全相同**（源码同一表达式 `ratioTo(bar.close, ref.eventClose)`）
  - `lowFromEventClose` ≡ `pullbackFromEventClose`：**213,536 / 213,536 行完全相同**
  - `path.turnover`：**213,536 行全 NULL（死列）**
  - `breakoutPrice`(= eventHigh) 与 `daysToBreakout` 为**每行重复存储的常数**
  - → 名义 24 列，**实际只有 19 个不同的量**。
- **🔴 新发现 P7：上游市值数据从未落库**——`liquidity_daily` 9,015,158 行中 `totalMarketCap` / `circulationMarketCap` **全 NULL**（`turnoverRate` 99.14% 正常）。下游 `ds_*_event.marketCap` / `floatMarketCap` 因此全 10,907 行为 NULL。**任何依赖市值/流通盘的特征与筛选均不可用**。需查 BaoStock 回填链路为何未写市值列。
- `event.industryCode` 全 10,907 行为 NULL —— 是 P4（`industry_assignments.effectiveFrom` 单点 2026-08-31 无法覆盖 2024 交易日）的**下游后果**，非独立缺陷。
- **方案升级（待用户决策）**：识别出**第二个正交切分维度 = 数据层级（原始事实 vs 派生指标）**。推荐 **五表三层血缘**：`event`(身份) + `prefix`(原始 ≤0) + **`post`(原始 ≥1，新增)** → `path`(衍生 ≥1) → `outcome`(聚合)。收益：血缘可证 / 衍生列可随时重算（不动昂贵的原始层）/ PIT 双重隔离 / 顺带清理冗余列。代价：每数据集表数 3→5、完整视图需 join（但**拆列不拆行，行数不翻倍**）。
- 文档更新：`docs/DATABASE_DESIGN.md` 39 行 → **923 行**，新增 §4.6（血缘实证）、§4.7（第二维度与五表方案）、附录 C（证据脚本清单）；§2.4.1/§2.4.2/§2.1.4 标注死列与重复列；§6 新增 P7/P8；§4.4 不变量扩到 I11；§4.5 差异对照改为四表/五表两列并列。
- 新增只读探针：`_coldup_probe.mts`（列级冗余 + t 日同价）/ `_lineage_probe.mts`（死列 + 血缘重算）/ `_lineage_probe2.mts`（差异归因）/ `_liq_probe.mts`（上游市值覆盖）。

### DATABASE-DESIGN-002 决策落定（2026-09-10 19:00）

- 用户三项拍板：**① 采纳五表方案**（原始/衍生分层）；**② 命名 `prefix` / `post` / `path`**（保留 path 名，下游改动最小）；**③ 清理范围 = 仅删除 2 对完全重复的列**（`returnFromEventClose` 与 `pullbackFromEventClose` 各删一个）。
- **用户未选择清理的遗留项（已在文档 §4.3.4 显式记录以免遗忘）**：`turnover` 死列随原始列移入 `prefix`/`post`（仍全 NULL）；`breakoutPrice`/`daysToBreakout` 仍为每行重复常数（更合理位置是 `event`）；P7 上游市值回填链路**未排查**。
- **文档定稿**：`docs/DATABASE_DESIGN.md` 923 → **987 行**。§4 改题「事件窗口五表分层（✅ 已定稿，尚未实施）」；§4.1 补问题 ④（列级冗余）/ ⑤（outcome 与 path 平级存放）；§4.2 改为五表 + 血缘 ASCII 图 + 两条核心论证；§4.3 重写为 4.3.1 event / 4.3.2 prefix(14 列) / **4.3.3 post(13 列，新增，与 prefix 同构可用同一 DDL 生成器)** / 4.3.4 path(15 列，仅衍生，已删 2 重复列) / 4.3.5 outcome(10 列，取数改从 post)；§4.4 不变量 I1/I4 扩到三表；§4.5 差异对照改为「现状 vs 已定稿目标」两列；§4.7 改为「设计取舍记录（为何选五表）」含三方案对比表；§5 新增 **§5.4 建议实施顺序 S1~S5**；附录 B 记录 39 → 41。
- **实施顺序（待授权，S1~S5）**：S1 naming/plugins 增 role + DDL + migration 0030 → S2 types/path/builder 拆分与 EventReference 改道 → S3 db/query/router/契约/前端适配 → **S4 一次性重建 smoke/v1/v2（同时收敛 P1 涨停口径）+ 不变量 I1~I11 断言** → S5 可选宽视图 + P7 排查。
- **状态**：文档定稿；代码与数据库结构**均未改动**；等待用户授权启动 S1。

## WORK DATABASE-DESIGN-003 — EventReference 改道方案定稿（文档修准，代码零改动）

- 用户追问：「第一个致命坑（`EventReference`）有什么解决办法吗」
- **核查结论：文档原表述「改从 `prefix(relativeDay=0)` 回读」是过度设计**。实查代码：ref 来自 `builder.ts:184` 用**内存中的 `event` 行对象**拼（`event.close/high/volume`），**根本没有回读表**。而 D0 行情原本就在 `relativeBars`（`buildRelativeBarsFull` 产出，`relativeDay=0`，`builder.ts:222-232`）→ **零 DB 往返**即可改道。
- **根因**：`FirstLimitPullbackEvent` 承担**双重身份** —— ① 落库行（event 表列）② 计算上下文（供 path/outcome 的 ref）。拆表只削 ①，② 被连带破坏。
- **解法**：新增纯函数 `eventReferenceFrom(bars)`（从 `relativeBars` 找 `rd=0`），`buildEventPathsAndOutcomes` 内部改用它。调用点（`builder.ts:597`）与 `path.ts` 的 `buildPathRows`/`buildOutcomeRows`/`buildPathRow` 签名**均不变**。
- **三条硬约束**：**C1** 只从 `relativeBars` 取（单源，保证与 prefix 行同源；禁从 `DailyBar` 取，否则将来 prefix 加复权/富集会分裂）；**C2** `EventReference.eventClose/eventHigh` 放宽 `number|null`、禁 `?? 0` 兜底（连带 `firstBreakoutRelativeDay` 的 `eventHigh` 参数需同步放宽并在 null 时短路）；**C3** D0 无行情则跳过事件（当前会产全 NULL 行；**会减行数，待用户确认**）。
- **风险等级 = 编译期可见**：删 event 行情字段后 `builder.ts:185-187` 立刻 tsc 报错，不可能被漏掉。
- **列归属定案（顺带修正文档内部矛盾）**：event **保留** `previousClose`/`limitUpPrice`/`turnover`/`marketCap`/`floatMarketCap`。关键理由：`previousClose` 若下移到 `prefix` 的 `rd=−1` 行，`preWindowDays=0` 时该行不存在 → `limitUpPrice` 不可回溯（违 I11）。
- **文档修正**：`docs/DATABASE_DESIGN.md` 987 → **1065 行**。发现并修正 4 处数字/表述不一致：event 表实际 **24 列**（原写 16）→ 目标 **18 列**（24−6，**非**"删 8 列"）；prefix 14 → **13 列**；post 字段表误含 `turnover`（去掉后恰 13）；§4.3.1 note 与字段表自相矛盾（称 previousClose 移除，但字段表保留了依赖它的 limitUpPrice）已统一。新增 **§5.3 关键实现细节：`EventReference` 改道（S2 必读）**；§5.4 影响面小结改为「唯一待确认 = C3」；§5.5 实施顺序 S2 指向 §5.3；I3 不变量改为显式列名断言（明确允许 `previousClose` 保留）；第 365 行交叉引用 `§5.1` → `§6 P2`。
- **状态**：文档定稿；**代码与数据库结构仍零改动**；等 S1 授权。

## WORK DATABASE-DESIGN-004 — relativeBars 概念澄清 + turnover 死列根因实证（文档修正，代码零改动）

- 用户连续追问术语：`D0` → `relativeBars`。已用两张时间轴图解释。
- **术语澄清**：`D0` = Day 0 = **事件日本身** = `t 日` = `relativeDay 0`。代码注释习惯写 `D0`/`D+1`（`path.ts:5,11,31`），文档正文写 `t 日` —— **两种写法完全同义**，是我造成的表述混用。铁律：按**交易日历**推进（周五 D0 → 下周一 D+1），非自然日。
- **`relativeBars` 定义**：**内存数组**（`RelativeBar[]`，`path.ts:19-29`），**不是 DB 表**。由 `buildRelativeBarsFull()`（`builder.ts:202`）从**完整交易日历**切出 `[eventIdx - preWindowDays, eventIdx + postWindowDays]` 一段，每天一个元素（`relativeDay`/`tradeDate`/OHLCV/amount/turnover）。
- **三个关键设计**：① `relativeDay = idx - eventIdx`（相对日 = **索引差**，`builder.ts:223`）；② 缺失日 `bar?.x ?? null` **占位不跳行**（保相对日连续，`builder.ts:225-231`）；③ 越界 `Math.max(0, ...)` / `Math.min(len-1, ...)` **夹取不臆造**（不往前编历史、不越界取未来，`builder.ts:217-218`）。
- **它在五表改造中的地位**：它是 `prefix`/`post`/`path`/`outcome` 的**唯一原料** —— 拆表就是按 `relativeDay` 切片（`prefix = filter(rd ≤ 0)`、`post = filter(rd ≥ 1)`）。也是 `EventReference` 改道的来源（`rd = 0` 行即 D0 行情）。
- **🔴 修正我上一轮的事实错误**：`event.turnover` 实测 **10,561 / 10,907 = 96.83% 有值**（来自 `liquidity_daily.turnoverRate`），**不是"上游缺失"**。真正的死列是 `path.turnover`（**0 / 213,536 全 NULL**），**根因 = `buildRelativeBarsFull` 硬编码 `turnover: null`**（`builder.ts:231`，注释"当前 null，诚实不伪造"）—— **从未尝试填充**，与上游数据无关。
- **文档修正 3 处**：§2.4.2 补死列根因、§4.3.1 改为实测覆盖率、§5.3 明确区分「`event` 的富集 turnover」与「`path` 的死列 turnover」（此前自相矛盾）。新增探针 `scripts/_turnover_probe.mts`。
- **推论**：若未来要让换手率进入特征窗口（`prefix`/`post`），**必须先接通 `buildRelativeBarsFull` 的富集链路**，否则加列仍为空（与 P7 同类问题）。
- 状态：文档 1065 行；**代码与数据库结构仍零改动**。

## WORK DATABASE-DESIGN-005 — 问题存在性裁定 + 最终设计定稿 + 附录 D 完整 DDL

- 用户问：「所以上面的问题到底存不存在，给出最终设计」——要对前几轮反复讨论的议题做**一次性裁定**并收敛设计。
- **新增只读探针 `scripts/_d0_probe.mts`**（D0 完整性，C3 决策依据）实测：
  - 三持久版本 `relativeDay=0` 行的 `close`/`open`/`high`/`low`/`volume`/`amount` **100% 非 NULL**（516/151/10240 全对）。
  - **孤儿事件（有 event 无 rd=0 行）= 0**。
  - ⚠️ **`MIN(relativeDay)=0`、`neg_rows=0`** —— 负相对日**从未落库**（三版走 legacy 默认 `preWindowDays=0`）。
- **🔴 因此修正文档 §4.1 问题 ②**：原文把"t 日之前行情被写入 path（负 rd）"写成现存事实，实为**设计缺陷、现存数据未触发**。已加实查校正段：临时验证版本曾产出 3,605 行负相对日，**一旦以非零 `preWindowDays` 建版本即触发**，故仍须在改造中解决。
- **C3 结论翻转**：从「会减行数、需确认」→ **「零代价防御，已采纳」**（无孤儿事件 → 不减少任何现存行）。§5.3 表格 + §5.4 影响面小结同步改写；**至此无遗留待决策项**。
- **新增 §4.0「问题存在性裁定」**：8 项议题三分类（存在于数据 / 存在于设计 / 已证伪），逐项附实测依据。核心：**1/3/4/5 真实值得改造；2 设计缺陷待触发；6 编译期可见（非静默）；7/8 我先前表述不准已证伪**。
- **新增附录 D「目标结构完整 DDL（S1 可直接执行）」**：D.1 event(18) / D.2 prefix(13) / D.3 post(13, 与 prefix 严格同构) / D.4 path(15) / D.5 outcome(10) / D.6 `dataset_definition` 增 `prefixTableName`+`postTableName`(migration 0030) / D.7 列数对照速查（合计 58 → 69 列）。DDL 风格与 `plugins.ts` 生成器一致（`ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_bin`），显式标注**必须自包含**（禁 `LIKE 模板表`）。
- **文档定稿：`docs/DATABASE_DESIGN.md` 1083 → 1244 行**；§4 标题改为「✅ 已定稿，无遗留待决策项，待授权实施」；口径声明、附录 B/C 同步（附录 C 补 `_turnover_probe.mts` / `_d0_probe.mts`）；P2 状态改为「方案已定稿，待 S4 落地」。
- **状态**：文档与设计**定稿**；**代码与数据库结构仍零改动**；等 S1 授权（或 S4 一次性重建授权）。
- 教训：**"设计缺陷"与"数据事实"必须分开表述** —— 我上一轮把 path 负相对日的**设计风险**说成了**现存污染**，本轮实查纠正。裁定表三分类（存在于数据 / 存在于设计 / 已证伪）应作为后续文档的标准写法。

## WORK DATABASE-DESIGN-006 — 文档拆分：现状结构 vs 改造方案（两个独立文件）

- 用户要求：「把数据库设计文档，跟后续的说明拆开，不要混在一起」。
- **拆分结果**（按锚点脚本切分 + 节号重排 + 交叉引用改写，**非手工转录**）：
  - **`docs/DATABASE_DESIGN.md`（663 行）= 现状结构说明书**，唯一职责"数据库现在是什么"：§1 分层总览 / §2 逐表字段说明 / §3 横向设计约定 / 附录 A 生成与复核方式 / 附录 B 证据脚本。**不含任何改造方案**。
  - **`docs/DATABASE_REDESIGN.md`（594 行，新建）= 改造方案书**，唯一职责"要改成什么、为什么、怎么改"：§1 现状问题与存在性裁定（原 §4.0+§4.1）/ §2 目标设计（原 §4.2~4.7）/ §3 迁移影响分析（原 §5）/ §4 已知问题清单（原 §6，P1~P8）/ 附录 A 完整 DDL（原子节 D.1~D.7 改 A.1~A.7）/ 附录 B 表数量变更记录。
- **节号重排映射**：§4.0→§1.1、§4.1→§1.2、§4.N→§2.(N-1)（N≥2）、§4.3.x→§2.2.x、§5.x→§3.x、§6→§4、附录 D→附录 A。用正则 `§([456])(?!\d)(\.\d+(?:\.\d+)?)?` 统一重写，**负向断言 `(?!\d)` 避免误伤外部引用 `ROADMAP §44.1`**（已验证未被改写）。
- **双向交叉引用**：DESIGN 内 8 处缺陷标注统一改为「见《重构方案》§4 P#」；两文件头部互相声明定位与状态；DESIGN §1 补「L4 表数量 3 → 5」提示。
- 顺带修正：DESIGN 附录重编号（原 A/C → A/B）、**补回此前实际未落盘的两行探针**（`_turnover_probe.mts` / `_d0_probe.mts`）、去除拼接产生的**重复分隔线 3 处**。
- 备份 `docs/.DATABASE_DESIGN.md.bak` 在验证通过后删除。
- 🔴 **教训（重要）**：**同一文件在同一批次并发提交多个 Edit 会丢改动** —— 本轮发现此前两次编辑（「附录 C 补两行探针」「§5.4 待确认改写为已定」）工具均报成功但**实际未落盘**。此后凡多编辑同一文件，**必须逐条提交并回读复核**。

---

## 2026-09-10 20:15 — DATABASE-DESIGN-007：事件窗口五表分层 S1~S4 全量落地（用户指令「按 DATABASE_REDESIGN.md 重做数据集构建功能」）

### 目标
按 `docs/DATABASE_REDESIGN.md` 把 Dataset 物理表由 3 张（event/path/outcome）改为 **5 张**（event/prefix/post/path/outcome），要求「能删就删、功能不大幅漂移、页面说明文字删掉」。

### 代码（S1~S3）
- `naming.ts`：`DATASET_ROLES` → `event/prefix/post/path/outcome/feature`（顺序即层级）；`buildDatasetTableNames` 返回 6 个表名。
- `plugins.ts`：`eventCreateSql` 删 6 个日线列（24→18 列）；新增 `rawBarCreateSql(table, keyPrefix)` 生成器，`prefixCreateSql`/`postCreateSql` 复用之（**同构零重复，I10**）；`pathCreateSql` 删 9 列（6 原始 + `turnover` 死列 + 2 对重复列 → 15 列）；插件 `physicalTables` = 5 项。
- `types.ts`：`DatasetDefinition` 增 `prefixTableName`/`postTableName`；`FirstLimitPullbackEvent` 删 6 日线字段；新增 `FirstLimitPullbackRawBar`（prefix/post 共用）；`FirstLimitPullbackPath` 删 9 列；`DatasetBuildResult` 增 `prefixes`/`posts`。
- `path.ts`（重写）：`EventReference` 三字段 → `number|null`（C2 禁 `?? 0`）；新增 `eventReferenceFrom(bars)`（**C1 单源，只从 relativeBars 的 rd=0 取，零 DB 往返**）；新增 `buildRawBar`/`buildRawBars`/`partitionRawBars`；`buildDerivedRows` 只产 rd ≥ 1（C3：ref 为 null 则返回空）。
- `builder.ts`：新增 `buildEventWindows()` 一次产出 `{prefix, post, paths, outcomes}`；Phase 2 更名 `buildWindowsAndOutcomes`；`buildRelativeBarsFull` 去掉 `turnover: null` 硬编码。
- `db.ts`：新增 `insertPrefixes`/`insertPosts`（共用私有 `insertRawBars(table, rows)`）。
- `query.ts`：新增 `DatasetRawBarRole`/`RawBarPageQuery`/`listRawBarsPage`；`DatasetVersionCounts` 增 `prefixCount`/`postCount`，`rowCount` = 五表和。
- `router.ts`：新增 `listPrefix`/`listPost`。
- 契约 + `drizzle/schema.ts` + 前端（`DatasetPreviewTable` 五表 Tab、`VersionDetail` 增 Prefix/Post Tab、`datasetPreviewState` 五态）。

### S4-a 就地迁移（`scripts/applyDatasetWindowLayering.mts`，新增）
**关键决策：不做 DROP+重建，改用就地迁移**（213,536 行跨境重建成本极高；本次拆分不产生新信息）。
1. 建 prefix/post（DDL 解析自 `plugins.ts`，**列清单唯一权威**，不写第二份）；
2. `path` rd ≤ 0 → `prefix`（**10,907 行**）；3. `path` rd ≥ 1 → `post`（**202,629 行**）；
4. 删 `path` 的 rd ≤ 0 行；5. `path` 摘 9 列 / `event` 摘 6 列；6. 回填定义表名 + 刷新 `totalRows`。
**执行 37s 完成**，`event=10,907 / prefix=10,907 / post=202,629 / path=202,629 / outcome=32,721`；`totalRows` → smoke 1,801 / v1 14,456 / v2 443,536。**零数据丢失**（被摘列全是重复列/死列/已由 prefix 承载的原始 OHLC；实测 `EVENT vs PATH(rd=0)` 同值 10,907 行）。
坑：首跑时 `dataset_definition` 尚无 `prefixTableName`/`postTableName`（0030 未应用）→ 报 `ER_BAD_FIELD_ERROR`；补幂等 `ALTER TABLE ADD COLUMN` 后通过。

### S4-b 不变量断言（`scripts/verifyDatasetWindowLayering.mts`，新增）
REDESIGN §2.3 的 I1~I11 可执行化（结构层 + 数据层 + I11 抽样逐条重算）。**真实 TiDB：31 项全 PASS**（三版本）。含 I11a `path ← prefix(D0)+post`（v2 抽样 200 事件 / 3,943 行 / 不一致 0）与 I11b `outcome ← path`（不一致 0）。

### 口径修正 + 工具链同步
- **`runner.ts#markReady` 的 `totalRows` 原漏 event**（`prefixes+posts+paths+outcomes`），与 `DatasetVersionCounts.rowCount`（五表和）永久对不上 → 统一为**五表和**；`runDataset001Build.mts` 同步；`runner.test.ts` 断言 123 → **126**。
- `verifyDataset001.mjs` 重写为五表校验；`verifyDataset003b.mts` 阶段 2/6/7 改造（阶段 6 改为断言「prefix 含负相对日 + prefix 含唯一 rd=0 + **path 无 rd ≤ 0 行** + prefix 无衍生列」）。
- `applyDatasetRegistry.mjs`：注释声明物理表 DDL 权威在 `plugins.ts`；唯一约束改按**列序列**断言（兼容 0028 legacy 索引名与插件 DDL 名）；物理表缺失只告警不 fail。实测 8 表全在、8 个唯一约束全中。
- 文档：`DATABASE_REDESIGN.md` 状态改「S1~S4 已实施」+ §3.5 加状态列 + P2/P8 标已消除；`DATABASE_DESIGN.md` §2.4 重写为五表（含新列清单与 relativeDay 分布）、规模改 **41 表 / 435 字段 / 205 索引项**。
- ROADMAP：§44.1 Dataset 段改为五表快照表 + totalRows 口径；§47 append DATABASE-DESIGN-007。

### 验证
`npx tsc --noEmit` exit 0；目标套件 **18 文件 / 268 tests 全过**；全量 vitest 失败仍为 8 个既有环境依赖失败（零交集）。

### 遗留
- 🔴 **P1 涨停口径修复后的 smoke/v1/v2 重建仍未执行** → `ds_*` 仍是旧口径，策略结论仍不得产出（表结构已就位）。
- `completeJob → markReady` 两次写库非原子（lag 0~840ms）未修复。
- P7（`liquidity_daily` 市值列全 NULL）未排查。

---

## RESEARCH-001 — Research 数据库与领域对象（22:56，COMPLETE）

### 交付
- `drizzle/schema.ts` 追加 **10 张 `research_*` 表**（单数）；`drizzle/0031_research_core.sql`（38 语句，纯新增）；`scripts/applyResearchCore.mjs`（幂等 apply + **95 项 information_schema 断言，全 PASS**；二次运行幂等）。
- 新模块 **`server/researchCore/`**：`types.ts`（15 类枚举集中管理 + 10 领域对象）/ `config.ts` / `conditions.ts` / `results.ts` / `candidates.ts` / `serialization.ts` + `repository/{contract,errors,db,inMemory,index}.ts`。
- 测试 **5 文件 / 82 tests 全过**；真实 DB 端到端 `scripts/verifyResearchCore.mts` → **45/45 PASS**（跑 `createDbResearchRepositories()` 本体，逆序自清理零残留）。
- 文档：`docs/research/RESEARCH-001-{AUDIT,SCHEMA,report}.md`。

### 关键事实（长期有效）
- 规模：**10 表 / 103 列 / 28 idx + 2 uq + 10 PK**。全部引用为 soft reference（**零 DB FK**），引用合法性在 Repository 写入前校验（`ResearchReferenceError` + 机器可读码）。
- **遗留三表 vs 新增 10 表**：`research_experiments`/`research_runs`/`research_datasets`（复数）是 **STEP 6.x 链路**（字符串 `experimentId` + `strategyId` 驱动 + 单一 `snapshotJson`），与本任务的 `research_experiment`（`datasetVersionId` bigint 驱动）**非同一对象**。用户拍板：**严格按指令命名，遗留表零改动**，两套并存互不引用。**仅差一个 `s`，是长期混淆风险**。
- 🔴 **`drizzle/meta/_journal.json` 自 migration 0024 起停止维护**（journal idx 仅到 23、snapshot 仅到 0015，而 SQL 已到 0031）→ **`npm run db:push` 不可用**，**禁止 `drizzle-kit generate`**（会拿 schema.ts diff 0015 snapshot 产出垃圾）。项目**实际在用**的 migration 流程 = 「手写 `drizzle/NNNN_*.sql`（`CREATE TABLE IF NOT EXISTS` + `--> statement-breakpoint`）+ 幂等 `scripts/applyXxx.mjs`」，**不补写 journal**。
- **`server/researchCore/` 不并入 `server/research/index.ts` barrel** —— 那里已 star-export 同名不同物的 `ResearchExperiment`/`ResearchRun`，合并会冲突。消费方须显式 import。
- 模块命名遵守 §49.1（纯语义 camelCase 无数字）；测试文件与模块同主体。
- NOT 语义精确定义：`logicalOperator` 的 `NOT` = 「**AND NOT**」（取反 + AND），非连接符；「OR + 取反」借条件组表达。
- `strategyDefinitionId = NULL` 合法（未转正）；`CONVERTED` 必须有值；非 `CONVERTED` 不得已绑定 → 候选状态机与转正一致性由 `candidates.ts` 强制。

### 验证
`npx tsc --noEmit` exit 0；全量 vitest **178 文件 / 2720 tests 通过**，8 个失败文件**全为既有环境依赖失败**（Tushare token / 网络 / DB / 认证快照），零交集。

### 遗留
- RESEARCH Engine / tRPC 路由 / 前端 未做（后续任务）。
- 建议 **RESEARCH-002 = Research Engine**，但硬前置是确认 **DATASET-003B P1（涨停价四舍五入到分）修复后 `ds_*` 是否已重建**——当前仍是旧口径（封板样本漏判 38.03%），基于其上不得产出策略结论。
