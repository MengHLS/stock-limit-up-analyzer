RESEARCH-FINDING-001 B9 — 真实数据验收（首板回踩，dataset 390002，Run 570001）

## 结果：三问全部检出（§27 真实验收通过）

用真实 Dataset Version **390002**（实验 240002「正式数据，首板回踩与未来收益的关系」），
对 Run **570001**（13 个首板回踩 CONDITIONAL 分析，23978 事件）跑 Finding 检测：

- **只读 `research_result`**（208 行），未重扫 Dataset（§28 性能边界）。
- **检出 13 条 EFFECT Finding**（真实落库 id 1~13），`resetExisting:false` 幂等复跑 `reusedCount=0`/`createdCount=13`。

### §27 三问判定（真实数据，非演示造数）

| 问题 | 判定 | 真实 Finding 与证据 |
|------|------|---------------------|
| **A 回撤深度 → 收益** | ✅ 检出（越深越差，单调） | id 3/4/5：`close_ratio≥0.98` 超额 **-2.6%**、`0.95~0.98` **-5.1%**、`<0.95` **-8.8%**（样本 2377/3051/2218，全 STRONG） |
| **B 破位/未破位 → 收益** | ✅ 检出（破位明显差） | id 1：未破位 `holds_event_low==1` 超额 **+2.45%**（n=19080）；id 2：破位 `==0` 超额 **-10.0%**（n=4664，胜率 15.7%） |
| **C 组合** | ✅ 检出 | id 9：未破位 ∧ 缩量≤50% ∧ 收盘≥首板 → 超额 **+17.5%**（最强）；id 13：T+5 未破位 ∧ 缩量≤50% → **+4.0%** |

- 样本分级全部 `STRONG`（>1000）；benchmark 均取真实 `group=ALL` 行（无虚构基准）。
- 组合条件未覆盖的（§12）如实回传 `untestedInteractions`（20 条），**不产 Finding**。

## 真实 ID（§36 报告第 13 节）
- Dataset Version **390002** → Experiment **240002** → Run **570001** →
  Analysis **540001~540013**（CONDITIONAL）→ Finding **1~13**（EFFECT，全 DISCOVERED）。

## 证据
- 探针 `docs/evidence/_probe_b9_real_data.mts` + 输出 `_probe_b9_real_data.out.txt`
  （含 detect 统计、13 条 Finding 全文、verdict27 三问判定）。

## 诚实边界（§27「没有发现就必须如实显示」）
本 Run 的数据**确实证明了三问**，故如实显示检出结果；探针同时输出 `untestedInteractions`
（未覆盖组合），证明「组合被真实分析覆盖与否」是被如实区分、而非一律造 Finding。
