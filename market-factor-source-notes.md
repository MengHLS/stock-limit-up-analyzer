# 市场因子补齐数据源记录

日期：2026-08-26

## Tushare 官方字段

- [融资融券交易汇总（`margin`）](https://tushare.pro/document/2?doc_id=58)：以 `trade_date` 获取交易所级汇总，字段 `rzrqye` 为融资融券余额（元），SSE 与 SZSE 两行可相加得到两市两融余额。官方文档标注该接口需要 2000 积分权限；数据由交易所汇总，通常在下一交易日上午更新。
- [A股日线行情（`daily`）](https://tushare.pro/document/2?doc_id=27)：以 `trade_date` 获取全市场日线，`amount` 为成交额（千元）。逐交易日求和并除以 100,000 可换算为亿元的市场成交额。文档建议按交易日获取全市场数据。

## 项目数据质量发现

- `limit_up_records` 覆盖 2024-12-31 至 2026-08-26 的 202 个交易日。2026-08-26 的上交所公开两融文件返回 HTTP 404，因此该日保持缺失；没有以均值、上一日或占位值替代。
- 历史补齐后，`market_data` 有 201 个带“真实来源：Tushare daily（沪深成交额）+ 上交所/深交所公开两融汇总”标记的交易日，原有非真实来源记录为 0。
- 两市成交额仅累计 Tushare `daily` 返回中代码后缀为 `.SH` 与 `.SZ` 的证券 `amount`，不混入北交所；该数值反映成交活跃度，不用于推断资金净流入方向。
- `syncMarketData` 已改为调用同一真实快照流程。非交易日、尚未披露或来源暂不可用时返回 `skipped` 且不写入数据库，禁止写入静态占位数据。

## 风险研究使用边界

- 项目涨停数直接由 `limit_up_records` 按信号日计数，含义是“项目已录入涨停记录数”，并非交易所官方全市场涨停数。
- 沪深成交额与两融余额只有在来源标记已验证时才进入风险评分；缺失或未验证值的市场因子贡献均为 0。
- 两融余额使用信号日值相对**此前**最多 20 个已验证市场日期中位数的偏离度；至少需要 5 个历史值，且不会读取信号日之后的任何数据。
- 新增三项轻量市场因子合计最高 13 分：项目涨停数 ≤40 或 ≥120、沪深两市成交额 <10000 亿元或 ≥25000 亿元、两融余额偏离历史中位数绝对值 ≥2%。其余原有八项信号日因子与交易/退出规则保持不变。

## 2026-08-26 回填审计

| 指标 | 结果 |
| --- | --- |
| 项目涨停信号日期 | 202 个 |
| 已验证成交额/两融日期 | 201 个 |
| 未验证市场数据行 | 0 个 |
| 保持缺失的日期 | 2026-08-26（上交所公开文件 HTTP 404） |
| 严格回测候选信号日覆盖 | 2025-11-03 至 2026-08-25，共 186 日，三项市场因子均完整覆盖 |
