# 首板动态入场阈值竞争规则研究

沿用动态状态机入场：

- `T+1..T+5` 首次回撤 100bps，且未跌破首板开盘价，次日开盘买入。

入场后比较固定规则：

- 持有到 T+10。
- 首次收盘触及 `-2%` 时，下一可卖开盘止损，否则 T+10。
- 首次收盘触及 `+2%` 时，下一可卖开盘止盈，否则 T+10。
- 首次收盘触及 `±2%` 任一侧时退出。
- `±5%` 使用同样三种规则。

阈值以收盘确认，下一可卖开盘执行。所有规则在同一事件集上配对比较。
