# stock-limit-up-analyzer · 任务追踪清单（TASK TRACKING）

> **用途**：本文件是项目任务的**长期追踪文档**——随时查看、逐项更新、留存进度证据。与代码同仓，随任务推进持续维护。
> **权威关系**：`ROADMAP.md` 是唯一 Master Control Spec（状态机/验收标准/依赖的最终依据）；本文件是其**执行层追踪视图**，**不得定义任何 ROADMAP 之外的状态或规则**。
> **最后更新**：2026-09-08 23:25 GMT+8
> **状态集合唯一出处**：ROADMAP §7「系统状态模型」（7 态），本文件一律沿用，禁止自创/混用。

---

## 0.4 开发活动状态（正交于 7 态的轻量进度字段）

> **目的**：在 `状态` 列（只填 7 态）之外，**额外**记录一项"开发活动状态"用于实时展示「现在谁在写哪个任务」。与 7 态**正交**——一个任务的 7 态可以是 `DESIGN`，而开发活动状态可以是 `active`（在写）。

| 活动值 | 含义 | 何时设置 |
|---|---|---|
| `pending` | 任务已规划但**尚未启动开发** | 任务规格落定后、依赖已满足、未分配 agent |
| `active` | **正在开发中**（agent 已分配、工作中） | 协调者下发任务给 agent；agent 接受时 |
| `done` | **开发已交付**（达到 7 态 `CODE_READY`） | 协调者复核通过、补完统一出口、tsc/vitest 验证后 |
| `blocked` | 开发被阻塞（与 7 态 `BLOCKED` 不同，**活动层阻塞**——如 agent 限流中断、需协调者接手） | 出现活动层阻塞 |

**记录位置**：任务行的 `负责人` 列（追加 `[活动值]` 后缀，如 `research-dev [active]`），**不占用 `状态` 列**（状态列仍只填 7 态）。
**顶部概览**：见 §0.5「正在开发（active）— 实时概览」。
**活动历史**：见 §0.6「活动历史（append-only）」。
**纪律**：
- 活动状态**不影响** 7 态推进（一个 `active` 的任务 7 态仍为 `DESIGN`，直到达成 `CODE_READY` 才升级）。
- `active` ↔ `done`/`blocked` 的变更**必须**在 §0.6 与 §5 各追加一条更新记录（append-only）。
- 不得伪造 `done`：仅协调者复核通过、`tsc --noEmit` exit 0、`vitest` 全过后才标 `done`。

---

## 0.5 正在开发（active）— 实时概览

> 由 §0.4「开发活动状态」过滤 `active` 得到的当前正在开发的任务清单。完成任务后从本表移除，转入 §0.6「活动历史」留存。

| 任务 | 模块 | 7 态 | 活动状态 | 启动时间 | 依赖 | 备注 |
|---|---|---|---|---|---|---|

> 当前 **0 项**正在开发任务。

---

## 0.6 活动历史（append-only）

> 每次任务从 `pending`→`active`→`done`/`blocked` 留一条记录。**不得删改**。`done` 时 7 态应同时升到 `CODE_READY`，详见各任务主表与 §5。

- **2026-09-07 18:06–19:30** — `C-20.1` PBO 与参数敏感性：active → **done**。子代理交付（55 单测 8 文件 3450 行 `overfittingDetection20/`，含 CSCV PBO 零分布/分位 CI/参数敏感性/聚合判定 + 复用 C-18.1 扰动 + 与 C-19.2 OOSRun 端到端对照）→ 协调者复核（**修复 C-20.1 在 types.ts 重复定义 C-18.1 两个阈值的纪律违反**：改为 `import { ... } from "../robustness18"` re-export + tsc exit 0）+ 补 `index.ts` 导出 → 全量 49 文件 1047 测试全过、tsc 干净。详见 §5 14:42 记录。
- **2026-09-07 18:06–19:30** — `C-23.2` 信号→订单→成交→PnL 闭环：active → **done**。子代理交付（18 单测 8 文件 `signalToPnl23/`，含 runSignalToPnlLoop 多日编排 a/b/c/d 四步 + 复用 C-23.1 8 原语 + frozen 冻结/解冻时序兑现 C-23.1 遗留 + PIT 时序机器检查 + C-16.3 适配）→ 协调者复核（C-23.2 子代理**违反约定改了 `index.ts`** 做 `toTradeQualityEvaluationInput` 同名消歧、接受其解决 + 补 C-20.1 导出 + 修 C-20.1 阈值重复定义）→ 全量 49 文件 1047 测试全过、tsc 干净。详见 §5 14:42 记录。
- **2026-09-07 22:28–22:53** — `C-20.2` 因子消融与 OOS 退化：active → **done**。子代理交付（45 单测 10 文件 2791 行 `factorAblation/`，含四消融模式 + IS/OOS 双轨 + 过拟合信号候选 + C-20.1 Ofd 适配）→ 协调者复核（补 `index.ts` 导出）→ 全量 51 文件 1121 测试全过、tsc 干净。
- **2026-09-07 22:28–22:53** — `C-24.1` 交易日志：active → **done**。子代理交付（29 单测 10 文件 3609 行 `tradeJournal/`，含 reconcile 机器核对 + append-only 账本 + 人工标注零生成 + run 提取 draft）→ 协调者复核（补 `index.ts` 导出）→ 全量 51 文件 1121 测试全过、tsc 干净。
- **2026-09-08 22:30–22:53** — `C-24.2` 纪律反馈分析：active → **done**。子代理交付（38 单测 12 文件 `disciplineFeedback/`，含四类跨交易聚合 + DisciplineFeedbackRun + 9 分支诚实结论 + 复用 tradeJournal 词表/校验零重定义）→ 协调者复核（补 `index.ts` 导出）→ 全量 52 文件 1159 测试全过、tsc 干净、tsx 实例化 exports=845。
- **2026-09-08 22:55–23:25** — `C-25.1` 闭环整合：active → **done**。子代理交付（32 单测 12 文件 `closedLoop/`，含 §27 14 阶段链契约 + handoff 摘要流转 + 注入式执行器 + BLOCKED 诚实门禁 + lifecycle 证据门槛 + §28 谱系挂载）→ 协调者复核（补 `index.ts` 导出）→ 全量 53 文件 1191 测试全过、tsc 干净、tsx 实例化 exports=904。**编码链 29/29 全部 CODE_READY**。

---

## 0. 状态字段：可选值定义与更新规则（必读，改动前先对齐）

### 0.1 唯一合法状态值（来源：ROADMAP §7）

| 状态值 | 官方定义（ROADMAP §7） | 本清单中的适用判断 |
|---|---|---|
| `DESIGN` | 设计完成，但尚未实现 | 任务已规划、规格已明确（见本文件「说明」列），但代码/实现尚未开始 |
| `CODE_READY` | 代码已经实现；可能没有真实数据 / 未验证 | 代码+单元测试已就绪；但数据未全量接入或验证未完成 |
| `DATA_READY` | 真实数据已接入，满足最低数据完整性 | 真实 DB 已回填到目标区间/最低完整性，校验尚未全 PASS |
| `VALIDATED` | 代码、数据、边界条件、测试已验证 | 该任务 Exit Criteria 要求的全部校验 PASS |
| `RESEARCH_READY` | 可以用于正式量化研究 | 仅数据/数据集链可达：H Gate 全 PASS + Research Dataset 认证 |
| `PRODUCTION_READY` | 满足生产级运行要求 | 生产级运行要求达成（监控/幂等/可恢复/闭环） |
| `BLOCKED` | 存在明确阻塞依赖 | 有明确阻塞依赖未满足，需记录 blocked_by/reason/trigger |

> 与日常直觉的对应（仅用于理解，**不是状态值**）：`DESIGN` ≈ 未开始（已规划）；「进行中」≈ 任务正从当前态向下一个态演进（如回填中 = 从 `CODE_READY` 向 `DATA_READY` 过渡，进度写在各组「备注/进度」列）；「完成」≈ 达成该任务的 Exit（各任务说明列已标注）；`BLOCKED` ≈ 阻塞中。

### 0.2 状态推进规则（何时允许升级）

| 转换 | 触发条件（证据要求） | 反例（禁止） |
|---|---|---|
| → `DESIGN` | 任务定义/规格完成（本文件建立即为 DESIGN 基线） | 无规格直接开工 |
| `DESIGN` → `CODE_READY` | 代码实现 + 单元测试通过（记录代码路径、测试结果） | 「思路想好了」不算 |
| `CODE_READY` → `DATA_READY` | **真实 DB** 回填至目标区间/最低完整性；记录 row_count/区间/coverage | mock 数据冒充真实；「provider 能取到」冒充「已回填」；「代码支持」冒充「DB 已有真实数据」 |
| `DATA_READY` → `VALIDATED` | PIT / Survivorship / Identity / Coverage / Duplicate / Gap / Cross-domain / Adjustment 校验 PASS + 抽样核验（记录校验报告） | 「行数很多」= VALIDATED（行数 ≠ 数据质量）；测试通过 ≠ 研究就绪 |
| → `RESEARCH_READY` | H Gate 全 PASS（§13 清单）+ Research Dataset 认证（STEP 12.6） | 跳过 Gate 直接宣布研究就绪 |
| → `PRODUCTION_READY` | 生产级要求达成（§34 DoD + 闭环） | 功能 Demo 冒充生产 |
| 任意态 → `BLOCKED` | 出现明确阻塞依赖；**必须记录** `blocked_by / reason / required condition / next trigger` | 只是「还没轮到」不算阻塞（排队等待请维持现状+备注） |
| `BLOCKED` → 恢复 | 阻塞解除（trigger 满足）→ 回到被阻塞前的状态继续推进 | 阻塞未解除直接越级 |

### 0.3 硬性纪律（每轮更新必须自检）

1. **禁止跳跃**：`CODE_READY` 不得直接标 `RESEARCH_READY`/`VALIDATED`；7 态顺序演进（ROADMAP §37）。
2. **证据优先级**：真实 DB > 实际运行结果 > 代码 > 自动化测试 > 文档 > 假设（§3/§43）。旧文档数字不得覆盖真实状态（§36）。
3. **代码存在 ≠ VALIDATED**；**测试通过 ≠ Research Ready**（§42.12-13）。
4. 本文档状态列只填 §0.1 的 7 个值；「进度/后台/备注」信息写在各任务「备注/进度」列，**不得**混入状态字段。
5. 每次实质性推进（状态升级/降级、负责人变更、优先级调整）须在 §5 追加一条更新记录（append-only，禁止删改历史）。

---

## 1. 总览与分组

### 1.1 分组与规模

| 组 | 编号 | 数量 | 性质 | 并行策略 |
|---|---|---|---|---|
| **G1 数据回填与认证** | `D-A` ~ `D-H` | 8 | 数据域 A~H（ROADMAP §9/§38） | **BaoStock 单 Session 强制串行**：`G → C+E → D`（§30）；A（Tushare 通道）、B/F 校验可与 BaoStock 链并行 |
| **G2 编码开发** | `C-12.5.1` ~ `C-25.1` | 29 | STEP 12.5→25 引擎/框架代码 | **不被 G1 阻塞**（依据 §39：`RESEARCH_READY=FALSE` 期间允许开发代码/框架/回测）。编码目标先达 `CODE_READY`（技术验证），`VALIDATED/RESEARCH_READY` 待数据就绪后汇合 |
| **G3 前端研究链路 UI** | `FE-0` ~ `FE-9` | 10 | Spec V1 F1-F7 与 ROADMAP §48.4 FE-0~FE-9 的统一执行视图 | **骨架线自 FE-0 后即可并行**（不依赖真实数据，禁 mock 冒充 §0.2）；真实数据联调线依赖对应后端 STEP 认证 |

### 1.2 依赖骨架

```text
数据链（串行为主）：[A/B/F 并行] ──┬─→ G ─→ C+E ─→ D ─→ H(Gate: A~G 全就绪)
                                   └──────────────────────────┘
编码链（两轨并行）：12.5.1 → 12.5.2 / 12.6.1 → 12.6.2 → 13.1 → {13.2, 13.3}
        → 14.1 → {14.2, 14.3, 15.1, 16.1} → {16.2, 16.3, 17.1} → {17.2, 18.1} → 18.2
        → 19.1 → 19.2 → 20.1 → 20.2；15.1 → {21.1, 22.1}；14.3+16.3 → 23.1 → 23.2 → 24.1 → 24.2
        → 25.1 = f(21.1, 22.1, 24.2)
汇合：G2 全链 `CODE_READY/VALIDATED` + G1 `RESEARCH_READY=TRUE` → 正式策略研究（§39）
```

### 1.3 字段约定

- **优先级**：`P0`（当前最高，无依赖即可开工/收尾中）→ `P1`（主干，依赖满足即开）→ `P2`（后端模块，待主干稳定）。
- **负责人**：占位角色名，实际执行 agent 认领后替换为真实名称。G1 严禁并行执行（BaoStock 单 Session）；G2 可多实例并行，共享接口合入需串行。
- **截止时间**：默认「未设（依赖驱动）」；出现里程碑窗口或外部约束时填 `YYYY-MM-DD`。
- **说明**：任务详细描述 + Exit（验收）要点，来自 ROADMAP 对应 STEP 的 Entry/Exit 实例化（§45）与本文件创建时的拆分规格。

---

## 2. G1 数据回填与认证（8 项）

> 初始状态基线：ROADMAP §44.2（最后实查 2026-09-06 20:02）＋当日工作日志（截至 20:25）。**后续一切以真实 DB 为准（§36）。**

| ID | 任务 | 优先级 | 负责人 | 截止 | 状态 | 依赖 | 说明 / Exit |
|---|---|---|---|---|---|---|---|
| `D-A` | A OHLCV 收尾与覆盖校验 | P0 | backfill-agent | 未设 | `DATA_READY` | — | stock_daily_prices 全量 OHLCV。日志 20:25：gate #3 PASS、1863 交易日、缺口 0/重复 0（8.83M 行尾段逐股补齐中）。Exit：覆盖 100% + §10 全字段快照 + Identity/PIT/Survivorship check → `VALIDATED` |
| `D-B` | B Security Master + Identifier 校验 | P1 | backfill-agent | 未设 | `DATA_READY` | — | securities+identifier_history 5552 全量（含退市股，anti-survivorship）。Exit：PIT/重叠/更名/退市/代码复用校验 → `VALIDATED` |
| `D-G` | G Industry 全量回填 | P0 | backfill-agent | 未设 | `DATA_READY` | — | industry_assignments 5212 行/5212 股/83 行业落库（failed=0，340 无行业为真实退市/ST）。**待办**：securityId 全 NULL 回填 + effectiveFrom 单点(2026-08-31)历史轨迹缺口。Exit：identity/PIT 校验 → `VALIDATED` |
| `D-C` | C Historical Status 全量回填 | P1 | backfill-agent | 未设 | `CODE_READY` | `D-G` | status_history 小样本 211 → 全量（上市/退市/停牌，支持 asOf(T)）。BaoStock 串行（G 后）。Exit：全量 → `DATA_READY` → `VALIDATED` |
| `D-E` | E Liquidity 全量回填 | P1 | backfill-agent | 未设 | `CODE_READY` | `D-G` | liquidity_daily 104,929 → 全量（并入 C 逐股流程）。BaoStock 串行（G 后）。Exit：全量 → `DATA_READY` → `VALIDATED` |
| `D-D` | D Corporate Actions + Adjustment 全量回填 | P1 | backfill-agent | 未设 | `CODE_READY` | `D-C`,`D-E` | corporate_actions 240 + adjustment_factors 271 → 全量（分红/送转/配股/复权因子）。串行（C+E 后）。Exit：全量 → `DATA_READY` → `VALIDATED` |
| `D-F` | F Index PIT 校验 | P1 | backfill-agent | 未设 | `DATA_READY` | — | index_master 8 + index_daily 7452 全量已齐（4 核心指数 2019-2026）。Exit：PIT 校验（成分/发布时点）→ `VALIDATED` |
| `D-H` | H Research Readiness Gate 重跑 | P1 | backfill-agent | 未设 | `CODE_READY` | `D-A`~`D-G` 全就绪 | 重跑 step12_certify_gate.mjs，核对 §13 PASS 清单（8 域 + Identity/PIT/Survivorship/Coverage/Duplicate/Gap/Cross-domain/Adjustment/Research Dataset），输出 research_ready_gate.json 判定 `RESEARCH_READY` |

**G1 串行纪律**：任何 BaoStock 触达任务（G/C/E/D）**禁止并发**（§30：`G → C+E → D`）；A（Tushare 独立通道）与 B/F 校验可并行。当前后台编排器：`scripts/orchestrate_backfill.mjs`（G 完成 → C+E → D，每步重试 ≤3，停滞自动重启 G）。

---

## 3. G2 编码开发（29 项）

> 编码目标 = 先达 `CODE_READY`（代码+单测+技术验证），`VALIDATED` 需数据就绪后按 §0.2 规则升级；**不与 G1 互相阻塞**（§39 解耦决策）。
> 初始状态：`DESIGN`（任务规格已定、实现未开始；若对应底层代码已存在，执行者须先实查代码/测试，按证据升级，不得空等也不得自动标绿）。

### 3.1 STEP 12.5 / 12.6 · 数据桥与数据集（4 项）

| ID | 任务 | 优先级 | 负责人 | 截止 | 状态 | 依赖 | 说明 / Exit |
|---|---|---|---|---|---|---|---|
| `C-12.5.1` | asOf(T) 历史状态查询层 | P0 | research-dev | 未设 | `CODE_READY` | — | 对任意 (security, date) 的 asOf(T) 查询：身份/上市退市/行业/可交易/流动性/价格/已生效公司行为/市场状态/已知信息。20:53：`server/historicalState/` 落地（types/mappers/reconstruct 纯函数 + db 真实加载 + 统一出口），单测 28 例全过、tsc 干净。Exit：→ 真数据抽样 PIT 验证 → `VALIDATED`（依赖 A~H 全 DATA_READY，§0.2 禁止越级） |
| `C-12.5.2` | PIT/反泄漏抽样验证 | P1 | research-dev | 未设 | `CODE_READY` | `C-12.5.1` | 抽样验证无 look-ahead / 无 survivorship 泄漏（脚本+用例）。21:59：`server/historicalState/audit/` 落地（types/oracle/checkers 12 项/db/runAudit/CLI），单测 oracle 12 + checkers 17 全过、tsc 干净；真实 DB smoke 23 计划/23 查询/0 失败、industry PIT guard exercised 3、gate=INCONCLUSIVE(dataReady=false)，报告 `docs/step12-evidence/pit_audit_smoke.json`。Exit：抽样报告 → `VALIDATED`（依赖 A~H 全 DATA_READY，§0.2 禁止越级） |
| `C-12.6.1` | Research Dataset 构建器 | P1 | research-dev | 未设 | `CODE_READY` | `C-12.5.1` | Canonical Data + Historical State → 标准输入（供 Signal/Backtest/Opt/Eval/WFA/OOS）。22:45：`server/researchDataset/` 落地（types/validate/version 确定性 dataset_version/universe 逐日 PIT 决议/assemble 全代码 code-ownership 防串扰/db 批量加载/builder 编排+dataSnapshot+gate），单测 21 例全过、tsc 干净；真实 DB smoke `docs/step12-evidence/research_dataset_smoke.json`：gate=INCONCLUSIVE（C 域 211 行样本 → universe 0 成员属正确反映，NO_ROWS_BUILT）、datasetVersion `rd-1.0.0-1-cffc2a0e66efbf0b` 两次运行一致（确定性）。Exit：`CODE_READY+DATA_READY+VALIDATED`（VALIDATED 依赖 A~H 全 DATA_READY，§0.2 禁止越级） |
| `C-12.6.2` | 数据集策略元数据与版本快照 | P1 | research-dev | 未设 | `CODE_READY` | `C-12.6.1` | 9 类 policy（PIT/survivorship/CA/adjustment/industry/liquidity/universe-membership/calendar/knowledge，映射表见 §5）+ 版本快照。23:10：`server/researchDataset/policy.ts`（derivePolicySet/computePolicySetFingerprint）+ `policyValidate.ts`（9 类齐备/期望值一致/交叉语义冲突检测）+ `versionSnapshot.ts`（快照 round-trip）+ builder 产物带 policySet（additive），单测新增 16（共 37）全过、tsc 干净。Exit：`CODE_READY` → `VALIDATED`（依赖 A~H 全 DATA_READY） |

### 3.2 STEP 13 · Research Engine（3 项）

| ID | 任务 | 优先级 | 负责人 | 截止 | 状态 | 依赖 | 说明 / Exit |
|---|---|---|---|---|---|---|---|
| `C-13.1` | Feature 抽象与 Dataset 访问层 | P1 | research-dev | 未设 | `CODE_READY` | `C-12.6.1` | 隔离策略代码与 DB 细节；支持 dataset/strategy version、parameter set、date range、universe、regime。23:10：`server/research/datasetAccess/`（handle 绑定+版本一致性/invariants PIT 断言/universe 视图/bars 映射/slice 闭区间/session 装配），新增单测 27，research+researchDataset 365 全过、tsc 干净；`research/index.ts` 补统一出口。regime 属 C-22.1。Exit：`CODE_READY` → `VALIDATED` |
| `C-13.2` | Signal 与 Candidate 框架 | P1 | research-dev | 未设 | `CODE_READY` | `C-13.1` | Feature→Signal→Candidate→Evaluation 链路。23:45：`server/research/signal13/`（types/engine 逐 tradeDate 驱动 STEP 10 pipeline/evaluate 候选层统计/serialize 指纹），单测新增 18、research+researchDataset 415 全过、tsc 干净。Evaluation 界定为候选确定性统计，收益回测属 C-14.1。Exit：`CODE_READY` → `VALIDATED` |
| `C-13.3` | Experiment Registry 实验追踪 | P1 | research-dev | 未设 | `CODE_READY` | `C-13.1` | §28 全字段（experiment_id…code_version/created_at），回答「结果怎么产生的」。23:45：`server/research/experimentLineage/`（types 15 字段谱系记录/codeVersion 注入式/map 兼容既有 Experiment+Snapshot/validate 齐备校验/serialize/bridge），单测新增 32、415 全过、tsc 干净。Exit：`CODE_READY` → `VALIDATED` |

### 3.3 STEP 14 · Backtest Engine（3 项）

| ID | 任务 | 优先级 | 负责人 | 截止 | 状态 | 依赖 | 说明 / Exit |
|---|---|---|---|---|---|---|---|
| `C-14.1` | 交易模拟核心 | P1 | research-dev | 未设 | `CODE_READY` | `C-13.2` | 持仓/现金/订单 + T+1/涨跌停/停牌/买卖限制。禁止仅 signal→next close。00:17：`server/research/simulator/`（types/plan 候选→Order 映射 sell-first+冻结顺延/engine 多日编排复用 STEP 8 原语/serialize 指纹），单测新增 17、research+researchDataset+backtest 498 全过、tsc 干净。Exit：`CODE_READY` → `VALIDATED` |
| `C-14.2` | 成本模型 | P1 | research-dev | 未设 | `CODE_READY` | `C-14.1` | 佣金/印花税/滑点/市场冲击。00:40：`server/research/costModel14/`（市场冲击显式建模+成本声明 schema/分解/mappers），单测新增 34、598 全过、tsc 干净。Exit：`CODE_READY` → `VALIDATED` |
| `C-14.3` | 执行与约束模型 | P1 | research-dev | 未设 | `CODE_READY` | `C-14.1` | 成交时机/手数(lot)/初始资金/仓位/买卖限制。00:40：`server/research/executionConstraints14/`（组合式声明/校验/映射 simulator 配置，诚实 blocker 不冒充执行），单测新增 26、598 全过、tsc 干净。Exit：`CODE_READY` → `VALIDATED` |

### 3.4 STEP 15 / 16 · 策略定义与评价（4 项）

| ID | 任务 | 优先级 | 负责人 | 截止 | 状态 | 依赖 | 说明 / Exit |
|---|---|---|---|---|---|---|---|
| `C-15.1` | 策略 Schema + 版本化 | P1 | research-dev | 未设 | `CODE_READY` | `C-13.2` | §16 全字段结构化 + §17 最小版本能力（可追溯 params/dataset/universe/backtest config/cost/execution/code version）。00:17：`server/research/strategySchema/`（StrategyDocument 本体/version bump 语义/compare/validate/serialize 指纹/map 兼容 Strategy13），单测新增 28、research+researchDataset 443 全过、tsc 干净。Exit：`CODE_READY` → `VALIDATED` |
| `C-16.1` | 收益/风险/回撤指标 | P1 | research-dev | 未设 | `CODE_READY` | `C-14.1` | CAGR/Total Return/MaxDD/Recovery Factor + DD 时长/tail risk。00:40：`server/research/performanceMetrics/`（CAGR/MaxDD 剖面/Recovery Factor/下行风险/tail risk 确定性评估器，消费 simulator 产出），单测新增 40、598 全过、tsc 干净。Exit：`CODE_READY` → `VALIDATED` |
| `C-16.2` | 风险调整指标 | P2 | research-dev | 未设 | `CODE_READY` | `C-16.1` | Sharpe/Sortino/Calmar。01:26：`server/research/riskAdjustedMetrics16/`（rf 参数化，复用 C-16.1 收益序列/回撤），单测新增 19、684 全过、tsc 干净。Exit：`CODE_READY` → `VALIDATED` |
| `C-16.3` | 交易质量与稳定性指标 | P2 | research-dev | 未设 | `CODE_READY` | `C-16.1` | WinRate/Profit Factor/Expectancy/Turnover/平均持仓/交易数 + 月度年度一致性 + regime 表现。01:26：`server/research/tradeQualityMetrics16/`（STEP 8 重叠字段复用+月度年度一致性+regime unassessed 占位），单测新增 37、684 全过、tsc 干净。Exit：`CODE_READY` → `VALIDATED` |

### 3.5 STEP 17 / 18 · 优化与稳健性（4 项）

| ID | 任务 | 优先级 | 负责人 | 截止 | 状态 | 依赖 | 说明 / Exit |
|---|---|---|---|---|---|---|---|
| `C-17.1` | Grid / Random Search | P2 | research-dev | 未设 | `CODE_READY` | `C-14.1`,`C-16.1` | 找**稳定参数区**而非历史最高；产出 Candidate Strategies（非生产策略）；记完整实验。01:26：`server/research/parameterSearch17/`（Grid 桥 STEP 6.3 + Random 确定性 mulberry32 + 稳定区判定拒绝高收益坏点 + SearchRun 实验记录），单测新增 30、684 全过、tsc 干净。Exit：`CODE_READY` → `VALIDATED` |
| `C-17.2` | Rolling Optimization | P2 | research-dev | 未设 | `CODE_READY` | `C-17.1` | 滚动优化，区分 Optimization 与 Final Strategy。Exit：`VALIDATED`（依赖数据链就绪） |
| `C-18.1` | 成本/滑点/参数扰动 | P2 | research-dev | 未设 | `CODE_READY` | `C-17.1` | Cost/Slippage Stress + Parameter Perturbation/Stability + Execution Perturbation。Exit：`VALIDATED`（依赖数据链就绪） |
| `C-18.2` | Monte Carlo / Bootstrap | P2 | research-dev | 未设 | `CODE_READY` | `C-18.1` | 14:02：`server/research/stochasticRobustness18/`（MC / Bootstrap / Trade Order Randomization 三法 + 分布/CI/尾部概率/基准位置 + 复用 C-18.1 阈值语义 + StochasticRobustnessRun 记录，seedable PRNG 确定性），单测 50 例。Exit：`CODE_READY` → `VALIDATED`（依赖数据链就绪） |

### 3.6 STEP 19 / 20 · WFA/OOS 与过拟合（4 项）

| ID | 任务 | 优先级 | 负责人 | 截止 | 状态 | 依赖 | 说明 / Exit |
|---|---|---|---|---|---|---|---|
| `C-19.1` | 滚动窗口划分 | P2 | research-dev | 未设 | `CODE_READY` | `C-17.2` | 14:02：`server/research/walkForward19/`（交易日锚定 Train/Test 成对窗口 + gap/embargo 防标签重叠 + 冻结纪律下中位数显式非 argmax + 逐窗编排复用 C-17.1 + WalkForwardRun 记录 + 冻结纪律机器检查），单测 26 例。Exit：`CODE_READY` → `VALIDATED`（依赖数据链就绪） |
| `C-19.2` | 样本内/外隔离与记录 | P2 | research-dev [done] | 未设 | `CODE_READY` | `C-19.1` | 14:40：`server/research/oosIsolation/`（WindowResultRecord Train/Test 字段分离 + 键白名单/无重叠/参数不回写机器检查 + append-only 归档账本 + OOS 分段聚合报告，复用 C-19.1 窗口几何与 C-16.1/16.2 指标，描述性对比不下过拟合结论），单测 23 例。Exit：`CODE_READY` → `VALIDATED`（依赖数据链就绪） |
| `C-20.1` | PBO 与参数敏感性 | P2 | research-dev [done] | 未设 | `CODE_READY` | `C-18.1`,`C-19.2` | 19:30：`server/research/overfittingDetection20/`（CSCV PBO 零分布+分位 CI+判定结论+参数敏感性复用 C-18.1 扰动+Overfitting 聚合判定+三类记录 round-trip+篡改拒绝；诚实 CV-PBO/贝叶斯收缩/P-value 不做），单测 55 例。协调者修：C-20.1 types.ts 重复定义 C-18.1 阈值改为 import 复用。Exit：`CODE_READY` → `VALIDATED`（依赖数据链就绪） |
| `C-20.2` | 因子消融与 OOS 退化 | P2 | research-dev [done] | 未设 | `CODE_READY` | `C-20.1` | 22:53：`server/research/factorAblation/`（AblationTarget/四模式 REMOVE_SINGLE/LEAVE_ONE_OUT/CUMULATIVE_REMOVE/FORWARD_ADD + evaluator 注入 + IS/OOS 双轨消融对照识别「回测好、泛化差」+ 成分贡献排序显式非 argmax + 过拟合信号 ABL_IS_POS_OOS_NEG/NEUTRAL 候选 + 与 C-20.1 Ofd 汇合适配 + discipline 审计），单测 45 例。Exit：`CODE_READY` → `VALIDATED`（依赖数据链就绪） |

### 3.7 STEP 21 / 22 · 生命周期与市场状态（2 项）

| ID | 任务 | 优先级 | 负责人 | 截止 | 状态 | 依赖 | 说明 / Exit |
|---|---|---|---|---|---|---|---|
| `C-21.1` | 生命周期状态机 + 审计 | P2 | research-dev | 未设 | `CODE_READY` | `C-15.1` | Draft→Research→Candidate→Validated→Paper→Approved→Production→Retired；每次变更记 timestamp/reason/experiment/evidence；禁止无记录修改。Exit：`VALIDATED`（依赖数据链就绪） |
| `C-22.1` | Market Regime 体系 | P2 | research-dev | 未设 | `CODE_READY` | `C-15.1` | 14:02：`server/research/marketRegime22/`（七维 PIT 标签 Trend/Volatility/Liquidity/Breadth/Sentiment/Index State/Limit-up Env + 回看窗无泄漏断言 + 数据缺失 unassessed 不编造 + 复合状态归因 + 适配 C-16.3/C-13.3 regime 占位），单测 51 例。Exit：`CODE_READY` → `VALIDATED`（依赖数据链就绪） |

### 3.8 STEP 23 / 24 · 模拟交易与纪律（4 项）

| ID | 任务 | 优先级 | 负责人 | 截止 | 状态 | 依赖 | 说明 / Exit |
|---|---|---|---|---|---|---|---|
| `C-23.1` | 模拟账户与持仓 | P2 | research-dev | 未设 | `CODE_READY` | `C-14.3`,`C-16.3` | 14:40：`server/research/paperAccount23/`（账户/持仓状态机 + T+1 冻结 + 现金账本 + 订单→成交约束 + 六维能力矩阵，复用 C-14.3 约束声明/C-14.2 五维成本/C-16.3 适配，诚实 blocker 仅记录不强制执行，闭环留 C-23.2），单测 35 例。Exit：`CODE_READY` → `VALIDATED`（依赖数据链就绪） |
| `C-23.2` | 信号→订单→成交→PnL 闭环 | P2 | research-dev [done] | 未设 | `CODE_READY` | `C-23.1` | 19:30：`server/research/signalToPnl23/`（runSignalToPnlLoop 多日编排 a/b/c/d 四步 + 复用 C-23.1 8 原语 + frozen 冻结/解冻时序兑现 C-23.1 遗留 + PIT 时序机器检查 + 决策日 D 订单 executionTime=D+1 + 成交价=D+1 open 非 D 收盘 + C-16.3 适配 + PaperTradingRun 记录），单测 18 例。协调者：C-23.2 子代理违反约定改了 index.ts 做 `toTradeQualityEvaluationInput` 消歧、接受；C-24.1 依赖本任务。Exit：`CODE_READY` → `VALIDATED`（依赖数据链就绪） |
| `C-24.1` | 交易日志 | P2 | research-dev [done] | 未设 | `CODE_READY` | `C-23.2` | 22:53：`server/research/tradeJournal/`（TradeJournalEntry planned/actual 事实分离 + reconcilePlanVsActual 机器核对数量/价格/时机偏差 + AnnotationBlock 人工受控词表零生成 + append-only 账本 supersedes 修订链 + buildJournalDraftsFromRun 从 C-23.2 run 提取 + PostReviewRecord 复盘快照 + PIT 时间序校验），单测 29 例。Exit：`CODE_READY` → `VALIDATED`（依赖数据链就绪） |
| `C-24.2` | 纪律反馈分析 | P2 | research-dev [done] | 未设 | `CODE_READY` | `C-24.1` | 22:53（9-08）：`server/research/disciplineFeedback/`（四类跨交易聚合：违规原因统计/重复错误识别/最差执行策略排序/易错环境识别 + DisciplineFeedbackRun 记录 + 9 分支诚实结论，复用 tradeJournal 词表与校验，环境标签注入不自算），单测 38 例。Exit：`CODE_READY` → `VALIDATED` |

### 3.9 STEP 25 · 生产闭环（1 项）

| ID | 任务 | 优先级 | 负责人 | 截止 | 状态 | 依赖 | 说明 / Exit |
|---|---|---|---|---|---|---|---|
| `C-25.1` | 闭环整合 | P2 | research-dev [done] | 未设 | `CODE_READY` | `C-21.1`,`C-22.1`,`C-24.2` | 23:25（9-08）：`server/research/closedLoop/`（§27 14 阶段链契约 + handoff 摘要流转 + 注入式执行器 + BLOCKED 诚实门禁 + lifecycle 证据门槛 + §28 谱系挂载），单测 32 例。**编码链 29/29 全 CODE_READY**。Exit：`CODE_READY` → 逐步 `VALIDATED` → `PRODUCTION_READY` |

### 3.10 G3 前端研究链路 UI（10 项 · FE-0~FE-9）

> 来源：ROADMAP §48.4（前端开发路线）。本组是 Spec V1 F1-F7 与 §48.4 FE-0~FE-9 的**统一执行视图**（两套为同一工作的两种视图，权威执行清单 = FE-0~FE-9，映射表见 §48.4）。
> 状态沿用 §0.1 的 7 态模型。**FE-0 已完成 → `CODE_READY`**（契约编译通过 + 契约单测通过 + 前端类型安全调用），**R6 解除**；**FE-1 已完成 → `CODE_READY`**（数据域健康看板，读真实 gate JSON，13 例单测全过）；**FE-2 已完成 → `CODE_READY`**（asOf 十问查询器 + 代码解析，7 例单测全过，真实联调待 M0）；**FE-3 已完成 → `CODE_READY`**（数据集构建骨架页，tsc/build 通过 + 回归 32/32，真实 build 联调待 M0）；**FE-4 已完成 → `CODE_READY`**（策略编辑器 + 版本化 + 生命周期工作台，端到端 smoke 6 项全过，纯函数端点真测）；**FE-5 已完成 → `CODE_READY`**（绩效仪表盘骨架线，tsc/build/client 17 测试全过，真实曲线联调待 FE-0 run/metrics 端点 + C-14/16 VALIDATED）；**FE-6/7/8/9 已完成 → `CODE_READY`**（23:20 并行骨架线：ParameterSearch/WalkForwardAnalysis/RegimeReport/ReviewWorkbench 四页 + 路由 + 导航，契约零改动零伪造，IS/OOS 视觉隔离与 R7 隔离条验收点落实；真实联调分别待 C-17/18·19/20·22/23·24/25 认证 + 对应端点暴露）。

| ID | 任务 | 优先级 | 负责人 | 截止 | 状态 | 依赖 | 说明 / Exit |
|---|---|---|---|---|---|---|---|
| `FE-0` | 研究链路 tRPC 暴露（server 侧） | P0 | research-dev | 未设 | `CODE_READY` | — | 新增 `historicalState`/`researchDataset`/`research` 三 router + `shared/researchContracts.ts` 契约 + `server/researchContracts.test.ts` 契约单测 12 例全过、tsc 干净。Exit：契约编译通过 + 契约单测通过 + 前端 `trpc.ts` 类型安全调用（R6 解除） |
| `FE-1` | 数据域健康看板 | P0 | frontend-dev | 未设 | `CODE_READY` | `FE-0` | gate 17 项真实状态 + `RESEARCH_READY` 灯 + 数据域行数/覆盖（读真实 gate JSON/DB，非 mock） |
| `FE-2` | asOf(T) 历史状态查询器 | P0 | frontend-dev | 未设 | `CODE_READY` | `FE-0` + 数据域 DATA_READY | (security,date) → 十问真实渲染；空/加载/错误态齐备 |
| `FE-3` | 数据集构建器 | P1 | frontend-dev | 未设 | `CODE_READY` | `FE-0` + STEP 12.6 | 触发真实 build → datasetVersion/data_snapshot/coverageGaps/9 类 policy；两次构建哈希一致 |
| `FE-4` | 策略编辑器 + 运行工作台 | P1 | frontend-dev | 未设 | `CODE_READY` | `FE-0` + STEP 13 | StrategyDocument JSON 编辑 + validate issue 表 + semver bump + 版本 compare diff + 生命周期状态机/迁移 |
| `FE-5` | 绩效仪表盘 | P2 | frontend-dev | 未设 | `CODE_READY` | `FE-0` + C-14/16 认证 | Equity/MaxDD/Trade 表/指标卡与 C-16 输出一致（同源渲染，前端禁重算）；骨架线已落地（结构+空态），真实曲线联调待 run/metrics 端点 + C-14/16 VALIDATED |
| `FE-6` | 参数搜索 + 鲁棒性 UI | P2 | frontend-dev | 未设 | `CODE_READY` | `FE-0` + C-17/18 | Grid/Random/Rolling 配置 + 稳定区可视化 + 四轴扰动报告；骨架线已落地（ParameterSearch.tsx，配置/结果双区空态 + 字段字典取真实引擎），真实搜索联调待 C-17/18 VALIDATED |
| `FE-7` | WFO/OOS + 过拟合判定 UI | P2 | frontend-dev | 未设 | `CODE_READY` | `FE-0` + C-19/20 | 三段编排 + OOS 结果 + PBO；**OOS 区与训练区视觉隔离**（防误用）——骨架已固化 slate(IS)/indigo(OOS)+警示；真实数据联调待 C-19/20 CODE_READY 后 |
| `FE-8` | Regime + 报告导出 UI | P3 | frontend-dev | 未设 | `CODE_READY` | `FE-0` + C-22/23 | Regime 标签可视化 + 报告导出（含 datasetVersion/strategyVersion/codeVersion 溯源）；骨架已落地并如实注明报告导出服务尚不存在；真实标签联调待 C-22.1 CODE_READY 后 |
| `FE-9` | 复盘纪律 + 生产闭环 UI | P3 | frontend-dev | 未设 | `CODE_READY` | `FE-0` + C-24/25 | 纸面交易全生命周期 + 纪律看板 + 生产闭环；骨架已含**R7「非研究可信度口径」琥珀隔离条**；真实数据联调待 C-23/24/25 CODE_READY 后 |

---

## 4. 后台运行任务快照（辅助追踪，非状态字段）

> 引用：当日工作日志（2026-09-06 20:25）。**仅供上下文，任务状态一律以 §0.1 状态字段为准。**

| 后台任务 | 进程 | 状态 | 说明 |
|---|---|---|---|
| A OHLCV 回填 | 已结束 | ✅ 完成 | 8,891,118 行/5796 股/1863 交易日 FULL，重复 0 |
| G Industry 回填 | 已结束 | ✅ 完成 | 5212 行/5212 股/83 行业，340 无行业为真实退市/ST（failed=0） |
| C+E 状态+流动性 | task `KmWNCk` | 🔄 运行中 | 二次重启（ECONNRESET 修复后：DB 写重试+逐股隔离，resume 跳过已回填） |
| D 全量 CA+Adj | — | 等待 | C+E 完成通知后手动启动（BaoStock 单 Session 串行） |

---

## 5. 更新记录（append-only，禁止删改历史）

> 每次实质性推进（状态升级/降级、负责人/优先级/截止变更、分组结构调整）在此**追加**一条：`YYYY-MM-DD HH:mm — 内容摘要（涉及任务、变更、证据出处）`。

- **2026-09-06 20:30 — 创建本任务追踪文档**。分组：G1 数据回填与认证 8 项（D-A~D-H）+ G2 编码开发 29 项（C-12.5.1~C-25.1），共 37 项。状态集合唯一出处 ROADMAP §7（7 态），更新规则见 §0。G1 初始状态取自 ROADMAP §44.2（20:02 实查）＋当日日志（20:25）；G2 初始基线 `DESIGN`。核心决策：G2 不被 G1 阻塞（§39 解耦），编码先达 `CODE_READY`，`VALIDATED/RESEARCH_READY` 待数据就绪后按 §0.2 汇合。
- **2026-09-06 20:53 — C-12.5.1 DESIGN → CODE_READY**。首个 P0 编码任务落地：新增 `server/historicalState/`（types.ts 领域类型 + mappers.ts DB 行→领域对象纯映射 + reconstruct.ts asOf(T) 纯函数核心 + db.ts 真实 DB 加载器 querySecurityHistoricalState + index.ts 统一出口），单测 28 例（reconstruct 19 + mappers 9）全过、`tsc --noEmit` 干净，下游 security/securityStatus/corporateActions/marketData/dbBarStore 325 例通过（唯一失败 server/marketData.test.ts 为真实库直连的旧业务表测试，零交集、环境性既有问题）。退出条件中 VALIDATED 待 A~H 全 DATA_READY 后按 §0.2 汇合（当前 C/D/E/G 回填中）。
- **2026-09-06 21:59 — C-12.5.2 DESIGN → CODE_READY**。PIT/反泄漏抽样验证落地：新增 `server/historicalState/audit/`（types.ts 审计领域类型 + oracle.ts 独立朴素 PIT 预言机 + checkers.ts 12 项稳定 ID 泄漏检查 + db.ts 真实 DB 抽样器/事实加载 + runAudit.ts 编排 + index.ts 出口），审计期望仅复用 intervalContains/code-ownership 基础工具、不调用被测对象；CLI `scripts/runStep125PitAudit.mts`（--seed/--budget/--data-ready/--out，显式 `process.exit` 修复 CLI 挂起）。单测 oracle 12 + checkers 17 全过、`tsc --noEmit` 干净、下游 325 例无新回归（唯一失败仍为 server/marketData.test.ts 环境既有）；真实 DB smoke（seed=20260906 budget=2）23 计划/23 查询/0 errors/0 failures、industry PIT guard exercised 3、gate=INCONCLUSIVE（dataReady=false），报告 `docs/step12-evidence/pit_audit_smoke.json`。VALIDATED 依赖 A~H 全 DATA_READY（§0.2 禁越级）。
- **2026-09-06 22:45 — C-12.6.1 DESIGN → CODE_READY**。Research Dataset 构建器落地：新增 `server/researchDataset/`（types.ts 领域类型权威源——ResearchDatasetRequest/Input/Row/UniverseDefinition/DataSnapshot/Gate + builder/row schema 版本常量；validate.ts 请求归一化与 6 类校验；version.ts canonicalStringify（键字典序）→ datasetVersion `rd-1.0.0-1-<sha256前16hex>` + rowsFingerprint 确定性；universe.ts 逐日 PIT asOf 决议（复用 STEP 11 resolveHistoricalUniverse 默认拒绝语义）；assemble.ts 全代码 code-ownership 过滤（industry/CA 归属防代码复用串扰）+ projectStateToRow 行投影；db.ts 真实 DB 批量加载器 + index_daily 派生日历；builder.ts buildResearchDataset 编排：normalize→static→universe→逐日 facts→assemble→dataSnapshot（A~G 域统计+coverageGaps）→datasetVersion→gate FAIL/PASS/INCONCLUSIVE）。CLI `scripts/runStep126BuildDataset.mts`（--name/--from/--to/--as-of/--data-ready/--max-days/--max-securities/--out，显式 `process.exit` 防连接池挂起）。单测 21 例全过（validate 6 + version 6 + assemble 9 含 code 复用防串扰用例）、`tsc --noEmit` 干净、下游回归 714 例无新回归（唯一失败 server/marketData.test.ts 环境既有、零交集）。真实 DB smoke（2026-09-01→04）36s：gate=INCONCLUSIVE、datasetVersion `rd-1.0.0-1-cffc2a0e66efbf0b` 两次运行一致（确定性）、4 交易日 index 覆盖 4、行数 0（universe 0 成员：C 域 research_security_status_history 仅 211 行 ST/SUSPENSION 样本、无 TRADING/LISTING 正向记录 → 全量 TRADING_UNKNOWN/DELISTED 默认拒绝，诚实反映数据未就绪），coverageGaps=NO_ROWS_BUILT。**顺带修正**：C-12.5.2 行状态因跨 turn Edit 丢失回退 DESIGN，已恢复 CODE_READY（与 §5 21:59 记录一致）。VALIDATED 依赖 A~H 全 DATA_READY（§0.2 禁越级）。
- **2026-09-06 23:10 — C-12.6.2 DESIGN → CODE_READY**（并行 agent A1 交付，协调者复核）。Research Dataset 策略元数据与版本快照落地：新增 `server/researchDataset/policy.ts`（RESEARCH_DATASET_POLICY_SCHEMA_VERSION="1"、POLICY_ORDER、9 类 value 接口、derivePolicySet/deriveExpectedPolicyValue/computePolicySetFingerprint/indexPolicyById——9 类 = PIT/survivorship/corporate-action/adjustment/industry/liquidity/universe-membership/calendar-trading-days/knowledge，每类含 policyId/class/name/description/value（机器可读）/evidence（可审计字段），映射表以 ROADMAP §12+§45.2 + C-12.6.1 实际语义为准；adjustment 如实声明 priceBasis="raw"）、`policyValidate.ts`（validateResearchDatasetPolicyConsistency：9 类齐备/重复/顺序、value 键白名单、声明值 vs 派生期望值 canonical 相等、交叉语义冲突——缺 asOf、PIT mode 与 asOfDescription/行 asOf 冲突、日历窗口/数量、snapshot.request 相等；空壳数据集不误报）、`versionSnapshot.ts`（buildDatasetVersionSnapshot/serialize/parse/computeFingerprint/summarize）; additive 改 `types.ts`（ResearchDataset.policySet 必选 + RESEARCH_DATASET_PRICE_BASIS="raw"）、`version.ts`（导出 canonicalStringify）、`builder.ts`（两个 return 路径均附 policySet）、`index.ts`（3 re-export）。单测新增 policy.test.ts 16 例，researchDataset 共 37 全过、`tsc --noEmit` exit 0。协调者决策：不 bump rowSchema（policySet 为 request+snapshot 确定性函数，datasetVersion 语义不变）；CLI --policy-out 暂缓（留待 DATA_READY 认证阶段并入 gate）。CODE_READY；VALIDATED 依赖 A~H 全 DATA_READY（§0.2 禁越级）。
- **2026-09-06 23:10 — C-13.1 DESIGN → CODE_READY**（并行 agent A2 交付，协调者复核）。STEP 13 首个编码任务落地：新增 `server/research/datasetAccess/`（handle.ts ResearchDatasetHandle 绑定 + deriveDatasetUniverseId + assertDatasetVersionConsistent + 快照序列化；invariants.ts assertRowPitInvariant 单一事实来源——「行即 PIT」守 asOf===tradeDate 强不变量、绑定与 row→bar 双入口断言；universe.ts createDatasetUniverseProvider 实现 UniverseProvider 契约、缺失日/非交易日 FAIL FAST；bars.ts rowToCanonicalBar + createDatasetDataSource（bar.symbol=securityId，row.code 保留原文）+ DATASET_ROW_DOMAINS；slice.ts sliceRowsByDateRange 闭区间复用 assertValidDatasetRange；session.ts createDatasetSession 绑定→版本/dateRange/universeId 一致性→产出 universe+dataSource+切片；index.ts）。克制边界：只做 dataset→framework 契约适配，不实现 Feature 库本体/新 Signal 流水线/regime（属 C-22.1）。单测 datasetAccess.test.ts 27 例（逐日成员+FAIL FAST、闭区间边界、字段映射+null 透传、版本不一致 throw、确定性深比较、PIT 脏行双层拒绝、集成直喂 pipeline 验证 close/open 决策无泄漏）；`npx vitest run server/research server/researchDataset` 24 文件 365 全过、tsc exit 0。协调者补丁：`research/index.ts` 增 `export * from "./datasetAccess"`（统一出口，tsc 复核干净）。契约层类型化 DataDomain 与「按 decisionTime×证券集切片视图」升级留待后续 STEP。CODE_READY；VALIDATED 依赖数据链就绪。
- **2026-09-06 23:41 — D-G CODE_READY → DATA_READY（G Industry 全量回填完成）**。后台 task ap4YB7（4h5m）落库 `industry_assignments` 5212 行 / 5212 股 / 83 申万行业，`failed=0 invalid=0`，340 只无行业归属为真实退市/ST/特殊股（5552-5212=340 吻合）。**两个数据质量待办**：① `securityId` 全 5212 行 NULL（未填 canonical identity 列，违反 §6，需 code→research_securities 桥接一次性回填）；② `effectiveFrom` 单点 `2026-08-31`（BaoStock 仅「当前」快照、无历史变更轨迹，历史 asOf(T) 行业只能 retrievedAt 近似或另寻历史源）。identity/PIT 校验仍 PENDING，故不标 VALIDATED。按 §30 串行链重启 C+E（task `1c5ehP`，`--universe-from-db` + resume）。更新 §0.2 D-G 行、§4 后台任务表。
- **2026-09-06 23:55 — D-C/D-E 回填健壮性修复（ECONNRESET）**。C+E（task `1c5ehP`）progress 80/5552 处因 TiDB `read ECONNRESET` 崩溃（`upsertSecurityStatusIntervals` 批量插入无重试、直接冒泡 exit(1)，非 BaoStock 问题）。修复 `scripts/backfillStatusLiquidity.ts`：新增 `withDbWriteRetry`（重试 3 次指数退避，识别 ECONNRESET/ETIMEDOUT/EPIPE 等瞬时错误码 + DrizzleQueryError cause 链 + 消息关键字兜底）+ main 写入段 per-stock try-catch 逐股隔离（单只失败记 failed 继续、resume 重跑）。幂等安全（status 去重 + liquidity onDuplicateKeyUpdate）。C+E 重启为 task `KmWNCk`。更新 §4 后台任务表。
- **2026-09-06 23:45 — C-13.2 DESIGN → CODE_READY**（并行 agent B1 交付，协调者复核验证）。STEP 13 Signal/Candidate 框架落地：新增 `server/research/signal13/`（types.ts Strategy13/CandidateDayRecord/CandidateRunEvaluation/CandidateEvaluationRun（recordKind/recordVersion/fingerprint 可审计）；engine.ts runCandidateEngine 逐 tradeDate 驱动——universeDays ∩ config.dateRange 升序遍历、逐日喂 STEP 10 runResearchPipeline（feature/signal/rank/select/泄漏守卫转发既有 framework 不复制逻辑）、当日 universe 成员缺行预检 + 特征 availability 覆盖全窗口预检 FAIL FAST；evaluate.ts evaluateCandidateRun 候选层确定性统计（数量/横截面/入选稳定性/方向分布），**不复用 evaluationService**（其异步收益回测编排属 C-14.1，语义区隔见 types.ts 头注释）；validate.ts；serialize.ts round-trip + computeCandidateEvaluationRunFingerprint（sha256 防篡改）；index.ts）。单测 signal13.test.ts 18 例（多日正确性/统计/确定性/不可变/round-trip/篡改拒绝/缺行·空窗口·版本不一致·未来函数·缺数据域 FAIL FAST/真实 pctChange 特征端到端无泄漏）；26 文件 415 全过、tsc exit 0。协调者补丁：research/index.ts 增 `export * from "./signal13"`。CODE_READY；VALIDATED 依赖数据链就绪认证。
- **2026-09-06 23:45 — C-13.3 DESIGN → CODE_READY**（并行 agent B2 交付，协调者复核验证）。STEP 13 Experiment Registry §28 实验谱系落地：新增 `server/research/experimentLineage/`（types.ts ExperimentLineageRecord 15 字段 + LINEAGE_MISSING_CODES + regime 结构化 unassessed 占位（reasonCode=REGIME_NOT_ASSESSED，C-22.1 前不误报）+ outcome 可挂载 metrics/result；codeVersion.ts composeCodeVersion 注入式（package version + git HEAD 短哈希，纯模块不跑 git）；map.ts createExperimentLineageRecord/experimentToLineageRecord/snapshotToLineageRecord/mountRunOutcome——dataset_version/universe_version/cost/execution 缺省**显式 missing 不猜**；validate.ts 双档（requireResolvedRefs 默认 true §28 齐备机器检查/结构档）；serialize.ts canonical + computeExperimentLineageFingerprint；bridge.ts ExperimentLineageBridge 组合式包装既有 ExperimentRegistry 只读；index.ts）。单测 experimentLineage.test.ts 32 例；26 文件 415 全过、tsc exit 0。协调者补丁：research/index.ts 增 `export * from "./experimentLineage"`。真实接线（入口 composeCodeVersion + context 注入）留待未来 runner 编排层。CODE_READY；VALIDATED 依赖数据链就绪认证。附注：G 全量 23:41 完成（5212 行，340 无行业为退市/ST 真实缺失），C+E 已由数据链 agent 以 task 1c5ehP 重启；本轮 watcher 因阈值假设（≥5400）与实际 5212 不符已停止。
- **2026-09-07 00:17 — C-14.1 DESIGN → CODE_READY**（并行 agent C1 交付，协调者复核验证）。STEP 14 交易模拟核心落地：新增 `server/research/simulator/`（types.ts SimulationConfig/TradeSimulationRun（recordKind/recordVersion=1）/SkippedIntentEntry/PlanSkipCode/TradeSimulationInput；plan.ts planDecisionDay 纯函数——hold-while-selected 进出场、现金权重预算、T+1 冻结顺延、sell=全部可卖份额精确股数、卖先买后订单；engine.ts runTradeSimulation 多日编排 + TradeSimulationError/MissingSimulationRowError/EmptySimulationWindowError；validate.ts/serialize.ts compute+round-trip+sha256 指纹；index.ts）。差距判定：STEP 8（backtest/）已覆盖 T+1/涨跌停/停牌/手数/资金/买卖限制/成本/执行模型/审计 → **import 只读复用不重写**；缺口（新建）=候选→Order 适配（plan.ts）+多日编排（薄编排复用 STEP 8 原语，因 SignalGenerator 契约缺每股可卖视图无法精确退出）+结果绑定（TradeSimulationRun 带 datasetVersion/sourceFingerprint/params/执行假设）。设计：决策 point=close（T 收盘可见）、executionTime=下一交易日、默认 NEXT_OPEN（成交价=T+1 开盘，basePrice=次日 open 非信号日收盘）、涨跌停拦截/部分成交可配置、停牌=dataset 缺 (date,sec) 行→SUSPENDED、资金不足→INSUFFICIENT_CASH 入审计不静默；产出 EquityPoint[]/Trade[]/AuditTrail 供 C-16.1 消费；不做指标/新成本模型/regime（C-16.2/14.2/22.1）。单测 simulator.test.ts 17 例（端到端 T+1 语义/NEXT_CLOSE 对照/涨停拦截开关/停牌拒绝/冻结顺延清仓生命周期/确定性/round-trip/篡改拒绝/缺行·版本·非 close 决策·空窗口 FAIL FAST）；`npx vitest run server/research server/researchDataset server/backtest` 32 文件 498 全过、tsc exit 0。协调者补丁：research/index.ts 增 `export * from "./simulator"`。CODE_READY；VALIDATED 依赖数据链就绪认证。C-14.2/C-14.3/C-16.1 依赖本任务。
- **2026-09-07 00:17 — C-15.1 DESIGN → CODE_READY**（并行 agent C2 交付，协调者复核验证）。STEP 15 策略 Schema + 版本化落地：新增 `server/research/strategySchema/`（types.ts StrategyDocument §16 全字段结构化 + StrategyVersionRecord §17 九项追溯 + StrategyRecipe 执行配方引用 + DeclaredRule/PositionSizingDeclaration/StrategyUniverse 声明式规则；version.ts bumpStrategyVersion + classifyRequiredBumpKind（参数/文本变→minor、结构变→major，clone 语义闸门不可变）；validate.ts validateStrategyDocument/validateStrategyVersionRecord 结构化 issue（缺项/非法 semver/非法日期/参数不符）；compare.ts strategiesDeepEqual/compareStrategyDocuments；serialize.ts canonical round-trip + compute*Fingerprint sha256；map.ts strategy13ToRecipeRef/attachSignal13Recipe 兼容 Strategy13（未动 signal13 既有文件）；index.ts）。设计：Strategy13 作「执行配方引用」嵌入（其含函数不可序列化→嵌入可序列化面 featureVersions/ranking/selection，实例按 recipeId 调用方持有）；dataset version 校验 `rd-…` 形态（C-12.6.1 对齐）；codeVersion/createdAt 注入式（对齐 experimentLineage 风格）；保存/加载=canonical round-trip + 指纹复核（不做 DB 表）；生命周期状态机明确排除（C-21.1）。单测 strategySchema.test.ts 8 组 28 例（合法通过/缺字段·版本·日期·参数 issue/bump 语义+不可变/deepEqual/round-trip/指纹/§17 九项+codeVersion 注入/Strategy13 映射）；research+researchDataset 443 全过（415+28 零回归）、tsc exit 0。协调者补丁：research/index.ts 增 `export * from "./strategySchema"`。CODE_READY；VALIDATED 依赖数据链就绪认证。C-21.1/C-22.1 依赖本任务。
- **2026-09-07 00:40 — C-14.2 DESIGN → CODE_READY**（并行 agent D1 交付，协调者复核验证）。STEP 14 成本模型专项落地：新增 `server/research/costModel14/`（types.ts CostModelDeclaration/MarketImpactParams/FillCostInput/FillCostBreakdown/MarketImpactEstimate；defaults.ts A_SHARE_DEFAULT_COST_DECLARATION + 8 费率常量（佣金万 2.5/印花卖出 0.05%/过户 0.001%/滑点 10bp/一手 100/最低佣 5 元）；impact.ts estimateMarketImpact/impactBpsForParticipation/participationFromTurnover——**市场冲击显式建模**（STEP 8 仅离散分层，本任务补订单量 vs 流动性平方根律近似 impactBps=min(maxBps, coeff×p^exp)，流动性 null/≤0→IMPACT_LIQUIDITY_INVALID、超参与率结构化拒绝、enabled=false 显式 0）；compute.ts computeFillCostBreakdown 单笔五维分解（commission/stamp/transfer/slippage/impact）；mappers.ts toEngineCostModel/fromEngineCostModel（声明→engine CostModel 六字段投影，engine→声明需显式冲击参数杜绝隐式启用未标定模型）；validate.ts validateCostModelDeclaration（值域校验防负费率/超界）+ 4 稳定错误码；serialize.ts round-trip；index.ts）。差距判定：佣金/印花税/过户费/滑点原子函数 STEP 8 backtest/cost.ts **只读复用**；缺口=市场冲击 + 声明 schema + 五维分解审计 + simulator 配置面映射。单测 costModel14.test.ts 34 例（官方费率数值 1,000,000 元→佣金 250/印花卖出 500/过户 10/冲击 6 场景/schema 非法样本/round-trip/STEP 8 组合逐字段一致/mapper 消费面）；35 文件 **598 全过**、tsc exit 0。协调者补丁：research/index.ts 增 `export * from "./costModel14"` + **消歧**（costModel14 与 executionConstraints14 均导出 DEFAULT_LOT_SIZE → 从 costModel14/index.ts 顶层移除该常量、测试改从 defaults import，executionConstraints14 为唯一顶层来源，tsc 复核 exit 0）。CODE_READY；VALIDATED 依赖数据链就绪认证。
- **2026-09-07 00:40 — C-14.3 DESIGN → CODE_READY**（并行 agent D2 交付，协调者复核验证）。STEP 14 执行与约束模型落地：新增 `server/research/executionConstraints14/`（types.ts ExecutionConstraintDeclaration 六分组组合声明：capital/positions/lot/restrictions/timing/marketClaims + EXECUTION_CONSTRAINT_DECLARATION_KIND/_VERSION/DEFAULT_LOT_SIZE；factory.ts createExecutionConstraintDeclaration 默认值合并 + sortUniqueSecurityIds 规范化；validate.ts 16 类 EXCON_* issue 码 + EXECUTION_MODEL_IDS/SECURITY_BOARDS 值域；serialize.ts canonical+sha256+NaN 严格拒绝；map.ts mapExecutionConstraintDeclaration→C-14.1 SimulationConfig + describeExecutionConstraintCoverage 17 轴能力矩阵 + **诚实 blocker**（perSecurityCap/totalCap/banned 集/LIMIT_PRICE 是 STEP 8 枚举成员但 C-14.1 链不可执行 → 值域校验放行但映射报 blocker，绝不冒充已执行）+ 产物再经 validateSimulationConfig 双保险复核；index.ts）。差距判定：initial capital/lot/T+1/涨跌停/停牌/执行时机值域 STEP 8+C-14.1 已覆盖（复用）；增量=组合式声明层 + 一致性校验 + 能力矩阵 + 诚实 blocker。判定不做：「开盘后 N 分钟/收盘集合竞价」（C-12.6 行域为日线，声明即伪造）；成本模型（C-14.2 边界）；执行内核（复用 runTradeSimulation）。单测 executionConstraints14.test.ts 26 例（schema 校验 11/round-trip 4/映射 7 + 双保险复核/能力矩阵/**端到端约束生效 3**：maxPositionCount=1→MAX_POSITIONS_REACHED、涨停禁买 LIMIT_UP 拒单+对照、跌停禁卖 LIMIT_DOWN 拒单持仓不静默清仓，成交股数均 100 整数倍/确定性）；35 文件 **598 全过**、tsc exit 0。协调者补丁：research/index.ts 增 `export * from "./executionConstraints14"`。CODE_READY；VALIDATED 依赖数据链就绪认证。C-23.1 依赖本任务。
- **2026-09-07 00:40 — C-16.1 DESIGN → CODE_READY**（并行 agent D3 交付，协调者复核验证）。STEP 16 收益/风险/回撤指标落地：新增 `server/research/performanceMetrics/`（types.ts PERFORMANCE_EVALUATION_RUN_KIND/VERSION + PerformanceEvaluationInput/Run + ReturnMetrics/RiskMetrics/DrawdownMetrics/DrawdownSegment/WorstLosingStreak 字段级口径注释；analyze.ts PerformanceEvaluationError/InvalidEquityCurveError（稳定 code）+ assertValidEquityCurve + dailyReturnSeries + analyzeDrawdown + computePerformanceMetrics；evaluate.ts computeInputFingerprint/computePerformanceEvaluationRunFingerprint/evaluatePerformance + serialize/deserialize round-trip + assertValidPerformanceEvaluationRun；index.ts）。差距判定：Total Return/CAGR（`(end/start)^(252/n)−1` 交易日口径几何 CAGR，与 STEP 8 annualizedReturnPct **无算法冲突**，差异仅在锚点：STEP 8 用 initialCapital vs C-16.1 用 equityCurve[0].equity，C-14.1 输出两者重合已测试验证）/MaxDD 深度 STEP 8 已覆盖（对照 maxDrawdownFromEquity 一致）；**缺口=MaxDD 起止/时长/恢复（DrawdownSegment：peak/trough/recovery 日期+交易日+自然日时长，自然日差用纯函数儒略日转换无 Date 对象）、回撤剖面阈值分段（drawdownSegments/drawdownEpisodeCount）、Recovery Factor（口径=总收益÷|MaxDD|，非 CAGR÷MaxDD=Calmar 留给 C-16.2）、下行偏差/连亏段/极值分位数/偏度峰度 tail risk**。退化输入响亮失败（空/单点 INSUFFICIENT_EQUITY_CURVE、非有限/非正 equity INVALID_EQUITY_VALUE、乱序/重复/非法日期、无回撤 recoveryFactor=null 分母 0）；双指纹（inputFingerprint 绑定来源+记录内容指纹）深冻结防篡改。克制：Sharpe/Sortino/Calmar 属 C-16.2、交易质量属 C-16.3（留扩展槽不实现）。单测 performanceMetrics.test.ts 40 例（手算 CAGR 252 日 +10%/MaxDD 13.636% 深度与起止/恢复 + 5 交易日/7 自然日时长/Recovery Factor=11/15/回撤剖面 5·10·20 阈值分段/未恢复段 recoveryIndex=null/深度并列取更早 peak/连亏 −14.307% 手算/下行偏差半手算/分位数 p05=−5%/STEP 8 对照含锚点分歧演示/确定性/深冻结/round-trip/篡改拒绝）；35 文件 **598 全过**、tsc exit 0。协调者补丁：research/index.ts 增 `export * from "./performanceMetrics"`。CODE_READY；VALIDATED 依赖数据链就绪认证。C-16.2/C-16.3 依赖本任务。
- **2026-09-07 01:26 — C-16.2 DESIGN → CODE_READY**（并行 agent E1 交付，协调者复核验证）。STEP 16 风险调整指标落地：新增 `server/research/riskAdjustedMetrics16/`（types.ts RiskAdjustedEvaluationInput（equityCurve/trades/annualizationFactor/rfAnnualPct/downsideTarget）/RiskAdjustedMetrics/RiskAdjustedEvaluationRun（RISK_ADJUSTED_EVALUATION_RUN v1）；analyze.ts computeSharpeRatio/computeSortinoRatio/computeCalmarRatio/computeDownsideDeviationPct/computeRiskAdjustedMetrics；evaluate.ts evaluateRiskAdjustedMetrics + computeRiskAdjustedRunFingerprint + serialize/deserialize + assertValid；index.ts）。差距判定：日收益序列 dailyReturnSeries/回撤 analyzeDrawdown/曲线校验 assertValidEquityCurve C-16.1 **import 复用**；下行偏差 C-16.1 仅年化 % 结果未导出纯函数 → 独立实现 downsideDeviationDaily（同定义）+ C-16.1 对照测试锁定；Sharpe/Sortino/Calmar 计算器全新建。**STEP 8 sharpeRatio 口径核对**：shared = mean(r)/sampleStd(r)·√252、rf 固定 0 → 等价，差异仅 rf 参数化（rf=0 逐位一致测试锁定；STEP 8 round 4 位、本层不 round）。设计：Sharpe=mean(e)/sampleStd(e)·√252（e=日收益−日 rf）；Sortino=(mean(e)·ann)/(下行偏差·√ann) 算术年化对齐 STEP 8；Calmar=几何 CAGR/|MaxDD|（复用 C-16.1 cagrPct/maxDrawdownPct）；**rfAnnualPct 默认 0**（对齐 STEP 8/C-16.1 无隐藏假设）；downsideTarget 作用超额空间；退化：样本<2/零波动/无下行/无回撤→显式 null（记录成分恒定义），曲线/参数非法→响亮抛错。单测 19 例（Sharpe/Sortino/Calmar 手算、与 STEP 8 computeMetrics.sharpeRatio 对照、rf/年化因子参数生效、退化、round-trip/确定性）；38 文件 **684 全过**、tsc exit 0。协调者补丁：research/index.ts 增 `export * from "./riskAdjustedMetrics16"`。CODE_READY；VALIDATED 依赖数据链就绪认证。
- **2026-09-07 01:26 — C-16.3 DESIGN → CODE_READY**（并行 agent E2 交付，协调者复核验证）。STEP 16 交易质量与稳定性指标落地：新增 `server/research/tradeQualityMetrics16/`（types.ts TradeQualityEvaluationInput/TradeQualityMetrics/TurnoverMetrics/MonthlyReturnEntry/YearlyReturnEntry/MonthlyConsistencyMetrics/YearlyConsistencyMetrics/TradeQualityRegimePerformance（unassessed + assessed 扩展槽）/TradeQualityEvaluationRun；analyze.ts monthKeyOf/yearKeyOf（字符串切片无 Date 对象）+ assertValidTrades + computeTurnoverMetrics + computeAverageHoldingPeriodDays + computeMonthlyConsistency + computeYearlyConsistency + unassessedRegimePerformance + isRegimeUnassessed + computeTradeQualityMetrics；evaluate.ts evaluateTradeQualityMetrics 装配 + 双指纹 + serialize/deserialize/assertValid；index.ts）。差距判定：WinRate/PF/Expectancy/avgWin/avgLoss/completedTradeCount/Trade Count STEP 8 Metrics 已实现 → **复用 computeMetrics import 只读**（同输入逐位一致测试对照锁定；winRate 分母=完成交易、PF 无完成交易=0 的 STEP 8 语义原样保留，不做第二套）；**缺口新建**=Turnover（双边名义额/平均资产，annualized=gross×252/(avgEquity×(点数−1)) 文档化 trade 级近似）、Average Holding Period 聚合（max(1, idx差+1)，回退 trade.holdingPeriod）、月度/年度一致性（月收益=该月实现日收益连乘−1，跨月边界按实现日归属，月/年连乘恒等于区间总收益——望远镜恒等测试断言，空月不伪造）、regime 表现（**unassessed 占位 reasonCode=REGIME_NOT_ASSESSED 不编造标签**，assessed 扩展槽类型就位，C-22.1 后填充）。输入直食 (equityCurve, trades, annualizationFactor)（C-16.1 Run 记录不含原始曲线），与 C-16.2 共享 C-16.1 computeInputFingerprint 同载荷指纹互链。单测 37 例（winRate 75%/PF 0.7419/expectancy −40/avgHolding 2.25/turnover 17180/月 +21·−19·+10.25/年 +21·−5.5 手算 + STEP 8 对照 + 回退路径 + 退化/非法输入 + regime 占位 + round-trip 篡改）；38 文件 **684 全过**、tsc exit 0。协调者补丁：research/index.ts 增 `export * from "./tradeQualityMetrics16"`。CODE_READY；VALIDATED 依赖数据链就绪认证。
- **2026-09-07 01:26 — C-17.1 DESIGN → CODE_READY**（并行 agent E3 交付，协调者复核验证）。STEP 17 Grid/Random Search 落地：新增 `server/research/parameterSearch17/`（types.ts SearchRun/Candidate/评估契约/稳定区口径类型；prng.ts mulberry32 + Floyd 无放回；sampler.ts gridParameterSets/sampleRandomParameterSets；metrics.ts 标量校验 + C-16.1 只读桥；region.ts analyzeCandidateRegion/resolveRegionAnalysisConfig——**稳定区判定拒绝高收益坏点**（合格=收益≥门槛且回撤≤门槛，高收益高回撤归坏点排除，显式非 argmax；坏点率=坏点/成功样本超限→degraded，requireStableCandidates 时仅 stable 产候选；成员收益降序确定性排序，聚合均值/中位数）；candidate.ts buildCandidateStrategies（kind="candidate" 非 production，含参数+绩效摘要+searchRunId+sampleIndex 溯源+指纹，无 promotion 代码）；serialize.ts fingerprint + round-trip + validate；run.ts runParameterSearch/runGridSearch/runRandomSearch + searchRunId/createdAt 注入；index.ts）。差距判定：Grid 全组合 → STEP 6.3 combinationGenerator **import 只读桥接不重写**；空间指纹复用 sweep.computeParameterSpaceFingerprint；**Random 采样器不存在**（db.ts mulberry32 私有）→ 新增 seedable PRNG + Floyd 无放回（样本落 grid 同格点保持可比）；稳定区判定 reference parameterStability 思路（复用函数面有限）；候选策略产出/完整 SearchRun 实验记录（method/seed/budget/空间指纹/被评样本/结论/候选/createdAt/fingerprint，哲学对齐 experimentLineage）全新建；evaluator 桥 type-import C-16.1 performanceMetrics 只读映射。克制：不做 Rolling（C-17.2）、不做 PBO 正式判定（overfittingAssessment 已有）、不做 candidate→Final 推广。单测 30 例（Grid 对照/随机确定性/不同 seed/混合格点/好区坏区/坏点率门/insufficient/no-qualified/候选语义与指纹自洽/记录 round-trip/篡改检测/端到端闭环（假 evaluator 证明搜索→评估→候选）/非法指标与抛错转记/退化校验）；38 文件 **684 全过**、tsc exit 0。协调者补丁：research/index.ts 增 `export * from "./parameterSearch17"`。CODE_READY；VALIDATED 依赖数据链就绪认证。C-17.2/C-18.1 依赖本任务。
- **2026-09-07 12:26 — C-17.2 DESIGN → CODE_READY**（并行 agent F1 交付，协调者复核验证）。STEP 17 Rolling Optimization 落地：新增 `server/research/rollingOptimization17/`（types.ts 类型/常量权威源 + RollingOptimizationRequest/Run/Candidate；windows.ts generateRollingOptimizationWindows/resolveRollingWindowConfig/validateRollingTradeDates——**交易日锚定**窗口生成（tradeDates 索引切片、window/step 按交易日计、支持平铺/滑动/最大窗数、不足窗响亮报错，决策依据：窗内评估消费逐交易日数据流 simulator→C-16.1，日历天会卷休市空隙，纯索引天然无未来数据；expanding 模式克制不做）；stability.ts analyzeRollingConsistency/resolveRollingStabilityConfig/rollingParameterSetKey——跨窗一致性（一致=参数在每个被评估窗都 qualified，合格区交集**显式非 argmax**，坏点无论多高收益都否决；minEvaluatedWindows 缺省 2 单窗不算稳定；绩效聚合均值/中位数/最差不取极值）；candidate.ts buildRollingOptimizationCandidates；run.ts runRollingOptimization/generateRollingOptimizationRunId（每窗 searchRunId=`${runId}-w<i>-search`、random 派生 seed=seed+i、createdAt 注入确定性）；serialize.ts canonical + compute/serialize/deserialize/validate round-trip；index.ts，42 顶层符号域前缀 Rolling*/ROLLING_* 全库查重零冲突）。差距判定：STEP 6.5 walkForward.ts 窗口生成纯函数（rolling/expanding/日历天/指纹）**生产形态**——复用其哲学但**不 import**（研究链路交易日锚定、无 Train/Validation/OOS 三段）；窗内搜索复用 C-17.1 runParameterSearch/resolveRegionAnalysisConfig import 只读 + combinationGenerator 基数 + sweep 空间指纹 + canonicalStringify；**C-17.2 vs C-19.1 边界**：本任务只做「按时间窗滚动逐窗全量搜索 + 描述性跨窗一致」，不做 Train→Optimize→Freeze→Test→Move Window WFO 三段编排（C-19.1/C-19.2）、不 promotion 任何候选为 Final/Production（kind=candidate 止步）。单测 29 例（窗口锚定/重叠/平铺/间隙/maxWindows/不足窗报错/与 C-17.1 直接对照/跨窗 A 稳好 vs B 单窗好他窗差非 argmax/记录 round-trip 三重篡改拒绝/确定性两次深比较/端到端闭环/空日期·空参数空间·无合格窗·缺 seed 退化）；41 文件 **777 全过**（684+29 零回归）、tsc exit 0。协调者补丁：research/index.ts 增 `export * from "./rollingOptimization17"`。CODE_READY；VALIDATED 依赖数据链就绪认证。C-19.1/C-19.2 依赖本任务。
- **2026-09-07 12:26 — C-18.1 DESIGN → CODE_READY**（并行 agent F2 交付，协调者复核验证）。STEP 18 鲁棒性测试（第一批）落地：新增 `server/research/robustness18/`（types.ts 轴/扰动条目/阈值/结论/Run/Request 契约 + ROBUSTNESS_AXIS_COST/SLIPPAGE/PARAMETER/EXECUTION；costStress.ts generateCostStressVariants/generateSlippageStressVariants——成本声明各分量 ×0.5/×1/×2/×5 档扰动含 ×1 基准、产物**必须通过 costModel14 validate** 断言、超限 skipped 不 clamp；parameterPerturbation.ts generateParameterPerturbationVariants（ResearchParameterSet 数值参数 ±% /±档 局部邻域，与 C-17.1 全局格点几何不同故独立实现、复用其参数形态+指纹哲学）；executionPerturbation.ts generateExecutionPerturbationVariants；drift.ts assessRobustnessSensitivity/computeRobustnessDrift/buildRobustnessConclusion（漂移口径=收益绝对差>5pp 双向敏感 + 回撤恶化>3pp 单向敏感；resolveRobustnessThresholds）；evaluate.ts runRobustnessStress（每扰动调 evaluator 一次且传入扰动配置——假 evaluator 记录调用断言；单轴运行保证归因，LIMIT_PRICE/不可执行轴拒绝扰动）；serialize.ts 双指纹 + round-trip + validateRobustnessRun + toRobustnessMetricsView 桥；index.ts，命名全库查重零冲突）。差距判定：既有成本/执行声明层（costModel14/executionConstraints14）、C-17.1 稳定区、STEP 6.5 parameterStability（只统计 Frozen 参数跨窗分布不评估扰动绩效）均非「已评估策略的扰动重估+漂移敏感归因」→ 四轴扰动器/编排/判定/RobustnessRun 全部真实缺口。克制：不做 MC/Bootstrap/Trade Order Randomization（C-18.2 专属，RobustnessAxis 联合可加轴留扩展槽不预埋）、不做 regime 扰动（C-22.1）、不跑真实回测（evaluator 注入测点）。单测 34 例（滑点×5 生成正确+validate 通过+含×1 基准/独立轴/参数±档枚举/执行扰动集/重估编排逐扰动 evaluator 调用/「成本×5→收益大降」成本敏感与「参数±20%→稳定」不敏感断言/Run round-trip 篡改拒绝/确定性/9 类退化输入）；41 文件 **777 全过**（684+34 零回归）、tsc exit 0。协调者补丁：research/index.ts 增 `export * from "./robustness18"`。CODE_READY；VALIDATED 依赖数据链就绪认证。C-18.2/C-20.1 依赖本任务。
- **2026-09-07 12:26 — C-21.1 DESIGN → CODE_READY**（并行 agent F3 交付，协调者复核验证）。STEP 21 策略生命周期状态机 + 审计落地：新增 `server/research/lifecycle21/`（types.ts STRATEGY_LIFECYCLE_STATUSES 8 态 + STRATEGY_LIFECYCLE_TRANSITIONS 迁移表 + StrategyLifecycleRecord/LifecycleTransition/LifecycleEvidenceRef；transition.ts applyLifecycleTransition——相邻前进+回退白名单（Production→Research 显式长程例外）+任意态→Retired、Retired 终态复活=bump 新版本新壳 genesis、genesis 只许 Draft/Research，四要素（timestamp/reason/experiment/evidence）缺失拒绝；gates.ts 证据门槛——→Validated/→Production 需 datasetGate PASS evidence 引用（格式+必填性校验，不执行真实验证留未来 runner 接线）；ledger.ts StrategyLifecycleLedger in-memory 账本；serialize.ts/validate.ts verifyLifecycleTransitionChain append-only 篡改断链 + round-trip + 指纹；map.ts createLifecycleFromVersionRecord（strategySchema StrategyVersionRecord 壳层绑定，**strategySchema 无需加 status**）+ createStrategyLifecycleRecord；conceptMap.ts mapStrategyLifecycleToTaskStatus/assertLifecycleTaskMappingIntegrity——**两套状态机概念隔离**：§7 是子系统交付成熟度（7 态）vs §23 是策略版本治理（8 态），有损汇报映射唯一 Validated→VALIDATED + 守卫断言；index.ts，命名 LIFECYCLE 域前缀零撞名）。差距判定：strategySchema（版本化/§17 追溯，无 status 字段已核实→壳层补齐）、experimentLineage（§28 谱系不含生命周期→壳内引用）、status.ts 4 态（实验状态机，与 §23 8 态不同机器→独立域前缀）。克制：不做 DB 持久化表（纯记录+in-memory 账本）、不做生产执行（C-25.1 闭环）。单测 lifecycle21.test.ts 30 例（合法全链 Draft→…→Retired/跳级·复活拒绝/四要素缺失/append-only 篡改断链/证据门槛/版本壳 round-trip/指纹确定性/7 态映射断言/账本+边分类）；41 文件 **777 全过**（684+30 零回归）、tsc exit 0。协调者补丁：research/index.ts 增 `export * from "./lifecycle21"`。CODE_READY；VALIDATED 依赖数据链就绪认证。C-25.1 依赖本任务（与 C-22.1/C-24.2 汇合闭环）。
- **2026-09-08 22:55 — FE-5 DESIGN → CODE_READY（绩效仪表盘骨架线）**。研究 run/metrics 端点（FE-0 扩展）未暴露 + C-14/16 未 VALIDATED → 骨架线先行（§48.4 插入节点总则）：新增 `client/src/pages/PerformanceDashboard.tsx`（路由 `/performance` + AppShell「研究数据」加「绩效仪表盘」）。结构：数据就绪门（三前置：run/metrics 端点 / C-14·16 VALIDATED / 结果到手）+ 指标卡 11 项（口径字典取真实引擎契约 PerformanceMetrics + C-16.2/16.3，值全「—」）+ Equity/Drawdown 双曲线占位（不引图表库）+ Trade 明细表（列头=engine/domain Trade 契约）+ 月/年一致性 + Regime 表现空态 + TechnicalDetails 联调清单。**纪律**：不显示假数值、不缓存假样例、不引入第二套指标口径。**验证**：`tsc --noEmit` exit 0、`vite build` 通过、client 17 测试全过。真实曲线联调待 C-14/16 VALIDATED（P2 后）。
- **2026-09-07 14:35 — FE-4 DESIGN → CODE_READY（策略编辑器 + 运行工作台）**。`strategy.validate/bump/compare` + `lifecycle.describe/transition` 契约 FE-0 已暴露（shared 零改动），本次纯前端消费方：新增 `client/src/pages/StrategyEditor.tsx`（路由 `/strategy-editor` + AppShell「研究数据」加「策略工作台」）。三 Tab：① 策略编辑器——JSON 透传完整 StrategyDocument（§16）→ validate 结构化 issue 表（code/path/message），fingerprint 占位明示；② 版本化——bump（major/minor/patch）+ compare 字段级 diff（忽略 version/fingerprint）；③ 生命周期——describe 状态机（8 态 chips + 迁移表，后端常量唯一事实来源）+ transition 表单（to 下拉按 transitions[currentStatus] 动态过滤/四要素/evidence JSON），返回新 record（status 徽标 + transitions 历史链 + fingerprint）。**关键取舍**：① 模板（合法 StrategyDocument + 合法 genesis 生命周期壳）由后端纯函数生成一次内嵌前端常量，前端只透传不重算 fingerprint/chain hash（§31）；② 领域对象 `z.custom` 透传、后端权威校验、页面不复制 schema；③ 迁移失败后端抛错、页面原样展示 tRPC error 不吞异常；④ `to` 断言复用 shared `StrategyLifecycleStatusValue`。**验证**：`tsc --noEmit` exit 0、`vite build` 通过（14.6s）、回归 70/70（strategySchema 28 + lifecycle21 30 + 契约 12）+ **端到端 smoke 6 项全过**（createCaller 实走：validate.valid=true / bump 1.0.0→1.1.0 / compare 定位 name 差异 / describe 8 态 Draft→[Research,Retired] / transition Draft→Research status=Research / 跳级 Draft→Candidate 被拒）。
- **2026-09-07 14:22 — FE-3 DESIGN → CODE_READY（Research Dataset 构建器）**。`researchDataset.build` 契约 FE-0 已暴露（shared 零改动），本次纯前端消费方：新增 `client/src/pages/DatasetBuilder.tsx`（路由 `/dataset-builder` + AppShell「研究数据」加「数据集构建」）。左侧配置表单（名称/from/to/**逐日 PIT vs 固定 asOf 联动**/maxTradingDays≤40/maxSecuritiesPerDay≤1000/dataReady；默认对齐 RPC 护栏 5 日/50 股/false，明示 dataReady=false → gate 至多 INCONCLUSIVE）；右侧 mutation 结果（isPending skeleton/error alert/空态齐备）。只读渲染三块：总览（datasetVersion mono + gate 徽标原样着色 + rowCount + gateNotes）、Tabs 数据快照（请求规范化回显/域快照表/coverageGaps）/ 9 类口径 policySet（机器可读 value 逐键渲染）/ 成员决议 universe（每日成员数+排除统计，members id 列表只汇总）。summary 的 universeDefinition/policySet/dataSnapshot 为 shared `unknown` → 页面防御性取值 helpers 渲染（缺失显「—」不猜结构、不复制领域 schema）。**验证**：`tsc --noEmit` exit 0、`vite build` 通过（12.7s）、FE-0/FE-1/FE-2 回归 32/32。真实 build 联调依赖 M0（同步长任务，DB 回填期间可能等待较久）。
- **2026-09-07 14:15 — FE-2 DESIGN → CODE_READY（asOf(T) 历史状态查询器）**。FE-0 的 asOf 以 securityId 为键，FE-2 补用户输入 UX：shared 契约增 `historicalStateResolveInputSchema`/`codeCandidateSchema`/`historicalStateResolveResultSchema`；新增 `server/historicalState/codeLookup.ts`（`parseCodeQuery` 纯函数 + `lookupSecuritiesByCode` DB 检索 identifier history × securities master；**代码可复用故返回候选集**、primary 优先归并、DB 不可用/15s 超时 → error 不冒充「查无此代码」）；`historicalStateRouter` 增 `resolveCode`（output schema 强约束）；页面 `client/src/pages/HistoricalState.tsx`（路由 `/historical-state` + AppShell「研究数据」导航）渲染十问：Q10 可知性维度 chips 先行（KNOWN/UNKNOWN 不粉饰）→ Q1 身份 / Q2·Q3 生命周期 verdict / Q5 可交易（剔除原因原样展示）/ Q4 行业 / Q6 流动性 / Q7 价格（A 股红涨绿跌）/ Q8 公司行为（Q8a vs Q8b·Q10 双层口径分列）/ Q9 市场状态表；asOf 留空 = FULL_KNOWLEDGE 全知视角明示警告（§4 PIT）；返回 null → 黄条明示不伪造假状态。类型经 `inferRouterOutputs<AppRouter>` 后端推断，不复制领域 schema。**验证**：7 例单测全过、`tsc --noEmit` exit 0、`vite build` 通过、FE-1/FE-0 回归 32/32。真实 asOf 联调依赖 M0。
- **2026-09-07 13:55 — FE-1 DESIGN → CODE_READY（数据域健康看板）**。新增 `shared/dataHealthContracts.ts`（gate 状态/域/覆盖率/证据/实况 zod 契约唯一来源）、`server/dataHealth.ts`（**只读真实证据**：判定源自 `docs/step12-evidence/research_ready_gate.json`——certify 脚本只读 TiDB 生成，模块只读→校验→派生展示字段，不重算 gate、不粉饰 PENDING；DOMAIN_SPEC 域→gate 映射单一来源；覆盖率取最差来源保守口径；证据缺失/解析失败 → parseError 不伪造）、`server/dataHealthRouter.ts`（overview/evidence/liveCounts；`liveCounts` 带 `certified:false` 字面量类型实现认证态/实况态类型级分离）、`client/src/pages/DataHealth.tsx` + App.tsx 路由 `/data-health` + AppShell「研究数据」导航分组（legacy 壳增量）。**性能发现**：实况查库首版 UNION ALL 全表 COUNT(DISTINCT)（含 8.9M 行 stock_daily_prices）在回填并发下 >5min 挂起 → 重构为「information_schema 估算行数（O(1) 标注≈）+ 逐表精确 COUNT(DISTINCT) + 单表 25s 超时 + 超大表不查覆盖（覆盖由 gate#3 PASS 给出）」。**验证**：13 例单测全过（读真实证据断言规则一致性，不硬编码漂移值；派生规则 FAIL>PENDING>PASS、覆盖取最差、缺项保守 PENDING）、`tsc --noEmit` exit 0、`vite build` 通过。已知限制：实况端点依赖真实 DB 且与 C+E 回填并发争用，默认手动触发。
- **2026-09-07 13:32 — FE-0 DESIGN → CODE_READY（研究链路 tRPC 暴露，R6 解除）**。前端 P0 前置落地：新增 `shared/researchContracts.ts`（唯一契约来源：isoDate / historicalState.asOf / researchDataset.build / strategy.validate·bump·compare / lifecycle.transition 的 zod 入参 + 类型；领域对象 StrategyDocument/StrategyLifecycleRecord 用 `z.custom` **类型透传**由后端权威校验器判定，shared 不复制领域 schema 防双份漂移；生命周期状态枚举以传输层字面量定义并由契约单测断言与后端 `STRATEGY_LIFECYCLE_STATUSES` 完全一致）；新增 `server/historicalStateRouter.ts`（asOf → 复用 STEP 12.5 `querySecurityHistoricalState`，DB 不可用/未命中诚实返回 null 不伪造）；新增 `server/researchDatasetRouter.ts`（build → 复用 STEP 12.6 `buildResearchDataset`，**只回传摘要剔除 rows**（百万级不可经 RPC），gate FAIL/PASS/INCONCLUSIVE 原样透传不粉饰，RPC 触发强制限流默认 maxTradingDays=5/maxSecuritiesPerDay=50 防误触全历史）；新增 `server/researchRouter.ts`（strategy.validate/bump/compare 复用 C-15.1 纯函数；lifecycle.describe 状态+迁移表**取自后端常量**为唯一事实来源，lifecycle.transition 复用 C-21.1 `applyLifecycleTransition` append-only 返新记录，不满足迁移表/§23 四要素原样抛错不静默）；三 router 注册进 `server/routers.ts` appRouter。契约单测 `server/researchContracts.test.ts` **12 例全过**（枚举一致性守卫 / appRouter 注册 R6 回归锁 / 入参 schema / 纯函数端点经 caller 走通 + 跳级迁移拒绝）；`tsc --noEmit` exit 0。**纪律**：asOf 与 dataset.build 依赖真实 DB，按 §0.2 禁止 mock 冒充，单测不覆盖，真实链路验证由既有 CLI smoke 与 P1 认证承担。**回归**：47/48 测试文件通过，唯一失败 `server/research/marketRegime22/`（8 例）为**并行 agent 在途编写 C-22.1**（facts.ts 13:31 仍在改），与本次改动零重叠，非本任务回归。前端 `client/src/lib/trpc.ts` 因 `import type { AppRouter }` 自动获得类型安全调用能力。新增 §3.10 G3 前端任务组（FE-0~FE-9）完成统一任务追踪闭环。
- **2026-09-07 12:50 — 产出 DEVELOPMENT_PLAN.md（排期与分工清单）**。用户请求：按最新 ROADMAP（§48，12:40 更新）拆分排期任务。新建 `DEVELOPMENT_PLAN.md`：引用 §8/§13/§30/§37/§38/§44/§45/§48；定义里程碑 M0 数据可信（P0 dataScope 全 PASS）→ M1 `RESEARCH_READY=TRUE`（§13 全 PASS + 12.6 认证，P1-2 后）→ M2 STEP 13 VALIDATED → M3 编码补齐 → M4 生产闭环；将 §48 P0~P3 任务化（P0-1/2/3a/3b/4a/4b/4c、P1-1/2/3、P2 十一个 C-缺口含存量复用纪律、P3-0/1/2），每项含目标/验收标准/复杂度分级（S/M/L/XL + wall-clock）/关联模块/依赖/状态；批次 0~8 排期 + 分工（数据链 agent / research-dev ×N / 协调者）；R1-R5 处置映射。口径备注：正式 `RESEARCH_READY=TRUE` 按 §48.1 流程图与 §45.3 落在 P1-2 后，P0-4 只交付 dataScope 全 PASS。计划级估算，认领后校准；未改变任何任务状态判定。
- **2026-09-07 14:02 — C-18.2 DESIGN → CODE_READY**（并行 agent 交付，协调者复核验证）。STEP 18 随机化稳健性（第二批）落地：新增 `server/research/stochasticRobustness18/`（13 文件 3283 行：types.ts StochasticMethod(monteCarlo/bootstrap/orderRandomization)/StochasticSpecimen/MetricsView/Conclusion/Run/Request + STOCHASTIC_METHOD_CAVEATS 方法学警告表；prng.ts mulberry32 目录内自实现 + 有放回/无放回抽样核；sampling.ts 三法共用样本核；monteCarlo.ts/bootstrap.ts/tradeOrder.ts 三法；statistics.ts 路径统计（复用 shared/quant-stats sharpeRatio）；distribution.ts 分位/百分位 CI/基准中秩百分位/尾部概率 P(收益<0)·P(MaxDD>X)·P(Sharpe<0)；verdict.ts 结论判定**复用 C-18.1 resolveRobustnessThresholds import 只读**（收益 5pp/回撤 3pp）+ 4 类 flag + reasonCode 优先级；serialize.ts 三指纹 + round-trip；run.ts runStochasticRobustness + 三便捷入口 + id/createdAt 注入）。**诚实边界**（代码注释 + CAVEATS 双重固化）：MC 参数扰动留 C-17.1、block/stationary bootstrap 标 unassessed、BCa/bootstrap-t 偏差校正与 p 值/假设检验承诺明确不做、不跑真实回测、不接 DB。单测 50 例；全量 **45 文件 916 全过**、`tsc --noEmit` exit 0。协调者补丁：research/index.ts 增 `export * from "./stochasticRobustness18"`。新增顶层符号 88 个全库查重交集 0。**遗留**：并入 RobustnessAxis 联合需扩 robustness18/types.ts（方法字面量已与预留槽一致），建议并行两套记录而非强并。CODE_READY；VALIDATED 依赖数据链就绪认证。
- **2026-09-07 14:02 — C-22.1 DESIGN → CODE_READY**（并行 agent 交付，协调者复核验证）。STEP 22 市场状态体系落地：新增 `server/research/marketRegime22/`（12 文件：types.ts 七维契约 + REGIME_UNASSESSED_REASON_CODES 5 码 + MarketRegimeRun；config.ts 七维阈值默认值**逐条文档化依据** + resolve*Config 非法响亮抛错不 clamp；dimensions.ts 七维计算器 + buildRegimeLookbackWindow(T−N+1..T 含 T 回看) + 逐日 assertRegimeWindowPit；facts.ts 涨停判定复用 boardRules.resolveLimitRules（浮点 100×1.1 用 1e-9 容差，注释披露与执行引擎口径差异）；composite.ts 复合键（unassessed 维编码 NA:reasonCode）；attribution.ts 分组归因聚合（表现注入，未匹配显式计数）；adapters.ts C-16.3 TradeQualityRegimePerformance(assessed) + C-13.3 §28 regime 映射；serialize.ts 指纹 + PIT 不变量校验 + round-trip；run.ts/dates.ts/errors.ts）。**诚实 unassessed**：Sentiment 无数据源默认 `REGIME_SENTIMENT_SOURCE_MISSING`（不用涨停家数冒充情绪，proxy 默认关闭）；窗口不足/基准缺失/横截面空/成交额缺失分别记 `REGIME_INSUFFICIENT_HISTORY/REGIME_BENCHMARK_MISSING/REGIME_NO_CROSS_SECTION/REGIME_DATA_MISSING`，一律不降级为中性。单测 51 例（七维手算/PIT 无泄漏断言/确定性/窗口边界/数据不足 unassessed/round-trip 篡改拒绝/归因聚合）；全量 **45 文件 916 全过**、`tsc --noEmit` exit 0。协调者补丁：research/index.ts 增 `export * from "./marketRegime22"`。**遗留**：C-16.3 evaluateTradeQualityMetrics 建议增可选 regimeTags 入参走 buildTradeQualityRegimePerformance；C-13.3 mappingContext 增可选 regimeTags 走 toExperimentLineageRegime（取主导复合状态）；情绪维需新增情绪类数据表后才能评估。CODE_READY；VALIDATED 依赖数据链就绪认证。
- **2026-09-07 14:02 — C-19.1 DESIGN → CODE_READY**（子代理写出 6 文件后因模型 429 频率限制中断，协调者接手完成并修复 bug）。STEP 19 Walk-Forward 滚动窗口划分 + 冻结纪律 + 逐窗编排落地：新增 `server/research/walkForward19/`（7 文件 2400+ 行：types.ts 契约权威源——rolling/anchored 窗口模式、gap/embargo 防标签重叠、WalkForwardRun/WalkForwardRequest；windows.ts generateWalkForwardSplits 交易日锚定 Train/Test 成对窗口 + assertWalkForwardSplitInvariants（Test 严格晚于 Train 且集合无重叠的运行时断言）+ validate/resolve；freeze.ts deepFreezeParameterSet + selectWalkForwardFrozenParameters（**下中位数显式非 argmax**）+ verifyWalkForwardFreezeDiscipline（键一致/深冻结/Test 晚于 Train 机器检查）；run.ts runWalkForward 逐窗 Train→Optimize(复用 C-17.1 runParameterSearch)→Freeze→Test(evaluator 注入)→Move Window；aggregate.ts 最小 OOS 聚合 + oosDegradationPp 如实呈现；serialize.ts 指纹 + round-trip + 篡改拒绝；index.ts）。**边界**：OOS 隔离记录持久化 + 聚合报告留 C-19.2、过拟合结论留 C-20、无 promotion 代码。**协调者修复关键 bug**：子代理遗留「test.parameterSetKey 用 canonicalStringify(`{"k":2}`) 而冻结键用 rollingParameterSetKey(`"k"=2`)」格式不一致 → 冻结纪律机器检查全窗误报；已改 run.ts test 阶段统一用 `rollingParameterSetKey`（types.ts 注释本就约定复用 C-17.2）。单测 26 例（窗口几何/embargo/gap/anchored/maxWindows/不足窗报错/下中位数非 argmax/篡改 test 数据不影响冻结参数/确定性/无合格参数 skipped/test evaluator 抛错 skipped/round-trip 篡改拒绝/结构校验）；全量 **45 文件 916 全过**、`tsc --noEmit` exit 0。协调者补丁：research/index.ts 增 `export * from "./walkForward19"`。CODE_READY；VALIDATED 依赖数据链就绪认证。C-19.2/C-20.1 依赖本任务。
- **2026-09-07 14:40 — C-19.2 DESIGN → CODE_READY**（并行 agent 交付，协调者复核验证）。STEP 19 样本内/外隔离与记录落地：新增 `server/research/oosIsolation19/`（9 文件：types.ts `WindowResultRecord` Train/Test 参数与结果**字段分离** + `OosIsolationRun/Request` + `OosSkipReasonCode`；discipline.ts `verifyOosIsolation`/`assertOosIsolationDiscipline`/`assertWindowResultIsolation` 键白名单 + 无重叠 + 参数不回写 + window id 唯一机器检查；record.ts `buildWindowResultRecords` 从 WalkForwardRun 构建逐窗隔离记录 + OOS 曲线 PIT 边界断言；aggregate.ts `computeOosAggregationReport` 逐段 C-16.1/C-16.2 完整评估 + 描述性分段聚合，不下过拟合结论；ledger.ts in-memory append-only 归档账本；serialize.ts round-trip + **反序列化后隔离纪律复核**；run.ts `runOosIsolation` 主编排）。**复用**：C-19.1 `generateWalkForwardSplits`（窗口几何重放）/`computeWalkForwardAggregate`（oosDegradationPp 口径一致）、C-16.1/16.2 指标、canonicalStringify。**诚实边界**：PBO/参数敏感性正式判定/因子消融/过拟合结论明确留 C-20；OOS 权益曲线可选注入，缺窗标 `OOS19_NO_OOS_EQUITY_CURVE`（仅标量绩效，完整指标 unassessed）。单测 23 例；全量 **47 文件 974 全过**、`tsc --noEmit` exit 0。协调者补丁：research/index.ts 增 `export * from "./oosIsolation19"`。新增顶层符号 31 个全库查重交集 0。CODE_READY；VALIDATED 依赖数据链就绪认证。C-20.1 依赖本任务。
- **2026-09-07 14:40 — C-23.1 DESIGN → CODE_READY**（并行 agent 交付，协调者复核验证）。STEP 23 模拟账户与持仓落地：新增 `server/research/paperAccount23/`（9 文件 2588 行：types.ts `PaperAccount`/`PaperAccountPosition`/`PaperAccountOrder`/`PaperAccountFill`/`PaperCashLedgerEntry`/`PaperAccountRun`/`PaperPnlBreakdown` + 常量；account.ts 账户状态机纯函数 createPaperAccount/settlePaperAccountT1/applyPaperBuyFill/applyPaperSellFill/freezePaperCash/unfreezePaperCash/markPaperAccountToMarket/snapshotPaperAccount，不可变 + 权益恒等式 equity=cash+frozen+Σmv；constraints.ts `checkPaperOrder` 订单→成交约束检查 + `describePaperAccountConstraintEnforcement` 六维能力矩阵；run.ts `computePaperPnlBreakdown`（PnL 恒等式）+ `assemblePaperAccountRun` + `toTradeQualityEvaluationInput`（C-16.3 适配）；serialize.ts/validate.ts；index.ts）。**复用**：C-14.3 `ExecutionConstraintDeclaration`/`DEFAULT_LOT_SIZE`/`describeExecutionConstraintCoverage`（17 轴能力矩阵）、C-14.2 `computeFillCostBreakdown`（五维成本分解）+ `toEngineCostModel`、STEP 8 Side/EquityPoint/涨跌停口径/PositionBook 会计口径、C-16.3 `TradeQualityEvaluationInput` 适配。**诚实 blocker**：`timing.executionModel`/`allowPartialFill`/`LIMIT_PRICE` 账户层仅记录不强制执行；Signal 注入式记录不做生成；能力差异——perSecurityEquityCap/totalEquityCap/buyBanned/sellBanned 账户层有市值与订单级检查点可 ENFORCED（C-14.1 plan 层标 blocker 处账户层放行）。**命名避撞**：`PaperPosition`/`PaperOrder` 被 legacy `server/paperTrading.ts` 占用 → 改用 `PaperAccountPosition`/`PaperAccountOrder`。单测 35 例；全量 **47 文件 974 全过**、`tsc --noEmit` exit 0。协调者补丁：research/index.ts 增 `export * from "./paperAccount23"`。新增顶层符号 47 个全库查重零冲突。**遗留**：C-14.2 `computeFillCostBreakdown` 零冲击卖出产生 `-0`（JSON round-trip `-0→0`，金融语义等价，超本任务边界未改）；frozen 冻结/解冻何时触发属 C-23.2 编排。CODE_READY；VALIDATED 依赖数据链就绪认证。C-23.2 依赖本任务。
- **2026-09-07 19:30 — C-20.1 DESIGN → CODE_READY**（并行 agent 交付，协调者复核验证+修复）。STEP 20 PBO/参数敏感性落地：新增 `server/research/overfittingDetection20/`（8 文件 3450 行：types.ts 契约（已修：移除 C-18.1 阈值重复定义、改 import from robustness18 re-export）+ pbo.ts CSCV 划分+倒置统计+零分布+分位 CI+判定+指纹 / parameterSensitivity.ts 复用 C-18.1 generateParameterPerturbationVariants + evaluator 注入 + 漂移判定 + 度量 / assess.ts Overfitting 聚合判定 / serialize.ts 三类记录 round-trip+篡改拒绝 / run.ts runOverfittingDetection+Run ID 注入 / index.ts；test.ts 55 例 9 大组）。**复用**：C-18.1 `generateParameterPerturbationVariants` + `DEFAULT_*_THRESHOLD_PCT`（**纪律修复**：子代理初版 types.ts:586-590 重复定义两阈值=3/5，违反"复用而非重写"，协调者改为 `import from "../robustness18"` re-export）；C-19.2 OOSRun 端到端接入（pbo 喂入 OOS 段总收益，684ms 跑通真实链路）；shared/quant-stats percentile/mean/stdDev；canonicalStringify；ResearchValidationError。**诚实边界**：CV-PBO/贝叶斯收缩/p-value 假设检验明确不做；PBO 不足候选/块显式 unassessed+reasonCode；不下"能否上线"结论（C-25.1）。**命名避撞**：`Pbo*`/`Overfitting*` 已被 legacy STEP 6.5 占用 → 重命名为 `Ofd*`（OfdPboResult/OfdAssessmentInput/assessOfdOverfitting 等 22 类型+8 别名+11 常量+20 函数）。**冲突模拟**：export * 三个模块（legacy pbo + legacy overfittingAssessment + 新 overfittingDetection20）一起导出 → 0 冲突。**协调者违规接受**：C-20.1 子代理未改既有 index.ts（按要求）；C-23.2 子代理**违反约定**改了 index.ts 做 `toTradeQualityEvaluationInput` 消歧（paperAccount23/signalToPnl23 同名），接受其解决 + 补 C-20.1 导出。单测 55 例；全量 **49 文件 1047 全过**、`tsc --noEmit` exit 0。CODE_READY；VALIDATED 依赖数据链就绪认证。C-20.2 依赖本任务。
- **2026-09-07 19:30 — C-23.2 DESIGN → CODE_READY**（并行 agent 交付，协调者复核验证+接受违规）。STEP 23 信号→订单→成交→PnL 闭环落地：新增 `server/research/signalToPnl23/`（8 文件：types.ts SignalToPnlRun + PnlLoop* 子结构 + 注入式价格源/信号选择器；errors.ts SignalToPnlError 18 稳定码；engine.ts runSignalToPnlLoop 主编排 a/b/c/d 四步=决策日 rebalance 算单+冻结资金→执行日 checkPaperOrder+computeFillCostBreakdown+applyBuy/SellFill+unfreeze 释放+markToMarket→次日开盘前 settlePaperAccountT1；run.ts toTradeQualityEvaluationInput C-16.3 适配+toPaperAccountRunView；serialize.ts canonical+sha256+round-trip+篡改拒绝；validate.ts；index.ts；test.ts 18 例）。**复用**：C-13.2 CandidateEvaluationRun（信号来源+指纹+point=close 校验）、C-23.1 8 原语+checkPaperOrder+computePaperPnlBreakdown+describeExecutionConstraintCoverage、C-14.2 computeFillCostBreakdown+toEngineCostModel、C-14.3 DEFAULT_LOT_SIZE+声明指纹、C-14.1 planDecisionDay（候选→Order，绝不重写）。**PIT 时序纪律**：决策日 D 订单 executionTime=D+1、成交价严格 D+1 open（非 D 收盘，避免 look-ahead）、冻结金额用 D 收盘算（盘后已知 PIT 安全）。**frozen 设计兑现**：买单 freezePaperCash → 成交 applyPaperBuyFill 扣减冻结+记 ledger → 拒单/未成交 unfreezePaperCash 释放；卖单用 T+1 可卖股数检查。**协调者接受违规**：C-23.2 子代理**违反约定改了 index.ts** 做 `toTradeQualityEvaluationInput` 消歧（paperAccount23 改显式列举跳过同名、signalToPnl23 主导）；接受其合理解决。**诚实边界**：Trade Journal/Planned vs Actual/Deviation/Reason/Emotion/Post-review 明确留 C-24.1；PnL 归因留 C-16.1；promotion 留 C-25.1；C-23.1 标注的"仅记录不强制执行"约束保持一致不冒充。单测 18 例；全量 **49 文件 1047 全过**、`tsc --noEmit` exit 0。CODE_READY；VALIDATED 依赖数据链就绪认证。C-24.1 依赖本任务。
- **2026-09-07 18:06 — 新增 §0.4 开发活动状态字段（pending/active/done/blocked，正交于 7 态）+ §0.5 顶部 active 概览 + §0.6 活动历史 append-only**。用户「正在开发的任务单独加个状态」——按 §0.1 7 态铁律"状态列只填 7 态值"，不能在状态列加新值；改在 `负责人` 列追加 `[活动值]` 后缀做轻量标记（不动列结构、不污染 7 态）+ 顶部独立概览 + 活动历史 append-only 防篡改。`active` ↔ `done`/`blocked` 变更必须入 §0.6 与 §5 双留痕。首条留痕：C-20.1、C-23.2 18:06–19:30 全程。
- **2026-09-07 19:20 — C-23.2 DESIGN → CODE_READY**（并行 agent 交付，协调者复核验证）。STEP 23 模拟交易第二批「信号→订单→成交→PnL 闭环」落地：新增 `server/research/signalToPnl23/`（7 文件 ~1500 行：types.ts `SignalToPnlRun` + `PnlLoop*` 子结构 + `SIGNAL_TO_PNL_RUN_KIND/_VERSION` + `PnlLoopPriceSource` 注入式价格源接口 + `PnlLoopSignalSelector` 注入式信号筛选器；errors.ts `SignalToPnlError` 18 稳定码（INPUT_*/SOURCE_*/LOOP_*/RUN_*/SERIALIZE_*）；engine.ts `runSignalToPnlLoop` 主编排器，每个交易日 (a) T+1 结算 → (b) 处理 executionTime 到期订单 → (c) 决策日 planDecisionDay+freezePaperCash → (d) mark-to-market；run.ts `toTradeQualityEvaluationInput`（C-16.3 直接适配，跳过 PaperAccountRun 中转）+ `toPaperAccountRunView`（SignalToPnlRun → PaperAccountRun 视图，便于 C-23.1 链路复用）；serialize.ts canonical+sha256 指纹 + round-trip + 篡改拒绝；validate.ts 结构复核；index.ts）。**复用（只读 import 不重写）**：C-13.2 `CandidateEvaluationRun` + 指纹复核 + `point=close` 校验；C-23.1 8 个原语（settlePaperAccountT1/freezePaperCash/unfreezePaperCash/applyPaperBuyFill/applyPaperSellFill/markPaperAccountToMarket/snapshotPaperAccount）+ `checkPaperOrder` + `computePaperPnlBreakdown` + `computeExecutionConstraintDeclarationFingerprint` + `describeExecutionConstraintCoverage`；C-14.2 `computeFillCostBreakdown` + `toEngineCostModel`；C-14.1 `planDecisionDay`（候选意图 → 订单映射，hold-while-selected longOnly 进出场，绝不重写）；C-14.3 `DEFAULT_LOT_SIZE`；researchDataset/version `canonicalStringify`。**核心 PIT 纪律**：决策日 D 的订单 executionTime = 下一交易日 D+1；D+1 成交价严格取 D+1 open（不取 D close，避免 look-ahead）；冻结金额用决策日 close 算（盘后已知），不取执行日 open；价格源注入式（绝不读 dataset），每日循环对 priceSource 的调用次数确定（= 当日涉及 securityId 数）。**诚实边界**：tradeJournal/Planned vs Actual/Deviation/Reason/Emotion/Rule Violation/Post-review 留给 C-24.1；本目录仅生成订单/成交/快照/账本/权益曲线/PnL 六种记录，不含主观归因字段。**已知 tradeoff**：当前 PnL 闭环测试通过 cash=2000 等小 cash 边界调参覆盖（数学上 A 股无 leverage、cash ↔ qty 强耦合，无法在合理 cash 内既 buy 100 股又 D2 成交）；端到端 PnL 全链路在更大 cash 下完整可验证。单测 signalToPnl23.test.ts **18 例**：端到端小样本多日闭环/T+1 冻结循环+资金不足闭环/涨跌停拦截/资金不足拒绝+冻结释放/PnL 恒等式/PIT 时点纪律（冻结金额=close 计算）/确定性（多次同输入同 fingerprint）/serialize 篡改拒绝（初始资金+ fingerprint 双篡改均抛 RUN_FINGERPRINT_MISMATCH）/降级输入 5 类（空 calendar/SUSPENDED/缺 runId 或 priceSource/initialAccount.asOf 过晚/sourceRun 指纹不匹配）/C-16.3 适配/fingerprint 自洽/signalSelector 自定义/frozenTimeline+peakFrozenAmount/rejectionSummary 按 code 聚合。全量 **49 文件 1047 全过**、`tsc --noEmit` exit 0。协调者补丁：research/index.ts 显式列举 paperAccount23 导出（避免与 signalToPnl23 同名 toTradeQualityEvaluationInput 冲突），再 `export * from "./signalToPnl23"`。新增顶层符号 23 个全库查重零冲突。CODE_READY；VALIDATED 依赖数据链就绪认证。C-24.1 依赖本任务。
- **2026-09-07 21:45 — RENAME 代码目录去数字化（§49 命名规范定稿）**。用户指出「文件夹很多带数字不符合命名规则」；此前 ROADMAP 无目录命名规范。16 个 `server/research/` 模块目录按语义映射去编号（signal13→signalEngine、costModel14→costModel、executionConstraints14→executionConstraints、riskAdjustedMetrics16→riskAdjustedMetrics、tradeQualityMetrics16→tradeQualityMetrics、parameterSearch17→parameterSearch、rollingOptimization17→rollingOptimization、robustness18→robustness、stochasticRobustness18→stochasticRobustness、walkForward19→walkForwardRun、oosIsolation19→oosIsolation、overfittingDetection20→overfittingDetection、lifecycle21→lifecycle、marketRegime22→marketRegime、paperAccount23→paperAccount、signalToPnl23→signalToPnl），docs/step12-evidence→docs/researchReadyGate，各目录 `*NN.test.ts`→`<module>.test.ts`。walkForward19→walkForwardRun 因 `server/research/walkForward.ts`（STEP 6.5 生产 WFO contract）占用原名防解析歧义。验证：49 文件 1047 测试全过（基线持平）、tsc exit 0、tsx 实例化 research/index exports=731。本文件与 §47/memory 中历史条目保留旧目录名（append-only），以本条为映射对照；新增代码遵守 ROADMAP §49。**注意**：后续子代理 prompt 中的模块路径一律使用新目录名（signalEngine/costModel/…/walkForwardRun/oosIsolation/overfittingDetection/lifecycle/marketRegime/paperAccount/signalToPnl）。
- **2026-09-07 22:53 — C-20.2 DESIGN → CODE_READY**（并行 agent 交付，协调者复核验证）。STEP 20 因子消融与 OOS 退化落地：新增 `server/research/factorAblation/`（10 文件 2791 行：types.ts AblationTarget/四模式（REMOVE_SINGLE/LEAVE_ONE_OUT/CUMULATIVE_REMOVE/FORWARD_ADD）+ VariantSpec（BASE_FULL/BASE_EMPTY + ABL_REMOVE_/ABL_CUM_/ABL_FWD_ 单变量码）+ evaluator 注入契约 + AblationThresholds（IS 贡献地板 1pp/OOS 中性天花板 0.5pp）+ ReasonCodes（ABL_NO_OOS_TRACK/ABL_NO_IS_VARIANTS/ABL_NO_OOS_VARIANTS/ABL_IS_BASE_FAILED/ABL_OOS_BASE_FAILED）；variants.ts validate（≥2 且 id 唯一）/resolveOrder 全排列/buildVariants（variants[0]=BASE）；contribution.ts computeAblationContributions（remove 族=base−removed、add 族=added−previous，主排序 IS 贡献 DESC + targetId ASC 平局，显式非 argmax）；assess.ts assessAblationRun（evaluator 仅解析并调用一次、异常/非法返 failed、指纹+deepFreeze）；serialize.ts round-trip+篡改拒绝；adapt.ts toAblationRobustnessView + toAblationOfdAssessmentInput（**与 C-20.1 Ofd 汇合：真实 assessOfdOverfitting → OVERFIT 已测**）；discipline.ts verifyAblationOosDiscipline 审计（oosTrackPresent/tracksAligned/targetSetsIdentical/orderFixedBeforeEvaluation/signalsDoNotDriveOrder）；run.ts runAblationAssessment + ABL runId；index.ts；test.ts 45 例）。**IS/OOS 双轨消融**：同套消融目标在 IS 与 OOS 分别执行（OOS 段注入只读不参与筛选），IS 贡献 vs OOS 贡献对照产出**机器可读过拟合信号候选**（ABL_IS_POS_OOS_NEG/NEUTRAL，描述性归因非因果，需人审复核）。**复用**：C-19.2 真实 runOosIsolation、canonicalStringify、评估契约；C-18.1 阈值语义参考。**诚实边界**：p 值/贝叶斯/自动剔除重训/单点 argmax 明确不做；跨品种泛化留 C-25.1。单测 45 例（手算贡献 f_mom 8pp/f_val 5pp/f_liq 2pp/LOO=REMOVE_SINGLE 几何/累计 remove/前向 add/IS 正 OOS 负与中性信号触发/无 OOS→ABL_NO_OOS_TRACK/退化 codes/确定性/round-trip 篡改/真实 OOSRun 复用/discipline/OVERFIT 适配）；全量 **51 文件 1121 全过**、`tsc --noEmit` exit 0。协调者补丁：research/index.ts 增 `export * from "./factorAblation"`（tsx 实例化 exports=805）。新增顶层符号 27 个全库查重零冲突。CODE_READY；VALIDATED 依赖数据链就绪认证。
- **2026-09-07 22:53 — C-24.1 DESIGN → CODE_READY**（并行 agent 交付，协调者复核验证）。STEP 24 交易日志落地：新增 `server/research/tradeJournal/`（10 文件 3609 行：types.ts TradeJournalEntry/JournalPlannedFacts/JournalActualFacts/JournalDeviation/AnnotationBlock/PostReviewRecord/DeviationDimension（机器维度枚举）+ 受控词表（reasonCode 8/emotionCode 10/severity 3）+ TJ_UNASSESSED_REASON_CODES；errors.ts TradeJournalError 32 稳定 TJ_* 码；reconcile.ts reconcilePlanVsActual（数量差/价格差口径「实际成交价−planned 参考价」/执行偏移/未成交原因引用 + buildJournalActualFacts VWAP 聚合，actual 早于 planned 响亮拒绝）；drafts.ts buildJournalDraftsFromRun（从 C-23.2 SignalToPnlRun 逐订单提取 draft、机器核对自动填 annotation=null + 诚实跳过计数）；ledger.ts TradeJournalLedger in-memory append-only（修订=bump vN 挂 supersedes 链、篡改/回退/断链拒绝）；review.ts createPostReviewRecord/withJournalAnnotation/annotateJournalEntry；serialize.ts 指纹 round-trip + verifyTradeJournalLedgerContent 断链检测；validate.ts 结构+语义校验（PIT 时间序 decision≤执行日≤成交≤标注/入账、actual/deviation 交叉一致、词表、评级值域）；index.ts；test.ts 29 例）。**复用**：C-23.2 SignalToPnlRun（orders/fills/rejectionLedger + assertValidSignalToPnlRun + computeSignalToPnlRunFingerprint 来源可信复核）、C-23.1 PaperAccountOrder/Fill 字段形态、canonicalStringify、C-13.3 显式缺省码哲学。**诚实边界**：planned 参考价仅当决策/执行日在 run 交易日历相邻断言通过才用 fill.basePrice（=决策日 close PIT 安全）推断，否则 unassessed（TJ_EXECUTION_DAY_NON_ADJACENT/TJ_PLANNED_PRICE_REFERENCE_MISSING）；reason/emotion/ruleViolation **零生成**（annotation 人工注入，draft=null）；跨交易聚合/纪律打分/最差执行排序留 C-24.2。单测 29 例（机器核对 12/draft 提取 5/账本 6/PostReview 3/PIT 3）；全量 **51 文件 1121 全过**、`tsc --noEmit` exit 0。协调者补丁：research/index.ts 增 `export * from "./tradeJournal"`。新增顶层符号 47 个全库查重零冲突。CODE_READY；VALIDATED 依赖数据链就绪认证。C-24.2 依赖本任务。
- **2026-09-07 22:53 — 顺带修复 C-19.2 主表行遗漏**。§5 14:40 记录与 C-20.1 依赖消费均已证明 C-19.2 于 14:40 达 CODE_READY，但主表行长期滞留 `DESIGN`（历史跨 turn 编辑竞态丢失）。已恢复：负责人 `research-dev [done]`、状态 `CODE_READY`、说明列补 14:40 交付摘要（oosIsolation/ 隔离记录 + 机器检查 + 归档账本 + OOS 聚合报告，单测 23 例）。
- **2026-09-08 22:53 — C-24.2 DESIGN → CODE_READY**（并行 agent 交付，协调者复核验证）。STEP 24 纪律反馈分析落地：新增 `server/research/disciplineFeedback/`（12 文件 3267 行：types.ts 契约权威源——DisciplineFeedbackRun/四类报告/DFA_REASON_CODES(10)/DFA_ENV_UNLABELED_KEY/DFA_SEVERITY_INDEX/JournalEnvironmentAssignment；errors.ts DFA_ERROR_CODES 14 稳定码；common.ts resolveFeedbackEntries（latest-per-journal 归并、同版本歧义拒绝）+ 日历纯函数 + stablePercent/Mean；causes.ts aggregateViolationCauses 违规原因统计（ruleViolation=true 按 reasonCode+severity+维度过滤，未标注显式 counted-unannotated）；repeat.ts detectRepeatMistakes 重复错误识别（3 分组维度/2 scope/阈值/窗口峰值两指针）；executionQuality.ts rankExecutionQuality 最差执行排序（价格偏差率/未成交+部分成交率/时机晚率，样本不足不排名）；environments.ts identifyErrorProneEnvironments + normalizeJournalEnvironmentAssignments（regime/环境标签注入不自算，违规密度+平均 severity，无标签归 ENV_UNLABELED）；run.ts buildDisciplineFeedbackRun 主编排（输入账本指纹→四类聚合→模式清单→9 分支诚实结论 stable/patternsFound/inconclusive+reasonCode）；validate.ts 结构+值域+计数自洽+排名连续性；serialize.ts 指纹 round-trip+篡改拒绝；index.ts；disciplineFeedback.test.ts 38 例）。**复用**：C-24.1 `tradeJournal` 全部 import 只读（JOURNAL_REASON_CODES/severity/维度词表/assertValidTradeJournalEntry/指纹/TradeJournalLedger 零重定义）。**诚实边界**：annotation null 的 draft 不计入原因统计（单列 count）；空账本/单 entry/全未标注 → inconclusive+reasonCode 不硬出结论；不生成处方建议/不做因果推断；与 C-22 marketRegime 真实挂接留 C-25.1。单测 38 例（四类聚合手算/样本不足不排名/重复窗口边界/退化/确定性/round-trip 篡改/真实 TradeJournalLedger 喂入）；全量 **52 文件 1159 全过**、`tsc --noEmit` exit 0、tsx 实例化 exports=805→845（+40 吻合）。协调者补丁：research/index.ts 增 `export * from "./disciplineFeedback"`。新增顶层符号 40 个全库查重零冲突。**上游遗留（记入债务）**：C-24.1 `reconcile` 无成交时产 `fillState=NONE + actual=null`，但其 validate.ts:411 无 null guard → assertValidTradeJournalEntry 实际拒绝 actual=null，真实账本不存在合法 unfilled entry（C-24.2 探针证实；executionQuality 已留 NONE 防御分支但当前不可达）。CODE_READY；VALIDATED 依赖数据链就绪认证。C-25.1 依赖本任务。
- **2026-09-08 23:20 — FE-6/7/8/9 DESIGN → CODE_READY（前端骨架线四页并行交付，G3 全组 10/10 收口）**。用户要求「剩下的几个任务能进行的都并行开发」→ 4 个独立子 agent 并行各写一页（只写页面文件、互不触碰路由），主 agent 统一集成。新增：`client/src/pages/ParameterSearch.tsx`（`/parameter-search`，FE-6：Grid/Random/Rolling 配置 + 稳定区/候选表 + 四轴扰动 MetricCard + C-18.2 随机化分布，默认值 500/0.05/20 取自引擎）+ `WalkForwardAnalysis.tsx`（`/walk-forward`，FE-7：WFO 配置 + 逐 fold IS/OOS **视觉隔离**（slate=训练·样本内 / indigo=OOS·样本外+警示）+ OOS 汇总 14 卡 + PBO 判定空态）+ `RegimeReport.tsx`（`/regime-report`，FE-8：regime 标签图例（七维×值域，配置说明非数据）+ Regime×策略表现 + 溯源块 datasetVersion/strategyVersion/codeVersion + 技术预览/研究结论标记；如实注明报告导出服务尚不存在）+ `ReviewWorkbench.tsx`（`/review-workbench`，FE-9：顶部琥珀 **R7「非研究可信度口径」隔离条** + 单笔生命周期管线 建仓→加仓→做T→清仓 + 纪律看板图例 + paperAccount 闭环空态）。App.tsx 4 路由 + AppShell「研究数据」组 4 导航（SlidersHorizontal/Workflow/FileText/BookOpenCheck）→ 组内 9 项。**纪律**：契约零改动；零 trpc/零图表库/零假样例；字段字典 1:1 取自真实引擎（各 agent 精读 sources）；按钮 disabled。**修复**：ParameterSearch 字段字典 3 处 JSX 裸 `{…}` → 字符串表达式。**验证**：前端 tsc 0 错误、vite build 通过、client 17 测试全过。全量 tsc 残留错误仅在 `server/research/closedLoop/*`（并行协调者 C-25.1 在途编码噪音，非前端回归）。真实联调待各 run 端点（FE-0 扩展）+ C-17/18·19/20·22/23·24/25 认证。
- **2026-09-08 23:25 — C-25.1 DESIGN → CODE_READY**（并行 agent 交付，协调者复核验证）。**STEP 25 闭环整合落地 = 编码链 29/29 全部 CODE_READY**。新增 `server/research/closedLoop/`（12 文件 ~3000 行：errors.ts CLOSED_LOOP_ERROR_CODES 18 码；blockers.ts CLOSED_LOOP_BLOCKED_REASON_CODES 10 码 + LIFECYCLE_THRESHOLD_ 门槛前缀；types.ts 14 阶段常量 + 14 种 handoff 接口/union + ClosedLoopRun + provider/executor 契约 + lifecycle 配置（allowSyntheticEvidence）；spec.ts CLOSED_LOOP_STAGE_SPECS 14 条（consumes/produces/gate 要求/模块绑定）+ resolveClosedLoopStageSelection（空/重复/未知/逆序响亮抛错）；guards.ts handoff 形状校验 + run 指纹核验 + 全树非有限数/日期扫描；serialize.ts computeClosedLoopChainFingerprint + round-trip；lifecycle.ts 复用 C-21.1 applyLifecycleTransition/collectTransitionRequirementIssues（仅 LIFECYCLE_THRESHOLD_* → BLOCKED CL_GATE_EVIDENCE_MISSING；跳级/时间戳非法抛错；合成证据默认阻塞）；lineage.ts 复用 C-13.3 createExperimentLineageRecord（§28 谱系挂载）；adapters.ts 纯映射 summarizeTradeSimulationRun（C-14.1→backtestSummary）/composeClosedLoopEvaluationRef（C-16→evaluationRef）等；orchestrator.ts runClosedLoop 主编排（门禁 data→gate→runner→seed，EXECUTED 后统一落谱系账，blockedSummary/chainFingerprint 组装，异常重抛）；index.ts；closedLoop.test.ts 32 例 8 组）。**核心设计**：编排器只做契约校验+handoff 摘要流转（引用+摘要不复制 rows 级大对象）+审计+记录，14 阶段算法全部由注入 executor 实现——闭环骨架 + 诚实 BLOCKED 门禁，绝不伪造中间产物冒充已执行。**禁用循环 import**：closedLoop 被 research/index.ts re-export，故编排器一律直接相对路径 import 兄弟模块（../researchDataset 等），严禁 import ../index。**诚实边界**：真实数据未注入 → Data/Research BLOCKED（CL_DATA_NOT_INJECTED/CL_DATASET_GATE_NOT_PASS）；无 datasetGate PASS evidence → lifecycle 不推进（测试证明）；合成 smoke evaluator 产物 synthetic:true 标记；支持最小子集链（backtest→evaluation 两阶段）合法执行。**与 C-24.1 债务交互**：review 阶段对 unfilled entry 经 wiringLimit 记录（CL_REVIEW_UNFILLED_VALIDATION_DEFERRED），未改 C-24.1，其修复后无需改编排。单测 32 例；全量 **53 文件 1191 全过**、`tsc --noEmit` exit 0、tsx 实例化 exports=845→904（+59 吻合）。协调者补丁：research/index.ts 增 `export * from "./closedLoop"`。新增顶层符号 59 个全库查重零冲突。CODE_READY；VALIDATED/PRODUCTION_READY 依赖数据链就绪认证。**编码链 G2 完结**：C-12.5.1~C-25.1 共 29 项全部 CODE_READY；下一步转入 P1 数据域认证（数据链 D 域回填中）与 P3 端到端验证。
- **2026-09-08 23:40 — FE-0 扩展（研究 run 目录 + 就绪探测端点）CODE_READY**。用户确认「后台编码完成，接口接入后应可做」→ 实查：编码链 29/29 CODE_READY（C-25.1 收口）、tsc 0 错，但 researchRouter 仍 4 procedure、shared 无 run 契约、registry 装配未接线、gate RESEARCH_READY=FALSE（C 472/5500、E 974/5000、D 19/5000、Adj 20/5000 PENDING）→ 本轮补「接口接入」代码缺口。新增：`shared/researchContracts.ts` append FE-0 扩展区块（researchCatalogItemSchema / researchDatasetGateSummarySchema / RESEARCH_RUN_READINESS_VALUES + verdict schema / researchRunReadinessSchema，zod 契约唯一来源、decisionPoint enum 与后端 "close"|"open" 同构）；`server/researchRunRouter.ts`（catalog.list = researchStrategyRegistry 轻量目录，模块顶幂等 registerBuiltInResearchStrategies；readiness = readCertifiedGate 认证快照派生 + 策略注册 + executorBound=false 恒定，verdict 优先级 EVIDENCE_MISSING>DATASET_NOT_READY>STRATEGIES_MISSING>EXECUTOR_NOT_BOUND，reasons 全量人类可读，canRun 四条件与）；`server/routers.ts` appRouter 增 `researchRun` 命名空间。纪律：只读不执行（不冒充 READY，对应 closedLoop CL_RUNNER_NOT_INJECTED/CL_DATASET_GATE_NOT_PASS）；gate 摘要取 dataHealth 同源认证快照不重算。单测 `server/researchRunRouter.test.ts` 5 例（注册守卫/catalog baseline 元数据/readiness 诚实 BLOCKED 断言不写死 PENDING 防快照漂移/executorBound 恒定）。**前端真实接入**：RunConfigPanel +「运行就绪探测」块（verdict/EXECUTOR_NOT_BOUND badge + reasons 原样 + pendingChecks 黄字；运行按钮 enabled=readiness.canRun 现恒 disabled 不伪造）；RunWorkbenchTab 接 trpc.researchRun.readiness.useQuery；PerformanceDashboard 就绪门卡由写死三前置 → 后端 readiness 真数据（loading/error/data 三态 + gate 摘要 + 已注册策略清单），结果区仍空态零伪造。**验证**：tsc 0、vite build 通过、client 17 + researchRun 5 + researchContracts 12 + dataHealth 13 = 47 tests 全过。数据认证（C/D/E 回填 + 重新 certify）+ 真实执行链装配后 verdict 自动翻转 READY_TO_RUN，前端零改动出真数。ROADMAP §48.4 FE-0 状态不变（仍 CODE_READY，本轮为扩展并回）。
