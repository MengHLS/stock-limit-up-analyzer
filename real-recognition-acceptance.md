# 真实图片识别验收记录

## 验收输入

本次使用现有图片 `/home/ubuntu/upload/1231.png`，通过统一的图片识别流程执行一次真实视觉模型调用。接口传入日期为 `2026-08-09`，用于验证“接口日期优先于图片日期”的规则。

## 验收结果

| 检查项 | 结果 |
|---|---|
| 接口日期 | `2026-08-09` |
| 解析后的日期 | `2026-08-09` |
| 识别股票数量 | `57` |
| 股票代码带交易所后缀数量 | `57/57` |
| 首条股票 | `002361.SZ 神剑股份` |
| 首条涨停时间 | `14:56:30` |
| 首条题材 | `商业航天` |
| 首条关键词 | `商业航天+军工(客户成飞)+碳纤维+轮胎` |

图片底部可见统计文字为“涨停57家”，模型识别出的股票数量为57，与图片汇总数据一致。解析器同时将无后缀代码规范化为 `.SZ`、`.SH` 或 `.BJ`，并过滤缺少代码或名称的残缺行。

## 结论

优化后的提示词和统一解析器完成了一次真实图片验收：日期优先级、57条记录数量、关键字段样本及股票代码后缀均通过检查。可重复的结构化回归测试位于 `server/recognition.test.ts`，真实验收原始摘要位于 `real-recognition-acceptance.json`。
