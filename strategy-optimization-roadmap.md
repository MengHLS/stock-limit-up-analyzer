# 量化策略优化路线图

> 我是 AI，不是持牌投资顾问；以下内容是策略研究与回测工程建议，不构成保证性建议，投资风险需自行承担。

## 当前策略的正确定位

当前策略已经具备较扎实的交易模拟底座：仅对主板 1–4 板候选建模，使用真实交易日历，以 T+1 开盘入场，考虑整手、费用、滑点、最大持仓、现金约束、低开预期过滤，以及一字跌停的卖出限制。风险管理策略现在还会在每个实际收盘后更新持仓期最高收盘价，并在达到启动浮盈后，以回撤阈值触发日线近似止盈。

截至 **2026-08-26**，以初始资金 100,000 元、最大持仓 5、启动浮盈 6%、回撤阈值 3%、止损 5%、最多持有 5 个交易日运行的当前样本回测，已完成出清 155 笔、期末持仓 3 笔、峰值持仓 5、最低可用现金 0.09 元。该结果说明约束链路在本样本中正常工作；**不能据此认定参数或策略具有样本外盈利能力**。

## 优化原则：先提高证据质量，再增加信号复杂度

当前最优先的工作不应是继续微调“6%/3%”等阈值，而是建立一个能淘汰偶然有效参数的验证框架。只有在不同时间段、不同市场情绪、不同成本假设下都保持稳定的改动，才应进入实盘观察池。

| 优先级 | 优化项目 | 具体做法 | 通过标准 |
| --- | --- | --- | --- |
| P0 | 滚动样本外验证 | 按时间划分训练窗与后续测试窗；每轮参数只由训练窗选取，再在完全未参与调参的测试窗结算。 | 多数测试窗优于固定 T+2 基准，且不是由单一行情阶段贡献。 |
| P0 | 参数稳定性 | 对启动浮盈、回撤、止损、最多持有日做粗粒度网格，不追逐单点最高收益。 | 相邻参数组合表现接近，收益、回撤和交易数不出现断崖。 |
| P0 | 成本压力测试 | 在基准滑点外再测试更高滑点与更高交易费用。 | 成本上调后仍保持可接受的收益质量，避免依赖边际成交优势。 |
| P1 | 情绪分层 | 分别统计冰点、修复、发酵、高潮、退潮阶段的胜率、平均收益、最大回撤。 | 明确哪些阶段应交易、降仓或暂停，而非用一个规则覆盖全周期。 |
| P1 | 信号消融 | 每次只加入一个因子，并与“移除该因子”的版本比较。 | 新因子在样本外提高收益质量或降低回撤，并保留足够交易数。 |
| P2 | 组合风险预算 | 增加题材集中度、同日入场数与阶段总仓位上限。 | 在高相关题材共振或退潮期，组合最大回撤低于当前基准。 |
| P2 | 行情数据增强 | 补充高低价、成交额、换手率、量能与封板质量等字段；盘中级数据不足时明确只做日线结论。 | 字段可在信号时点取得，且覆盖率、缺失率和时间戳可审计。 |

## 最值得优先验证的三个方向

### 1. 先做“情绪阶段 × 策略”的分层比较

现有系统已经有最高连板和情绪周期，这是最有价值、也最贴近策略逻辑的条件变量。建议先按信号日所处阶段分层，比较固定 T+2、动态回撤止盈和风险续持三类退出方式。在退潮期，核心目标通常应是减少交易和压低尾部亏损；在修复和发酵期，才检验动态回撤是否能更好地保留趋势利润。

不要直接把“某阶段收益最高”变成规则。应同时检查完成交易数、平均单笔净收益、胜率、盈利因子、最大回撤和成本上调后的结果。样本过小的阶段只作观察，不应用来单独优化参数。

### 2. 把动态回撤止盈从单点参数变成稳健区间

建议先测试一组**粗粒度研究网格**，而不是直接寻找一个“最佳数值”：启动浮盈可比较 4%、6%、8%，回撤阈值可比较 2%、3%、4%，并固定其他交易规则。随后再对少数稳定区域测试止损和最多持有日的组合。

如果只有“启动浮盈 6%、回撤 3%”表现突出，而相邻组合明显下降，应优先怀疑过拟合；如果 4%–8% 启动、2%–4% 回撤的结果都相对稳定，则该规则更可信。评估时必须与固定 T+2 作为同一候选池、同一费用和同一成交限制下的基准对照。

### 3. 用增量因子完善入场，而不是堆叠评分

候选评分目前已覆盖连板、题材、流通市值等信息。下一步建议按以下顺序单独接入并消融验证：

| 因子 | 研究假设 | 必要约束 |
| --- | --- | --- |
| 题材热度变化 | 题材由扩散转为聚焦时，龙头延续概率可能更高。 | 使用信号日及以前的题材统计，不看次日题材结果。 |
| 个股在题材内的相对地位 | 同题材中更早涨停、连板更高或强度更高的标的可能更具辨识度。 | 对同题材同日候选做横截面排序，避免绝对阈值漂移。 |
| T+1 开盘预期 | 当前低于预期过滤已具备，可进一步按开盘溢价区间分层，而不是仅设单边阈值。 | 仍只使用 T+1 开盘价，不能引入当日收盘筛选。 |
| 流动性与换手 | 合理的成交额、换手和量能结构可能改善可成交性与持续性。 | 必须补充可追溯的日线字段；没有盘中数据时，不应声称识别了封单质量。 |

## 必须固化的验证流程

每一个新增因子或规则都应遵循相同顺序：先冻结候选范围与交易成本，再在训练窗提出规则；然后在后续、未参与调参的测试窗执行；最后按情绪阶段、题材集中度和成本压力拆分结果。建议同时输出总收益、最大回撤、平均单笔净收益、胜率、盈利因子、完成交易数、期末未出清数和资金利用率。

> 一项改动只有在样本外、成本压力和多情绪阶段下仍改善风险调整后的表现，才进入默认策略；否则保留为研究分支，不替换基线策略。

## 建议的开发顺序

首先实现“策略对比实验台”：固定 T+2、动态回撤止盈和风险续持在相同候选池中并列展示，并按情绪阶段和时间窗口输出结果。其次实现滚动样本外验证与参数热力图，避免页面只显示一组历史总收益。最后接入成交额、换手率和高低价等可审计日线字段，并逐个进行消融检验。

## 研究披露

| 要素 | 当前口径 |
| --- | --- |
| 基础 | 主板 1–4 板候选，T+1 开盘入场；日线收盘近似动态回撤止盈；费用、滑点、整手、现金、最大持仓和涨跌停限制纳入模拟。 |
| 时间 | 当前内部样本核验截至 2026-08-26。 |
| 假设 | 仅以信号日及此前可见信息形成候选；退出判断仅使用对应开盘或收盘已经出现的日线数据。 |
| 数据与置信度 | 项目内涨停记录与 Tushare 日线价格；缺少盘中盘口、封单和逐笔成交数据，故不能证明盘中可成交性。 |
| 合规 | 本文是研究与分析，不是个性化投资建议。 |
