# 2026-09-07 工作日志

## 00:17 — C-14.1 + C-15.1 并行交付（均 CODE_READY）

并行双 agent（C1/C2），协调者复核验证后统一收尾。

- **C-14.1 交易模拟核心** → `server/research/simulator/`（types/plan/engine/validate/serialize/index）。关键决策：STEP 8 `server/backtest/` 的 T+1/涨跌停/停牌/成本/执行/审计原子能力 **import 只读复用不重写**；缺口仅=候选→Order 适配（plan.ts 卖先买后+冻结顺延）+ 多日编排（STEP 8 SignalGenerator 缺每股可卖视图，故薄编排复用其原语）+ TradeSimulationRun 结果绑定（datasetVersion/sourceFingerprint 追溯）。决策 point=close、默认 NEXT_OPEN（T+1 开盘成交）、产出 EquityPoint[]/Trade[]/AuditTrail 供 C-16.1。
- **C-15.1 策略 Schema + 版本化** → `server/research/strategySchema/`（types/version/validate/compare/serialize/map/index）。StrategyDocument §16 全字段（semver 严格 x.y.z）+ StrategyVersionRecord §17 九项追溯 + Strategy13 作配方引用嵌入（函数面按 recipeId 调用方持有）+ bump 语义（结构→major/参数→minor）。生命周期排除（C-21.1）。
- **验证**：`npx vitest run server/research server/researchDataset server/backtest` 32 文件 498 全过（415+simulator 17+strategySchema 28 零回归）；tsc exit 0（含出口补丁后复核）。协调者补丁：research/index.ts 增 simulator/strategySchema 两出口。
- **数据链**：C+E 回填 task `KmWNCk`（ECONNRESET 修复后重启，per-stock 隔离 + withDbWriteRetry）；G 5212/5212 DATA_READY（securityId NULL + effectiveFrom 单点两待办）。
- **文档**：TASK_TRACKING 行 124/132 → CODE_READY + §5 append 两条（00:17）；ROADMAP §44.4 两条 + §47 append 两条。
- **编码链进度**：STEP 12.5/12.6/13/14/15 核心任务全 CODE_READY。待办 C-14.2/C-14.3/C-16.1（依赖 C-14.1）可并行；C-21.1/C-22.1 依赖 C-15.1。跨午夜：本日志自 09-07 起，旧记录在 2026-09-06.md。

## 00:40 — C-14.2 + C-14.3 + C-16.1 三 agent 并行交付（均 CODE_READY）

- **C-14.2 成本模型专项** → `server/research/costModel14/`（10 文件）。STEP 8 cost.ts 原子函数只读复用；新增市场冲击显式模型（impactBps=min(maxBps,40×p^0.5) 平方根律参与率近似，流动性无效响亮拒绝 IMPACT_LIQUIDITY_INVALID）+ A 股默认税率声明（万 2.5/0.05%/0.001%/滑点 10bp/最低佣 5 元）+ 五维分解 computeFillCostBreakdown + engine CostModel 双向 mapper。测试 34。
- **C-14.3 执行与约束模型** → `server/research/executionConstraints14/`（7 文件 2066 行）。六分组组合声明（capital/positions/lot/restrictions/timing/marketClaims）+ 16 类 EXCON_* issue + →SimulationConfig 映射（产物再经 validateSimulationConfig 双保险）+ 17 轴能力矩阵 + **诚实 blocker**（perSecurityCap/banned/LIMIT_PRICE 在 C-14.1 链不可执行 → 值域放行但映射 blocker 不冒充）。判定不做：分钟级执行时机（行域日线即伪造）。测试 26（含端到端约束生效 3）。
- **C-16.1 收益/风险/回撤指标** → `server/research/performanceMetrics/`（5 文件）。CAGR 与 STEP 8 算法一致（锚点差异文档化）；新增 MaxDD 起止/时长/恢复（DrawdownSegment，儒略日纯函数）、回撤剖面、Recovery Factor=总收益÷|MaxDD|、下行偏差/连亏/tail risk。退化输入响亮失败无静默 NaN、双指纹。Sharpe/Sortino/Calmar 留 C-16.2。测试 40。
- **验证**：35 文件 598 全过（415+simulator 17+strategySchema 28+costModel 34+executionConstraints 26+performanceMetrics 40 零回归）；tsc exit 0。
- **协调者消歧补丁**：costModel14 与 executionConstraints14 同导 DEFAULT_LOT_SIZE → TS2308；从 costModel14/index.ts 顶层移除（defaults.ts 保留内部），executionConstraints14（执行约束域）为唯一顶层来源，测试改 from defaults。
- **编码链进度**：STEP 12.5~16 的 P1 核心编码任务全部 CODE_READY（12.6.1/12.6.2/13.1/13.2/13.3/14.1/14.2/14.3/15.1/16.1）。数据链 C+E 回填运行中（task KmWNCk）。C-17.1 Grid/Random Search（依赖 C-14.1+C-16.1）与 C-16.2/16.3（依赖 C-16.1）下一批可并行。

## 01:26 — C-16.2 + C-16.3 + C-17.1 三 agent 并行交付（均 CODE_READY）

- **C-16.2 风险调整指标** → `server/research/riskAdjustedMetrics16/`（5 文件）。Sharpe/Sortino/Calmar；rfAnnualPct 默认 0 参数化；下行偏差独立实现（C-16.1 仅年化 % 结果）并与 C-16.1 对照锁定；STEP 8 sharpeRatio 口径核对等价（rf=0 逐位一致，STEP 8 round 4 位差异文档化）；退化→显式 null。测试 19。
- **C-16.3 交易质量与稳定性** → `server/research/tradeQualityMetrics16/`。WinRate/PF/Expectancy/Trade Count **复用 STEP 8 computeMetrics**（import 只读，语义原样保留不做第二套）；新建 Turnover/平均持仓/月度年度一致性（月/年连乘望远镜恒等）/regime unassessed 占位（REGIME_NOT_ASSESSED，C-22.1 前不编造标签）。测试 37。
- **C-17.1 Grid/Random Search** → `server/research/parameterSearch17/`（10 文件）。Grid 桥 STEP 6.3 combinationGenerator import 只读；Random 用 mulberry32+Floyd 无放回（样本落 grid 同格点可比）；稳定区判定拒绝高收益坏点（合格=收益且回撤双门槛，显式非 argmax）；kind=candidate 无 promotion 代码；SearchRun 完整实验记录。测试 30。
- **验证**：38 文件 684 全过（598+19+37+30=684 ✓ 零回归）；tsc exit 0（含出口补丁复核，无符号冲突）。
- **编码链进度**：STEP 12.5~17 的 P1+P2 编码任务全部 CODE_READY（再增 16.2/16.3/17.1）。数据链 C+E 回填仍运行中。C-17.2 Rolling（依赖 C-17.1）、C-18.1 鲁棒性（依赖 C-17.1）、C-19.1 滚动窗口（依赖 C-17.2）后续可并行。
- **协调惯例沉淀**：跨 agent 统一出口冲突已两见（DEFAULT_LOT_SIZE 等）——教训：新目录顶层导出常量名需先查重，统一出口聚合后必须 tsc 复核。

## 01:32 — 数据同步进度实查（用户汇报）

直连 DB 实测（MySQL2 裸连 DATABASE_URL，临时 .mjs 用完即删）：
- **已完成**：A OHLCV 8,891,118 行/5796 股/1863 交易日 FULL；B securities+identifier 5,552 FULL；F index 7,452/4 指数 FULL；G industry 5,212 行/83 行业（securityId 全 NULL + effectiveFrom 单点 2026-08-31 两待办未变）。
- **进行中**：C+E 回填（task KmWNCk 跨会话后台）**确认仍活跃**——本会话 PowerShell/tasklist 进程视图枚举不到 node，但 DB 实测写入在增长（E 间隔 8s +471 行；01:29→01:32 E 由 1,440,436→1,462,438，C 由 5,103→5,177）。判定存活须以 DB 写入为准，勿信进程视图。进度：C ~280 股（ST 110+SUSPENSION 4,993 段）、E ~470 股，约 universe 5,552 的 5%/8.5%。注意 retrievedAt/createdAt 不可作写入时钟（为抓取时点/含历史值）。
- **未启动**：D corporate_actions 240 + adjustment_factors 271（19/20 股小样本，C+E 后排队）。
- 教训：ROADMAP §44.1 数字滞后（C 记录 211 实为旧样本），本次覆盖式更新 §44.1/44.2/44.3/44.5 + §47 append。

## 01:38 — C+E 完成时间估算（用户追问）

90s 窗口实测（T0 01:36:22 → T1 01:37:56）：E codes 493→501（**5.33 股/min**，9,219 行/min）、C codes 294→298（2.67 股/min，因多数股无状态事件）。累计口径（task KmWNCk 自 23:55 resume，若起点 ~80）约 4.1 股/min。
- **C+E 剩余 ≈ 5,051 股**（5552−501），按 4.1~5.3 股/min → **约 16~21 小时**（~0.7-0.9 天）。
- **D 全量（CA+Adj）** 紧随其后串行，估 ~8-12 小时 → C+E+D 合计约 **1~1.3 天**。
- 前提：跨会话任务 KmWNCk 持续存活（历史中断 3 次：会话清理/ECONNRESET）；中断则需手动 resume 重启，时间顺延。以 E codes 作进度主指标（每股约 3,080 行、不可用行数估速）。

## 01:45 — C+E 慢因分析（用户追问）

查 `scripts/backfillStatusLiquidity.ts` + `server/marketData/providers/pythonBridge.ts` + `scripts/providers/baostock_probe.py`。**慢因三层**：
1. **每股 spawn 新 Python 进程 + 完整 login/logout**（pythonBridge.execFile → probe 每股 login→query→logout）：每股固定 ~2-3s，5552 股 ≈ 4h+ 纯浪费。对照：D 脚本 backfillCorporateActionsBaostock.ts 已用批量驻留模式（daily_adjust_factor_range 单进程遍历交易日 / dividend_data_batch 单进程逐股），C+E 未沿用。
2. **拉了全历史**：CLI --from 默认 1990-01-01，运行任务未收敛 → 长上市股按 5 年切片（PAGED_CHUNK_DAYS，单次 4000 行上限）分页 7~8 次。实测 E 表 **pre2019 占 49.7%**（804,462/1,617,365 行，01:45），而研究窗口 A OHLCV 仅 2019+ → 近半行数与分页往返用不上。
3. **TiDB Cloud 远端写入**：每股 1 次状态去重查询 + 2 次 upsert（几千行公网）≈2-4s；+ intervalMs≥300ms 与 ECONNRESET 退避。
- 结论：串行约束（BaoStock 单 Session，§30）不可绕过，但 1/2 可优化——若改造为「单进程驻留 + --from=2019-01-01」，C/E 全量可由 ~18h 压至 ~6-8h。正在跑的任务中途改口径需先停 KmWNCk（跨会话，进程不可见）。

## 01:48 — 暂停决策（用户：明天再继续）

- 尝试停止 C+E 任务失败：本机进程域（普通 + dangerouslyDisableSandbox 非沙箱 PowerShell，Get-CimInstance 全量过滤 node/python/tsx）**均枚举不到任何进程**；automation 列表亦无回填任务（仅一条无关 PAUSED 资讯）。但 DB 实测仍活跃增长：01:47:49→01:48:04 E 1,648,209→1,654,944（15s +6,735 行），01:48:23 E codes 531→532、C 6,076→6,131 → **任务不在本机可控制范围**（另一台机器/其它执行环境），无法远程停止。
- 断点快照（01:48）：E ≈1,654,944 行/532 股、C ≈6,131 行/~300 股。resume 幂等（status 去重 + liquidity onDuplicateKeyUpdate），断点无损，明天 `--universe-from-db` 启动自动跳过已完成股。
- **明日恢复前必须先确认旧进程已死**（BaoStock 单 Session 互踢，否则新任务 login 即「用户未登录」）。需用户在任务实际运行处停止。

## 12:11 — C+E 提速改造（方案 B）完成并重启

用户 12:02 指示「改造提速并重新运行」。确认旧任务已停（断点 E 539 股/167.7 万行、C 320 股）。
- **改造**：`scripts/providers/baostock_probe.py` 新增 `session` 驻留命令（login 一次 + stdin JSONL 逐行 {id,cmd,code,start,end}→响应循环、请求级异常隔离、EOF logout、stdin/stdout reconfigure utf-8 + flush=True）；`scripts/backfillStatusLiquidity.ts` 新增 `BaostockSession` 类（spawn 单驻留进程、stdout 按行按 id 路由 pending、120s 超时看门狗 kill 后自动重生、exit/error rejectAll、module 单例 getSession），主循环 fetchStockDailyWithRetry 改走会话，universe 读取加 withDbWriteRetry，run() finally close 会话。**范围收敛**：重启显式 `--from=2019-01-01`（研究窗口 2019+）。
- **验证**：tsc/py_compile 干净；dry-run 3 只每股 1863 行（2019+ 交易日数正确）会话连续 0 失败；真写 2 只 + resume skip；dry-run 20 只/56s ≈ 21.4 股/min；真实含 DB 写 30s 窗口 **8.0 股/min**（旧 5.3），TiDB 写抖动 withDbWriteRetry 兜住。
- **重启**：task `319415` 后台（12:07），预计 ~10h 完成剩余 ~4,990 股（今晚 22 时前后）；完成后手动启动 D 全量。日志 `scripts/_ce_full_backfill.log`。
- 教训：loadUniverseFromDb 无重试首次启动可能遇瞬时错误 → 已包 withDbWriteRetry。dry-run vs 含写速率差 ~2.6 倍 → DB 写（TiDB 公网 upsert 1863 行/股）是真瓶颈，若需再提速方向是批量化 DB 写而非抓取侧。

## 12:26 — C-17.2 + C-18.1 + C-21.1 三 agent 并行交付（均 CODE_READY）

- **C-17.2 Rolling Optimization** → `server/research/rollingOptimization17/`（8 文件 2233 行）。**交易日锚定**窗口生成（tradeDates 索引切片，非日历天——研究链路逐日数据流，STEP 6.5 walkForward 哲学复用不 import）；跨窗一致性=合格区交集显式非 argmax（坏点无论高收益否决，minEvaluatedWindows=2）；候选 kind=candidate 止步不升 Final；每窗 searchRunId/派生 seed 确定性。**边界**：C-17.2=滚动窗逐窗搜索+描述性跨窗一致，三段 WFO 编排属 C-19.1/C-19.2。测试 29。
- **C-18.1 鲁棒性测试** → `server/research/robustness18/`（9 文件 2024 行）。Cost/Slippage/Parameter/Execution 四轴确定性扰动器（各含 ×1 基准、扰动产物必须通过 costModel14 validate、超限 skipped 不 clamp）+ 逐扰动重估（evaluator 注入记录调用断言）+ 漂移归因（收益>5pp 双向/回撤>3pp 单向敏感，阈值可配 resolveRobustnessThresholds）。MC/Bootstrap 属 C-18.2（可加轴扩展槽不预埋）。测试 34。
- **C-21.1 生命周期状态机** → `server/research/lifecycle21/`（10 文件）。§23 8 态迁移表（相邻前进+回退白名单 Production→Research 长程例外+Retired 终态复活=bump 新版本）+ 四要素审计 append-only 篡改断链 + 证据门槛（→Validated/→Production 需 datasetGate PASS，不执行真实验证留 runner 接线）+ strategySchema 版本壳层绑定（无需加 status）+ **conceptMap 两套状态机隔离**（§7 交付成熟度 vs §23 策略治理，唯一 Validated→VALIDATED 映射）。测试 30。
- **验证**：41 文件 777 全过（684+29+34+30=777 ✓ 零回归）；tsc exit 0（含出口补丁复核）。协调者补丁：research/index.ts 增 rollingOptimization17/robustness18/lifecycle21 三出口。
- **数据链**：C+E 方案 B 提速后 task `319415` 运行中（12:07 起，8 股/min，预计 22 时完成 ~4,990 股，日志 `scripts/_ce_full_backfill.log`）；12:06 实测 E 1,679,111 行/540 股、C 6,295 行/320 股与断点一致推进中。D 待 C+E 完成后手动启动。
- **编码链进度**：STEP 12.5~18 核心编码任务 CODE_READY（再增 17.2/18.1），STEP 21 首任务 CODE_READY（再增 21.1）。测试基线 777。待办可并行：C-18.2（MC/Bootstrap 依赖 C-18.1）+ C-22.1（regime 依赖 C-15.1）+ C-19.1（WFO 依赖 C-17.2）三向独立。编码链余量正被蚕食——注意 VALIDATED 认证仍全卡数据链（C+E ~10h + D ~8-12h 后才有全量 gate 认证可能）。

## 12:40 — 重新整理路线图（聚焦未完成部分）

**用户请求**：据当前路线图与任务拆分最新进展，重新整理更新路线图，保留已完成内容，聚焦未完成部分，输出优先级/依赖/范围/验收标准。

**实查**：gate PASS 12/PENDING 5/FAIL 0（`RESEARCH_READY=FALSE`，PENDING=#6 Status 477 股/#9 Liquidity 996 股/#10 CA 19 股/#11 Adj 20 股/#12 Universe）；C+E task `319415` 运行中（E 2,402,764 行/996 股、C 6,712 行/477 股）；D 未启动（CA 19/Adj 20）。

**动作**：ROADMAP.md 新增 **§48「后续迭代开发路线」**（四阶段）：
- **P0 数据回填收尾**（C+E→D→G 质量修复→H gate）
- **P1 数据域认证**（12.5 VALIDATED→12.6 DATA_READY+VALIDATED→13 VALIDATED）
- **P2 编码补齐**（7 缺口：C-18.2 MC/Bootstrap、C-19 WFO、C-20 Overfitting、C-22 Regime、C-23 Paper、C-24 Review、C-25 闭环，可并行）
- **P3 端到端验证**（真实数据跑通全闭环）
每阶段含优先级/依赖/范围/验收标准；标注 R1-R5 风险。

**关键风险识别**：R1 gate #6 阈值 5500 不合理（status 仅事件态 SUSPENSION+ST，非全市场 LISTING；LISTING/DELISTING 由 securities.listedDate/delistedDate 提供，需修正 gate #6 口径）；R2 G 行业 effectiveFrom 单点（历史轨迹缺失）；R3 OHLCV 5796>5552 对账。

**代码现状结论**：STEP 12.5~18/21 已 CODE_READY（大量 C-x.y 编码任务已完成），编码缺口仅 7 个（18.2/19/20/22/23/24/25）。数据链（C+E ~10h + D）是 RESEARCH_READY=TRUE 的唯一硬阻塞。

## 12:50 — 产出排期分工清单 DEVELOPMENT_PLAN.md（用户请求）

**用户请求**：按最新 ROADMAP 拆分排期任务，每任务含目标/验收标准/预估复杂度/关联模块。产出 `DEVELOPMENT_PLAN.md`（根目录）：
- 引用 ROADMAP §8/§13/§30/§37/§38/§44/§45/§48；快照 gate PASS 12/PENDING 5、`RESEARCH_READY=FALSE`（12:40 实查）。
- **里程碑**：M0 数据可信（P0 dataScope 全 PASS）→ M1 `RESEARCH_READY=TRUE`（P1-2 后 §13 全 PASS）→ M2 STEP 13 VALIDATED（§39 解禁）→ M3 编码补齐 → M4 生产闭环。
- **任务化**：P0 拆 7 项（P0-1 C+E 收尾/运行中、P0-2 D 全量/排队、P0-3a securityId 桥接、P0-3b effectiveFrom 口径声明、P0-4a gate#6 修正(R1)、P0-4b cross-domain 对账(R3)、P0-4c H gate 重跑）；P1 拆 3 项（12.5/12.6/13 认证）；P2 拆 11 个 C-缺口（含存量复用纪律：server/research 有 legacy walkForward*/trainValidationOos/oosEvaluation/pbo/overfittingAssessment/parameterStability/stochasticRobustness18，须差距判定 import 只读复用，防重复造轮子）；P3 拆 3 项。
- **排期批次 0~8**：批次 0=C+E 后台；批次 1 立即并行 4 agent（C-19.1/C-18.2/C-22.1/C-23.1，依赖均已 CODE_READY）；批次 5=C-25.1；批次 6/7=P0-4→P1 认证（M0 后）；批次 8=P3。R1-R5 处置映射 + 维护规则。
- 与 TASK_TRACKING ID 互通（D-*/C-* ↔ P0-x/P1-x/P2/P3）；复杂度/窗口为计划级估算。未改任何状态判定。

## 12:59 — 补全 ROADMAP §48.4 前端任务规格（用户请求）

**核查**：ROADMAP 已于 12:51（并行 agent）建立 §48.4 前端路线（FE-0 tRPC 前置 + FE-1~FE-9 页面，含范围/依赖/优先级/交付成果，R6/R7）；实查前端 client/src/pages **12 页 5,098 行**全 legacy、appRouter 无 research 路由，与 §48.4 现状描述一致。缺口=**职责/工作量/完成标准/插入节点四要素**。
**动作**：§48.4 新增「前端任务职责、工作量与插入节点」规格表——每 FE 任务补执行/复核角色、预估工作量（S/M/L+agent-日）、逐任务 Exit、插入节点（骨架线 FE-0 后即并行 vs 真实数据联调线对应 P0/P1/P2 里程碑）；§48.1 P4 行 + §48 头注时间戳更新至 12:59；§47 append 记录。DEVELOPMENT_PLAN.md 顶部加范围注（前端 P4 不在其中，指向 §48.4）。未动后端任务状态。
**下一步候选**：批次 1 四编码任务（C-19.1/C-18.2/C-22.1/C-23.1）或 FE-0 tRPC 暴露均可开工。

## 12:52 — 补充前端开发路线（ROADMAP §48.4）

**用户请求**：核查路线图是否含前端开发任务；若无则补充，并明确范围/依赖/优先级/交付成果。

**实查结论**：ROADMAP 无前端任务（§48 P0-P3 全为后端数据回填/认证/编码/验证；仅 §2/§41 两处 UI 为负面铁律）。前端 `client/src/pages/` 12 页 5,098 行全 legacy（涨停复盘/大盘/情绪/龙头/组合回测/纸面交易/上传/录入/同步/预警/日志），无研究链路 UI；后端研究链路 `server/research/` 26,938 行已 CODE_READY（STEP 12.5~21），但 tRPC 未暴露（appRouter 无 research/historicalState/researchDataset 路由）。

**动作**：新增 §48.4「前端开发路线（研究链路 UI）」—— FE-0（tRPC 前置，server 侧）+ FE-1~FE-9 页面任务（数据域健康看板/asOf 查询器/数据集构建器/策略编辑器/绩效仪表盘/参数搜索+鲁棒性/WFO+过拟合/Regime+报告/复盘+生产闭环）；§48.1 总览加 P4 阶段；§48.3 加风险 R6（tRPC 未暴露）/R7（legacy 与研究链路两套口径）。每项含范围/依赖/优先级/交付成果。

**教训**：并行发多个 Edit 到同一文件会竞态丢改（仅最后一条生效），必须串行。

## 13:08 — 治理对齐（Spec V1 → 现有 V2）+ 产出状态报告

用户下发「MASTER ROADMAP + TASK TRACKING INTEGRATION SPEC V1」。按 §3/§4「先扫描、在原 Roadmap 升级而非重建」执行：
- **扫描确认**：治理体系已完备（ROADMAP V2 §0-48 / TASK_TRACKING 37 任务 / DEVELOPMENT_PLAN / §48.4 前端 FE-0~FE-9 / docs 审计+step12-evidence），无需重建。
- **真实 DB 实查（13:08）**：E 3,087,475 行/1,424 股、C 7,125 行/617 股、D CA 240/19 + Adj 271/20 未启动、G securityId 全 5,212 行 NULL；gate PASS 12/PENDING 5/FAIL 0，`RESEARCH_READY=FALSE`。E 28 分钟 +428 股 ≈ 15 股/min（提速后）。
- **治理对齐（§46 #7-10）**：技术栈沿用 React（非 Vue）、状态模型沿用 7 态（非 10 态）、前端以 FE-0~FE-9 为权威（非 F1-F7 重建）、legacy 壳不重置。§48.4 补 F1-F7 映射表。§44 快照更新至 13:08。§47 append。
- **产出**：`docs/MASTER_ROADMAP_STATUS_REPORT.md`（§41 21 节）。
- **下一步**：FE-0 tRPC 暴露（P0）+ C-19.1/C-18.2/C-22.1/C-23.1 四编码缺口并行；数据链 C+E 运行中（~4,128 股待回填，今晚）、D 排队。

## 13:32 — FE-0 研究链路 tRPC 暴露 CODE_READY（R6 解除）

**交付**：`shared/researchContracts.ts` + `server/historicalStateRouter.ts` + `server/researchDatasetRouter.ts` + `server/researchRouter.ts`，三 router 注册 appRouter。

**关键设计决策（可复用经验）**：
1. **领域对象不复制 schema**：StrategyDocument/StrategyLifecycleRecord 用 `z.custom` 类型透传，语义校验交给后端既有权威校验器（validateStrategyDocument / assertValidStrategyLifecycleRecord）。shared 层复制领域 schema 必然双份漂移。
2. **枚举漂移用测试守护**：生命周期 8 态在 shared 定义传输层字面量，契约单测断言 `=== STRATEGY_LIFECYCLE_STATUSES`（顺序+内容），而非运行时 import 后端常量（避免 server 代码进 client bundle）。
3. **dataset build 只回传摘要剔除 rows**：rows 百万级不可经 RPC；另加 RPC 限流默认 maxTradingDays=5/maxSecuritiesPerDay=50 防误触全历史构建。gate 原样透传不粉饰。
4. **evidence 是后端 union**：shared 用 `z.record(z.string(), z.unknown())` 透传 + router 边界 cast，结构校验交给后端 gates（曾因 loose object schema 触发 TS2345）。
5. **依赖 DB 的端点不写单测**：asOf/build 按 §0.2 禁 mock，真实验证交给既有 CLI smoke + P1 认证；单测只覆盖 schema/纯函数/注册守卫。

**验证**：契约单测 12 例全过、tsc exit 0；回归 47/48 文件通过——唯一失败 `server/research/marketRegime22/`（8 例）是**并行 agent 在途写 C-22.1**（facts.ts 13:31 仍在改），与本次零重叠，勿误判为回归。

**教训**：跑回归遇到失败先 `git status` + 看文件 mtime 确认是否有并行 agent 在途改同一区域，再判断是否自己的回归。

---

## 14:03 — FE-1 数据域健康看板 CODE_READY（P0 首个前端研究页）

**交付**：`shared/dataHealthContracts.ts`（gate 状态/域/覆盖率/证据/实况 zod 契约唯一来源）、`server/dataHealth.ts`（只读真实证据——判定源自 `docs/step12-evidence/research_ready_gate.json`，模块只读→校验→派生展示字段，不重算 gate、不粉饰 PENDING；DOMAIN_SPEC 域→gate 映射单一来源；覆盖率取最差来源保守口径；缺失/解析失败→parseError 不伪造）、`server/dataHealthRouter.ts`（overview/evidence/liveCounts，`liveCounts` 带 `certified:false` 字面量实现认证/实况类型级分离）、`client/src/pages/DataHealth.tsx` + App.tsx `/data-health` 路由 + AppShell「研究数据」导航分组（legacy 壳增量）。

**验证**：13 例单测全过（读真实证据断言规则一致性，不硬编码漂移值）、tsc exit 0、vite build 通过（2847 模块）；与 FE-0 合跑 25/25。

**关键坑（记牢）**：实况查库首版用 UNION ALL 全表 COUNT(DISTINCT)（含 8.9M 行 stock_daily_prices）在 C+E 回填并发写下 >5min 挂起零输出；甚至 `information_schema.tables` 也挂（TiDB serverless 被回填占满 RU）。→ 最终方案：information_schema 估算行数（O(1)，标注≈）+ 逐表精确 COUNT(DISTINCT) + 单表 25s 超时 + 全局 30s 硬截止 + 超大表不查覆盖（覆盖由 gate#3 PASS 给出）+ 超时返回部分结果+error。**教训**：TiDB serverless 在回填期间任何 DB 查询都可能挂起，实时查库类端点必须带全局超时兜底，且 smoke 验证不要反复重跑给 DB 加负。

---

## 14:05 — 并行三任务 C-18.2 / C-22.1 / C-19.1 全部 CODE_READY

**交付**（用户「启动」确认后下发三并行子代理）：
- **C-18.2** `server/research/stochasticRobustness18/`（13 文件）：Monte Carlo / Bootstrap / Trade Order Randomization 三法 + 分布/CI/尾部概率/基准位置，复用 C-18.1 `resolveRobustnessThresholds`（收益 5pp/回撤 3pp）。诚实边界：block bootstrap/BCa/p 值标 unassessed/不做。单测 50。
- **C-22.1** `server/research/marketRegime22/`（12 文件）：七维 PIT regime 标签（Trend/Volatility/Liquidity/Breadth/Sentiment/Index State/Limit-up Env）+ 回看窗无泄漏 + 数据缺失 unassessed 不编造（Sentiment 无源）。单测 51。
- **C-19.1** `server/research/walkForward19/`（7 文件）：Train/Test 成对窗口 + gap/embargo + 冻结纪律下中位数非 argmax + 逐窗复用 C-17.1。

**协调者收尾**：补 3 处 `server/research/index.ts` 导出；全量 **45 文件 916 测试全过、tsc exit 0**；更新 TASK_TRACKING（3 行状态 + §5 三条）、ROADMAP（§44.4 三条推进 + §47 三条）、本日志。

**两个关键坑（记牢）**：
1. **子代理 429 限流中断留半成品**：C-19.1 子代理在 `walkForward19/` 写出 6 个源文件（2100 行）后因模型频率限制中断，缺 index.ts + 测试 + 未验证。接手方法：`ls` 看目录 mtime 判断是否在途 → 逐文件 Read 审阅 → 补 index.ts/测试 → 跑 vitest 暴露真实 bug。
2. **键函数不一致 bug**：子代理 freeze 用 `rollingParameterSetKey`（`"k"=2`）而 test 阶段用 `canonicalStringify`（`{"k":2}`）→ 冻结纪律机器检查全窗误报。修复：统一 `rollingParameterSetKey`（types.ts 注释本就约定复用 C-17.2）。**教训**：接手半成品必须跑测试暴露跨文件契约不一致，不能只看单文件自洽。

**并行协作注意**：本批次期间另一会话在并行写 `dataHealth.ts`/`dataHealthRouter.ts`（FE-1）和 `researchContracts.ts`（FE-0），tsc 曾短暂出现它们的半成品错误、随后其自行修复。跑 tsc/vitest 见异常先 `git status` + 文件 mtime 判断是否他人在途，勿误判为己方回归。

---

## 14:15 — FE-2 asOf(T) 历史状态查询器 CODE_READY

**交付**：shared 契约补 `historicalStateResolve*`（代码解析入/出契约，契约先行）；`server/historicalState/codeLookup.ts`（`parseCodeQuery` 纯函数：6 位数字 ±.SH/.SZ/.BJ 后缀 + 非法输入可读错误；`lookupSecuritiesByCode`：identifier history × securities master 检索——**代码可复用故返回候选集**、primary 优先归并、DB 不可用/15s 超时 → error 明示不冒充「查无此代码」）；`historicalStateRouter` 增 `resolveCode`（output schema 强约束）；`client/src/pages/HistoricalState.tsx`（路由 `/historical-state`、AppShell「研究数据」分组）渲染十问。

**关键设计**：① asOf 端点类型经 `inferRouterOutputs<AppRouter>["historicalState"]["asOf"]` 从后端推断，页面不复制领域 schema（无第二份口径）；② asOf 留空 = FULL_KNOWLEDGE 全知视角，页面黄条明示「研究必须显式填 asOf」（§4 PIT）；③ 返回 null（DB 不可用/未命中）→ 黄条明示，不伪造「全 UNKNOWN」假状态；④ Q7 价格 A 股红涨绿跌（涨 红/跌 绿）；⑤ Q8 公司行为 Q8a 已生效 vs Q8b·Q10 asOf 已知双层口径分列两栏。

**验证**：7 例单测（parse 边界：空/大小写/后缀/非法；无 DB lookup 诚实失败；接线守卫）、tsc exit 0、vite build 通过、FE-1/FE-0 回归 32/32。真实 asOf 联调依赖 M0（TiDB 回填饱和，resolve/asOf 均带超时兜底——沿 FE-1 教训）。

**下一步候选**：FE-3 数据集构建器（P1，FE-0 的 dataset.build 已带 5 日/50 股 RPC 限流）；或并行后端编码缺口 C-19.1/C-18.2/C-22.1（agent 在途）/C-23.1。

---

## 14:22 — FE-3 Research Dataset 构建器 CODE_READY（纯前端骨架）

**交付**：`client/src/pages/DatasetBuilder.tsx`（路由 `/dataset-builder` + AppShell「研究数据」加「数据集构建」）。shared **契约零改动**（`researchDataset.build` 入参契约 FE-0 已完整），本次纯消费方页面。

**关键设计**：① summary 的 `universeDefinition`/`policySet`/`dataSnapshot` 在 shared 契约为 `unknown`（RPC 透传防第二份口径漂移）→ 页面用**防御性取值** helpers（isRecord/pickStr/pickNum）渲染，字段缺失显「—」，不猜结构；② gate 徽标 PASS/FAIL/INCONCLUSIVE 原样着色不粉饰；③ mutation 结果三块只读渲染：总览（datasetVersion mono 指纹 + rowCount + gateNotes）、Tabs 数据快照（请求规范化回显/域快照表/coverageGaps）/ 9 类口径 policySet（机器可读 value 逐键渲染成 chips，不依赖具体 schema）/ 成员决议 universe（每日成员数+排除统计，members id 列表只汇总不逐条回展）；④ 表单默认对齐 RPC 护栏 5 日/50 股/dataReady=false，UI 明示「dataReady=false → gate 至多 INCONCLUSIVE」；⑤ rows 永不回传只显 rowCount。

**验证**：tsc exit 0、vite build ✓（12.7s）、FE-0/FE-1/FE-2 回归 32/32。真实 build 联调依赖 M0。

**经验**：页面类型推导用 `NonNullable<ReturnType<typeof trpc.X.useMutation>["data"]>`（与 FE-2 useQuery 同款模式），不要自造 hooks 中转模块。

**下一步候选**：FE-4 策略编辑器 + 运行工作台（P1，FE-0 已暴露 strategy.validate/bump/compare + lifecycle 全链，纯函数可先行真测）；或后端编码缺口 C-19.1/C-18.2（无人认领）。C+E 回填 ~4,128 股今晚跑完，D 排队。

---

## 14:35 — FE-4 策略编辑器 + 运行工作台 CODE_READY（首个端到端真测 FE）

**交付**：`client/src/pages/StrategyEditor.tsx`（路由 `/strategy-editor` + AppShell「研究数据」加「策略工作台」）。shared **契约零改动**（`strategy.validate/bump/compare` + `lifecycle.describe/transition` FE-0 已完整），纯消费方页面。

**三 Tab**：① 策略编辑器——JSON 透传 StrategyDocument（§16）→ validate 结构化 issue 表（code/path/message），fingerprint 占位明示；② 版本化——bump（major/minor/patch）+ compare 字段级 diff（忽略 version/fingerprint）；③ 生命周期——describe 状态机（8 态 + 迁移表，后端常量唯一事实来源）+ transition 表单（to 下拉按 transitions[currentStatus] 动态过滤/四要素/evidence JSON），返回新 record（status + transitions 历史链 + fingerprint）。

**关键取舍（记牢）**：① 模板（合法 StrategyDocument + 合法 genesis 生命周期壳）由**后端纯函数生成一次**（`npx tsx` 脚本调 createStrategyDocument/createStrategyLifecycleRecord 拿真实 fingerprint/hash）后内嵌前端常量，前端只透传不重算 hash（§31）；② 领域对象 z.custom 透传、后端权威校验、页面不复制 schema；③ 迁移失败后端抛错、页面原样展示 tRPC error；④ `to` 断言复用 shared `StrategyLifecycleStatusValue`（不另造字面量）。

**验证**：tsc exit 0、vite build ✓（14.6s）、回归 70/70（strategySchema 28 + lifecycle21 30 + 契约 12）、**端到端 smoke 6 项全过**（createCaller 实走 validate/bump/compare/describe/transition + 跳级拒绝）。

**经验**：FE 页面要「端到端真测」的关键 = 用 `appRouter.createCaller({req:{},res:{},user:null})` 直接实走纯函数端点（不碰 DB），比只跑单测更能发现「页面调用的 input 形状 vs 后端 schema」不一致。纯函数端点（strategy/lifecycle）是 FE 里唯一能完整真测的，DB 端点（asOf/build）只能骨架等 M0。

**下一步候选**：FE-5 绩效仪表盘（P2，依赖 C-14/16 认证，尚不可行）；后端编码缺口 C-19.1/C-18.2（无人认领，可并行）。前端 P0/P1 主线（FE-0~FE-4）已全部 CODE_READY。

---

## 14:42 — 并行双任务 C-19.2 / C-23.1 全部 CODE_READY

**交付**（用户「继续」后下发两并行子代理）：
- **C-19.2** `server/research/oosIsolation19/`（9 文件）：IS/OOS 隔离记录 Train/Test 字段分离 + 键白名单/无重叠/参数不回写机器检查 + in-memory 归档账本 + OOS 分段聚合报告（复用 C-16.1/16.2 + C-19.1 窗口几何/退化口径），描述性对比不下过拟合结论。单测 23。
- **C-23.1** `server/research/paperAccount23/`（9 文件 2588 行）：账户/持仓状态机 + T+1 冻结 + 现金账本 + 订单→成交约束 + 六维能力矩阵，复用 C-14.3/C-14.2/C-16.3，诚实 blocker 仅记录不强制执行。单测 35。

**协调者收尾**：补 2 处 `server/research/index.ts` 导出；全量 **47 文件 974 测试全过、tsc exit 0**；更新 TASK_TRACKING（2 行状态 + §5 两条）、ROADMAP（§44.4 两条推进 + §47 两条）、本日志。

**本批关键观察（记牢）**：
1. **并行时序 tsc 短暂污染是常态**：C-19.2 子代理回报时报告 `paperAccount23/validate.ts(75,7)` 的 TS18046 错误（`'value.initialCapital' is of type 'unknown'`），是并行 C-23.1 子代理在途未修复；C-23.1 子代理自己交付时已修好，协调者复跑 tsc exit 0。**教训**：子代理 A 报的 tsc 错误若指向并行子代理 B 的新目录，先等 B 交付再复跑，勿误判为己方回归。
2. **命名避撞**：`PaperPosition`/`PaperOrder` 被 legacy `server/paperTrading.ts` 占用，C-23.1 改 `PaperAccountPosition`/`PaperAccountOrder` 前缀避撞——再次印证「全库查重」纪律的必要性。
3. **遗留**：C-14.2 `computeFillCostBreakdown` 零冲击卖出产 `-0`（JSON round-trip `-0→0`，金融等价，超 C-23.1 边界未改，根治需在 C-14.2 侧归一化）。

**下一步可解锁**（依赖已满足）：C-20.1（PBO/参数敏感性，依赖 C-18.1+C-19.2 ✓）、C-23.2（信号→订单→成交→PnL 闭环，依赖 C-23.1 ✓）。C-20.2 依赖 C-20.1、C-24.1 依赖 C-23.2，均待再下一批。

## 18:30 — C+E v2 报错诊断 + 提速改造（v2 启动）

**用户报告**：后台任务一直报错。诊断：v1 旧任务（task 319415，12:07 启动）于 18:08 后停止（最终断点 E 3020 股/560 万行、C 6361 行）；尾部日志 394 只 failed / 790 次 retry 全是「网络接收错误」——BaoStock 高峰期服务端抖动，**旧 retry 间隔 600/1200ms 太激进反而加剧服务端雪崩**。

**v2 改造**（`scripts/backfillStatusLiquidity.ts`）：
1. `MAX_FETCH_RETRIES` 3→6；retry 间隔改 **3s/6s/12s/24s/48s 指数退避**（与 intervalMs 解耦，不再拖慢正常路径）
2. **连续失败 5 只触发 30s cooldown**（防服务端雪崩）
3. 失败股票落 `scripts/_failed_stocks_ce.json`，下轮启动按 `lastFailedAt` 升序提到队首优先重试（不放回队尾）—— 修复 `const universe = ...` 重新赋值 bug（改 let）
4. `--interval` 默认 300ms → 启动时传 1000ms（贴近 BaoStock 1.43 req/s 上限）

**提速评估（实测为依据）**：
- BaoStock 单账号硬限速 ~1.43 req/s（实测 8 req/5.6s），单进程再优化无收益
- Tushare `daily_basic` 限速 1 次/分钟（积分不足），**不能作流动性替代**
- **多账号并行可提速**（2 账号 → ~5h，3 账号 → ~3.5h），但需用户额外注册免费 baostock 账号
- Tushare `daily` 接口不限速（1379 行/0.11s），但返回 OHLCV 不含 turnover_rate/circ_mv/total_mv

**v2 启动后实测**：8 分钟 3018→3085 股（~8.4 股/min），零错误零重试。剩 ~2467 股预计 ~5h 完成（今晚 23:30 前后）。日志 `scripts/_ce_v2_backfill.log`、task `uG0LTo`。

**空股发现**：universe 5552 中 **2493 只空股**（identifier_history 有但 liquidity_daily 无行；已退市/无数据）—— 每次 fetch 返回 0 行也耗 ~1s。无预知手段跳过（除非做 discovery pass），接受 1.4h 空股耗时。

**下次提速路径**（若用户配合）：
1. 注册 2-3 个免费 baostock 账号 → 多账号并行 → 全量 ~3-5h
2. Discovery pass：扫一遍空股落 `_empty_stocks.json`，下轮跳过

## 19:50 — SQL 版回填脚本（用户「我自己执行导入」方案）

**用户决定**：把回填脚本改成 **SQL 文件输出**模式，避开云端 DB 写入速率限制，由用户拿到 .sql 后 `mysql < file.sql` 本地导入。

**复制 `scripts/backfillStatusLiquidity.ts` → `backfillStatusLiquidity_sql.ts`**：
- **删 DB 写**：withDbWriteRetry / upsertLiquidityDaily / upsertSecurityStatusIntervals + 关联常量/函数（isTransientDbError/TRANSIENT_DB_ERROR_CODES/MAX_DB_WRITE_RETRIES）整套移除
- **新 SqlWriter 类**：open() 写头部元数据注释 + appendLiquidityBatch（`INSERT ... ON DUPLICATE KEY UPDATE` 累积多行 VALUES，retrievedAt 用 NOW() 与 DB 默认值语义一致） + appendStatusBatch（纯 `INSERT ... VALUES` 累积多行） + flush() 每只股写后立即落盘 + close() 写尾统计
- **CLI 收敛最简**：必填 `--sql-output=<path>`（自动 mkdir）；可选 `--board=main/cyb/kc/bj`（逗号分隔）；默认 --from=2019-01-01；默认 --interval=1000；非 --board 隐式 --skip-bj
- **universe 来源**：删除 `loadUniverse`（走 baostock 易截断到 2000），保留 `loadUniverseFromDb` 单点读云端 universe——universe 与持久化解耦
- **关键修复**：原版 `import { dirname, fileURLToPath, resolve } from "node:path"` 报错（fileURLToPath 在 node:url）→ 拆分到两 import

**沙箱限制**：sandbox 内 `MARKETDATA_PYTHON=...python.exe` 经 Git Bash spawn ENOENT（即使是 `/c/Users/A/...` 形式，sandbox 阻断生成子进程命令）。本会话无法跑端到端验证，但 tsc --noEmit exit 0、SQL 头部格式（小样本头部 893 字节含 generated_at/范围/板块/pid/行数/import_hint）已验证合规。

**原 DB 版 `backfillStatusLiquidity.ts` 原样保留**——用户可选两种落库模式：(a) 沿用 DB 直写云端 / (b) 本 SQL 版导出+本地导入。

**校验脚本关键认知**：用户偏好执行类脚本时使用 `/c/Users/...` 路径而非 `C:/Users/...`，否则在 Git Bash spawn 子进程环境下报错 ENOENT。

**ROADMAP §44.1/44.3 + §47 append 同步**。


## 18:50 — 板块分级 + `--board=` CLI（用户策略）

**用户决定**：主板优先、创业板次之、科创板再次、北交所先不同步数据。**关键发现**：identifier_history 里**没有北交所的股**——所以"跳过北交所"实际上没影响（数据源里就没）。用户也希望"把脚本给我自跑、不同板块用命令区分"。

**v1/v2 残留清理**：
- task 319415 (v1) 主进程已 SIGKILL（系统通知）
- task uG0LTo (v2) 我手动 Stop-Process
- PID 773 (v1 残留子进程) 主动 kill -9——避免与新任务 BaoStock 单会话互踢

**v3 改造**（`scripts/backfillStatusLiquidity.ts`）：
- 新增 `--board=<list>` 参数：main/cyb/kc/bj 逗号分隔
- 新增 `--include-bj`（默认跳过北交所）
- `boardPriority()` 按 SH 主板→SZ 主板→创业板→科创板→北交所 排序
- `boardKeyOf()` 板块 key 映射
- **数据异常 prefix 修复**：42 只 SZ `003xxx`（深主板新段，2024 起）+ 1 只 SZ `302xxx`（创业板扩展）+ 1 只 SH `689xxx`（科创板扩展）

**板块实测分布**：主板 3486、创业板 1447、科创板 619、北交所 0（universe 5552）。

**tsc 干净**；--board=kc 验证 618→619（含 689）；--board=bj 验证 0（合理）。

**当前进度**：E 3155 股（v1 残留最后阶段自跑）；D 未启动。

ROADMAP §44.1/44.3 + §47 append 同步。

---

## 前端产品化（STEP 13/15/16 · FE-3/FE-4）

**任务**：把「工程验证型前端」升级为「专业量化研究工作台」，不动后端 Contract / 数据模型 / PIT / Gate / Audit 语义。执行方式：8-Phase 连续推进（用户确认）。

**成果（Phase 2~8 全完成，未改任何 server/shared 契约文件）**：
- 新增统一状态体系 `client/src/lib/status.ts`（§12 唯一事实来源）+ 公共组件 `components/common/`（StatusBadge/MetricCard/EmptyState/ErrorState/TechnicalDetails/SectionCard/DataTable）。
- 新增 Adapter 层 `client/src/adapters/`（strategy/dataset/buildResult/runResult），wire↔ViewModel 无损往返（extra 透传未知字段 + hasDefaultValue 区分 null 默认值）。
- `pages/StrategyEditor.tsx`：双模式（可视化默认 + JSON 高级）+ Header + Entry/Exit/Risk 结构化条件表 + 仓位规则 + Run Workbench（运行工作台 Tab，结构预留）。
- `pages/DatasetBuilder.tsx`：五段结构（Configuration→Source Validation→Build Pipeline→Build Summary→Diagnostics）；INCONCLUSIVE/NO_ROWS_BUILT 产品化；Source vs Final rows 严格区分。
- `pages/DataHealth.tsx`：第三处并行状态色收敛到 styleForStatus。

**关键设计决策**：① 规则间隐式 AND（后端 DeclaredRule 无 logic 字段，不造 OR）；② Position Sizing 仅契约三值，无 Custom；③ 保存/运行按钮后端无端点 → 禁用+tooltip 不伪造；④ Pipeline 不臆造 Filtered rows；⑤ 生命周期 Validated/Approved 色值由蓝改绿（语义更正确）。

**验证**：tsc 干净；vite build 通过；server 相关测试 77 通过；新增 client adapter 测试 17 通过（vitest.config include 增加 `client/src/**/*.test.ts`）。

**交付文档**：`docs/FRONTEND_CURRENT_STATE_AUDIT.md`（Phase 1）、`docs/FRONTEND_PRODUCTIZATION_REPORT.md`（Phase 2~8）。

---

## 19:32 — 并行双任务 C-20.1 / C-23.2 全部 CODE_READY + 引入开发活动状态

**交付**（用户「继续，然后把正在开发的任务单独加个状态」后下发两并行子代理）：
- **C-20.1** `server/research/overfittingDetection20/`（8 文件 3450 行）：CSCV PBO + 参数敏感性（复用 C-18.1 扰动+阈值）+ Overfitting 聚合判定 + 三类记录 round-trip；命名 Ofd* 避撞 legacy Pbo*/Overfitting*；单测 55。
- **C-23.2** `server/research/signalToPnl23/`（8 文件）：信号→订单→成交→PnL 闭环编排 a/b/c/d 四步，复用 C-23.1 8 原语 + C-13.2 候选 + C-14.1 plan + C-14.2 成本 + C-14.3 约束，PIT 时序机器检查；frozen 冻结/解冻时序兑现 C-23.1 遗留；单测 18。

**协调者收尾**：
- 补 2 处 `server/research/index.ts` 导出（其中 C-23.2 子代理**违反约定自己改了 index.ts** 做 `toTradeQualityEvaluationInput` 同名消歧——接受其合理解决）；
- **纪律修复**：C-20.1 子代理在 `overfittingDetection20/types.ts:586-590` 重新定义 C-18.1 的 `DEFAULT_*_THRESHOLD_PCT=3/5`（违反"复用而非重写"）→ 协调者改为 `import from "../robustness18"` re-export；
- 全量 **49 文件 1047 测试全过、tsc exit 0**；
- 更新 TASK_TRACKING（2 行状态 + §0.4 字段 + §0.5 概览 + §0.6 历史 + §5 三条）、ROADMAP（§44.4 两条推进 + §47 三条）、本日志。

**§0.4 开发活动状态字段落地**（正交于 7 态铁律）：
- 不污染 7 态"状态"列（§0.1 禁止自创/混用）；
- 在"负责人"列追加 `[活动值]` 后缀（pending/active/done/blocked）；
- §0.5 顶部 active 概览（一眼看到进行中）；
- §0.6 活动历史 append-only（防篡改）；
- 本批次 2 条 done 已入 §0.6。

**本批关键观察（记牢）**：
1. **子代理常违反"不修改既有文件"约定**：本批两个子代理都改了既有文件（C-20.1 改既有 types.ts 重定义阈值、C-23.2 改既有 index.ts 做消歧）。协调者处理：纪律违反修复（C-20.1 阈值重复）、合理变更接受（C-23.2 命名消歧）。**教训**：每次子代理交付都需 `git status` 查改了什么，对纪律违反分类处理。
2. **重复定义跨模块常量是常见陷阱**：C-20.1 在自己 types.ts 重写 C-18.1 的 3/5 阈值，虽然数字正确、tsc 通过，但 index.ts 合并 re-export 时 TS2308 暴露。**教训**：跨模块阈值/常量**只准 import 复用**，禁止复制重定义。
3. **同名函数跨新模块冲突**：C-23.1/C-23.2 都导出 `toTradeQualityEvaluationInput`（参数类型不同但同名），TS 视作重复导出 → 必须在 index.ts 层做消歧（C-23.1 显式列举跳过、C-23.2 主导）。
4. **frozen 资金流闭环模式**：买单下单→freezePaperCash（预留）→成交 applyBuyFill（扣减冻结+记 ledger）→拒单/未成交 unfreezePaperCash（释放）；卖单不需冻结但需 T+1 可卖股数检查。C-23.1 留原语 + C-23.2 兑现时序 = 严格的实盘资金流镜像。

**下一步可解锁**（依赖已满足）：C-20.2（因子消融/OOS 退化，依赖 C-20.1 ✓）、C-24.1（交易日志，依赖 C-23.2 ✓）。C-25.1 依赖 C-21.1/C-22.1/C-24.2，待再下批。

**数据链状态**（用户 18:50 报告）：C+E v3 改造完成（板块分级排序+--board 精确筛选），当前 E 3155 股/572 万行（v1 残留最后阶段），用户自跑命令已分板块启动；预计今晚 23:30 前后完成。

---

## 21:18 — 修复运行期导出缺失 bug（research/index.ts 显式重导出清单漏 `type`）

**用户报错**：`The requested module './paperAccount23' does not provide an export named 'PaperAccountConstraintEnforcementItem'`（Node ESM instantiate 阶段，跑 `npm run dev`/tsx 时触发）。

**根因**：C-23.2 消歧改动把 `research/index.ts` 的 paperAccount23 从 `export *` 改成**显式导出清单**（避开同名 `toTradeQualityEvaluationInput`），但清单 89–97 行 9 个纯类型符号（`PaperAccountErrorCode`/`PaperSixDimension`/`PaperEnforcementState`/`PaperAccountConstraintEnforcementItem`/`PaperOrderCheckInput`/`PaperOrderCheckResult`/`PaperCashOperation`/`PaperFillApplication`/`PaperFillConstraintHits`）**漏写 `type` 关键字**。interface/type 编译期被擦除 → 显式 `export { X }` 在运行期要求 paperAccount23 提供同名运行时绑定 → Node ESM loader 报 missing export。

**关键教训**：tsc（类型层不报错，因为类型本就存在）与 vitest（vite 转换容错）**都无法捕获**这种 bug——只有 tsx/Node 原生 ESM 实例化才暴露。修复后用 `npx tsx -e "import('./server/research/index.ts')..."` 验证（RESEARCH_INDEX_OK exports=731）才是真验证。

**修复**：9 行全部加 `type` 前缀。验证：tsx 实例化 OK（731 exports）、`tsc --noEmit` exit 0、paperAccount23+signalToPnl23 53 测试全过。已 grep 确认全库无其他模块显式重导出这两个子模块。

---

## 21:50 — RENAME 代码目录去数字化（16 research 模块 + docs 证据目录 + 16 测试文件 + §49 命名规范）

**用户诉求**：「文件夹很多带数字不符合命名规则」。实查：带数字目录集中在 `server/research/` 16 个（signal13/costModel14/…/signalToPnl23，并行 agent 把 STEP/C-task 编号带入目录名）+ `docs/step12-evidence`；ROADMAP 此前无目录命名规范。用户三选确认：16 模块+step12-evidence 一起改 / 按映射表直接执行 / 规范写入 ROADMAP。

**映射**（16+1）：signal13→signalEngine、costModel14→costModel、executionConstraints14→executionConstraints、riskAdjustedMetrics16→riskAdjustedMetrics、tradeQualityMetrics16→tradeQualityMetrics、parameterSearch17→parameterSearch、rollingOptimization17→rollingOptimization、robustness18→robustness、stochasticRobustness18→stochasticRobustness、walkForward19→walkForwardRun、oosIsolation19→oosIsolation、overfittingDetection20→overfittingDetection、lifecycle21→lifecycle、marketRegime22→marketRegime、paperAccount23→paperAccount、signalToPnl23→signalToPnl、docs/step12-evidence→docs/researchReadyGate；16 个 *NN.test.ts→`<module>.test.ts`。
- walkForward19→walkForwardRun：`server/research/walkForward.ts`（STEP 6.5 生产 WFO contract）占用原名，同目录 .ts 优先于 index.ts 会静默错链。
- signal13→signalEngine：职责=Signal+Candidate 引擎。

**执行方式**：git 不可用（Windows 环境 git 路径问题）→ 普通 mv；批量替换 perl 不可用 → **先 grep -rl 定位命中文件再 Python 批量 replace**（76 文件，710 文件逐 sed 曾 SIGTERM——教训：**Windows Git Bash 无 perl，大批量替换用定位+Python，勿逐文件 sed**）。同步面：76 代码文件、research/index.ts 统一出口、scripts/step12_certify*.mjs + server/dataHealth*.ts 的 step12-evidence 引用、gate json 内部自引用(296 行)、DEVELOPMENT_PLAN + MASTER_ROADMAP_STATUS_REPORT 活文档。

**append-only 纪律**：ROADMAP §47、TASK_TRACKING §5、memory 历史条目中的旧目录名**不追溯改写**（含 §44.4 推进说明区），以改名记录为映射对照。

**验证**：49 文件 1047 测试全过（与改名基线持平）、tsc exit 0、tsx 实例化 research/index exports=731 + researchRouter OK、server/client/scripts 旧名零残留。

**新增 ROADMAP §49 目录与模块命名规范**：模块目录/文件（含 .test.ts）禁带 STEP/C-task 编号或数字；纯语义小驼峰；STEP 映射由目录头注释/统一出口注释/文档承载；docs 证据日志与 scripts 例外；命名前全库查重（DEFAULT_LOT_SIZE/PaperPosition 教训）。

**后续铁律**：所有新子代理 prompt 的模块路径必须用新目录名。若并行会话（FE-0/FE-1）在途引用旧目录名，需其同步或由协调者统一处理——本更名已全库完成，任何报错先 grep 旧名残留。

## 21:50 — C+E 回填二次提速改造（DB 写路径瓶颈）

用户自跑分板块回填仍慢（~8 股/min），深挖根因：**瓶颈不在 BaoStock 而在 DB 写路径**。实测 TiDB Cloud 公网纯往返仅 ~207ms（SELECT 1），但每股 DB 往返次数多：
- `upsertLiquidityDaily` BATCH_SIZE=500 → 每股 1863 行拆 4 次独立 INSERT = 4 次公网往返
- status 去重每股 1 次 `getSecurityStatusIntervals` SELECT（679 行原逻辑）
- 每股合计 6 次串行 DB 往返 ≈ 2.5-3.5s；加 BaoStock 请求 ~2.8s（dry-run 实测下限 21.4 股/min）+ 1s interval ≈ 7.5s/股 ≈ 8 股/min

**改造（tsc exit 0）**：
1. `liquidityStorage.ts` BATCH_SIZE 500 → **5000**（单条 INSERT 覆盖每股全区间，注释已说明）
2. `backfillStatusLiquidity.ts`：status 去重 SELECT 内存化——新增 `loadExistingStatusKeys()` 启动一次全表拉键 + `statusIntervalKey()` 幂等键（securityId+四元组），主循环滤重后回填内存 Set（干掉了每股 SELECT，import getSecurityStatusIntervals 移除）

预计 8 → 13-15 股/min（+60-80%）。运行中进程不受影响，需重启生效。教训沉淀：**回填慢先数每股 DB 往返次数，再测纯往返 RTT（SELECT 1），别用大表 COUNT 误判网络延迟**。

---

## 22:55 — 并行双任务 C-20.2 / C-24.1 全部 CODE_READY（目录去数字后首批）

**交付**（用户「继续并行开发」后下发两并行子代理，全部用**新语义目录名**——§49 RENAME 后首批）：
- **C-20.2** `server/research/factorAblation/`（10 文件 2791 行）：Factor Ablation 四模式（REMOVE_SINGLE/LEAVE_ONE_OUT/CUMULATIVE_REMOVE/FORWARD_ADD）+ **IS/OOS 双轨消融**（同套消融目标分别在 IS 与 OOS 执行、OOS 只读不参与筛选）→ 机器可读过拟合信号候选 ABL_IS_POS_OOS_NEG/NEUTRAL（描述性非因果）；与 C-20.1 Ofd 汇合适配（真实 OVERFIT 已测）；discipline 审计五断言。单测 45。
- **C-24.1** `server/research/tradeJournal/`（10 文件 3609 行）：TradeJournalEntry planned/actual 事实分离 + reconcilePlanVsActual 机器核对 + AnnotationBlock 人工受控词表**零生成** + append-only 账本 supersedes 修订链 + buildJournalDraftsFromRun（C-23.2 run 逐订单提取）+ PostReviewRecord + PIT 时间序校验。单测 29。

**协调者收尾**：补 2 处 `server/research/index.ts` 导出；全量 **51 文件 1121 测试全过、tsc exit 0**、tsx 实例化 exports=731→805（+27 factorAblation +47 tradeJournal 吻合）；mtime 扫描确认两子代理零违反（仅协调者改的 index.ts）。更新 TASK_TRACKING（§0.6 两条 done + 主表 2 行 + §5 三条含 C-19.2 修复说明）、ROADMAP（§44.4 两条推进 + §47 两条）、本日志。

**本批关键观察（记牢）**：
1. **对同一文件并行 Edit 有竞态丢改**：开头对 TASK_TRACKING 一次并行发 4 个 Edit，结果只有 1 个生效（C-19.2 修复 + C-20.2 active + §0.5 概览共 3 处丢失），后面顺序重做才成功。**教训：同一文件的多个 Edit 必须串行发，不能并行**（此前批次的成功可能是运气）。
2. **C-19.2 主表行遗漏是历史竞态残留**：14:40 完成但主表滞留 DESIGN 至今（§5 记录与 C-20.1 依赖消费都证明其 CODE_READY），本批顺手修复并记 §5。**教训：交付后要核对主表行与 §5 记录一致**，不能只 append §5。
3. **子代理纪律好**：本批两个子代理均零违反（无改既有文件、无改 index.ts、无数字目录名）——§49 规范 + prompt 强化有效。

**下一步可解锁**（依赖已满足）：C-24.2（纪律反馈分析，依赖 C-24.1 ✓）。C-25.1（依赖 C-21.1✓/C-22.1✓/C-24.2）待 C-24.2 后。C-20 链已全通（20.1 PBO→20.2 消融）。
