# 首页加载性能优化验收记录

## 优化内容

本次首页性能优化取消了重复的日期列表查询，直接从每日统计结果生成日历日期；取消首页重复的每日题材统计请求，改为从当前日期已加载记录在前端计算；新增 `limitUpDate + limitUpTime` 复合索引；登录状态下把每只股票的关注状态查询改为一次性获取关注列表；情绪预警明细改为打开铃铛弹窗时懒加载。

## 首屏测量

测量环境为本地开发服务器、无登录状态、同一Chromium隔离实例。页面等待到首页列表出现后读取Performance Resource Timing。

| 指标 | 优化前（已完成日期/题材/关注改造，预警明细仍首屏加载） | 优化后 |
| --- | ---: | ---: |
| 首屏tRPC请求批次数 | 2 | 2 |
| 首屏tRPC请求明细 | auth.me + sentiment.getAlerts + sentiment.getUnreadCount + limitUp.getDailyStats；随后当前日/前一日getByDate | auth.me + sentiment.getUnreadCount + limitUp.getDailyStats；随后当前日/前一日getByDate |
| 首屏API最大耗时 | 2853 ms | 279 ms |
| 首屏API累计耗时 | 3085 ms | 503 ms |
| 页面状态 | 首页列表可展示 | 首页列表可展示 |
| 当前日期列表 | 10只 | 10只 |

测量结果保存在本次运行输出中；优化后的首页已在等待8秒后确认页面状态为 `complete`、无“加载中”文本，并出现“涨停股票 10 只”。

## 数据库验证

已应用非破坏性迁移 `drizzle/0006_brief_living_tribunal.sql`，新增索引 `idx_date_time(limitUpDate, limitUpTime)`。执行计划确认按日期并按涨停时间排序的查询使用该索引，并保持索引顺序读取。

## 回归验证

TypeScript检查通过；首页数据工具、首板逻辑、统计接口和关注状态测试通过，共15项相关测试通过。首页查询结构已从多个重复请求收敛为每日统计、当前日和前一日明细三类核心数据请求；预警明细仅在打开铃铛后加载。

## 口径说明

本记录中的“优化前”不是历史版本完整基线，而是同一开发环境中、已完成首页按日加载和批量关注改造后，仅比较预警明细首屏加载开关的测量结果。数据库执行计划和页面测量均用于定位与验证，不代表生产环境固定延迟。


## 交互耗时测量

在同一隔离Chromium实例中，使用真实DOM输入和点击完成回归测量：搜索股票代码 `000001.SZ` 至出现“搜索结果”耗时约57毫秒；点击“当日首板”至股票列表标题更新耗时约123毫秒；点击非当前日期 `2024-12-01` 至题材统计日期更新耗时约404毫秒。三项交互均完成，页面未进入错误边界或登录跳转。
