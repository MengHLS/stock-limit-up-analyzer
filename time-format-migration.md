# 涨停时间格式迁移记录

本次统一规则为 `HH:MM:SS`。输入 `HH:MM` 时自动补为 `:00`，输入单数字小时（如 `9:25:00`）时补齐小时为 `09:25:00`；空值保留为空，其他非法时间拒绝保存。

迁移前，`limit_up_records` 中非空时间共 11,890 条，其中标准三段格式 8,782 条、两段格式 1 条、单数字小时三段格式 3,107 条。迁移先将单数字小时补齐，再将两段格式补秒。

迁移后复核结果：非空时间 11,890 条，符合 `HH:MM:SS` 的 11,890 条，不符合格式的 0 条。

代码层同时覆盖共享格式化工具、数据库单条创建、批量识别保存、手工编辑更新、网页和本地上传提示词、首页/上传页/大盘页显示与时间排序。相关测试共 41 项通过，TypeScript 检查通过。
