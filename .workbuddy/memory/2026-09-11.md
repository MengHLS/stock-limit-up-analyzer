# 2026-09-11 工作日志

## RESEARCH-002 — Research Engine MVP 交付完成（2026-09-11 00:26 GMT+8）

承接 09-10 深夜的引擎实现，本段完成「API 契约测试 + 真实 Dataset 端到端验收 + 报告 + ROADMAP」。

### 交付
- `server/researchEngine/` **21 文件 / 4,156 行**（实现）+ `server/researchEngineRouter.ts` 428 行 / **17 个 tRPC 端点**（注册进 `appRouter.researchEngine`）。
- 五类分析独立文件：`analyses/{descriptive,eventStudy,quantile,conditional,stability}.ts` + `registry.ts` 派发；`metrics.ts` 是全系统唯一指标实现（底层零重写，全复用 `shared/quant-stats.ts`）。
- 测试：**8 文件 / 103 例全过**（metrics 15 / variables 7 / pit 5 / conditionEvaluator 11 / conclusion 11 / engine 15 / analyses 21 / router 18）。
- `scripts/verifyResearchEngine.mts`（529 行）+ `docs/research/RESEARCH-002-e2e-evidence.json` + `docs/research/RESEARCH-002-report.md`（15 节）。

### 真实 Dataset 端到端（Dataset Version 390001，1130 事件，禁止 Mock）
- Experiment「首板换手率与未来5日收益研究」→ Hypothesis → Run → 5 分析全 COMPLETED → **138 结果行** → 结论 **`PARTIALLY_SUPPORTED`**（主观置信 0.5833，非 p-value）。
- **QUANTILE 顶底分位差 −3.0785%**：Q1 +3.0402%(n=107) vs Q10 −0.0383%(n=107)，t=−1.6799，p=0.0930 → 方向为负、**未过统计门槛**、且分位均值不单调（Q2 最高、Q8 为负，方向一致性 0.667）。引擎如实给 PARTIALLY_SUPPORTED，没有声称显著。
- **独立复算**（脱离引擎、直接读物理表自行算切点分组）得 −0.030785，与引擎**完全一致** → 可复现、非黑箱。
- 性能真实测量：引擎 **27,523 ms** = 装配 9,645 + 5 分析 9,770（2072/2122/2198/1724/1654）+ 结论与落库 ~8,108。分析纯计算是毫秒级 → **瓶颈是 TiDB 跨境 RTT（≈0.5s），延迟受限而非算力受限**，优化方向是减少往返次数。

### 本段发现并修复的真实缺陷（E2E 与测试各暴露一半）
1. **预检失败不落库 → Run 永久 PENDING**（违反可追溯）。修：`runIdentified` 边界；用 `startedAt` 空/非空区分「预检拒绝」vs「执行崩溃」；无法归因则绝不改写状态。
2. **EVENT_STUDY 无条件要求 `max_return_{h}d` 等 outcome 聚合变量** → T+1/T+3 在解析需求阶段 `UNKNOWN_VARIABLE` 崩溃（根因：path 视界 1..20 ≠ outcome 视界 {5,10,20}）。修：按目录过滤 + 缺失项如实记 notes。
3. **CONDITIONAL 同族缺陷**：无条件要求 `max_drawdown_{h}d` → path 独有目标视界崩溃。同样修。
4. **STABILITY 单组恒为 1.0** → 会被误读为「高度稳定」。修：分组<2 不产出 `STABILITY_RATIO` 并说明「无法评估跨期稳定性」（v1 只有 1 个板块，直接生效）。
5. **测试夹具比真实数据宽松**（outcome 视界误写 `[1,3,5,10,20]`）→ 掩盖缺陷 2/3。修：夹具改为与真实 Dataset 同构并加注。
6. **验收脚本分组样本数被后序指标覆盖** → CONDITIONAL 中 `MEAN_RETURN`(n=1065) 与 `MAX_DRAWDOWN`(n=1127) 分母不同却被覆盖。修：逐指标记录 n + 显式标注分母不一致。

### 验证
- `npx tsc --noEmit` exit 0。
- 聚焦回归 `datasetRegistry`+`researchCore`+`shared`：**19 文件 / 376 例全过**。
- 全量 vitest：**194 文件 / 2839 例**（2823 通过 / 16 失败）；**失败文件 8 个与 RESEARCH-001 基线逐字一致**（`dataHealth`/`image.uploadAndRecognize`/`limitUp`/`limitUp.watch`/`marketData`/`researchRunRouter`/`tushare.secret`/`tushareTradingCalendar`，全为既有环境依赖失败）。文件数 +8、通过数 +103 恰为本次新增 → **零新增失败**。

### ROADMAP
- §44 头部快照覆盖式更新至 2026-09-11 00:26；§44.5 队列 9 → 已完成，新增 10（DATASET-003B P1 重建，RESEARCH-003 硬前置）与 11（RESEARCH-003 建议）；§47 append 一条完整记录。

### 遗留（🔴 阻断策略结论）
- **DATASET-003B P1（涨停价四舍五入到分）修复后重建 smoke/v1/v2 仍未执行** → `ds_*` 仍是旧口径（封板样本系统性漏判 ≈38.03%）。**RESEARCH-002 只证明「引擎可用」，基于其上不得产出任何策略结论。**
- 上游数据：`liquidity_daily.totalMarketCap`/`circulationMarketCap` 全 NULL（9,015,158 行）→ 市值/流通盘特征恒 null；industry `effectiveFrom` 单点 → `industry` 维度绝大多数不可用。
- v1 窗口仅 1 个月且仅主板 → 时间/板块稳定性无法评估，需构建跨年跨板块 Dataset Version。
- 工程债：Drizzle 无外键且 `delete` 不级联 → 删 Experiment 留孤儿（E2E 脚本已 leaf-first 手动清理，产品化需补级联）。

## 行情同步页性能改造 — 位图索引 + 服务端分页（2026-09-11 01:25 GMT+8）
### 问题实测（真实 DB，非估算）
`stock_daily_prices` **8,893,077 行**；`limit_up_records` 去重后 **99,411 条**记录。
旧链路每次加载：`getStockDailyPricePairs()` 全表两列取回 = **700,376ms（≈11.7 分钟）**，再建 8.9M 字符串 `Set` +23,280ms（内存≈1GB）→ 单次加载 **12 分钟起**，且把 99,411 条整体回传浏览器做 filter/sort。

**根因定量**：跨境 TiDB 有效带宽约 **10~14K 行/秒**（38 万行 36s / 135 万行 94s / 1 年 903K 行 185s）。
反证「让 DB 算」也不通：`NOT EXISTS` anti-join 203s、`LEFT JOIN` anti-join 53s、`GROUP BY stockCode` 52s、`GROUP_CONCAT` 原始日期 93MB/307s。**结论：该表任何形态的批量读取都不可行，只能缓存 + 增量维护。**

### 交付
- **`server/stockPriceIndex.ts`（新）**：把「股票 × 交易日是否存在行情」压成**交易日序号位图**（起点 2019-01-01，每股票 512B，全量 5,797 只 ≈ 2.8MB，内存从 ~1GB → ~3MB）。磁盘快照 `.cache/stock-price-day-index.json`（4.1MB，**84ms 载入**）；缺失/过期时用一次 `GROUP_CONCAT(DATEDIFF(...))` 聚合重建（**42.8s**，事务内 `SET SESSION group_concat_max_len=4294967295` 防静默截断）；所有 `upsertStockDailyPrices` 写入点改为 `upsertStockDailyPricesTracked` 做**增量置位**（表只增不删，单调安全）。
- **服务端分页/筛选/排序**：`checkStockPriceSyncPage({page,pageSize,search,onlyMissing,sortDir,refresh})` → 只回传当前页 + summary + total + 索引元信息；索引未就绪返回 `{ready:false}` 由前端轮询。结果 TTL 缓存 5 分钟，写操作（同步/校正/停牌/上传）统一 `invalidateStockPriceSyncCache()`。
- **`server/db.ts`**：新增 `getStockDailyPriceDaySeries()`（聚合）、`getIndexDailyTradeDates()`（用 `index_daily` 7,452 行 / 0.22s 替代大表 `DISTINCT` 8.4s）；`expandSuspendedDatesByStock` 从 O(窗口×交易日) 嵌套扫描改为二分定位；`getStockDailyPricePairs()` 标记 **@deprecated 危险**（零调用方）。
- **前端 `StockSync.tsx`**：改服务端分页（`placeholderData: keepPreviousData`）、搜索 300ms 防抖、索引构建中轮询提示、显示「结果生成于 / 索引构建于」。路由新增管理员 `rebuildStockSyncIndex`；服务启动 `ensureStockPriceIndex()` 预热。

### 验收（`scripts/verifyStockPriceSyncIndex.mts`，真实 DB，**15/15 PASS**）
- 索引对位数 **8,893,077 == DB 行数**（差 0，证明 GROUP_CONCAT 无截断）。
- 抽样 300/300 命中；未同步日期 2026-09-11 与伪造代码 0 误判；缺失明细与 SQL 直查对账 0 冲突。
- summary：记录 99,411 / 完全同步 98,641 / 部分缺失 770 / 缺失对位 2,855。
- **冷构建 42.8s（旧 723s，↓94%）→ 之后磁盘快照 84ms；页面第 1 页 5.2s（含首次计算），第 2 页 2ms。回传 99,411 条 → 20 条。**
- `tsc --noEmit` 0 错误；聚焦回归 5 文件 / 26 例全过。

### 遗留
- 页面首屏仍约 5s（`getLimitUpRecordsForSyncCheck` 取 99,411 行 ≈ 7.6s），已由 5 分钟缓存覆盖；若要再降需把 summary 也搬进 SQL。
- `getMissingStockPriceRequirements` / `getLeaderCandidateDailyPriceMap` 仍走 `loadStockDailyPriceRows` 全量取回（889 万行 × 9 列），**同一性能问题未修**，是下一个该改的点。
- 绕过服务端的脚本回填（`backfillDaily.ts` 等）会滞后索引，靠快照 7 天过期兜底 + `rebuildStockSyncIndex` 手动重建。

## RESEARCH-002 复跑复核 + 下一步判定（2026-09-11 18:15 GMT+8）

### 复跑结论（同一脚本 / 同一 Dataset Version 390001 / 全新进程）
- **统计产物逐位一致**：`sampleCount=1130`、`resultCount=138`、结论 `PARTIALLY_SUPPORTED`、QUANTILE 顶底差 `-0.030784914547101534`（独立复算 `-0.030785`）。→ 可复现性由「单次通过」升级为「重复一致」。
- **只有耗时波动**：引擎 A 27,523 ms vs B 16,846 ms（1.6×）；波动集中在**往返最多的装配阶段**（9,645 → 1,353 ms，**7.1×**），各分析稳定 1.5~2.2 s。→ 强证据支持「延迟受限而非算力受限」。
- 复跑后再次实测 `research_*` 六表计数全 0、`ds_*` 未变 → 复跑同样零残留。
- 期间一次后台实例以 `RC=4` + 空输出退出（**同日志文件被并发实例争写**）：零副作用。**教训：同一验证据本不要并发跑，输出重定向到同一文件会互相截断。**

### 报告同步
`docs/research/RESEARCH-002-report.md` 新增 **§12.7 重复运行一致性复核**（含 RC=4 如实记录）；§13.1 改为**两次运行对比表**并给出「耗时波动 / 结果不波动」的判据；修正 §13.4 的 139 → 138 行。

### 下一步（真实状态，ROADMAP §44.5 第 10/11 条）
- **P1 涨停口径修复：代码已到位**（`server/data/boardRules.ts` 的 `exchangeLimitUpPrice` 含 `Math.round(...*100)/100`；`docs/DATABASE_REDESIGN.md:421` 确认「代码已修复、数据待重建」）→ **只差重建 `ds_*`**。
- 实查 `dataset_version`：390001 = `v1` READY（2026-08-01→08-31 / 1130 事件 / 60002 行）；390002 = `v2` **BUILDING 且 totalEvents/totalRows 均为 NULL**，窗口 2026-09-01→09-01 → 即 `completeJob → markReady` 两阶段非原子缺陷的**活体证据**（v2 卡死）。
- 口径警示：`runDataset001Build.mts` 明确「对已存在的版本改 flag 不会改变其口径 —— 需要新口径请用新的 version 标签」，且 upsert 为 `ON DUPLICATE KEY` → 原地重建需先清版本行，否则有**孤儿残留**风险（P1 只在绝大多数情形下是「只增不减」，非严格单调）。

## RESEARCH-002 前端工作台交付（2026-09-11 18:35 GMT+8）

用户决策：**做 Research 前端工作台 + 不要管 dataset 那边**。在已就绪的 17 个 `researchEngine` tRPC 端点之上构建完整工作台；**`ds_*` 未重建、DATASET-003B P1 未启动**（遵用户指令）。

### 交付文件
- `client/src/adapters/researchEngineAdapter.ts`（唯一展示契约层，DTO → VM）+ `researchEngineAdapter.test.ts`（26 例）。
- `client/src/components/research/`：表单纯函数 `createExperimentForm.ts`（16 例）/ `createAnalysisForm.ts`（31 例）+ 6 组件（`CreateExperimentDialog` / `CreateAnalysisDialog` / `AnalysisResultsView` / `ConclusionPanel` / `RunEngineButton` / `VariableCatalogCard`）+ `index.ts`。
- `client/src/pages/research/`：`ResearchList.tsx` / `ResearchDetail.tsx`（4 Tab 工作台）/ `index.ts`；路由 `/research`、`/research/:experimentId`。
- 修改：`client/src/lib/status.ts`（Research 状态色）、`client/src/App.tsx`、`client/src/components/AppShell.tsx`（「研究实验」入口）。
- `scripts/checkResearchWorkbenchApi.mts`（真实 DB 只读契约核对）。

### 三条「不造假」防线（有单测锁定）
1. **指标单位二义性**：`MEAN` 在 `turnover` 是 %、在 `market_cap` 是绝对数、在 `days_to_breakout_5d` 是「天」→ `metricUnitOf(code, variableName)` 先查自单位码表、否则按变量名判定、**判不出返回 NUMBER 绝不猜**。
2. **缺失指标不伪装 0**：`metricValue=null` → `formatMetricValue` 一律渲染 `—`（含 6 位小数的极小非零专门处理，避免 `0.0000%` 被误读为 0）。
3. **不编造统一分母**：同组内各指标样本数合法不同（`MEAN_RETURN` n=1065 vs `MAX_DRAWDOWN` n=1127）→ 分组表逐指标保留各自 `n=`，仅权重一致时才给 `uniformSampleCount`，否则显琥珀徽标。

### 类型安全枚举镜像
`as const satisfies ReadonlyArray<{value: ResearchType…}>` → 后端枚举一变前端**编译期即失败**（防静默漂移）；`primaryPriority` 显式镜像后端 `PRIMARY_PRIORITY`（让「主分析怎么选」在 UI 可见）。

### 顺带修复后端真实缺陷（构建消费侧时才暴露）
`server/researchEngine/conclusion.ts` 的 `evidence` 在**两个分支下形状不一致**（无主分析分支手写 `{disclaimer,policy,trace,analyses}`、主分支写 `{…,ruleTrace,contributingAnalyses}`）→ 下游需两套解析器。修：抽出单一构造入口 `buildEvidence()`，`primary: null` 表「本次无主分析」而非键缺失；`conclusion.test.ts` 加回归断言两分支键集相同（该文件 12 例全过）。

### 验证
- `npx tsc --noEmit` **exit 0**。
- 前端新增 **3 文件 / 73 例全过**；全链路聚焦（引擎 + 前端 + datasetRegistry + researchCore + shared）**32 文件 / 577 例全过**。
- `npx vite build` **RC=0**（✓ 2983 modules transformed）。
- **全量 vitest 197 文件 / 2913 例**（2897 通过 / 16 失败）：相对实施前 **+3 文件 / +74 例**，**失败数恒为 16、失败文件恒为同样 8 个既有环境依赖文件 → 零新增失败**。
- 生产 bundle grep 服务端专有标识（`engineAssert`/`FEATURE_VARIABLES`/`PIT 安全特征`/`neweyWestMeanTStat`/`ds_first_limit_pullback`）**全部未出现**（无服务端运行时泄漏）。
- 真实 DB 只读契约核对 **24/24 PASS**（含「`future_return_{h}d` 覆盖 path 全 1~20、聚合类结果变量**不为** outcome 未覆盖视界发明、`unavailableDimensions` 显式列 regime 及原因、不存在的 Version → NOT_FOUND」等不虚构不变量）。

### 关键契约事实（写前端时必须记住）
- `ResearchEngineRunResult` 是**扁平**的（`sampleCount`/`resultCount`/`durationMs`/`sampleBuildMs`/`analysisCount`/`conclusionType`/`analyses[]` 都在顶层，**无 `run` 包装**）。
- tRPC 条件 schema 的 `value: z.unknown()` 是**必填键** → `IS_NULL`/`IS_NOT_NULL` 必须发 `value: null`，**省略键会被入参校验直接拒掉**。
- 分析级 `notes` **不单独持久化**，只在 `evidence.contributingAnalyses[].notes`；UI 只能从结论面板呈现。
- `metricDefinition` 只对 DESCRIPTIVE 行存在（引擎写 `details.metricDefinition`）；前端**故意不注册** `research_analysis_metric`（复制即第二份权威）。
- 引擎只接受 `READY` 版本（`engineAssert(versionContext.status === "READY", …)`）。

### 报告 / ROADMAP 同步
`docs/research/RESEARCH-002-report.md` 新增 **§16 Front-end Workbench（8 小节）**，并同步 §11.1（+前端 3 文件 73 例）、§11.3（全量 197/2913，+3 文件/+74 例，零新增失败）、§11.4（缺陷 6 → **7 项**，补 evidence 形状）、附录 A（前端文件清单 + 修改清单）。ROADMAP §44 头部覆盖式更新至 18:35、§44.5 第 9 条加 **9b 子项**、§47 append 一条完整记录。

### 注意到但未动的结构问题（供后续治理参考）
ROADMAP 的 `# 47 更新记录` 区块存在**结构错位**：`# 49` 标题（约 2436 行）**之后**仍夹杂若干 09-08/09-09 的 §47 条目（约 2437–2520 行），即 §47 日志被 §49 标题割成两段。本次按「主日志尾部」追加（09-11 00:26 那条之后）。建议后续单独做一次章节重排，但**不要**在未确认前批量搬动 append-only 记录。

## 研究模块缺口审计（2026-09-11 18:50 GMT+8，纯代码证据，未改代码）

用户问「研究模块还缺哪些」。以代码/schema 为准逐项核实（**非**凭 ROADMAP 文字）：

### 引擎 API 完整性
- **`researchEngine` 17 个端点全是 query + create + runEngine，零 update / 零 delete**（`server/researchEngineRouter.ts` 全文无 delete/update/remove/archive）。
- **但仓储层早已支持**：`server/researchCore/repository/contract.ts` 已定义 `experiment/hypothesis/run/analysis/metric/conclusion/candidate/artifact` 的 `update` + `delete`，以及 `conditions.replaceForAnalysis`、`results.deleteByAnalysis`、`conditions.deleteByAnalysis` → **全部不可达**（tRPC 层没暴露）。
- **级联删除逻辑只存在于测试脚本**：`scripts/verifyResearchEngine.mts#cleanupExperiment`（leaf-first）未进领域层 → 产品化删实验会留孤儿（ROADMAP 已登记该工程债）。
- `setAnalysisConditions`（admin，整批替换条件）**有端点但前端未接** → 改筛选口径无 UI 入口；且改完不标记结果/结论失效。

### 前端工作台（RESEARCH-002 UI）
- 4 个 Tab（pipeline/results/conclusion/variables）**零** 导出 / 删除 / 编辑条件入口（grep 全空）。
- `listCandidates` 端点未被前端消费（候选池在 UI 不可见）。
- `getAnalysis` / `listRuns` 未用（非缺陷，UI 从 `getExperiment` 派生）。

### 研究链路其它页
- **FE-4「运行工作台」是占位符**：`StrategyEditor.tsx:946 RunWorkbenchTab` 用 `emptyRunResult()` + `RunResultPlaceholder`；`researchRun.readiness` 的 `executorBound = false` **硬编码** → 「策略 → 跑研究」在 UI 上断链。
- **闭环 executor 未装配**：`server/research/closedLoop/orchestrator.ts`（C-25.1）在，但无真实 executor 绑定（未把 simulator C-14.1 / 指标 C-16 / regime C-22 / journal C-24 接进 14 阶段）。
- **FE-8 报告导出无后端服务**：`RegimeReport.tsx` 只能客户端导出 canonical JSON 快照；「研究结论报告」服务不存在。
- **策略版本追溯在 UI 不可见**：`research.strategy.listVersions` / `loadVersion` / `delete` 未被前端调用（前端只用 validate/bump/compare/save/load/list/createVersion）→ 看不到历史版本、不能回滚、不能删除。

### 引擎能力
- **`regime` 维度不可用**：`listVariables.unavailableDimensions` 含 regime → 不能按市场状态分组。需把 `marketRegime22` 接成合法 `RegimeTagProvider`（ROADMAP §15.3 第 6 条）。
- 组内指标分母不统一（引擎已如实标注，未对齐）。
- `time_to_target` / `time_to_stop` 刻意不实现（依赖 Dataset 定义交易规则）→ **用户指令「不管 dataset」→ 保持不做**。

### 判定
「研究模块」的**引擎算法层已完整**（5 类分析 + 结论 + PIT），真正的缺口集中在 **① 可维护性（CRUD / 级联删除 / 编辑条件）② 运行闭环装配 ③ 维度与导出**。其中 ① 是**纯代码、零数据依赖、仓储层已就绪**的薄改动 → 最高性价比。

## 龙头候选池页面性能修复（2026-09-11 18:55 GMT+8，用户报告「数据太多卡住、无法渲染」）

用户约束：**保持全部历史口径不变、只做分页**；并要求**尽量少跑脚本**（DB 是跨境 TiDB，测试很费时）。

### 真实测量（临时探针 `scripts/_lc_perf.ts`，一次性；已删）
| 阶段 | 实测 |
|---|---|
| `getLeaderCandidates()` | **28,819 ms**（全表 99,577 条 `limit_up_records` + 情绪周期 + 当日价格） |
| `loadBacktestBaseContext({})`（全区间） | **196,167 ms**；99,577 条涨停记录 → **1,524,646 行价格行**（每股涨停日 ±44 天窗口并集）、1,863 交易日 |
| `getLeaderCandidateBacktest({observationDays:1})` 全链 | **304,900 ms**（= 196s 取数 + 80s 模拟 + 其余） |
| 明细 | **3,463 行 / 3,155,000 B**；整个结果 JSON **5,528,178 B**；`trades` 3,056 笔 |

**修正我先前的估算**：曾以为明细约 5 万行 / 30MB，实测只有 3,463 行 / 3.15MB。→ 「无法渲染」的实质是**页面 5 分钟不出内容**（+ 5.5MB 一次性序列化），不是 DOM 撑爆。**成本几乎全在跨境传输 152 万行价格行**。

### 交付 1：服务端分页 + 载荷裁剪（不动任何统计口径）
- `server/leaderCandidateHistory.ts`（纯函数，无 IO）：`paginateLeaderCandidateHistory` / `filterLeaderCandidateHistoryRows` / `stripLeaderCandidateHistory`。契约：页码页长夹取（≤200）、越界夹到末页（数据收缩不出空白页）、计数恒为筛选后全量、无过滤走零拷贝快路径、**保持上游排序**（候选日期倒序 → 评分降序）。
- `db.ts#getLeaderCandidateHistoryPage`：复用同一份结果缓存，只在服务端切片 → **翻页不重算**。
- `routers.ts`：`getLeaderCandidateBacktest` 改 `stripLeaderCandidateHistory(...)` → **不再回传 `historicalRows`**（改回传 `historicalRowCount`）；新增 `sentiment.getLeaderCandidateHistoryPage`。
- `sentimentCycle.ts`：抽出 `SENTIMENT_CYCLE_PHASES` 作阶段唯一权威顺序，zod schema 与漏斗 `phaseOrder` 共用（消字面量漂移）。

### 交付 2：明细磁盘快照（解决 5 分钟等待，**不改任何计算**）
- 新模块 `server/leaderCandidateBacktestSnapshot.ts`：`.cache/leader-candidate-backtest/<hash>.json`，TTL **6h**、只存 ≥256KB 的「昂贵」结果、原子写（tmp+rename）、最多留 6 份、**任何 IO/解析失败静默降级为未命中且绝不上抛**。
- `getLeaderCandidateBacktest` 读取顺序：内存(30min) → **磁盘(6h)** → 重算并落盘。→ 进程重启后（dev 下 `tsx watch` 频繁重启）不再重付 5 分钟。
- **失效（是正确性修复，不只性能）**：新增 `invalidateLeaderCandidateBacktestCaches()`，挂在 `createLimitUpRecord` / `createLimitUpRecordsBatch` / `updateLimitUpRecord` / `deleteLimitUpRecord` / 批量改代码 / `upsertStockDailyPrices` / `upsertSuspensionWindows` / `deleteSuspensionWindow` 之后清空内存 + 磁盘。**顺带修掉一个既有真实缺陷**：此前上传新涨停数据后，30 分钟内存缓存会继续回传旧数字。
- **数据版本号**：回测计算期间发生写入（结果可能基于半同步数据）→ **只回传不写缓存**，避免把中间态固化。

### 交付 3：前端（删掉 4,129 字符的巨型单行表格）
- 新 `client/src/components/CandidateHistoryTable.tsx`：只渲染服务端当前页；行类型直接 `inferRouterOutputs` 自 tRPC（防字段漂移）；加载/错误/空态 + 重试。
- 新 `client/src/components/PaginationBar.tsx`：首/上/下/末页 + 页码省略号 + 每页条数 + 第 x/y 页。
- `LeaderCandidates.tsx`：阶段筛选由「前端过滤整份明细」改为**服务端参数**（筛选变化自动回第 1 页）、`placeholderData: keepPreviousData` 翻页不闪、刷新按钮同时刷新明细。

### 验证（遵「少跑脚本」：以**零 DB 依赖**的聚焦测试为主）
- `npx tsc --noEmit` **exit 0**；`npx vite build` **RC=0**（17.6s）。
- 新增 3 个测试文件 **28 例**（`leaderCandidateHistory.test.ts` 12 / `leaderCandidateBacktestSnapshot.test.ts` 11 / `leaderCandidatesPage.test.ts` 5 源码级防回归）；聚焦套件 **6 文件 / 63 例全过，实测 2.75 s（不连库）**。
- 真实 DB 验收 `scripts/verifyLeaderCandidateHistoryPaging.mts`（一次性，兼作快照预热）**全项 PASS**：冷调用 **413.4s** → 翻页 **0ms**（命中缓存，与第 1 页重叠 0 行）；`totalRows=allRows=historicalRowCount=10,336`（207 页 @pageSize=50）；**单页响应 46.9KB vs 全量 10.83MB**（剥离明细后 1.30MB，≈230×）；**越界页码 99999 → 夹取 207/207**；阶段筛选「冰点试错」totalRows=0 如实返回；**磁盘快照命中 true 且含 10,336 行**（11,353,097 B）。
- 小窗口烟测 `scripts/_lc_smoke.mts`（2026-08）**14.1s** PASS：97 行 / 4 页、翻页 0ms、重叠 0、剥离后无 `historicalRows`；**97 行 < 256KB 阈值故未落盘**（符合设计）。
- 🔴 **两次测量行数不同（首探针 3,463 → 复核 10,336）不是本修复所致**：期间工作区被**另一会话并发改动** —— `git diff --stat HEAD` 显示 `server/downsideRisk.ts` 未提交 **248 行**、`server/leaderCandidates.ts` **109 行**，内容为「高位连板风控 `maxParticipatingBoards`/`boardHeightRiskContribution`/`positionScale`」；`downsideRisk.ts` mtime **18:50:41** 恰落在验收运行途中，且首次 `tsc` 报的 `Property 'maxParticipatingBoards' does not exist` 随后自行消失（类型与用法被分批写入的痕迹）。本修复只裁剪 **router 响应**，不触碰候选生成/评分/校准 → 两者正交。**注意：`downsideRisk.ts` / `leaderCandidates.ts` 现有未提交改动不属于本次任务。**
- 复核数值（快照回读）：`appliedMinScore=45` / `recommendedMinScore=45` / `calibrationSampleSize=523` / `trades=1213`。

### 遗留（明确未做，附原因）
1. **首次**全区间计算仍需 ≈305~413s（地板是 152 万行价格行跨境传输 ≈196s）。用户选择不改窗口，故首屏要等一次，之后 6h 内（含重启）走快照秒回。
2. 真正的 10× 提速要把价格窗口从「全部 99,577 条涨停记录」收窄到「可成为候选的记录」（3,463~10,336 / 99,577）；但 `strategy/strategyBacktest.ts` 第 3 步用 `rawRows` 的日期并集推导 `startDate/endDate` → 收窄可能裁剪最早候选的回溯窗口，**必须 A/B 逐字段对比才能改**（本轮风险 > 收益，未做）。
3. `getLeaderCandidates()` 28.8s 仍在关键路径（页面骨架要等它）；改法涉及有界回看（口径边界）→ 需单独评审。
4. `StockSync.tsx` 仍内联一套分页控件，可后续改用 `PaginationBar` 去重。


## 组合回测页风控优化：高位连板约束 + 历史缺失字段兼容（2026-09-11 18:57 GMT+8）

用户需求两条：① 策略对**高位连板股**（5/6 板及以上）风险评估不足，需限制参与或降仓；② **2025-11 前**大量数据缺 `limitUpTime`/`keywords`/`sector`，需降级处理且**不得影响 2025-11 之后的原有口径**。要求先梳理结构 → 说明方案 → 再实现。

### 定位到两个真实根因（不是「参数不够」，是口径缺陷）
1. `server/downsideRisk.ts#calculateRiskContributions` 旧口径把 `boards >= 4` **一律记 20 分** → 5/6/7 板与 4 板等价；硬过滤（`riskScore >= 65`）与质量门控只按风险总分裁决 → 高位标的一样过。
2. `normalizeSectorName` 把**缺失 sector 兜底成同一个「其他」题材** → 整日缺失记录聚合成数百只**巨型题材** → 虚增题材共振得分 + 让「题材支撑不足」永不触发 + 「封板偏晚」误报。**缺失被当成了强度证据**。

### 架构（两个新 shared 纯函数 = 唯一口径，无 IO，可单测）
- `shared/boardHeightRisk.ts`：`boardHeightRiskContribution` 阶梯 3/4/5/6/7+ = 12/22/34/46/60（2 板 5、≤1 板 0、**7 板以上封顶**）；`isBoardParticipationRestricted(boards, max=6)`；`boardHeightPositionScale` → 5 板 0.6、≥6 板 0.3、越上限 0。
- `shared/fieldAvailability.ts`：`hasFieldValue` / `isFieldCoverageReady`（阈值 0.6）/ `buildFieldCoverage(ByDate)` / `buildFieldCoverageReport` / `resolveThemeWithFallback`（sector 缺失才用 keywords 首词兜底）。**口径：缺失既不作风险证据、也不作强度证据**。

### 三管齐下（分层职责未被打破）
- 风险分层：阶梯分进入 `calculateRiskContributions.boards`。
- 限制参与：`selectQualityGateProfileKeys` / `selectQualityGateRowKeys` / `buildTradeDifferences.hardFilterExcluded` 增加 `isBoardParticipationRestricted` 门槛；`baseline` **恒不施加**（对照基准）。
- 降仓：`LeaderCandidateBacktestRow.positionScale` → `server/realisticBacktest.ts:474` `plannedBudget = budget * (row.positionScale ?? 1)`，**不改排序、不删候选**。
- 顺带重构：抽出共用 `buildStrategyExperimentRows(...)`（原先 `buildExperiments` / `buildExperimentsWithWindowWeights` 两份重复口径）+ 新增共用 `ExperimentRunner` 类型。

### 缺失字段降级（`server/leaderCandidates.ts`）
- `buildLeaderCandidatesForDate`：题材聚合**只并入可解析题材**；整日缺失 → `neutralSectorCount = median(同日可解析题材家数)` 中性兜底（原先是「灌进一个巨型题材」）。
- `calculateRiskContributions` 的 `sectorCount`/`limitUpTime` 按 `sectorAvailable`/`limitUpTimeAvailable` **可用才计分**。风险标签区分「本股缺失」vs「整段缺失（历史未采集）」。
- 新增 `LeaderCandidateBacktestResult.fieldCoverage`；默认候选准入增加 `sectorAvailable && limitUpTimeAvailable` 前提。
- `keywords` 打通：`server/db.ts` 两处 select 补 `keywords`，`LeaderCandidateSourceRecord.keywords?`。
- 新增参数 `downsideRisk.maxParticipatingBoards`（`server/routers.ts` zod 1~20 可选）。

### 前端（`client/src/pages/Backtest.tsx`，仅增量，未动既有区块）
`BoardHeightRiskParamsCard`（参数页：梯度表实时预览「限制参与 / 降仓」+ 上限输入）、`FieldCoverageNotice`（overview/risk 页覆盖率提示条，**只在降级时出现**）、overview 新增 `[data-board-height-impact]` 各策略限制参与/降仓计数表。config 增 `maxParticipatingBoards: 6`。

### 真实库端到端验证（2025-09-01 ~ 2025-12-31，探针已删）
4,678 行涨停记录；覆盖率各 0.6905；降级月份仅 `2025-09`（时间/板块/关键词均 0.0%）。连板分布 1板:6 2板:54 3板:87 4板:60 **5板:38 6板:22 7板:13 8板:7 9板:3 10板:2 11板:1 12板:1**（5 板及以上 87 只，7 板及以上 27 只）。风控生效：`riskPenalty` 降仓 87；`hardFilter` 限制参与 27 / 降仓 59 / 剔除 28；`qualityGate` 限制参与 27 / 降仓 32 / 剔除 130。
**关键回归证据（证明不影响 2025-11 后口径）**：2025-11 前 vs 后 平均评分 **74.27 / 74.28**、平均风险分 **24.7 / 24.71**。

### 测试与回归
- 新增 3 文件 **26 例**：`shared/boardHeightRisk.test.ts`(6) / `shared/fieldAvailability.test.ts`(10) / `server/boardHeightRiskControl.test.ts`(10)。聚焦套件 `5 文件 / 47 例全过，2.75s（不连库）`；`npx tsc --noEmit` exit 0。
- 过程中修掉自己 4 个测试缺陷：① `buildExperiments` 误把 `penaltyWeight` 当 `penaltyWeightByDate` 传入（`tsc` 暴露）；② 测试要求 1→8 板严格递增但 7 板封顶 → 改为只对 3~7 板要求严格递增 + 单独断言 `contribution(7) === contribution(12)`；③ `boardHeightExcludedCount` 语义修正为「全部被限制参与的候选数」（原只统计 `riskScore < hardRiskThreshold` 的，漏 7 板 60 分标的）；④ 质量门控按同日横截面，4 板恰为中位数被保留 → 预期改 1。
- `server/backtestPage.test.ts` 曾因**外部并发编辑** `LeaderCandidates.tsx`（历史明细表拆到新组件 `client/src/components/CandidateHistoryTable.tsx`）而失败 2 断言（`"龙头/风险/净评分"`、`"风险分只使用每行信号日信息"` 文本随表格迁移）。**修法：让该测试合并读取两处源码**（保持断言强度不变），**未改任何被断言的产品代码**。修后 5 文件 47 例全绿。

### 遗留
- 全量回归：改动前 `16 failed | 2897 passed (2997)`，改动后 **`16 failed | 2981 passed (2997)` —— 失败文件集合完全一致**（8 文件：`dataHealth` / `image.uploadAndRecognize` / `limitUp` / `limitUp.watch` / `marketData` / `researchRunRouter` / `tushare.secret` / `tushareTradingCalendar`），全为环境类（Tushare token / 交易日历缓存超时 / marketData / 增量调度 mock），**与本次改动无关**。首次跑曾多出 `researchEngine/maintenance.test.ts` 1 例，重跑不复现 → **确认是外部并发编辑 `researchCore/conditions.ts` 的瞬时态**，非本改动引入。
- 阈值（6 板上限、0.6 覆盖率）目前为**经验值 + 可由页面参数覆盖**，尚未做样本外寻优 → 属 RESEARCH_READY 之前的工作。

---

## DATASET-PERF-001 数据集构建性能专项（18:58，任务：用户「构建速度慢、分片策略不好」）

### 根因（真实 TiDB 实测，探针跑完即删）
RTT ≈ **208ms/次** → 构建是**延迟受限**，不是算力受限。三条实锤：① **连接池形同虚设**（全程 `await` 串行，`connectionLimit=10` 实际只用 1 条；并发标定 1/2/4/8 → 7,704 / 8,425 / 12,733 / **17,862 行/s**）；② **取数粒度太粗**（Phase 1 逐日拉全市场 5,349 行/796ms、Phase 2 逐事件拉全市场，61 天全市场 219,455 行/16,925ms）；③ **分片维度用错**（按「天」切，真实单元是「事件集合」）。

### 五项优化（**零口径变更**）
1. **连接池真正生效** `server/db.ts`：`resolvePoolSize()`（`DB_POOL_SIZE`，默认 10 / 上限 64）+ `maxIdle`/`idleTimeout`/`queueLimit:0`/`keepAlive`/`connectTimeout`。
2. **SQL 下推粗筛超集** `detection.ts`：`LIMIT_UP_CANDIDATE_SQL_PREDICATE` = `` `closePrice` >= `preClosePrice`*1.045 OR (`preClosePrice` < 1.00 AND `closePrice` >= `preClosePrice`) `` + JS 等价 `isLimitUpCandidateBar`。**1.045 安全性有数值证明**：昨收 ≥ 1 元时 `Math.round(k*1.05)/k` 最小值 = **1.0458716**（k=109）> 1.045，余量 0.083%；昨收 < 1 元由 guard 子句兜住。
3. **定向取数替代整段全市场** `db.ts`：`fetchBarsForSymbolsInRange` / `fetchLiquidityForSymbolsInRange` + `monthShards` 月度并发。
4. **分片细化** 新增 `server/datasetRegistry/concurrency.ts`：`EVENT_CHUNK_DAYS=20` / `PATH_CHUNK_DAYS=30` / `SYMBOL_BATCH_SIZE=400` / `MAX_ROWS_PER_STATEMENT=2000`（占位符 65535 上限）/ `CANDIDATE_RANGE_MAX_MONTHS=1` / `INSERT_CONCURRENCY=2`。
5. **有界异步并发** `mapWithConcurrency` / `forEachWithConcurrency`。🔴 **明确不用 `worker_threads`**：`limitUpDays` 连板状态必须单线程串行推进，Node 无共享内存 → 「多线程」的正确等价物是异步并发。

**辅助**：ST 区间 + `industry_assignments`（5,212 行）改**全量内存索引**（`loadSecurityIndexes` + `resolveStSync`/`resolveIndustrySync`），逐 bar O(1)，省 ≈186s 往返。

### 验证（全部真实 DB）
- `npx tsc --noEmit` exit 0；`vitest run server/datasetRegistry` **12 文件 / 187 例全过**。
- 新增 `scripts/verifyDatasetBuildPerf.mts`（S1~S5）→ **S1~S4 共 21/21 断言全过**。S2：下推零漏判 + 压缩 **17.0×**（112,349行/14,118ms → 6,598行/2,563ms）。S3：定向取数逐行一致 + 压缩 **13.4×**。**S4：五表 60,002 行，行数一致 + 内容指纹一致 + 行级多重集差集 0 行（逐字节等价），25.3s / 2,368 行/s**。
- 临时版本（420001/450001/450002）全部级联清理，`dataset_version` 仅剩 v1/v2。
- ⚠️ **同窗口「旧 vs 新」端到端 A/B 基线未采集**（HEAD 是 DATASET-001 时代代码、3 表且无筛选，不可比）→ 提速证据取组件级压缩比与并发标定，**不宣称端到端倍数**。

### 🔴 顺带修正既有结论（涨停口径审计）
新增 `scripts/verifyLimitUpCaliber.mts`（8 项断言）实查：
- **v1(390001)**：1,130 事件中「向下舍入型」**436 = 38.6%**；`limitUpPrice` 逐元等于 `exchangeLimitUpPrice`（1,130/1,130）；原始行情交叉验证（`000007.SZ 2026-08-18` close=11.69 / 昨收 10.63 → 未舍入阈值 11.693000000000001 → **旧口径必然判否**，但 v1 确有该事件）。
- **v2(390002)**：13,579 事件中 5,039 = 37.1%；`limitUpPrice` 13,579/13,579 一致。
- **结论**：既有 `ds_*` 的涨停判定 = **【四舍五入到分】**，与现行代码同源 → ROADMAP 原「`ds_*` 仍是旧口径、漏判 38.03%」**前提不成立**。38.03% 实为 `_limitprecision_probe.mjs` 测得的**旧谓词对原始行情**的漏判率（该探针只读 `stock_daily_prices`，从未读 `ds_*`）。
- 🔴 **新发现**：**v2 构建不完整** —— 仅 `event=13,579`，`prefix`/`post`/`path`/`outcome` **全 0**，`status` 永久 `BUILDING`（`runner.ts` `completeJob → markReady` 非原子这一已知缺陷的下游后果）→ v2 **不可用于研究**，需先修非原子再补完构建。

### 三条教训（供后续会话避雷）
1. **S4 首轮假阴性的根因是比对脚本自身缺陷**：`tableFingerprints()` 把 **`datasetVersionId`** 也算进指纹，而两版本该列天然不同（390001 vs 450001）。推理：两版本写**同一批物理表**（仅 `datasetVersionId` 不同）→ 列类型一致 → `CAST(col AS CHAR)` 对相同值渲染必然相同 ⇒ 指纹不等只可能来自真实差异。修复（排除 `id`/`createdAt`/`datasetVersionId`）后逐列 **0 差异**。**判据已升级**：新增 `rowMultisetDiff()` —— 指纹只证「总和不冲突」，**行级差集 0 才证「每行都有逐字节相同的对应行」**，已固化为 S4 常驻断言。
2. **谓词级缺陷度量不能外推为「已落库数据」的属性**（38.03% → ds_* 的错误外推）。判据：**若某版本事件集含「向下舍入型」样本，它就不可能是未舍入旧口径的产物**（旧口径下 `close = 舍入值 < 未舍入阈值` 必然判否）。
3. **工具环境坑**：① `npx tsx -e "..."` 在本项目**静默无输出**（exit 0），临时探针要落成文件运行；② 临时探针**不要 `import` `server/data/boardRules.ts`** —— 会拉入 `server/engine/execution` 整个回测引擎依赖图导致**挂起**（实测被 SIGTERM），需要时内联等价实现；③ 用 `bash` 管道读 ROADMAP 会因 UTF-8/GBK 显示乱码，**读写该文件一律用工具而非 `sed`/`head`**。

### 接口变更同步面
`index.ts` 新增 `export * from "./concurrency"`；`testHelpers.ts#makeFakeIO` / `builder.test.ts#MemIO`（`fetchLimitUpCandidateBars` **复用 `isLimitUpCandidateBar`**，漏判类错误在单测即暴露）/ `scripts/verifyDataset003a.mts#ProbeBuildIO` 同步适配。已删除无调用方的 `fetchBarsForDay`/`fetchBarsRange`/`fetchSymbolBars`/`fetchLiquidity`/`fetchLiquidityForSymbols`/`fetchIndustry`/`fetchIndustryForSymbols`。**未新增 migration、未改表结构、未改任何判定/筛选口径。**

---

## DATA-INTEGRITY-001 原始数据表「重复行」防线审计（19:20，任务：用户问「重叠窗口回填会不会产生重复」）

### 结论：原始行情表**不会重复**（真实库判据，非代码推断）
防线 = **DB 级唯一约束 + `ON DUPLICATE KEY UPDATE` 幂等覆盖**，而非应用层 if-duplicate 判断。真实库 `information_schema` 逐表核实（NON_UNIQUE=0）：
`stock_daily_prices`(8,893,077 行, `uq_stock_daily_price_stock_date`) / `liquidity_daily`(9,015,158, `uq_liquidity_daily_security_date`) / `corporate_actions` / `adjustment_factors` / `index_daily` / `industry_assignments` / `backfill_checkpoints` / `stock_suspension_windows` —— **均存在覆盖业务键的唯一索引**。唯一写入口 `db.ts#upsertStockDailyPrices` / `marketData/liquidityStorage.ts` / `marketData/industryStorage.ts` 全部 `.onDuplicateKeyUpdate(...)`。实测按业务键 `GROUP BY ... HAVING COUNT(*)>1` **零重复组**。
- `stock_daily_prices` 唯一索引由 `ensureStockDailyPricesUniqueIndex` **惰性创建**（幂等；历史脏数据先删同键较大 id 行再补建）。位图索引 `registerSyncedPricePairs` 是**幂等置位**。
- 唯一真实代价：`updatedAt`/`sourceUpdatedAt` 被刷新 + `invalidateLeaderCandidateBacktestCaches()` 触发 ⇒ 重复回填表现为**缓存抖动**而非数据重复。「后写入者胜、与数据新旧无关」，但 Tushare 未复权历史日线值不随时间变，当前无实质风险。

### 🔴 两处真实缺口（都不在行情表）
1. **`research_security_status_history` 缺 UNIQUE(securityId, statusType, effectiveFrom)**（真实库仅 PRIMARY；当前实测 0 重复）。`securityStatus/timeline.ts#pickLatest` 同键多行时 tie-break = `retrievedAt` → `source.localeCompare`，属**「任取」** ⇒ 一旦写入重复，**PIT ST 判定会退化成由 source 字母序决定涨跌停比例 5%/10%**。待按 §迁移流程 补 DDL。
2. **`limit_up_records` 无业务唯一键**（仅 PRIMARY），**已累积重复**：26 组 / 166 行多余。含 **258 行「测试」字样污染**（`测试股票`/`测试题材`/`测试关键词`、`createdBy=1`），最重一组 **`2024-12-31/000001.SZ` 重复 142 条**（createdAt 2026-01-10 ~ 2026-09-04 反复插入），其余散布 `2026-07-21`(7)/`07-09`(4)/`05-25`(4)/`05-11`(4)。
   **影响已确认**：这些日期**都在回测窗口内**，而 `db.ts#getLimitUpCountsByDate` 用 `COUNT(*)` 按 `limitUpDate` 聚合、`#getDailySectorStats` 题材热度、`marketRegimeRouter.ts` 以涨停家数驱动市场情绪 regime ⇒ **涨停家数虚增 166 行**（99,577 → 实为 99,411），`2024-12-31` 由 44 只虚增成 186 条。**待用户确认后清理（破坏性，未执行）。**

### 产出与教训
- 新增只读护栏脚本 **`scripts/verifyRawDataUniqueness.mts`**：U1 约束存在性（`information_schema`；**「唯一索引存在 ⇒ 重复不可能写入」是逻辑证明，避免对 889 万行做大表全扫**）/ U2 仅对**无唯一键**的表做实测 `GROUP BY` 扫描（真正的风险集）/ U3 测试污染扫描。实测 **9/11 通过**，2 项失败即上述缺口。`npx tsc --noEmit` 全库 exit 0。
- ⚠️ **教训：审计探针的分组键必须与代码的语义分组键一致** —— 我首轮按 `(securityId, effectiveFrom)` 分组 `research_security_status_history`，得出「11 组**冲突**」的**假警报**（那些行实为不同 `statusType`，而 `resolveSecurityStatus` 正是**先按 `statusType` 分组**）。**正确键 = `(securityId, statusType, effectiveFrom)` → 0 重复。** 分组键选错会把「正交维度」误报成「冲突」。
- ⚠️ **探针列名来自 `drizzle/schema.ts` 而非猜测**：`limit_up_records` 实际列是 `keywords`（无 `reason`），首轮探针 `Unknown column 'reason'` 报错。
- **§44 无口径变更**；**未新增 migration、未改表结构、未删任何数据**；探针 `_dupProbe*.mts` 跑完即删。ROADMAP 已同步（§47 新增 DATA-INTEGRITY-001 条目；§44.5 队列新增第 12 条）。

---

## RESEARCH-002A 引擎维护层 + 工作台可维护性（19:15，任务：用户「不要管dataset功能与数据，需要你把研究模块完成，现在还缺哪些」）

### 流程：先审计缺口 → 用户定范围 → 再补
- 用户指令两层：① 先把**还缺哪些**列清楚（审计，不写代码）② 再补齐。
- 审计**以代码/schema 为证据**，不采信 ROADMAP 文字。经 `AskUserQuestion` 定范围 = **A（引擎 CRUD + 工作台可维护性）+ D（闭环 executor 装配）**，数据边界 = **只写代码用既有接口，不碰 `ds_*` 与 dataset 页面**。本次完成 A。

### 缺口审计结论（证据）
- `server/researchEngineRouter.ts` 17 端点**零 update/delete**；但 `server/researchCore/repository/contract.ts:151-239` 早已定义 `update`/`delete`/`replaceForAnalysis`/`deleteByAnalysis` —— **全部不可达**（数据层能力被 API 层挡住）。
- 级联删除规则**仅存在于** `scripts/verifyResearchEngine.mts#cleanupExperiment` → 产品化不可达（用户删不掉实验）。
- `setAnalysisConditions` 原语义只替换条件行、**不清旧结果** ⇒ UI 继续展示「按旧口径算出的数字」→ **静默不实，比抛错更危险**。
- 其余：`researchRunRouter.ts:114` `executorBound=false` 硬编码（「策略→跑研究」断链）、FE-4 运行工作台是 `emptyRunResult()` 占位、前端零导出/零删除/零条件编辑、`listCandidates` 无消费方。

### 交付
- **服务端**：新 `server/researchEngine/maintenance.ts`（**465 行**）导出 `deleteRunCascade`/`deleteExperimentCascade`/`deleteAnalysisCascade`/`deleteHypothesisCascade`/`invalidateAnalysis`/`replaceConditionsAndInvalidate`/`describeDeletionCounts`/`analysisIdsReferencedByConclusionEvidence`。`researchEngineRouter.ts` 428→588 行、**17→25 端点**（+7 维护端点全 admin：`updateExperiment`/`deleteExperiment`/`updateHypothesis`/`deleteHypothesis`/`deleteRun`/`updateAnalysis`/`deleteAnalysis`）。`types.ts` + `DELETE_CONFLICT`；映射 `HYPOTHESIS_NOT_FOUND`/`ANALYSIS_NOT_FOUND`→`NOT_FOUND`、`DELETE_CONFLICT`→`CONFLICT`。
- **前端**：新 `ConditionGroupsEditor.tsx` / `ConfirmDeleteButton.tsx` / `ExperimentActions.tsx` / `AnalysisConditionEditor.tsx` / `CandidatesPanel.tsx`；改 `createAnalysisForm.ts`（+`conditionGroupsToPayload`/`conditionPayloadToDraftGroups`/`validateConditionGroups`）、`CreateAnalysisDialog.tsx`、`ConclusionPanel.tsx`（JSON 导出，标注 `CLIENT_SIDE_API_SNAPSHOT`）、`ResearchDetail.tsx`、`index.ts`。

### 两条关键诚实性设计（可复用）
1. **结论归属不可猜**：`research_conclusion` **无 `runId`**，只有 `experimentId`+`hypothesisId` → 删 Run 时从 `evidence` **精确提取 `analysisId`**（`primaryAnalysis.analysisId` ∪ `contributingAnalyses[].analysisId` ∪ 旧键 `analyses[].analysisId`）与被删 Run 的分析集合**求交**判定；**提不出 id 就绝不删**，计入 `unattributedConclusions` 如实回显。形状不认识 → 返回空数组，**不做 `?? 0` 兜底**（猜错会删别人的结论）。
2. **改口径 = 让旧产物失效**（顺序不可颠倒：守卫 → 替换 → 删旧结果 + 删失效结论 + Analysis/Run **回退 `PENDING`**）。回退 PENDING 让既有 `runEngine` 的 `RUN_NOT_PENDING` 前置**恰好放行重跑** ⇒ **无需新增重跑入口**。另：`RUNNING` 时删父行会产出**幽灵行**（飞行中写入落到已删实体）→ `DELETE_CONFLICT` 显式拒绝。

### 🔴 顺带修掉真实缺陷：条件载荷**组号断号**（§11.4 第 8 项）
- **后端约束**：`server/researchCore/conditions.ts:181-183` `assertConditionSet` 要求**组号唯一且连续 0..n-1**；写库路径 `repository/db.ts#replaceForAnalysis:649` **真会调用**。（组内 `sortOrder` 只要求唯一，**不要求连续**。）
- **缺陷**：`conditionGroupsToPayload` 原用**草稿下标**当 `groupNo`，而「整组一字未填」的空组不产出行 → 空组在**前/在中**即组号断号（只含 `groupNo:1` 缺 0）→ 后端抛「条件组号不连续，缺少 0」。**可达**：新建 CONDITIONAL（默认第 1 组为空）先去填第 2 组再保存。
- **修法**：按**有效组**重排连续 0..n-1 + 组内 `sortOrder` 紧凑。**附带收益**：紧凑化使两条方向互为规范形 ⇒ **往返幂等**（「打开编辑、不改、保存」不改动库里任何口径）。
- ⚠️ **教训：原测试把错误行为写成了期望**（`expect(rows[0]).toMatchObject({ groupNo: 1 })` 配一个空的第 1 组）——「测试固化缺陷」会让**修复看起来像回归**。改断言时必须问一句「这条期望本身对得上后端约束吗」。

### 验收
`tsc --noEmit` exit 0；`maintenance.test.ts` 19 例、`researchEngineRouter.test.ts` 29 例、`client/src/components/research` 60 例（`createAnalysisForm.test.ts` 31→44）；聚焦 **19 文件 / 326 例全过**；`vite build` **RC=0**（2995 modules）；全量 **204 文件 / 3010 例**（2994 通过 / 16 失败），**失败文件与基线逐字一致（同样 8 个）⇒ 零新增失败**。**零数据依赖**：无迁移/回填/Dataset 重建，未触碰 `ds_*` 与 dataset 页面。

### 🔴 同期发现（需决策，未擅自处理）
`docs/researchReadyGate/research_ready_gate.json`（capturedAt **2026-09-09T15:07:48Z**）实为 **`researchReady=true`**（G0 PASS / G1 GAP / G2 PASS / G3 GAP / **G4 PASS** / G5 GAP，`pendingChecks=[]`）→ **推翻** §44 此前「RESEARCH_READY=FALSE 期间」表述，并使 `server/dataHealth.test.ts`（断言 `G4=GAP`）与 `server/researchRunRouter.test.ts`（断言 `verdict=DATASET_NOT_READY`，实返 `EXECUTOR_NOT_BOUND`）各 1 例失败。**非本次引入**；**未改快照、未改测试期望**（二者都是用动作掩盖状态）→ 须先裁定「快照该重认证回 FALSE，还是测试该更新」。

### 待办
- **A 段遗留（非阻塞）**：报告 §17 已写，A 段收口完成。
- **下一动作 = D 段（§44.5 第 9d 条）**：① `server/research/closedLoopWiring/` 14 阶段 stage executor 装配；② `researchRunRouter.ts:114` `executorBound` 改**真实探测** + 新增 `researchRun.loopRun` 端点；③ FE-4 `RunWorkbenchTab` 真实接线。**同样不得触碰 Dataset 侧**。
- 报告级观察（未修）：ROADMAP `#47 更新记录` 结构错位 —— `#49` 标题之后（约 2501~2605 行）仍夹 09-10/09-11 的 §47 式 `###` 条目；本次按「主日志尾部」追加，未搬动历史。

## 2026-09-11 19:35 — DATA-INTEGRITY-002 `limit_up_records` 连板高度口径审计（用户问「多日间隔涨停会不会重复」）

### 回答用户
- **「多日间隔涨停」本身不产生重复行**：`limit_up_records` 99,577 行 / 1,867 个交易日 / 4,324 只股票（活表，抓取自动化每日在写）；**3,726 只股票有 ≥2 个不同涨停日**；按 `(limitUpDate, stockCode)` 仅 **26 组多行 / 166 行多余**，其中 141 行是测试污染 ⇒ **真实重复仅 25 组 / 25 行**。「一股一交易日一行」是本表的正确语义。
- **「间隔」真正的代价在连板高度**：`boardCount` 是**死字段**（页面/统计都不读它），实际连板走 `server/db.ts#calculateConsecutiveBoards` —— 以「表内出现过的 `limitUpDate` 集合」做**邻接链重算** ⇒ 链上缺任何一天该股记录，连板就在该处**断裂并被低估为「首板」**。

### 查实的证据（新增 `scripts/verifyBoardHeightCaliber.mts`，1/4 通过）
- **H1** `boardCount` 两套约定并存（99,428 行）：空 2 / 首板 62,298 / 纯数字 10,744 / `N天N板` 24,088 / `N天M板`(M<N) 2,290 / 其他 6。
- **H3-缺口 133 行**（自称 N 连板 > 表内实际，如 `000078.SZ 2025-12-02` 自称 5 连板、表内只有 1 天）；缺口最密交易日 `2026-01-12`(12) / `2026-08-04`(8)。
- **H3-断裂 30 行**（`boardCount="1"` 却已连续 ≥2 天，**全部来自 OCR 链路**，`backfill=0/ocr=30`）。
- **H5 同日多行 25 组**：24 组内容不同 = **同一批次里同一股票被复盘图多个题材段落各识别一次**（如 `002990.SZ 2026-05-07` 两行 sector 分别「光通信*14」「算力租赁*12」，`createdAt` 同秒）；1 组逐列相同 = 真重复。
- **H2 ✅**：`index_daily` 区间内日期全是真实交易日；`2026-09-07~09-10` 晚于日历尾部 `2026-09-04` = **指数域同步缺口**（非本表口径错误），但同样让这几天连板链失去基准。

### 🔴 修正上一轮（DATA-INTEGRITY-001）的过宽判据
- 「测试污染」原记 **258 行是误报**：`LIKE '%测试%'` 会命中**真实股名**（`谱尼测试`/`西测测试`）与**真实涨停关键词**（`封装测试`/`芯片测试设备`）。改用前缀 `LIKE '测试%'` ⇒ **真污染 143 行**（全部 `000001.SZ 测试股票`：`2024-12-31` 142 + `2025-12-31` 1，`测试题材`/`测试关键词` 占位）。`verifyRawDataUniqueness.mts#U3` 已改并同时打印宽/严两个数字（仍 9/11）。

### 代码级潜在缺陷（未改）
- `scripts/backfillLimitUpRecords.mjs`：`const fresh = hits.filter(h => !existing.has(...))` 之后 `streak` **只在 `fresh`（本轮新增）上推进、不读在库历史** ⇒ **按间隔区间分批回填时，区间衔接那天的连板数会被重置为「首板」**。当前数据未显现（回填一次性跑完），但缺陷已存在。修法 = 连板判定用「已在库历史 ∪ 本轮新增」并集 + 日期基准改用 `index_daily`。

### 交付
- 新增只读护栏 `scripts/verifyBoardHeightCaliber.mts`（H1 形态 / H2 日期基准 / H3 连板链缺口+断裂 / H4 / H5 同日多行）。
- ROADMAP：§47 新增 DATA-INTEGRITY-002 条目 + §44.5 队列第 13 条 + 更正第 12 条的「258 行」→143 行。
- `npx tsc --noEmit` exit 0；**未加 migration、未改表结构、未改任何写入逻辑、未删任何数据**；探针（`_lupProbe`/`_boardProbe*`/`_imgProbe`/`_tp`）跑完即删。

### 待用户拍板（均未执行）
① 修 `backfillLimitUpRecords.mjs` 的 `fresh`-only 连板计算；② `limit_up_records` 是否加业务唯一键（须先定「同股同日多时段 / 多题材是否合法」语义）；③ 补数据缺口（补齐缺失交易日的上传/回填）；④ 清理 143 行测试占位数据。

### ⚠️ 结构性待办
- `.workbuddy/memory/MEMORY.md` 本轮已做去重合并（DATA-INTEGRITY 两轮合并、删重复「涨停价」表述、新增「LIKE 优先前缀」工具坑），但仍 ~18KB → **注入时会被截断**（实测阈值约 13KB）。建议专项把 `Research 层` 段搬进 `docs/` 并在 MEMORY.md 留指针。

## 19:38 — DATA-INTEGRITY-003 数据集 `ds_*` 物理表「多日（间隔）涨停是否重复」实查

用户澄清对象：不是原始成交表、也不是 `limit_up_records`，而是**数据集（Dataset Registry）里的 `ds_*` 表**。

**结论一：`ds_*` 五表行级零真重复（双证）**
- 真实库唯一键齐备：`event` `(datasetVersionId,eventId)`；`prefix`/`post`/`path` `(datasetVersionId,eventId,relativeDay)`；`outcome` `(datasetVersionId,eventId,horizon)`。
- `eventId = computeEventId(symbol, tradeDate) = "symbol@tradeDate"`（`detection.ts`，确定性派生），全部插入走 ON DUPLICATE KEY ⇒ 幂等。
- 实测重复组全 0（v1/v2），`event` 按**自然键** `(symbol,tradeDate)` 亦 0。
- ⇒ **同一股票多次（含间隔）涨停 = 多个不同 eventId 的事件各占一行，不是重复**。v1 有 227 只股票 ≥2 事件，最多 `601700.SH` 7 次（08-03/10/12/17/20/24/28，间隔 2~4 交易日）。

**结论二：事件窗口必然互相覆盖 = 同一根 K 线被存多份（结构性冗余，语义必需）**
- v1：`prefix` 23,730 行 / 不同 K 线 19,160 / 冗余 **4,570 行（19.3%）**；`post` 15,876 行 / 12,636 / 冗余 **3,240 行（20.4%）**；跨角色（同一 K 线既在 prefix 又在 post）**2,255 根**。
- 极端样本 `601700.SH`：`prefix` **147 行仅 40 根不同 K 线 = 73% 冗余**，`2026-07-31` 被存 **7 份**（rd -20/-16/-14/-11/-8/-6/-1）。
- ✅ **同源一致性已证**：多份副本 OHLCV **逐字节相同**（`COUNT(DISTINCT CONCAT_WS(...)) > 1` 组数 = 0）⇒ 是「同一行情源的多份引用」，**非「一根 K 线两套值」**。
- 🔴 新增使用侧禁令：① **禁按 `(symbol, tradeDate)` 去重**（不同事件的 relativeDay 语义不同，如 `2026-08-03` 既是事件日 rd=0 又是 08-10 事件的 rd=-5）；② 窗口表**行数不可当独立观测数**做聚合。

**固化**：`scripts/verifyDatasetWindowLayering.mts` 新增常驻断言 **I7b**（event 自然键唯一 —— 纵深防御：线上唯一键建在派生字符串 `eventId` 上，格式一变 DB 保护即静默消失）与 **I12**（重叠副本同源同值），并打印冗余率。标题改为 I1~I12。实跑 **v1 13/13 全绿**；总计 24 项通过 22、失败 2（**均为 v2 构建不完整的已知项 I4/I5**，非新增问题）。`npx tsc --noEmit` 全库 exit 0。

**工具坑（本轮新增）**：`shared/datasetRegistryContracts.ts` import 了 `shared/researchContracts.ts` ⇒ 后者被并发会话改坏时，**任何引 `datasetRegistry` 的脚本都会 import 期崩溃**（`Cannot access 'x' before initialization`），症状与本次改动无关但会挡住一切数据脚本。错误行号在我操作期间 533→654 漂移，可据此判断「仍在写入」。

**MEMORY.md 瘦身（执行了上一轮的建议）**：Research 层整段迁至 **`docs/research/LAYER_CONVENTIONS.md`**（保留全部约定），MEMORY.md 只留指针 + 3 条最常咬人的；并压缩「数据源与回填」段落。**18,213 → 15,949 字节**（仍偏大，未删除任何硬约定）。

**边界**：未新增 migration、未改表结构、未改判定/筛选口径、未删数据；一次性探针 `_dsdupProbe*.mts`（4 个）跑完即删。






---

## 19:45 — RESEARCH-002A · D 段交付（闭环装配 + readiness 真实探测 + loopRun + FE-4 接线）

**范围**：用户确认「A（引擎 CRUD/工作台可维护性）+ D（运行闭环 executor 装配）」，边界为「**只写代码用既有接口，不碰 `ds_*` 与 dataset 页面**」。A 段已于 19:15 交付；本次收口 D。

**D1 · 新建 `server/research/closedLoopWiring/`**（types / requirements / coverage / executors / index，测试 31 例）。
- **14 阶段装配声明表是唯一权威**：每个「未装配」阶段必须写明**缺什么 + 为什么现在不装 + 真实入口坐标**（测试断言 reason 长度 > 40，拒绝「暂未实现」式占位）。
- 已装配 6（data / research / strategy / backtest / evaluation / finalize）；留白 8（optimization / robustness / oos / overfitting / regime / paper / review / discipline），`regime` 标为最易补齐。
- **纪律**：只为「真的能跑」的阶段注册执行器 ⇒ `stageRunners` 表本身就是一句实话；缺前置产物抛 `ClosedLoopWiringError` 响亮失败，绝不返回占位摘要。

**🔴 D1 测试抓出的真实设计缺陷（值得记住）**：入参来源原设计成「来源之间 OR、**来源内也 OR**」（`ClosedLoopInputSource` 曾是 inputs/artifacts 判别联合）。后果：`research` 只要拿到 `dataset` 产物、**即使缺** experimentConfig/strategyContract/strategy13 也会被注册，直到运行期才抛 `CL_WIRING_INPUT_MISSING` —— 即**把「缺配置」伪装成「执行失败」**（调用方会去查执行器，而真问题是入参没给）。
修法：`ClosedLoopInputSource = { inputs?, artifacts? }`（**来源之间 OR、来源内 AND**），`via(inputs, artifacts)` / `viaArtifact(artifacts)` 构造；补 3 例回归（有产物缺入参 / 有入参缺产物 / `evaluation` 多来源 OR 仍成立）。
**教训**：契约里的「OR/AND 组合语义」必须用**双向**测试锁定，只测一侧会漏。

**D2 · `researchRunRouter.ts`**
- `executorBound`：硬编码 `false` → `assessClosedLoopWiringCoverage({}).executorBound`（**readiness 与 loopRun 共用同一探测函数**，杜绝两处口径漂移）；readiness 新增 `wiring` 明细；未就绪措辞改为**点名哪 8 个阶段尚无执行器**。
- 新增 **`researchRun.loopRun`**（mutation，**无状态**：不写库、不落 run 记录）：`createClosedLoopWiring(inputs, {requested})` → `runClosedLoop`，返回 14 阶段轨迹 + `chainFingerprint` + 装配覆盖。
- **两条防伪绑定**：① `evaluationInput` **必须与** `backtestSummarySeed` 成对（`backtestFingerprint` 只取 `seed.fingerprint`，不接受调用方另填；且 evaluation 阶段本就需要 `backtestSummary` 交接）⇒ 使 `CL_MISSING_UPSTREAM_HANDOFF` 在该端点下**不可达**；② `backtestSummarySeed` 与链内 `backtest` **互斥**（冲突即 BAD_REQUEST，**不静默丢种子**）。
- ⚠️ **覆盖率探测的边界（务必记住）**：`assessClosedLoopWiringCoverage` 判的是「执行器已装配 ∧ **闭包入参**可得」，**不含**编排器对 `ClosedLoopStageInputById`（同链前驱产出）的交接要求 → 后者以 `stages[].blocked` 为准。两处（代码注释 + 报告 §18.4）已声明。

**共享契约**（`shared/researchContracts.ts`）：新增 `CLOSED_LOOP_STAGE_ID_VALUES`（**由契约单测断言与后端 `CLOSED_LOOP_STAGE_IDS` 逐项一致**，杜绝双份漂移）+ wiring/dateRange/seed/lifecycle/runInput/runResult 六个 schema；readiness 加 `wiring`。
⚠️ **踩坑**：readiness 需要 wiring schema、runInput 需要 metrics schema ⇒ 出现 **block-scoped 前向引用**（TS2448）。**共享契约文件内 schema 的声明顺序 = 依赖顺序**，必须先把被依赖者前置（本次按依赖拆成「装配摘要前置 / 运行 DTO 后置」两块）。
⚠️ **真实连锁效应**：`shared/datasetRegistryContracts.ts` import 了 `shared/researchContracts.ts` ⇒ 本文件一旦处于「前向引用」中间态，**任何引 datasetRegistry 的脚本都会 import 期崩溃**（`Cannot access 'x' before initialization`），症状与改动内容无关。**编辑共享契约时要么一次成型，要么尽快修好，不要留在中间态。**

**D3 · FE-4**：新增 `client/src/adapters/closedLoopRunAdapter.ts`（**零计算**：评估标量只从 `evaluationRef` 直搬，缺字段一律 `null`；三节全缺 → 返回 `null` 而非「全 null 伪对象」；未知阶段状态降级 `UNKNOWN`；非对象/无 runId → `null`）+ `ClosedLoopRunResultPanel.tsx`。
- `RunConfigPanel` 增 `onRun`/`running`/`runError`；**「运行策略」按钮由 `canRun` 门控改为「是否注入 onRun」门控** —— 发起的是真实执行（入参齐备者真跑、其余如实 BLOCKED），有诊断价值；若继续用 `executorBound=false` 锁死，工作台永远是空壳。
- `RunWorkbenchTab` 由 `emptyRunResult()` 占位改真实接线（emptyRunResult 现仅用于「尚未发起」空态）。
- `client/src/lib/status.ts` 补闭环语义色。⚠️ 新状态必须登记到 `lib/status.ts`（§12 全站唯一状态色来源），否则静默回退 neutral。

**验收**：`tsc --noEmit` exit 0；wiring **31 例** / router **17 例**（原 2 例）/ adapter **17 例**；`client/src` **10 文件 172 例全过**；聚焦批次 **17 文件 276 例**（唯一失败仍是 gate 快照冲突）；`vite build` **RC=0**（2995→2997 modules）。

**边界**：零数据依赖；未触碰 `ds_*` / dataset 页面 / 认证快照；**未修** `dataHealth.test.ts` 的 gate 期望（数据域治理，待裁定）。
**遗留**：① `dataHealth.test.ts` 1 例失败（gate 快照 `researchReady=true` vs 断言 `G4=GAP`）待用户裁定；② 8 个未装配阶段，`regime` 为下一增量首选。

---

## 20:05 — 诊断：用户「新建 Run 后迟迟没结果」

**现象**：用户在研究实验模块新建 Run 后无结果。
**实查证据**（真实 DB）：
- `research_run` 全表仅 1 行：`id=180001 exp=180001 runNo=1 status=PENDING`，`startedAt=NULL` `sampleCount=NULL` `errorCode=NULL`，createdAt `2026-09-11T10:37:47Z`(18:37 GMT+8)。
- `research_analysis` **0 行**、`research_result` **0 行**、`research_conclusion` **0 行**。
- `research_experiment` 180001 → `datasetVersionId=390001`（dataset_version 390001 = `first_limit_pullback v1` **READY**，2026-08-01~08-31，1130 事件 / 60002 行）。
- `server/` 内**无任何**后台 worker/cron/queue 消费 PENDING run（找到的 cron 属 marketSync / paperTrading / heartbeat）。

**根因（非卡死，是没启动 + 不会自动启动）**：
1. `researchEngineEngineRouter.ts:332 createRun` 注释即写明「**只创建，不执行**」；
2. 真正执行入口是 `researchEngine.runEngine`（`researchEngineRouter.ts:385`）；
3. 前端 `RunEngineButton.tsx:54` `disabled = !executable || analysisCount === 0 || isPending` → 该 Run **零分析** ⇒ 按钮恒灰、点不动；
4. `createRun` 无异步续跑 ⇒ PENDING 会一直 PENDING。

**正确链路**：Experiment → Run → **Analysis**（定义算什么）→ 「运行引擎」→ result + conclusion。

**结论**：研究模块本身没坏（`verifyResearchEngine.mts` 证明链路可用）；是**产品语义**问题 —— 「新建 Run」听上去像「开始运行」，实际只是建容器。可考虑的产品改进：新建 Run 后自动弹出「新建分析」对话框，或在 Run 行内直接给出「+ 添加分析」主按钮。

## 20:27 — DATASET-LIFECYCLE-001 数据集「取消构建 → 重新构建」数据完整性实查（v2 卡死根因确证）

**用户问题**：数据集 version 取消构建后重新构建，数据会不会有问题？
**结论**：取消 = **只改状态、不删已落库数据**（数据层「暂停」而非回滚）；重建**不保证干净** —— 两个真实风险 + 一个已发生的死锁，全部用真实库实测坐实。

**一、取消的实际语义（读码确证）**
- `registry.ts#cancelJob` 只改状态（job→CANCELLED、version→FAILED）；`db.ts`（IO 层）**无任何 DELETE/TRUNCATE**，构建路径全程只有 upsert ⇒ 半成品数据**全部保留**。
- router `cancelBuildJob` 顺序 = 先 `service.cancelJob`（落库）→ 再 `runner.cancel`（置内存标志）；builder 只在**下一个 report 回调**才抛 `DatasetBuildCancelledError` ⇒ 取消与 flush 之间有竞态，取消瞬间仍可能落库。

**二、哨兵实验（临时版本 480002，确定性取消 + 脏数据注入）**
- 事实：`runner.ts` 恒传 `resumeCheckpoint: null`（**从不续跑、每次从零重建**）；插入 SQL 是 `ON DUPLICATE KEY UPDATE id = id` = **空更新（no-op）**，不是「后写覆盖」。
- **风险 A**：注入伪造事件 `999999.SZ@2026-08-15` → 重建后**仍存在**（event 表 1131 行 vs v1 1130），而 `version.totalEvents` 仍写 1130 ⇒ **声明行数 ≠ 实际行数**。
- **风险 B**：篡改 `000006.SZ@2026-08-13` 的 `industryCode='SENTINEL_IND'`、`turnover=999999.99` → 重建后**旧值仍胜出** ⇒ 上游/代码变化后重建**不会修正**，留下新旧混合数据。
- **对照**：同窗口下 prefix/post/path/outcome 四表 **行级差集 = 0**（vs v1 逐字节等价）⇒ 重建的「产出正确性」没问题，问题**只在陈旧残留**。
- ⚠️ 性能观察：本次构建 **169s**（`batchSize=500`），远慢于 S4 场景（26s）⇒ `batchSize` 是速度敏感参数，待标定固化。

**三、🔴 v2(390002) 卡死根因确证（首个已发生的死锁）**
- 作业时间线：`job1` CANCELLED（started 2026-09-10 12:38:19 → cancelled 2026-09-11 11:43:10，用户等约 23h 后取消）→ **27s 后 `job2` 重建**（11:43:37）→ `job2` `updatedAt=2026-09-11 11:47:27` 后**再无任何进度更新**（`processedRows=85325`、`chunks=200/484`）却**永久 RUNNING**。
- 根因：`DefaultDatasetBuildRunner` 运行态/取消标志是**进程内内存 map**，**全库无孤儿作业回收逻辑**（`listJobs` 只用于查询、`getRunningJobForVersion` 只用于守卫）⇒ 进程重启/热重载后 RUNNING 作业**无人接管、永不终态**。
- 后果（实测确认**死锁**）：`createJob` → `VERSION_NOT_BUILDABLE`；`deleteVersion` → `VERSION_HAS_RUNNING_JOB` ⇒ **v2 既不能重建也不能删除**。

**四、新增只读护栏 `scripts/verifyDatasetBuildLifecycle.mts`（L1~L3，实测 5/6）**
- 唯一失败项 L1a = v2 幽灵作业（真实缺口）；L2 声明↔实际（v1 60002/60002 ✅）；L3 可用版本清单（当前仅 v1）。`--stale-minutes` 控孤儿阈值。

**五、修复建议（本轮全部只读，未动手）**
1. 破 v2 死锁：RUNNING 且停更 >10min 的作业置 CANCELLED + 版本置 FAILED → 之后可重建/删除；
2. 治本 A：启动时/定时回收孤儿作业；
3. 治本 B：构建开始时 `DELETE FROM ds_* WHERE datasetVersionId=X`（比改 upsert 为真覆盖更稳，后者无法处理「本轮不再产出」的行）；
4. `completeJob → markReady` 合并单事务 + 取消改为「先置内存标志、再落库终态」。

**纪律**：未新增 migration、未改表结构、未改写入/判定逻辑、未删任何数据；探针 `_cancelProbe.mts`/`_ghost.mts` 跑完即删；临时版本 480001/480002 级联清理（实测零残留，`dataset_version` 仅剩 v1/v2）；`npx tsc --noEmit` exit 0。已同步 ROADMAP §47 DATASET-LIFECYCLE-001 + §44.5 第 10 条修正 + 队列第 14 条。
⚠️ **MEMORY.md 本轮再次瘦身**（16,975 → 14,594 字节：并入本轮发现 + 删解释性从句 + 压缩各节），**仍略超注入截断阈值** → 建议下一步把「数据完整性 / 构建生命周期 / 构建性能」三节外迁到 `docs/` 并在 MEMORY.md 留指针。

---

## RESEARCH-002B 增量补跑单个分析（20:35，功能真实缺失补齐）

**用户实报**：①「新建了一个 run，为什么迟迟没有结果」；②「执行了一次分析，后面又新建了一个分析，但是没有可以让他运行的按钮」。
**诊断（证据化，非猜测）**：`createRun` 注释即写「**只创建，不执行**」，真正入口只有 `researchEngine.runEngine`；**服务端无任何消费 PENDING Run 的 worker/cron**（`server/` 内 cron 全属 marketSync / paperTrading / heartbeat）；新建 Run 默认零分析 →「运行引擎」按钮恒灰 ⇒ **空 Run 永不自己出结果**（数据侧无问题：`dataset_version 390001` READY / 1130 事件 / 60002 行）。Run 一旦 `COMPLETED`，**双侧 `RUN_NOT_PENDING` 守卫**（前端 `RunEngineButton.tsx#executable`、后端 `engine.ts:119`）使按钮恒灰；而「新建 Run 重跑」**走不通**（分析以 `runId` 挂载、无跨 Run 继承、`updateAnalysis` 白名单不含 `runId`）⇒ **「给已完成 Run 补跑新增分析」是能力缺失**。另实查用户现场：run 180001 COMPLETED（1130 样本）挂 3 个分析（180001 QUANTILE 已 COMPLETE / 180002 / 180003 仍 PENDING），另有用户中途新建的空容器 run 210001。
**用户拍板**：语义 = **允许增量补跑单个分析**；执行 = **先不用代跑，自己在页面上点**。

**三条硬约束（先论证后实现）**
1. 增量**必须复用 Run 冻结基准**（`inputSnapshot` 的 `datasetVersionId` + 日期窗口），否则新老数字不可比。决定性依据：`sampleSet.ts#buildSampleSet` **不按条件过滤事件**（事件集只由「datasetVersionId + 日期窗口」决定，分析变量仅作**投影列**）⇒ 增量**并上**新变量**不改变样本集**。
2. 增量**不得生成结论**，且理由**可证明**：`AnalysisSummary`（effect/pValue/tStat/…）**未落库**、`diagnostics` 明确不写 `result_json` ⇒ **无法重建被跳过分析的历史摘要**，拼结论 = **编造**。
3. `inputSnapshot` **不可变**（RESEARCH-001 定死）⇒ 增量事实**另立追加式载体**，不改写快照。

**实现**：`research_run.executionLogJson`（`drizzle/0032_*.sql` + `scripts/applyResearchRunExecutionLog.mjs`，17 断言 PASS，幂等，`--dry-run`/`--check`）；`server/researchCore/executionLog.ts`（`assert…` **响亮失败不降级 `[]`** / `nextExecutionSequence` / `appendExecutionLogEntry` / `settleExecutionLogEntry`）；`engine.ts` **抽 4 个共用私有核心**（`buildCatalog`/`loadConditionSets`/`resolveAnalyses`/`executeAnalyses`）供 `run()` 与 `runIncremental()` 共用，`run()` 同时写 batch 1（FULL）；`runIncremental()` = 读快照基准 → `DATASET_VERSION_DRIFT` 断言 → 选可跑分析（`PENDING|FAILED|CANCELLED`）→ 变量需求并上 → **快照日期窗口**建样本 → 执行 → **不建结论** → Run 终态 `allCompleted ? COMPLETED : PENDING`；端点 `researchEngine.runIncremental`（admin）+ 4 错误码映射；前端 `incrementalRunForm.ts` / `RunIncrementalButton.tsx`（行内「补跑」+ 明示「本批次未生成结论」）/ `RunExecutionBatches.tsx`（「执行批次」表，空日志如实说明版本边界）/ adapter + `RunEngineButton`（COMPLETED 且有可补跑分析时直接指向行内「补跑」）。
**版本边界规则**：快照存在但日志空 ⇒ 下一批 `sequence = 2`（**不 backfill**，批次 1 已被历史整轮执行占用）。
**刻意保留**：删分析后日志里的悬空 `analysisId` **不清理**（append-only 历史事实）+ `maintenance.ts` 注释声明。
**验收**：`tsc` exit 0；新增 **3 文件 / 44 例**；聚焦 **23 文件 / 390 例全过**；`server/researchEngine`+`researchCore` **234 例全过**（共用核心抽出**零回归**）；migration 17 断言 PASS；真实 TiDB `verifyResearchEngine.mts --all` **RC=0（73.5s）**，自建自清 9 行（**未触碰用户 180001/180002**）；全量 **209 文件 / 3117 例**（15 失败，**全落在既有环境依赖基线内**，`researchRunRouter` 已由 D 段转通过）⇒ **零新增失败**。
**零数据依赖**：无回填 / 未重建 dataset / **未碰 `ds_*` 与 dataset 页面** / 未改认证快照。
**产物**：报告 §19（19.1–19.8）+ 附录 A/B（2f）；ROADMAP §44 头部 + §44.5 第 9f 条 + §47 本条目。
**遗留（未做，明确非缺陷）**：① 空 Run 仍需用户主动点「运行引擎」（未引入后台自动消费）；② 增量不产结论；③ 用户已有的 run 180002/180003（PENDING）现在**已有可点入口**（在分析列表行内「补跑」，或直接点 Run 的「运行引擎」——因仍有 PENDING 分析故按钮可用）。

## RESEARCH-002C 批量建分析（21:05，降低「建分析」重复劳动）

**触发（用户原话）**：「现在初步看起来分析研究是能用了，但是我需要手动建立很多分析，有没有什么办法可以减少这个过程」。
**重复劳动的两个来源（以代码为证）**：① `researchEngine.createAnalysis` 入参是**单个**分析，前端 `CreateAnalysisDialog` 的特征/目标/视界均**单选**；② 建完还得**单独点「补跑」**（`createRun` 只建不执行、无后台 worker 消费 PENDING，`RUN_NOT_PENDING` 守卫禁用 COMPLETED 覆盖 ⇒ 新增分析只能走 `runIncremental`）⇒ 「5 特征 × 3 视界 + 1 稳定性 × 3 维度」= N 轮往返 + N 次补跑。
**用户两项拍板**：路线 = **A 批量矩阵 / B 标准套件 / D 跨实验模板**（C 复制已有分析不做）；执行 = **只创建、不自动补跑**（自己点）。
**核心架构决策（防实现分叉）**：三条路线**共用同一条落库路径 `createAnalysesBatch`** —— 标准套件**不另写生成逻辑**（只是「用预设值填矩阵表单」，交给**同一个** `expandAnalysisMatrix`）；模板展开也调 `createAnalysesBatch` ⇒ **预检整批拒绝 / 部分失败如实回显 / 补偿删除**三条语义**自动继承**。
**两条设计纪律（错了就留半成品）**：① **预检整批拒绝、不产半成品** —— `assertBatchCreateItems` 在**写库前**预检，任一项不合法即 `BATCH_VALIDATION_FAILED`、**一个都不建**（消息含「未创建任何分析」+ 前 10 项明细）；预检项 = 空批次 / 名称空或超 200 / 类型未在注册表内 / CONDITIONAL 缺**有效**条件（`fieldName.trim() !== ""`）。② **执行期部分失败如实回显 + 补偿删除** —— 失败项**先删条件再删分析**（零 FK 不依赖级联），计入 `failed`（带 `index`）；**补偿再失败如实标注「⚠️ 回滚该项失败」绝不静默吞掉** ⇒ `created` 每项保证「**有 id 就能跑**」。
**刻意不重复实现条件结构校验**：组连续 0..n-1 / 组内 sortOrder 唯一 / 值元数等结构性合法性唯一权威是 `assertConditionSet`（`replaceForAnalysis` 写入时执行），批量路径**只做预检级「有没有条件」判断**。
**模板 = 头 + 明细两表**（`research_analysis_template` / `_item`，migration `0033`）：明细需确定性顺序、头表需唯一约束；**唯一例外**是明细**条件**用 `conditionsJson`（配置快照，不索引不约束）——**口径不降级**：展开成真分析时条件**仍写 `research_analysis_condition` 关系表**。
**实现**：`drizzle/0033_*.sql` + `scripts/applyResearchAnalysisTemplate.mjs`（**34 断言 PASS**、`--check` 幂等）；`server/researchEngine/batchCreate.ts`（`MAX_BATCH_CREATE_ITEMS=200`、`implementedAnalysisTypes()` **取自执行器注册表**）+ `templates.ts`（`assertTemplateDraft` / 双向转换，`readConditions` **逐行具名报错、不静默丢条件**）；`researchEngine` **+5 错误码 +5 端点**（`createAnalyses`(admin) / `listAnalysisTemplates`(**public**) / `createAnalysisTemplate` / `deleteAnalysisTemplate` / `applyAnalysisTemplate`），`createAnalysisItemSchema` **单建与批量共用**；`researchCore/repository` 新增 `templates`（`db.ts` 用 `loadTemplateItems` **批量取明细避免 N+1**、删模板**显式先删明细**；`inMemory.ts` 用 `cloneJson` 深拷贝）。前端：`analysisBatchForm.ts`（矩阵展开/标准套件/模板预览/校验纯函数，`MAX_BATCH_ITEMS` 仅是**显示用副本**，客户端**禁** import 服务端常量）+ `BatchAnalysisDialog.tsx`（三 Tab，**建前摊开清单** + skipped/truncated 如实回显）+ adapter 模板 VM + 5 条错误提示 + `ResearchDetail.tsx` 并列按钮。
**本段修掉一个真实缺陷**：`createAnalysisTemplate`/`deleteAnalysisTemplate`/`applyAnalysisTemplate` 里 `throw new ResearchEngineError(...)` **没走 `toTrpcError`** ⇒ tRPC **不自动映射领域错误**、一律落 `INTERNAL_SERVER_ERROR`（2 例测试失败暴露）→ 改为 `toTrpcError(new ...)`。**教训：新增端点时领域错误只能 `toTrpcError` 出去，直接 `throw` 不改 tRPC code。**
**验收**：`tsc --noEmit` **exit 0**；migration **34/34 断言 PASS** + `--check` 幂等；新增 `batchCreate.test.ts` **16 例** / `analysisBatchForm.test.ts` **27 例**；`inMemory.test.ts` 32 → **38 例**；`researchEngineRouter.test.ts` 32 → **43 例**；聚焦 **25 文件 / 450 例全过**；全量 **211 文件 / 3189 例**（15 失败 / 7 文件，**失败数与文件集合与基线逐项一致**）⇒ **零新增失败**。
**零数据依赖**：无回填 / 未重建 dataset / **未碰 `ds_*` 与 dataset 页面** / 未改认证快照。
**产物**：报告 §20（20.1–20.10）+ 附录 A/B（2g）；ROADMAP §44 头部 + §44.5 第 9g 条 + §47 本条目；`LAYER_CONVENTIONS.md` 新增 RESEARCH-002C 节。
**遗留（未做，明确非缺陷）**：① 批量建完**不自动补跑**（用户明确选「只创建，我自己点补跑」）；② C「复制已有分析」不做。

## DATASET-LIFECYCLE-001 治本落地（21:35，「取消 = 回滚 · 重建 = 从零 · 孤儿作业回收」）

**触发（用户原话）**：「**根治这个问题，取消构建后就要删除数据**」。承接 20:27 的实查（那轮只审计、不动手），本轮是落地。

**一、代码交付（4 文件 + 契约 + 前端）**
- `runner.ts`：`DatasetBuildRunner` 增 `waitForStop(jobId, timeoutMs?)`；运行态 `Map<BuildControl>` → `Map<BuildRun{control,promise}>`（`start()` 登记执行体 promise、其 `.finally` 摘除且**早于**等待方被唤醒）；`BUILD_STOP_TIMEOUT_MS=60_000`（最坏工作单元 = 月度候选下推实测 ~12.8s ⇒ ~4.7× 余量）；`execute()` 在 `builder.build` **之前**做取消检查 + `await service.purgeVersionRows(versionId)`；**失败分支补 `console.error`**（原先 `catch {}` 全静默 ⇒「版本 FAILED + 作业永久 RUNNING + 零线索」无法定位）。
- `registry.ts`：契约增 `listRunningJobs()`（全库 RUNNING）；新增 `purgeVersionRows()` / `cancelJobAndRollback()` / `reclaimStaleJobs()`；`DEFAULT_STALE_BUILD_MINUTES=10`；`CancelJobRollbackResult{job, rollback|null, rollbackSkippedReason, alreadyCancelled}`、`ReclaimStaleJobResult{..., rollbackSkipped}`；内存实现补 `updatedAt` 刷新（对齐真实库 `ON UPDATE CURRENT_TIMESTAMP`，使孤儿判据在内存下真实成立）。
- `physicalTables.ts`：`purgeVersionRows()` 分批 `DELETE ... WHERE datasetVersionId = X LIMIT 5000`，表不存在 → `tableMissing/deleted=0`（诚实）。
- `router.ts`：`cancelBuildJob` 顺序固定 **`runner.cancel` → `await waitForStop` → `cancelJobAndRollback`**；导出 `reclaimOrphanBuildJobs()` 供启动钩子。`server/_core/index.ts` 在 `server.listen` 后调用（env `DATASET_RECLAIM_STALE_MINUTES`）。前端按后端真实结果回显（有 rollback → success「清空 N 行」；无 → warning + 原因）。

**二、真实库验收**
- `tsc --noEmit` exit 0；`vitest run server/datasetRegistry` **12 文件 / 201 例全过**（+2：READY 未接管不回滚 / 孤儿回收 READY 跳过）。
- `scripts/verifyDatasetBuildLifecycle.mts` **7/7**（新增 **L4**「非 READY 且无 RUNNING 的版本不得残留数据行」）。
- **P1 取消=回滚**（真实孤儿 version 510001）：五表 **128,176 行 → 0（18.1s）**，作业 CANCELLED / 版本 FAILED，幂等重复取消不抛错，之后**可直接重建、可直接删除**。
- **P2 重建=从零**（哨兵，version 510004，2026-08 一月窗）：B1 180.4s 五表 **60,002**；**植入痕迹**（锚点 `000006.SZ@2026-08-13` 的 `eventId` 改名 `-GHOST` + `limitUpPrice`/`turnover` 置 999999.9999）；B2 220.3s ⇒ **幽灵行 0**、锚点事件重建为**正确值**（7.7 / 2.8771）、五表逐表行数与首次一致、`totalRows` **声明=实际**。⇒「新旧混合 / 声明≠实际」消除。
- **P3 死锁自动解困（生产已生效）**：卡死 23h 的 v2 幽灵作业 `540001` 被启动钩子回收（`orphan reclaimed：停更 55 分钟`）⇒ v2 可重建，应用侧 21:24 成功发起重建（`job 630008`）。**v1(390001) 零污染**（READY / 1130 / 60,002）。

**三、顺带修掉一个真实缺陷（自查发现）**
- 🔴 **「取消 READY 版本的重建」会静默销毁有效数据**：`markBuilding` 先于清场 ⇒ **版本仍 READY ⟺ 本轮未接管**；原 `cancelJobAndRollback` 无条件清空 ⇒ 点构建后约 0.6s（跨境 RTT × 4 查询）内取消即得「`READY` 却 0 行」的**谎报态**。已修为「READY 且未接管 ⇒ 不回滚 + `rollbackSkippedReason`」，reclaim 同判据。

**四、边界与未定位项**
- **未新增 migration、未改表结构与任何判定/筛选口径**；唯一新增副作用 = 取消/回收/重建前清场会**真删 `ds_*` 行**（即用户要求的语义）。
- ⚠️ `waitForStop` 只等**本进程**执行体 ⇒ 多实例部署下无法等到另一进程的在途写入（当前单实例不成立）。
- ⚠️ 上一轮两次观察到的「Phase 1 后构建停止推进」（`chunks=21/21` 后不再涨、status 永久 RUNNING）在本轮**清理掉两组泄漏探针进程**（20:46/20:59 启动的 `_cancelRollback.mts`，被工具层「停止」后底层 node 未退出）后**未再复现** ⇒ 疑为多进程争抢同一 TiDB，**未确证**，另立待查（失败日志已补）。
- **本轮踩坑（环境类）**：`ps -ef` 的 PID ≠ Windows PID，杀进程须用 PowerShell `Stop-Process`（Git Bash 下 `taskkill //F` 非法）；长跑探针被工具层「停止」后**底层 node 进程会泄漏**并继续抢占 DB，必须显式清理。
- 探针 `_cancelRollback/_rebuildCheck/_peek/_sentinelCheck/_state.mts` 跑完即删；临时版本 510001~510004 全部级联清理（残留 0），`dataset_version` 仅剩 v1/v2。`_audit-probe.mts`（09-05）/`_perf_probe.mts`（09-09）为更早会话遗留，未处理。

## 事故复盘 + 真实缺陷修复（22:55）：孤儿 RUNNING 卡死 + 我自己造成的两次中断

**用户报错（22:42）**：`请求失败 RPC_ERROR / Failed query: select MIN(relativeDay), MAX(relativeDay) from ds_first_limit_pullback_path where datasetVersionId = ? params: 390002`。

**一、逐条实查结论（以真实 DB / 进程为证）**
- **该 SQL 不是缺陷**：走 `idx_path_version_day(datasetVersionId, relativeDay)` 的索引边界扫描（`EXPLAIN` 为 `IndexRangeScan … keep order:true, desc` + `Limit count 1`）⇒ O(1)。单独实测 `getPathRelativeDayRange(390002)` **1.7s**、`getVersionContext(390002)` **2.5s**，均成功返回 `{min:1,max:20}`。
- ✅ **更正旧记忆：v2(390002) 已 READY 且五表齐备**（21:24 由 DATASET-LIFECYCLE-001 的启动钩子解困后重建成功）：events=**23,978**、path=**471,816**、outcome=**71,934**、prefix=503,538、post=471,816，窗口 2024-09-02 → 2026-09-01。**「仅 event 有行 / 永久 BUILDING」已作废。**
- **用户 Experiment 240002 / Run 330003 的真实时间线**（应用侧时间）：22:40:21 建 Experiment → **22:41:54 第一次执行失败**（`errorCode=INTERNAL_ERROR`，引擎按 `runIdentified` 正确落 Run FAILED）→ 用户重试 **22:43:29**（Run → RUNNING，进入数据集装配）→ **22:48:38 dev server 热重启** ⇒ 在途 Run 被杀，**永久停在 RUNNING**。
- **被阻塞的后果**：`run()` 要求 Run `PENDING|FAILED|CANCELLED`、`runIncremental` 要求 Run 非 `RUNNING` ⇒ 该实验**完全无法再执行**（UI 恒显示执行中）。

**二、🔴 真实缺陷（已修）：RUNNING 转换不清上一轮失败残留**
- `engine.ts` 的 RUNNING patch 只写 `status / startedAt / inputSnapshot / executionLog`，**不清 `errorCode` / `errorMessage` / `completedAt`**。后果：① 重试后 RUNNING 的 Run 带着上一轮 `errorCode`（UI 同时显示「执行中 + 上一次的错误」）；② `completedAt(22:41:54) < startedAt(22:43:29)`，破坏「`startedAt` 空/非空 + `completedAt`」区分「预检拒绝 / 执行中崩溃 / 已收口」的既有语义。
- **修复**：`run()`（约 L248）与 `runIncremental()`（约 L503）两处 RUNNING patch 均补 `completedAt: null, errorCode: null, errorMessage: null`；仓储 `runs.update` 是 `patch.X === undefined ? {} : {X}` ⇒ 传 `null` 真写 NULL（已验证）。
- **回归**：`engine.test.ts` 15 → **16 例**、`engineIncremental.test.ts` 14 → **15 例**（各自用「捕获 RUNNING patch」断言三字段为 `null`；无此修复时字段为 `undefined` ⇒ 断言会失败，测试有效）。

**三、已执行的修复动作（一次性脚本，跑完即删）**
- Run 330003 → `FAILED`（`errorCode=INTERNAL_ERROR` + errorMessage 说明「被热重启中断，请重新运行」）；Experiment 240002 → `FAILED` ⇒ **用户可重新点「运行引擎」**。
- 清掉我的 E2E 残留 **Experiment 240001**（走产品级 `deleteExperimentCascade`，leaf-first）：1 实验 / 1 Run / 5 分析 / 1 假设 / **138 结果行** / 2 条件 / 1 结论。
- 终态核对：`research_experiment` 仅剩 **240002**(FAILED) 与用户的 **180001**(COMPLETED，4 Run) —— 用户数据零损失。

**四、验收**
- `npx tsc --noEmit` **exit 0**；研究层聚焦（`server/researchEngine` + `server/researchCore` + `researchEngineRouter.test.ts`）**17 文件 / 286 例全过**。
- 真实 DB 证据（本轮唯一那次 `--all` E2E，虽被我在收尾前 stop）：在 **v2/390002** 上 5 类分析**全部 COMPLETED**（QUANTILE 3261 / DESCRIPTIVE 2681 / EVENT_STUDY 1955 / CONDITIONAL 1969 / STABILITY 2005 ms），Run/Experiment `COMPLETED`、结论 `SUPPORTED`（Q10−Q1 = −0.0475、t = −10.5747、n=23748）；**dataset 装配 529,740 ms** 是绝对瓶颈（分析合计仅 11,871 ms）⇒ v2 单次运行 ≈ **9 分钟**。

**五、⚠️ 两条必须记住的教训（本轮踩坑）**
1. 🔴 **`npm run dev` = `tsx watch server/_core/index.ts`** ⇒ **改任何 `server/**` 文件都会热重启并杀死在途的研究 Run**（v2 装配要 ~9 分钟，极易撞上）。**用户正在用页面时不要动 server 文件**；确需改动，先确认无在途 Run，或改完主动告知用户重跑。
2. 🔴 **不要在用户使用期间跑重活**：`npx tsx scripts/verifyResearchEngine.mts --all` 在 v2 上要 ~9 分钟且大量占用跨境 TiDB；本轮第一次失败（22:41:54）就发生在我那个 E2E 的窗口（22:31→22:45）内。

**六、建议下一步（未做，待用户决策）**
- **为研究 Run 补一个「启动时回收孤儿 RUNNING」钩子** —— 与既有 `reclaimOrphanBuildJobs()`（`server/_core/index.ts` 在 `server.listen` 后调用，DATASET-LIFECYCLE-001 已落地）同构。判据可选：① boot 时任何 `RUNNING` Run 即孤儿（最严格、最贴合「执行器随进程消亡」）；② 复用 `DATASET_RECLAIM_STALE_MINUTES=10` 的停更阈值（与 dataset 侧一致，但对 5 分钟即被杀的 Run 要等到 10 分钟才回收）。⚠️ 需先定「单实例假设」（项目对 dataset 构建已按单实例处理）。
- 未经此钩子时，孤儿 RUNNING 只能靠人工/脚本收敛（本轮即如此）。

