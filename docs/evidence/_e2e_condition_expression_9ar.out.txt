
=== 0. 前置：真坐标自证 + 清理同名残留 ===
  基线行数 {"candidates":9,"strategies":9,"versions":10,"provenance":8,"params":21}
  ok   前置：实验存在且 Dataset 坐标 = 390002 — {"id":240002,"datasetVersionId":390002}
  预清理同名残留：0 行

=== 1. 修正写法（派生字段 + 参数引用）→ 转正 → 声明 ↔ 执行对齐 ===
  ok   创建成功且 status = DRAFT — DRAFT
  ok   状态机 DRAFT → REVIEW → ACCEPTED — ACCEPTED
  strategyId=cand-450001 version=1.0.0 versionId=480001
  ok   promote 成功（修正写法不再被拒）
  ok   strategyId 确定性派生 = cand-<candidateId> — cand-450001
  ok   Strategy 侧读口可取到版本 bundle
  ok   entry.conditions 落库 3 条（与草稿条件数一致） — 实际 3
  ok   #0 声明字段 = bar.haircutFromEventLow（与运行时 featureId 同名 ⇒ 声明 = 执行） — field=bar.haircutFromEventLow runtimeFeatures=haircutFromEventLow/volumeRatio/isBullish/momentumFromEventClose
  ok   #0 valueType = PARAMETER_REFERENCE（不再降级成 CONSTANT） — valueType=PARAMETER_REFERENCE
  ok   #0 右值 = max_drawdown（∈ 配方参数表 ∩ 运行时可解析键） — value="max_drawdown" runtimeParams=max_drawdown/max_volume_ratio/require_bullish
  ok   #1 声明字段 = bar.volumeRatio（与运行时 featureId 同名 ⇒ 声明 = 执行） — field=bar.volumeRatio runtimeFeatures=haircutFromEventLow/volumeRatio/isBullish/momentumFromEventClose
  ok   #1 valueType = PARAMETER_REFERENCE（不再降级成 CONSTANT） — valueType=PARAMETER_REFERENCE
  ok   #1 右值 = max_volume_ratio（∈ 配方参数表 ∩ 运行时可解析键） — value="max_volume_ratio" runtimeParams=max_drawdown/max_volume_ratio/require_bullish
  ok   #2 声明字段 = bar.isBullish（与运行时 featureId 同名 ⇒ 声明 = 执行） — field=bar.isBullish runtimeFeatures=haircutFromEventLow/volumeRatio/isBullish/momentumFromEventClose
  ok   #2 valueType = PARAMETER_REFERENCE（不再降级成 CONSTANT） — valueType=PARAMETER_REFERENCE
  ok   #2 右值 = require_bullish（∈ 配方参数表 ∩ 运行时可解析键） — value="require_bullish" runtimeParams=max_drawdown/max_volume_ratio/require_bullish
  ok   🔴 语义对齐：声明 bar.volumeRatio ↔ 参数 max_volume_ratio —— 与配方门槛 volumeRatio <= max_volume_ratio 同名同义
  ok   运行时参数集可解析出三参数（不抛 RECIPE_PARAMETER_NO_DEFAULT） — {"max_drawdown":0.06,"max_volume_ratio":0.3,"require_bullish":1}

=== 2. 投影 ↔ Canonical 零漂移 + 参数投影 TUNABLE ===
  ok   投影与 Canonical Definition 零漂移（5 张投影逐字段比对）
  投影行数：parameters=3 entryRules=3 exitRules=3 executionRule=1 datasets=1
  ok   参数投影保留 parameterRole = TUNABLE（供 Parameter Search 消费） — max_drawdown:TUNABLE,max_volume_ratio:TUNABLE,require_bullish:TUNABLE
  ok   执行绑定 = 研究来源 Dataset（390002） — [390002]

=== 3. 负向：历史写法（算术右值）→ 响亮拒绝 + 零 Strategy 数据 ===
  ok   算术右值 promote 被拒，领域码 = STRATEGY_CANDIDATE_PROMOTE_SKETCH_INVALID — STRATEGY_CANDIDATE_PROMOTE_SKETCH_INVALID
  ok   错误信息点名路径 + 给出改写方向（派生字段 / 参数引用），不是静默降级 — [STRATEGY_CANDIDATE_PROMOTE_SKETCH_INVALID] 候选草稿内容非法：filterRule.groups[0].conditions[1].value — 右值 "prefix.rd0.volume * 0.3" 不是合法字段引用、也不是已声明的参数 code，但含字段引用 "prefix.rd0.volume" 与算术运算符：这看起来是「字段 × 系数」的算术
  ok   拒绝后零 Strategy 数据（strategies / strategy_versions 行数未变）
  ok   被拒候选保持 ACCEPTED（不产生任何 Strategy 数据，也不回退状态） — ACCEPTED

=== 4. 收尾：删除本探针自建的行，断言行数守恒 ===
  清理后行数 {"candidates":9,"strategies":9,"versions":10,"provenance":8,"params":21}
  ok   行数守恒（候选 / 策略 / 版本 / 溯源 / 参数 全部回到基线）
  ok   研究侧记录未被本探针改动（结论仍在）

== ALL PASS == checks=27 failures=0
