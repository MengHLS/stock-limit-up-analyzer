# 本次验证记录

2026-08-25：`/leader-candidates` 开发预览可正常打开，页面顶部与加载占位可渲染。截图工具未在其等待窗口内取得完整大体量回测响应；等价本地查询已返回 HTTP 200，耗时 2.36 秒、响应约 788 KB，说明回测接口和本次交易日历改动可正常工作。TypeScript 无错误；浏览器扩展的等待超时不代表服务端回测失败。

2026-08-26：组合回测审计发现三项关键问题。第一，当前“买点涨幅”以实际成交价相对同一日开盘价计算，实际退化为滑点值：真实回测 957 笔成交中 956 笔为 0.10%，并未反映信号日收盘至买点的跳空幅度。第二，入场前要求已有 T+2 收盘价，形成未来数据泄漏；应仅凭 T+1 开盘价决定能否买入，T+2 价格仅用于后续出清。第三，事件循环先处理当日收盘出清再处理当日开盘买入，会错误复用同日尚未收盘的卖出资金；应改为开盘入场在前、收盘出清在后。

2026-08-26：修复后用同一真实参数重新请求回测。买点涨幅改为“实际成交价（含滑点）相对信号日收盘价”，477 笔入场的结果范围为 -9.96% 至 10.30%，均值约 3.18%，不再退化为固定 0.10% 滑点。模拟现只凭 T+1 开盘行情决定入场，T+2 缺价保留持仓并期末估值；同日顺序已改为开盘买入在先、收盘出清在后。

2026-08-26：固定百分比止盈已替换为日线近似的动态回撤止盈。仓位仅在每个实际收盘事件更新持仓期最高收盘价；当该高点相对实际买入价达到启动浮盈后，后续收盘从该已知高点回撤至阈值才出清，创新高当日不卖出。开盘止损与一字跌停可成交性仍优先。以主板1–4板真实候选样本、初始资金100,000元、最大5仓、启动浮盈6%、回撤3%、止损5%、最多5个交易日复核：回测接口HTTP 200，完成出清155笔、期末3仓、峰值持仓5、最低可用资金0.09元，无现金透支；订单原因已返回“动态回撤止盈”。该结果仅为历史日线模拟，不代表投资建议。

2026-08-26：扩展历史行情回填后，`stock_daily_prices` 覆盖105,533行，其中低价与日线成交额均已回填105,531行。全量同步按上交所实际交易日历对每个涨停信号覆盖信号日及后续10个实际交易日，共请求109,074个股票—日期组合，写入105,531行，3,543个组合因停牌、上市时间或未来交易日无日线返回而保留缺失。下行风险标签改为T+1开盘后观察期内最低价相对T+1开盘价的最大不利波动，缺失低价时才降级使用收盘价；信号日成交额仅作为可见特征。真实回测接口返回低价完整标签353个、信号日成交额可用样本353个，并生成3个滚动窗口（默认前置90个交易日校准、后续30个交易日验证；三个窗口标签样本分别为119、114、120）。结果只用于历史日线研究，未包含盘中成交路径或盘口流动性。

2026-08-26：龙头候选池已接入与下行风险研究完全相同的信号日风险评分函数；候选日风险分仅读取连板、题材支撑、封板时间、信号日成交额、流通市值、情绪阶段、市场高度和原始龙头评分，不读取T+1及后续行情。风险扣分固定为风险分×0.35，净评分为原始龙头评分减扣分；候选准入、原始排序、评分阈值及图表筛选仍使用原始龙头评分，避免改变既有候选口径。真实候选接口返回示例：楚天龙原始78、风险42（中风险）、扣分14.70、净分63.30；浙江世宝原始70、风险27（低风险）、扣分9.45、净分60.55。该展示为复盘研究风险参照，不构成投资建议。

2026-08-26：下行风险实验的每个滚动窗口现默认在固定权重网格`[0, 0.15, 0.35, 0.55, 0.75, 1]`内仅用其前置训练期寻优。预定义目标为“训练期收益率 − 0.5 × 最大回撤”，平局依次比较收益率、较低回撤、已出清笔数，最后选择更小权重；选中值只应用于紧随其后的验证段。真实默认参数返回3个窗口，依次选出权重`1`、`1`、`0.75`，训练目标依次为`115.975`、`68.280`、`15.985`。真实tRPC请求HTTP 200、约3秒、响应约1.76MB；全量本地Vitest（排除外部Tushare密钥连通性）24个文件150项通过，TypeScript检查通过。训练/验证结果仅用于历史日线研究，不构成投资建议。

2026-08-26：新增样本外累计拼接曲线。系统把全部无重叠验证窗口的候选按日期合并后，在同一个连续资金账户中重新执行原始、风险扣分和高风险过滤三版模拟；风险扣分仍按每个信号日所属窗口的前置训练期选中权重执行，窗口边界不重置资金或持仓，故整体收益为复利口径。真实默认参数返回3个窗口的拼接结果：原始策略累计收益`73.92%`、最大回撤`46.96%`、已出清`139`笔；风险扣分累计收益`242.41%`、最大回撤`24.78%`、已出清`132`笔；高风险硬过滤与原始策略相同。真实tRPC请求HTTP 200、约4秒、响应约1.77MB；24个本地测试文件151项及TypeScript检查通过。结果仅为历史日线研究，不构成投资建议。

2026-08-26：核对页面中的`474.69%`后确认，它来自全样本主组合回测`realisticSimulation`，使用原始候选评分，**未**施加下行风险扣分，也不是滚动样本外结果。页面现将其明确标为“全样本原始收益（未扣风险分）”，并在同一组绩效卡中新增“样本外风险扣分收益”，该值来自连续拼接的验证窗口，逐信号日使用其前置训练窗口选出的权重。默认参数下该风险扣分策略为`242.41%`累计收益、`24.78%`最大回撤、`132`笔已出清；页面结构、下行风险和全量本地测试（24文件151项）及TypeScript检查均通过。仅供历史研究，不构成投资建议。

2026-08-26：回测页顶部新增“全周期三策略收益对比”。原始评分、风险扣分和高风险硬过滤均使用全部主板1–4板历史候选、相同初始资金、成本、资金/最大持仓、T+1入场、成交限制及唯一风险管理退出策略，在同一连续资金账户中回测并绘入同一收益曲线。自动寻优开启时，风险扣分仅在具有前置训练窗口的日期使用该窗口选出的权重；首个训练段和未覆盖尾段使用手动回退权重，不从未来路径选权。真实接口返回全周期2025-11-03至2026-08-25、734个原始候选，原始策略累计收益`474.69%`；请求约3秒、响应约2.58MB。全量本地Vitest（排除外部Tushare密钥连通性）24文件152项及TypeScript检查通过。仅供历史研究，不构成投资建议。

2026-08-26：在全周期三策略曲线下新增逐笔交易差异对比表。每行以`信号日 + 股票代码`对齐，返回原始评分、风险扣分与高风险硬过滤三版的状态、评分、买卖日期、价格、股数、收益率和原因；风险分达到硬过滤阈值而未进入该策略模拟时，明确显示“高风险过滤”。默认仅显示存在执行或评分差异的订单，并支持按代码、名称或信号日搜索。真实接口返回1个`tradeDifferences`集合，首行楚天龙显示原始评分71、风险扣分64.7及相同资金不足跳过原因；全周期响应约4秒、约3.15MB。订单对齐、硬过滤排除和风险扣分评分变化的回归断言通过；全量本地测试24文件152项及TypeScript检查通过。仅供历史研究，不构成投资建议。

2026-08-26：复核三策略全周期收益后确认，原始策略与主组合回测在候选数`734`、期末资金`574,688.83`、累计收益`474.69%`、最大回撤`41.17%`、入场`284`笔及已出清`283`笔均完全一致。风险扣分策略使用相同734个候选与交易约束，但扣分会改变同日资金分配排序，故结果为累计收益`260.58%`、最大回撤`45.49%`、入场`283`笔；这不是曲线计算误差。硬过滤仅剔除一鸣食品1个风险分66的候选，而该候选在原始策略中本就因“资金按评分排序优先分配”未入场，故硬过滤路径与原始路径相同。前端已修正全周期曲线起点，仅从首个信号日`2025-11-03`开始绘制，避免将模拟器的历史初始资金点误展示为回测期；逐笔表限制为`42rem`高度并启用纵向滚动与粘性表头。全量本地测试24文件152项及TypeScript检查通过。仅供历史研究，不构成投资建议。

2026-08-26：新增风险扣分表现归因。真实全周期订单对齐显示，原始与风险扣分共同成交`234`笔，且共同成交订单收益率完全一致；差异来自扣分改变同日评分排序后出现的候选替换。原始独有成交`50`笔，合计净利润`89,006.08`元；风险扣分独有成交`49`笔，合计净利润`-68,755.10`元，因此全周期风险扣分累计收益`260.58%`低于原始`474.69%`，且在2026-06-24高点至2026-07-21低点的回撤达到`45.49%`。自动权重覆盖`353`个信号、手动回退权重覆盖`381`个信号，故全周期结果并非纯样本外检验；严格滚动样本外拼接反而显示风险扣分收益`242.41%`、回撤`24.78%`，相对原始`73.92%`、回撤`46.96%`更优。页面已展示该归因与样本外对照；全量本地测试24文件152项及TypeScript检查通过。仅供历史研究，不构成投资建议。

2026-08-26：新增八项风险评分因子的冻结权重单因子消融。每次只移除一个信号日扣分贡献，固定现有自动/回退权重、候选范围、资金、成本和唯一退出策略，不以消融后的未来结果重新调权；结果同时报告全周期和严格滚动样本外相对完整风险扣分策略的Δ收益与Δ回撤。真实结果没有任何因子同时在全周期与样本外表现为“移除后收益提高且回撤下降”，故目前**没有可确认的核心负向因子**。连板高度（影响718个信号、平均11.54分）与日线成交额（影响26个信号、平均6.92分）在全周期看似负向：移除后分别为收益`+181.90%`/回撤`-3.95%`及收益`+252.36%`/回撤`-5.91%`；但严格样本外移除后反而分别为收益`-187.92%`/回撤`+23.40%`及收益`-36.53%`/回撤`+8.08%`。因此两者属于“全周期表象负向、样本外未复现”，暂不应删除或降低权重。相位、市场高度等虽覆盖较多信号，但本样本未改变实际资金排序，消融增量为零。新增未来价格变化不影响信号日因子覆盖/平均贡献的无前视测试；全量本地Vitest 24文件153项及TypeScript检查通过。仅供历史研究，不构成投资建议。

2026-08-26：滚动样本外验证默认改为前置`45`个交易日训练、后续`14`个交易日验证；当前真实行情覆盖下形成`10`个连续无重叠验证窗口，验证期为2026-01-07至2026-08-24。每个窗口仅使用其前置训练期按“收益率 − 0.5×最大回撤”选择的权重，权重依次为`1、1、1、0.35、0.55、0.55、0、0.75、1、0.75`。十窗口连续样本外拼接结果：原始策略累计收益`189.69%`、最大回撤`42.14%`、已出清`210`笔；风险扣分累计收益`203.67%`、最大回撤`39.65%`、已出清`209`笔。风险扣分10窗中`8`窗正收益，`5`窗同时实现相对原始更高收益与不更高回撤。页面新增泛化稳定性及权重分布摘要；完整本地Vitest 24文件154项和TypeScript检查通过，真实接口约5秒、响应约3.88MB。仅供历史研究，不构成投资建议。

2026-08-26：新增三项市场因子并完成真实历史补齐。`limit_up_records` 的202个项目交易日中，201日已由 Tushare `daily` 的`.SH`/`.SZ`成交额汇总、上交所公开XLS与深交所公开XLSX两融汇总写入并标记真实来源；2026-08-26因上交所公开文件HTTP 404保持缺失，未使用占位值。严格回测候选信号日为2025-11-03至2026-08-25共186日，项目涨停数、已验证沪深成交额和两融余额均为186/186覆盖。风险矩阵由8项扩展至11项：项目涨停数、沪深两市成交额和两融余额相对前20个**此前**已验证日期中位数的偏离；来源未验证或字段缺失时贡献为0。三项市场因子总分上限13分，仍参与固定网格自动选权与冻结权重单因子消融。真实默认回测保持10窗口，样本外拼接结果为原始`189.69%`/回撤`42.14%`/210笔，风险扣分`203.67%`/回撤`39.65%`/209笔；新增11项因子、市场覆盖和无前视/缺失处理测试通过。仅供历史研究，不构成投资建议。

2026-08-27：统一策略评价与高级评分扩展完成。新增固定**质量复合评分**（原始候选强度68% + 信号日安全度32% + 早封、题材共振、充足日线成交额各2分）及**质量门控策略**（质量复合分不低于同信号日中位数，且风险分低于硬阈值）；评分、门控和准备买入排序只读取信号日及此前可见字段。真实默认请求返回734个全周期主板1–4板候选：原始`474.69%`/回撤`41.17%`/期末`574,688.83`元，风险扣分`543.14%`/`39.63%`/`643,141.24`元，高风险硬过滤`193.91%`/`48.50%`/`293,911.68`元，质量复合`222.48%`/`48.76%`/`322,479.26`元，质量门控`151.04%`/`53.12%`/`251,042.90`元。严格滚动样本外仍为10个连续窗口（2026-01-07至2026-08-24）：原始`189.69%`/`42.14%`，风险扣分`203.67%`/`39.65%`，硬过滤`189.69%`/`42.14%`，质量复合`52.91%`/`48.62%`，质量门控`-30.47%`/`66.20%`。因此质量复合与门控当前**未在严格样本外优于基准**，仅保留为可审计的固定规则对照，不应据全周期表现宣称有效。最新计划快照五策略均有1笔未出清持仓；质量门控有3个准备候选并剔除3个。完整本地Vitest（排除外部Tushare密钥连通性）30文件173项、TypeScript检查与生产构建通过；仅供历史研究，不构成投资建议。

2026-08-27：新增五策略的风险调整后评价，统一基于同一连续资金账户的**相邻交易日收盘权益**计算；无风险年化收益率固定为`0%`（未模拟现金管理收益），年化系数为`252`个交易日。夏普比率使用年化收益/年化总波动，索提诺使用年化收益/年化下行波动，卡玛使用年化收益/最大回撤，Ulcer指数使用逐日回撤平方均值开方。真实全周期（211个日收益）中，风险扣分的夏普`13.035`、索提诺`21.743`、卡玛`20.776`、年化波动`63.16%`、Ulcer`14.38%`，优于原始策略的`10.832`、`18.014`、`17.178`、`65.29%`、`15.21%`。严格10窗口样本外（同为211个日收益）中，风险扣分的夏普`5.038`、索提诺`8.358`、卡玛`6.982`、年化波动`54.94%`、Ulcer`14.26%`，也优于原始策略的`4.551`、`7.524`、`6.080`、`56.29%`、`15.34%`。质量门控样本外夏普为`-0.531`且Ulcer为`37.34%`，进一步说明该固定规则当前不具备稳健优势。指标只评价已执行的历史资金曲线，不参与候选评分、门控、权重选择或交易决策；仅供历史研究，不构成投资建议。
