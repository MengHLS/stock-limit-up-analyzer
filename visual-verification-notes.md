# 视觉核验记录

## 2026-08-26 回测页面

两次全页预览均稳定显示回测参数区域与加载占位，但截图采集窗口内未等待到大体量回测响应完成，因此未把截图加载态当作服务端失败。独立真实数据库核验脚本已成功返回：186个候选信号日的三项市场因子完整覆盖、11项因子、10个滚动窗口及样本外结果。后续可在交互预览中点击“刷新回测”做人工等待验收；本次功能正确性以完整本地测试、类型检查和真实数据库调用为准。

## 2026-08-27 三策略完整订单

两次全页截图在预览采集时仍停在回测查询加载态，未能直接呈现订单表；该行为与前次大体量回测截图一致，且未在开发服务日志发现运行时异常。三策略订单区仅复用服务端已返回的全周期实验订单，不引入新的网络请求或交易计算；其完整订单映射、策略切换、硬过滤剔除项、固定高度滚动和粘性表头均由 TypeScript 与回归测试覆盖。后续可在交互预览中等待回测完成后验证按钮切换与滚动操作。

## 2026-08-27 三策略持仓与次日计划

两次全页截图在预览采集时同样停在回测查询加载态，因此未将初始加载状态误判为持仓面板异常。独立真实数据库核验已返回回测截止日与最新信号日均为 `2026-08-26`；三种策略各有 1 笔未出清模拟持仓、4 个剩余仓位和 3 个准备候选。面板结构、策略切换、持仓字段、开盘前置条件和“非交易建议”提示由页面结构测试覆盖；完整回归、类型检查和生产构建均通过。

## 2026-08-27 统一五策略评价

重启开发服务后采集 `/backtest` 全页截图，参数区正常渲染；截图采集期内仍停在大体量回测查询加载占位，未直接取得统一评价、曲线、订单或持仓表的完成态。该现象与此前真实数据库回测的长响应一致，不能据此判定为页面异常。本次五策略数据连通性、统一评价结构、曲线字段、订单切换、门控剔除和持仓计划由完整本地回归测试、TypeScript 检查和生产构建验证；待交互预览完成查询后可进一步人工检查横向表格滚动与策略切换。
