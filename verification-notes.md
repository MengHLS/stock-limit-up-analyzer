# 本次验证记录

2026-08-25：`/leader-candidates` 开发预览可正常打开，页面顶部与加载占位可渲染。截图工具未在其等待窗口内取得完整大体量回测响应；等价本地查询已返回 HTTP 200，耗时 2.36 秒、响应约 788 KB，说明回测接口和本次交易日历改动可正常工作。TypeScript 无错误；浏览器扩展的等待超时不代表服务端回测失败。

2026-08-26：组合回测审计发现三项关键问题。第一，当前“买点涨幅”以实际成交价相对同一日开盘价计算，实际退化为滑点值：真实回测 957 笔成交中 956 笔为 0.10%，并未反映信号日收盘至买点的跳空幅度。第二，入场前要求已有 T+2 收盘价，形成未来数据泄漏；应仅凭 T+1 开盘价决定能否买入，T+2 价格仅用于后续出清。第三，事件循环先处理当日收盘出清再处理当日开盘买入，会错误复用同日尚未收盘的卖出资金；应改为开盘入场在前、收盘出清在后。

2026-08-26：修复后用同一真实参数重新请求回测。买点涨幅改为“实际成交价（含滑点）相对信号日收盘价”，477 笔入场的结果范围为 -9.96% 至 10.30%，均值约 3.18%，不再退化为固定 0.10% 滑点。模拟现只凭 T+1 开盘行情决定入场，T+2 缺价保留持仓并期末估值；同日顺序已改为开盘买入在先、收盘出清在后。

2026-08-26：固定百分比止盈已替换为日线近似的动态回撤止盈。仓位仅在每个实际收盘事件更新持仓期最高收盘价；当该高点相对实际买入价达到启动浮盈后，后续收盘从该已知高点回撤至阈值才出清，创新高当日不卖出。开盘止损与一字跌停可成交性仍优先。以主板1–4板真实候选样本、初始资金100,000元、最大5仓、启动浮盈6%、回撤3%、止损5%、最多5个交易日复核：回测接口HTTP 200，完成出清155笔、期末3仓、峰值持仓5、最低可用资金0.09元，无现金透支；订单原因已返回“动态回撤止盈”。该结果仅为历史日线模拟，不代表投资建议。

2026-08-26：扩展历史行情回填后，`stock_daily_prices` 覆盖105,533行，其中低价与日线成交额均已回填105,531行。全量同步按上交所实际交易日历对每个涨停信号覆盖信号日及后续10个实际交易日，共请求109,074个股票—日期组合，写入105,531行，3,543个组合因停牌、上市时间或未来交易日无日线返回而保留缺失。下行风险标签改为T+1开盘后观察期内最低价相对T+1开盘价的最大不利波动，缺失低价时才降级使用收盘价；信号日成交额仅作为可见特征。真实回测接口返回低价完整标签353个、信号日成交额可用样本353个，并生成3个滚动窗口（默认前置90个交易日校准、后续30个交易日验证；三个窗口标签样本分别为119、114、120）。结果只用于历史日线研究，未包含盘中成交路径或盘口流动性。
