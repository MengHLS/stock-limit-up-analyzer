# 2026-09-09 工作日志

## 全系统代码审计（AUDIT-002）

按 QUANT MASTER AUDIT 宪章执行全系统代码审计（范围：代码结构/模块实现/配置/接口/依赖；不含 DB 实际数据）。产出：
- 报告：`docs/audit/reports/2026-09-09_AUDIT-002_FULL_SYSTEM_CODE_AUDIT_REPORT.md`
- 状态：`docs/audit/MASTER_AUDIT_STATE.json`（覆盖式更新）

### 关键结论
- 代码层整体 CODE_READY，研究链 29/29 编码完成，PIT/survivorship 铁律执行到位，未发现 look-ahead/survivorship 致命泄漏。
- **2 CRITICAL**：C-1 生产凭据硬编码（.env 弱 JWT + 根目录 `_tushare_probe*.py` 硬编码真实 TUSHARE_TOKEN，.gitignore 只覆盖 `._*` 不覆盖 `_*.py`，仓库为公开 GitHub）；C-2 根目录残留大量探针/临时脚本 + 4 个 ~8.6MB 大 JSON。
- **3 HIGH**：H-1 `step12_certify_gate.mjs:112` Migration 检查恒 false（epoch ms `1788676035482` vs datetime 字符串永不相等 → RESEARCH_READY 永久被卡）；H-2 测试套件非封闭，vitest run 实测 14 failed/2305（tushare 真实网络 + marketData/limitUp 直连 DB 未 mock）；H-3 `buildSecurityMaster.ts:87` 缺 listedDate 的退市股被拒 → survivorship 缺口。
- **11 MEDIUM / 6 LOW**：三套回测引擎并存、researchRouter 强转无校验、tsconfig 未覆盖 scripts/drizzle、StrategyEditor 硬编码虚构种子文档、Map.tsx 死代码含 VITE_ key、status 历史表非幂等、asOf=null 双语义、schema 三源 drift 等。

### 验证证据
- `tsc --noEmit` exit 0（仅 client/shared/server）。
- `vitest run` 157 文件 / 2305 用例：6 文件 14 用例失败。
- 三路子代理（server/client+shared/scripts+drizzle）交叉审计，关键发现人工复核确认。

### 待办（用户确认后处理）
1. 轮换凭据 + 清理探针 + 补 .gitignore
2. 修复 gate 时间口径
3. 测试封闭化 + 退市股核对

## 回测性能优化 + 生产回测退出缺口定位（用户选定「修回测性能」）

### 性能优化（4 项，冷缓存 37.5s → 16.7s）
1. `server/db.ts` `getLeaderCandidateDailyPriceCoverage` 加独立 10 分钟 TTLCache（890 万行全表 COUNT/SUM 8.9s→热 0）。
2. `loadBacktestBaseContext` 五路独立 DB 查询改 `Promise.all` 并行（DB 段 24s→13s，收敛到价格行 JOIN 12.2s）。
3. `server/data/validation.ts` `isValidDate` 改数值范围判定（去 `new Date().toISOString()` 往返，validateMarketBar 0.76s→0.24s）。
4. `server/strategy/strategyBacktest.ts` `viewOf` 提升 `buildLatestStockNameMap` 为一次性构建。

结果：冷缓存 16.7s（DB 12.4 + 计算 4.3），参数变更 4.3s。tsc exit 0，107 例相关测试全过。

### 关键正确性发现（硬阻断「帮我决策」）
生产引擎回测 `runLeaderCandidateEngineProbe` **无退出策略**：`leaderCandidateBaselineStrategy` 只产 BUY、无 SELL，持仓推到期末 `finalizeOpenTrades` 才标记 openAtEnd。实测：tradeCount=5（maxPositions=5 卡满）、completedTradeCount=0、winRatePct=null、5 笔全 2025-10-17 建仓持有到期末、决策日志 1105 BUY / 0 SELL。`maxHoldingDays/stopLoss/trailing` 退出语义仅在 research-legacy 模拟器（realisticBacktest.ts），新引擎未接。

**下一步建议**：专项实现「生产引擎退出策略」（ReadonlyPortfolioSnapshot 补 entry 日期/成本 + baseline 策略产 SELL，或桥接 legacy 退出语义），优先级高于继续压性能。剩余性能可优化项：feature pipeline 962ms（visibleBars+MarketBarSeries 逐快照重排序）。

## 前端端点接入盘点 + FE-5 绩效仪表盘真实接入（用户：前端很多端点没接入）

### 盘点结论
- FE-1~FE-4（数据健康/历史状态/数据集/策略编辑器）已真实接入可用。
- **FE-5~FE-9 五页是空壳骨架**（FE-5 仅接 researchRun.readiness 1 端点；FE-6~9 零端点），根因=后端 17 个研究引擎模块（performanceMetrics/riskAdjustedMetrics/tradeQualityMetrics/parameterSearch/rollingOptimization/robustness/stochasticRobustness/walkForwardRun/oosIsolation/overfittingDetection/marketRegime/tradeJournal/disciplineFeedback/paperAccount 等）**CODE_READY 但未暴露 tRPC 端点**。
- 另有 25 个 legacy 端点「后端有前端没调」（多为管理/增删改，次要）。

### 本轮落地（阶段 1 = FE-5）
1. `shared/researchContracts.ts` 新增 `equityPointSchema`/`tradeSchema`/`metricsEvaluateInputSchema`。
2. `server/researchRouter.ts` 新增 `metrics.evaluate` 端点（三套指标 C-16.1/16.2/16.3 一次性求值，纯函数）。
3. `client/src/pages/PerformanceDashboard.tsx` 接入「技术预览」数据源：生产回测 `getLeaderCandidateBacktest` 的 realisticSimulation → 转换 → metrics.evaluate，指标卡/曲线/回撤/交易/月年一致性全真实渲染，R7 标注「技术预览·非 RESEARCH_READY」。

### 真实数据端到端验证
回测 17s → 431 点权益曲线 + 5 笔交易 → 总收益 18.28%/CAGR 10.34%/MaxDD 35.56%/Sharpe 0.572/Sortino 0.814/Calmar 0.291。**诚实暴露退出缺陷**：5 笔全 openAtEnd → 胜率/PF/期望 null（0/5）。验证：tsc exit 0、vite build 通过、research 152 例测试全过。

### 剩余（阶段 2~5）
FE-6 参数搜索（parameterSearch/rollingOptimization/robustness/stochasticRobustness）、FE-7 WFO（walkForwardRun/oosIsolation/overfittingDetection）、FE-8 Regime（marketRegime）、FE-9 复盘（tradeJournal/disciplineFeedback/paperAccount）——各引擎模块入口函数已就绪（如 runParameterSearch/runRollingOptimization/assessOfdOverfitting/buildDisciplineFeedbackRun），后续逐个暴露端点 + 接前端即可。TaskList 已建（#150 完成，#151~154 待做）。

## 涨停表（limit_up_records）历史回填到 2019-01-01（主板）

用户要求把涨停表从「只回溯到 2025-10」补齐到 2019-01-01，只用 DB 现有数据，主板优先、创业板/科创板暂跳过。

### 方案与脚本
- 新脚本：`scripts/backfillLimitUpRecords.mjs`（纯 Node + mysql2，幂等，`--dry-run` / `--start` / `--end` / `--batch-size`）。
- 涨停判定：收盘价 >= 涨停价，涨停价 = 前收价 × (1+比例) 四舍五入到分（**纯整数分运算**，round-half-up，规避浮点）。主板非 ST +10%，ST（据 `research_security_status_history` statusType='ST'） +5%。
- 判定逻辑已用真实数据交叉验证：2026-09-08 主板反推 70 只涨停与已录数据 **100% 命中**。
- 名称：本地 `scripts/_baostock_stock_basic_dump.json`（5552 只当前名称快照）。
- 回填字段：stockCode/stockName/limitUpDate/boardCount（"首板"/"N天N板"，按 index_daily 交易日历连续计算）/turnover（成交额亿元，由 stock_daily_prices.amount 千元 ÷1e5 换算）；limitUpTime/circulationValue/sector/keywords 留空（日线不含分时与题材；liquidity_daily.circulationMarketCap 主板 2019-2024 全空）。

### 结果（已落库并验证）
- 写入 **104,625 条**，主板涨停表范围 2019-01-02 → 2026-09-08，共 118,423 条（原 13,798）。
- 按年：2019=11759 / 2020=15385 / 2021=19427 / 2022=17485 / 2023=9482 / 2024=18335 / 2025=15805 / 2026=10745。
- 2019-01-01~2025-10-15 覆盖 1644 个交易日。

### 已知限制
- **名称为当前名称**（非历史名）：ST 状态后来变化的股票，历史记录名会带当前 ST 前缀（如 000056 在 2019 实为「皇庭国际」，显示当前名「*ST皇庭」）。仅影响展示，不影响涨停判定（判定用 status history）。后续如需可改用 BaoStock 日线 code_name 派生历史名，或剥离 ST 前缀后按 ST 区间重加。
- ST 区间来自 baostock-daily 推断（statusValue 仅 'ST'，无 '*ST' 区分）。
- 创业板/科创板未回填（用户明确暂跳过）。

## 02:05 主板 D 域回填收尾 + certify gate 刷新
- 后台跑完核实：checkpoint completed=3039 = 主板 AF 3039 只全覆盖；_failed_stocks_d.json 无；无残留进程
- DB 实况：主板 corporate_actions 20528 行/2932 只（updatedAt UTC 17:56=本地 01:56，刚写完）；主板 AF 3039 只；空日期字段 0 行；CA 主板 vs AF 差 107 只=无分红送转事件股票（正常，checkpoint 3039 均已执行抓取）
- 重跑 certify gate（node scripts/step12_certify_gate.mjs，72s）：17 checks → PASS 13 / PENDING 4 / FAIL 0，researchReady=false
  - #10 CA 2933/5000 PENDING：主板已完成，创业板/科创板 corporate_actions 未回填（用户此前只要主板）
  - #11 AF 5025/5000 **PASS**（全市场复权因子达标）
  - 其余 3 PENDING 与本次无关：#6/#12 C 域 status 10150 行/1716 只(需 5500)、#9 E 域 liquidity 4593/5000(差 407)
- 看板：certify 快照已刷新到 capturedAt 2026-09-08T18:02:48Z；页面点「刷新」即见

## 组合回测页面回填后卡死修复（回填到 2019 后 limit_up_records 1.3万→12万）

### 根因
回填后 `loadBacktestPriceRows` 的「每股 MIN/MAX 涨停窗口」JOIN 从 ~33 万行膨胀到 **489 万行**（每股最早涨停 2019、最晚 2026，窗口 5-7 年），冷加载传输+反序列化超时/内存爆，页面打不开。实测：全区间每股窗口 JOIN = 489 万行（COUNT 13.5s）；精确窗口 DISTINCT 全字段 = 56.8s（DISTINCT 临时表太慢，不可行）。

### 方案（已落地，用户选「默认最近 2 年 + 日期范围选择器」）
给回测加「回测区间」参数，默认最近 2 年，历史数据按需分段回测：
1. `server/leaderCandidates.ts` `LeaderCandidateBacktestOptions` 加 `startDate?`/`endDate?`。
2. `server/db.ts`：
   - `loadBacktestPriceRows(range)` 把日期区间下推到「每股窗口」子查询内（`limitUpDate >= start AND <= end`）。
   - `loadBacktestBaseContext(range)` 接受 range，records 查询加 `gte/lte`，缓存改 `Map<rangeKey, value>`（按区间隔离）。
   - `getLeaderCandidateBacktest`/`getLeaderCandidateResearch`/`runMonkeyBenchmarkForBacktest`/`runCostSensitivityForBacktest` 传 range。
   - 新增 `recentBacktestRange(days)` helper；纸面交易两处无参调用改为「前向功能只需近期数据」：`createPaperTradingRun` 用最近 365 天、`advancePaperTradingRunToLatest` 用 `lastProcessedDate-45 天`（避免纸面交易也因回填卡死）。
3. `server/routers.ts` `backtestOptionsSchema` 加 `startDate`/`endDate`（`^\d{4}-\d{2}-\d{2}$` 可选）。
4. `client/src/pages/Backtest.tsx`：新增 `dateRange` state（默认今天-2年）、`setRecentRange(years)`、日期范围选择器（两个 date input + 近1/2/3年快捷按钮），input useMemo 加 startDate/endDate。

### 验证
- 端到端（最近 2 年）：加载层 29s（records 36859 / rawRows 103 万 / tradingDates 513）+ 回测计算 15.5s，冷缓存 ~44s 出结果，缓存命中 0ms。totalSamples=1583、successRate=46.6%。
- 最近 2 年数据量：涨停记录 36859 条、每股窗口价格 103 万行（SQL COUNT 4s）。
- `tsc --noEmit`：我改的文件零错误（仅 `server/paramSearchRouter.ts` 有 4 个**预先存在**的 z.record/RegionAnalysisConfig/ResearchParameterSet 类型错误，与本次改动无关，esbuild/tsx 不做类型检查不影响运行）。

### 注意
- 单次回测可行区间上限约 2 年（>2 年价格行 >150 万会明显变慢/超时）。「全历史」不可行，UI 已移除全历史按钮，改为「建议单次 ≤2 年，更早历史分段选择」提示。
- 精确窗口化（每个涨停日前后 DISTINCT）实测不可行（DISTINCT 全字段 56.8s），保持「每股连续窗口」方案。

## limit_up_records ST 5% 涨停清理（20,726 条删除 + 回填脚本过滤）

用户反馈「涨停表里很多 5% 涨停的 ST 股，本质不该存在，已被 ST 的后续也不应加入」。审计与处置：

### 关键审计结论（防误删）
- 全表 120,220 条，名称带 ST 前缀 12,137 条（214 股）——但**名称 ≠ 当日状态**（回填用当前名称快照，很多是后来才被 ST）。
- 用 `research_security_status_history`（PIT 当日 ST 区间）+ 当日实际涨幅（主板 ST≈5%/正常≈10%）交叉验证：
  - 当日确实处于 ST 状态的主板记录 = **20,726 条**（名称带 ST 5,619 + 名称不带 ST 15,107（后来摘帽））；
  - 名称带 ST 但当日是真实 10% 涨停 = 6,518 条（保留，名称待对齐）。
- 创业板/科创板 ST 涨跌幅 20% 非清理目标（审计为 0 条）。

### 已执行
1. `scripts/purgeStLimitUp.ts`：备份（JSON+SQL 回滚至 `scripts/backup/limit_up_st_records_*.json/.sql`）→ 删除 20,726 条 → 残留校验 0。表总量 99,494。
2. `scripts/backfillLimitUpRecords.mjs`：命中涨停时 `isStOn` 即跳过（不再按 5% 收录 ST 涨停）。dry-run 回归 2022/2025 上半年新增 0 条。
3. ROADMAP §47 append + §44.5 队列登记。

### 遗留待办（用户选定暂缓）
- 6,518 条「当日 10% 涨停但名称被套当前 ST 名」（208 股），清单 `scripts/backup/st_name_fix_targets.json`。
- Tushare namechange 实测限频 **1 次/小时**（当前 token 档位），208 只需 208+ 小时 → 待更高积分 token 后执行。

## 组合回测退出语义修复（持有 N 天）+ 价格加载精确窗口化（02:40）

用户反馈「回测只需涨停后几天数据，查太多了」。两项修复（详见 ROADMAP §47 02:40）：

1. **退出语义**：生产引擎回测此前无 SELL（baseline 只产 BUY，持仓推到期末 openAtEnd → 胜率/回撤无意义）。新增 `maxHoldingDays`：`domain.ts` BacktestConfig 加字段、`portfolio.ts` 加 `openPositionEntries()`、`engine.ts` 收盘后对持有满 N 天持仓产 SELL（next-open 成交）、`strategyBacktest.ts`/`leaderCandidateStrategyBacktest.ts` 传参（默认 5，与 legacy 一致）。前端「最多持有日」首次真正生效。

2. **精确窗口**：`loadBacktestPriceRows` 原「每股 MIN~MAX 连续窗口」回填到 2019 后每股窗口膨胀到 5-7 年。改为：查涨停日 → `mergeLimitUpWindows()` 应用层合并相邻区间 → 连接级临时表 `tmp_limitup_window` → JOIN，只取每个涨停日前 30 自然日（SMA20 回溯）～ 后 14 自然日（持有期+长假 buffer）。SQL 直做去重不可行（DISTINCT 全字段 55s、OR 展开 SQL>1.7MB 超时）。

**结果**：最近 2 年价格行 100 万→50.5 万（减半）、加载 29s→26.5s；353/367 笔 6 天退出（平均 6.3 天）、8 笔停牌/缺失顺延、5 笔期末附近（合理边界）；81 例 engine/strategy 测试全过 + tsc 干净 + vite build 通过。**遗留**：精确窗口 JOIN（DATE_FORMAT）16.8s 仍是冷缓存地板；停牌股持有期因 forward 窗口边界会人为顺延（真实近似）。

## 03:35 certify gate 口径修正 + RESEARCH_READY=TRUE（里程碑）
- 用户：「把 C 域和 D 域不足 5000 的问题修正一下」
- DB 实查：CA 全表 31641 行/4824 股（主板2932+创业板1338+科创板553，用户已跑完 cyb/kc）；E 5131 股（用户跑命令③生效）；AF 5025 股/31337 行；status 10373 行/1830 只(SUSPENDED 1830+ST 734)
- 关键证据：AF-only 缺口 201 只实查 9 样本(002021/300010 等) BaoStock dividend_data 全 0 行 → CA 4824=事件态上限，5000 不可达；_failed_stocks_ce.json 332 条 stale(E 已 332/332 覆盖，9/7 spawn ENOENT 假跑遗留)
- 修正 scripts/step12_certify_gate.mjs：statusCoverMin/caCoverMin 移除 → statusEventCoverMin=1500(#6/#12 SUSPENDED+ST 齐备且≥1500，LISTING 由 #4 承载) + caGapTolerance=250(#10 CA≥AF-250 动态对齐)；adjCover 上提共用
- 结果：certify 17/17 全 PASS，RESEARCH_READY=TRUE（gate json capturedAt 09-08T19:33:43Z）；ROADMAP §44.1/44.2/48.2/48.3 R1 覆盖更新 + §47 append
- 遗留：G 域 2 质量待办（securityId NULL + effectiveFrom 单点）→ STEP 12.5 PIT 审计前必修
- 认知：certify 阈值必须区分「事件态表」(status/CA 只存有事件证券)与「逐日型表」(liquidity/AF/OHLCV 全证券都有) —— 事件态表按全证券数判永不达标

## 03:40 FE-6/7/8/9 四页并行接入 + 4 个新 router 合并（研究链路前端收尾）
- 用户「继续开发，可以并行进行」→ 4 个 general-purpose 子代理并行，各建自持新 router 文件（避免 appRouter 并发编辑冲突）：paramSearchRouter(FE-6)、walkForwardRouter(FE-7)、marketRegimeRouter(FE-8)、reviewRouter(FE-9)，前端四页全重写接真实数据 + R7「技术预览」提示条。
- 关键架构：研究引擎是纯函数+注入式同步求值器 `(params)=>outcome`，但生产回测异步、tRPC 不能序列化函数 → 端点服务端「异步预计算所有参数组合回测到 Map → 同步查表求值器」。regime 的 facts 用 index_daily 收盘 + limit_up_records 涨停数（PIT asOf===tradeDate），缺源维度显式 unassessed。
- 主 agent 收尾：appRouter 合并 4 router + 修 RegimeReport 2 处 TS2352。验证：tsc exit 0、vite build 16.27s、vitest 2289/2305（16 失败全环境依赖：tushare token/网络 + 真实 DB 状态 + researchRunRouter 优先级断言）。
- 附带复核退出策略（02:40 已实现）全历史生效：1185 笔/1180 平仓/胜率 38.47%/总收益 57.62%/回撤 22.14%。
- 遗留性能：`getLeaderCandidateBacktest({})` 全历史冷缓存 ~2m22s（引擎循环 1800 交易日 × 逐候选 feature pipeline，visibleBars/MarketBarSeries 逐快照重排序是已知热点），「帮我决策」交互待优化；暖缓存（3min TTL）命中后仅重算引擎计算层。

## 03:41 清理 3 个悬挂后台任务（占 TiDB 连接）
- 用户问「还有几个后台任务在跑」→ 排查发现 dev server 外还有 3 个并行子代理遗留的临时验证脚本进程：`_verify_e2e.ts`(01:50)、`_verify_backtest_range.mts`(02:26)、`_probe_batch.mts`(02:58)，源文件已被子代理删除，但进程各自还握 1~6 条 TiDB Cloud(:4000) ESTABLISHED 连接、CPU 近 0 —— 「主任务已跑完、连接池未 close、事件循环不退出」悬挂态。
- 处置：PowerShell 递归收集三棵进程树（根 bash 39596/2272/37192）共 15 进程 → Stop-Process -Force，全部终止；dev server（`tsx watch server/_core/index.ts`，pid 9976/34528/37008/34376）不受影响正常。
- 经验：临时验证脚本记得末尾 `pool.end()` 或 `process.exit()`，否则结束后挂住占连接；Git Bash 里 `taskkill //F //T //PID` 和 `cmd //c "taskkill..."` 都会参数损坏，Windows 杀进程树用 PowerShell 递归算后代 + Stop-Process 最稳。

## 12:24 .git 损坏恢复（.git/refs 丢失 + pack 数据丢失）

用户报「.git 文件夹被删了」。诊断：`.git` 目录仍在，但 `.git/refs/` 整个丢失（git 报 "not a git repository"）+ `objects/pack/*.pack` 3 个数据文件丢失（只剩 .idx）+ 松散对象仅 4 个。**工作区代码文件零丢失**（最新状态全在磁盘）；`logs/HEAD` reflog 完整记录了本地 20 个未推送 commit 的 hash+消息。

### 恢复方案（用户选「先备份整个项目再恢复」）
1. 全量备份：`tar` 打包到 `C:\work\sourcecode\stock-limit-up-analyzer-backup-20260909.tar.gz`（8.3M，排除 node_modules，1279 文件，含损坏 .git 的 reflog）。
2. `mv .git .git.broken`（保留 reflog 供对照）→ `git init -b main` → `remote add origin` → `fetch` → `git reset --mixed <远程hash>`（保留工作区）。
3. 结果：main 恢复到远程 origin/main `0d60b56`，本地领先的 **26 修改 + 147 新增** 文件全部变为「未提交修改」，`git status` 正常，upstream 已设 `origin/main`。

### 踩坑（重要经验）
- `git fetch origin` 返回成功、reflog 也写了，但 `refs/remotes/origin/main` ref 文件**间歇性写不进**（`update-ref` 返回 exit 0 但文件不落盘，`mkdir refs/remotes/origin` 成功后又消失）。最终手动 `mkdir -p .git/refs/remotes/origin && echo <hash> > .git/refs/remotes/origin/main` 直接写文件成功。疑似外部进程（安全软件/同步工具）间歇干扰 `.git/refs/remotes/origin` 子树，`refs/heads` 不受影响。

### 遗留
- 20 条 commit 消息清单已固化到 `docs/git-recovery-commit-log.md`（供后续重新分批提交对照）。
- `.git.broken/` 已加入 .gitignore（确认不再需要后可手动删除）。
- 待用户决定：26+147 改动是一次性提交还是按原 20 条消息重新分批提交。

## AUDIT-003 量化系统总审计（13:19）
- 产出 `AUDIT-003_FULL_QUANT_SYSTEM_MASTER_AUDIT.md`（唯一交付物，26 节 + Q1/Q2/Q3）。
- 核心结论：`RESEARCH_READY=TRUE` 语义膨胀 = 实为 DATA_FOUNDATION_READY（certify gate 最终判定只查 dataScope 15 项）。
- 独立实查 DB（非引用旧文档）：OHLCV 8,891,500/5,796 股；securities 5,552(退市337)；status 10,373/1,830(事件态)；CA 31,641/4,824；AF 31,337/5,025；liquidity 9,015,158/5,131；industry 5,212(securityId 全 NULL + effectiveFrom 单点 2026-08-31)。
- **关键证据**：运行产物 research_experiments=0、research_runs=0、backtest_runs=1；strategy_versions/paper_trades/trade_journal_entries 三表 ER_NO_SUCH_TABLE（in-memory）。
- 架构债务 DEBT-1~4：Research→Legacy 耦合（前端 paramSearch/walkForward/marketRegime/PerformanceDashboard 全用 getLeaderCandidateBacktest）；legacyTransactionSimulator 桥接；生产引擎 baseline 无 SELL（无退出策略→胜率/回撤失真）；策略/纸面/日志无持久化。
- P0 四硬门槛：gate 分层重构 / Industry PIT 缺口 / 生产引擎退出策略 / 研究链零真实执行。

## QUANT-ROADMAP-001 建立（13:32）
- 交付 `QUANT-ROADMAP-001.md`（唯一任务执行路线图，取代 ROADMAP.md §44-47 跟踪职责，ROADMAP.md 降级为规格/铁律参考）。
- 6 态任务模型落地：PLANNED→IMPLEMENTING→CODE_READY→REAL_DATA_SMOKE→VALIDATED→CERTIFIED（废除 DONE），CODE_READY≠VALIDATED 铁律。
- 6 级 Gate：G0 DATA_FOUNDATION → G1 HISTORICAL_STATE → G2 RESEARCH_DATASET → G3 RESEARCH_ENGINE → G4 RESEARCH_READY → G5 PRODUCTION。
- 24 任务（Phase0=3/1=2/2=3/3=3/4=4/5=4/6=5），依赖 DAG + 并行规则 + 跨阶段禁止 + Evidence 规范（docs/quantRoadmap/evidence/<TaskID>/）。
- 实查复核（非引用）：gate L369-370 researchReady=dataChecks.every(PASS)；industry 5212 行 securityId 全 NULL eff 单点 2026-08-31；5 表缺失；research 44 测试文件/1104 用例 E2E=0；baseline 只产 BUY 无 SELL。

## Master Directive 产品化第一阶段（13:43）
- 交付 6 文件：docs/PRODUCT_GAP_MATRIX.md、docs/MASTER_PRODUCT_ROADMAP.md、docs/MASTER_TASK_TRACKING.md、docs/RESEARCH_GATE_SPECIFICATION.md、docs/FINAL_PRODUCT_ACCEPTANCE_CRITERIA.md、FINAL_PRODUCTIZATION_STATUS_REPORT.md（根）。
- QUANT-ROADMAP-001.md 标记 SUPERSEDED（保留历史）。
- 关键修正：数据回填已 FULL（09-07 报告 C+E 仅 25%，09-09 实查 liquidity 9.01M/CA 31,641/AF 31,337 全量）；研究 tRPC router 已接入 appRouter（R6 已解除）；阻塞从「数据回填」转为「Gate 语义+Industry PIT+引擎退出+真实 E2E」。
- 状态口径：6 态（PLANNED→IMPLEMENTING→CODE_READY→REAL_DATA_SMOKE→VALIDATED→CERTIFIED+REJECTED），映射 Directive §42 与旧 7 态。
- 结论：VALIDATED=0，research_runs=0，31 任务（5 CERTIFIED + 26 PLANNED）。
- 安全：.env 未跟踪；tushare.secret.test.ts 走 process.env（无硬编码）。

## P1-T1 G0 gate 固化（20:40）
- 交付：分层 Gate G0~G5 落地，`researchReady` 从「dataScope 全 PASS」降级为「G4 语义」（恒 false 直到真实研究 E2E）。
- 实跑：dataFoundationReady=true，researchReady=false，gates=G0 PASS / G1~G5 GAP；两次运行判定一致（可复现）。
- 改 7 文件：step12_certify_gate.mjs（分层判定 + G1~G5 真实探测）、dataHealthContracts.ts（gateLevelSchema）、dataHealth.ts（透传 + DOMAIN_SPEC C/D 口径修复）、dataHealth.test.ts（分层语义断言）、researchRunRouter.ts（reason 文案）、DataHealth.tsx（分层徽章）、RESEARCH_GATE_SPECIFICATION.md（GCP-001/002）。
- 附带修复两处**预先存在** coverage bug：DOMAIN_SPEC.C targetKey 应为 minDistinctEventSecurities；DOMAIN_SPEC.D 覆盖率主口径收敛为 AF（CA 事件态表无法静态表达）。
- 验证：dataHealth 13 + researchContracts 12 = 25 全绿；tsc 通过；全量测试 14 失败全为 Tushare token/网络 + DB 集成测试（预先存在，与本次无关）。
- evidence 落盘 docs/quantRoadmap/evidence/G0/P1-T1/。状态 VALIDATED。

## P1-T2/T3/T4 并行推进（20:52）
- **P1-T2 VALIDATED**：新增 `scripts/freezeDataFoundation.mjs`，冻结 Data Foundation v1 → `docs/quantRoadmap/snapshots/data-foundation-v1.json`，SHA-256 fingerprint `781854d0da819916daf80e08acc72313461e0f579091aeec3fb4b4b4fee3edb8`，连续 3 次运行一致（可复现）。冻结内容=G0 15 项判定 + snapshot（剔除 capturedAt，键排序稳定序列化）。
- **P1-T3 VALIDATED**：新增 `scripts/backfillIndustrySecurityId.mjs`，industry_assignments.securityId 回填 5212/5212。映射规则 `SUBSTRING_INDEX(code,'.',1)=identifier.code AND UPPER(SUBSTRING_INDEX(code,'.',-1))=exchange AND identifierType='primary'`；歧义 0、孤儿 0、幂等（重跑 affectedRows=0）。G1 的 industrySecurityIdNull 5212→0。
- **P1-T4 PARTIAL**：`docs/quantRoadmap/reports/industry-historical-coverage.md` 覆盖报告 VALIDATED；历史 PIT 重建 BLOCKED（CONDITIONAL）。实查：effectiveFrom 单点 2026-08-31、source 100% baostock、无多区间 code。数据源边界：BaoStock 无历史 / Tushare index_member_all 需 5000 积分 / akshare 未装 / 证监会无免费 API。诚实标注，不伪造历史归属。
- **关键事实**：identifier_history 仅 primary 类型 5552 行一一对应；research_securities 无 securityCode 字段（代码在 identifier_history，6 位无后缀 + exchange enum）。
- **G1 现状**：Listing/Delisting/Suspension/ST/Identifier 已就绪；仅 Industry 历史 PIT CONDITIONAL（securityId 已关联但 effectiveFrom 单点）。G0 不受影响（截面覆盖仍 PASS）。
- 更新：MASTER_TASK_TRACKING.md（P1-T2/T3 VALIDATED，P1-T4 PARTIAL + 更新记录）、MASTER_PRODUCT_ROADMAP.md（PHASE 1 状态表 + Status PARTIAL + 关键路径注释）。

## 组合回测页「又变慢」A+B 止血包（21:40）
用户反馈组合回测页加载又变慢。诊断根因：`getLeaderCandidateResearch` 单一研究端点串行承载全部重计算——单次请求约 143 次 legacy simulate（滚动窗口选权 10窗×6权重=60 + 验证 10窗×5策略=50 + 全周期/验证各5策略=10 + 因子消融 11因子×2=22）+ 过拟合防护 2000 次 bootstrap；且前端默认 refetchOnWindowFocus=true、参数页每敲击立即 refetch（无 debounce）、服务端缓存 TTL 5min/3min 过短。近期 feature（风险因子 8→11、策略 3→5、因子消融、六层评价）全堆进同一端点，越加越慢。

### 落地（A+B 止血包）
- A1 `client/src/pages/Backtest.tsx`：getLeaderCandidateResearch useQuery 加 `refetchOnWindowFocus:false`、staleTime 5min→30min。
- A2 同文件：参数输入 debounce——新增 `debouncedConfig` state + useEffect 800ms 防抖，input useMemo 改用 debouncedConfig、依赖 `[debouncedConfig, dateRange]`，页面即时显示仍用 config。
- B `server/db.ts`：backtestResultCache TTL 5min→30min、maxEntries 64→256；BACKTEST_BASE_CONTEXT_TTL_MS 3min→15min。
- 验证：tsc --noEmit exit 0、backtestPage.test.ts 2/2 过、vite build 通过（17s）。

### 遗留（用户未选）
- C 按需拆分端点：把 walkForward/因子消融/过拟合防护拆成独立 procedure 按 tab 懒加载（overview 只算 ~11 次 simulate 秒开），架构级改动最大。
- 保存回测边界：debounce 后 800ms 内点「保存回测」会存旧 input（可后续抽 buildInput(config,dateRange) 解决）。

## PHASE 2 完成（P2-T1/T2/T3 → VALIDATED，21:46）
- **P2-T1**：`research_datasets` 表持久化。drizzle/schema.ts 加 researchDatasets(18列)；scripts/migrate_add_research_datasets.ts 幂等 DDL；server/researchDataset/persist.ts（persistResearchDataset，datasetId=DS-<datasetVersion> 唯一幂等）；CLI runStep126BuildDataset.mts 加 --persist。
- **关键阻塞发现+修复**：首次真实构建 universe=0/rows=0。根因=research_security_status_history 只回填 SUSPENSION(9532)+ST(841)，**缺 TRADING 维度**；而 resolveHistoricalUniverse 把 TRADING 当「正向确认」（UNKNOWN→拒绝）→ 全市场默认拒绝。修复=scripts/backfillTradingStatus.mjs 从 securities.listedDate/delistedDate 派生 5552 条 TRADING（幂等，DATE_FORMAT 时区坑：mysql2 读 date 为本地 Date，用 getFullYear 等本地方法格式化，勿用 toISOString/slice）。效果 universe 0→20391，excludedReasonCodes 去掉 TRADING_UNKNOWN。
- **P2-T2**：scripts/verifyDatasetConsistency.mts 串行两次 buildResearchDataset，datasetVersion 一致 rd-1.0.0-1-879c5142a56cca67，rowsFingerprint/policySetFingerprint/rowCount/memberDayCount 全一致 diff 为空。
- **P2-T3**：9 类 policy 冻结（policySet 齐备顺序正确 + fingerprint 8ee5e42b4d96094e 两次构建稳定），冻结文档 docs/quantRoadmap/reports/research-dataset-policies.md。
- **真实数据**：research_datasets 落库 2 条（1 条 20391 行真实 + 1 条修复前 0 行）；9 类 policy=pit,survivorship,corporate-action,adjustment,industry,liquidity,universe-membership,calendar-trading-days,knowledge。
- **遗留**：G Industry=0（industry effectiveFrom 单点 2026-08-31 不覆盖 2024，属 P1-T4 已知 CONDITIONAL）；构建慢 ~3-5 分钟（全量加载 securities/identifiers/status/industry/CA + 逐日价格查询）。
- 验证：tsc 通过；policy(16)+version(6)=22 测试全绿；两次构建 diff 为空。
- evidence：docs/quantRoadmap/evidence/G2/{P2-T1,P2-T2,P2-T3}/。

## STEP — Research Dataset V2（第一步：审计，22:10）
- 用户下达「Research Dataset Builder Productization & Dataset V2」任务，要求第一步只做审计+gap matrix+计划，确认后再改码。
- 审计结论（已实查 TiDB）：后端 C-12.6 ResearchDataset 已 VALIDATED（research_datasets 表 2 行/20391 行真实 dataset、9 policy 冻结、两次构建 diff 为空、datasetVersion 确定性）。五条结构性缺口：①FE-3 仅最小冒烟构建器（无 8+ 维度）；②Validation 三态非 PASS/WARN/FAIL；③无语义版本 V1/V2/V3（仅 content-addressed）；④**双 Dataset 概念并行**——legacy ResearchRun/engineAdapter 绕过 C-12.6（这是最大风险）；⑤Industry 历史 PIT CONDITIONAL（effectiveFrom 单点 2026-08-31）。
- 产出 3 份文档：docs/researchDataset/{DATASET_CURRENT_STATE_AUDIT.md, DATASET_CAPABILITY_MATRIX.md, DATASET_V2_IMPLEMENTATION_PLAN.md}。计划 DS-01~DS-24 分 11 阶段，待用户确认 3 个决策点（D1 双概念收敛/D2 语义版本/D6 validation 分级）。
- 关键事实：research_runs=0；legacy engineAdapter→runStrategyEngineBacktest 用 ResearchDatasetSpec（datasetVersion optional 且注释过期）；signalEngine/simulator 走 C-12.6 但未真实 E2E。

## P3-T1 生产引擎退出策略接入（baseline BUY→BUY+SELL，22:13）
- 目标：Gap Matrix P0-3（生产引擎无退出策略只 BUY）→ baseline 可产 SELL。此前 02:40 只用引擎层 `maxHoldingDays` 补「时间退出」，策略层仍无「信号退出」。
- 实现：`server/strategy/strategies/leaderCandidateBaseline.ts` 增 `exitMode: "hold-while-selected" | "none"`（默认 hold-while-selected）+ `buildHoldWhileSelectedExitSignals`；evaluate 解构 `context.portfolio.openPositionSymbols`，持仓不在「minScore/featureMode 过滤后候选池（不受 maxSignals 截断）」→ SELL；signals = [...sell, ...buy]（sell 在前）；**无候选日 emptyDecision 不强制清仓**（对齐 planDecisionDay）。
- 接线：`leaderCandidateStrategyBacktest.ts` 增 `LEADER_CANDIDATE_PRODUCTION_EXIT_MODE` + 生产 config 显式 exitMode；`research/adapter.ts` 参数 schema 暴露 exitMode。
- 边界注释更正：legacyTransactionSimulator/realisticBacktest/engineNonEquivalence 由「不产生 SELL」改为「退出语义不同仍非等价」。
- 验证：tsc 干净；server/strategy(73)+engine(45)=99 全绿（+7 单测 +3 端到端）；research 44 文件 1104 全绿（experimentPersistence 参数集断言同步 exitMode 默认值）；端到端（synthetic）跑出 closed trade（openAtEnd=false，completedTradeCount≥1）。
- 状态：CODE_READY（synthetic fixture 验证，真实数据 VALIDATED 留待 P5-T1 Baseline）。
- 已知限制：`portfolio.sell` 清仓 `trade.reason` 硬编码 null（退出原因未传递到 Trade，留待 P3-T4）。
- evidence：docs/quantRoadmap/evidence/G3/P3-T1/。更新 MASTER_TASK_TRACKING / MASTER_PRODUCT_ROADMAP / PRODUCT_GAP_MATRIX / ROADMAP §47。
- 下一步：P3-T2（拆 Research→Legacy 耦合）∥ P3-T4（回测边界验证）可并行（都依赖 P3-T1）。

## P3-T2/T3 去 legacy 耦合闭环 + P3-T4 边界验证（G3 完结，22:26）
- **P3-T2 VALIDATED**：静态调用图确认 4 研究 router（paramSearch/walkForward/marketRegime/sentiment）已走生产引擎——`getLeaderCandidateBacktest`(db.ts:2005)→`runLeaderCandidateStrategyBacktest`（includeResearch=false + realisticSimulationOverride 注入），legacy 模拟器调用=0。实现由 P2-2 完成，本任务验收确认+证据固化。澄清验收措辞「4 router 不再调用 getLeaderCandidateBacktest」实为「不再走 legacy」。
- **P3-T3 VALIDATED**：legacy 模拟器降级为 research-only 唯一出口 `RESEARCH_LEGACY_SIMULATION_SOURCE`（非移除），仅 3 研究模块引用（downsideRisk 5 处 / overfittingGuard 2 处 / leaderCandidates:1038 研究分支兜底），生产路径 0 引用；engineNonEquivalence.test.ts 锁定「legacy≠生产引擎」防伪等价。
- **P3-T4 CODE_READY**：新增 `server/engine/edgeCases.test.ts`（18 用例）逐项验证 12 项边界——8 覆盖（T+1/涨跌停可成交性 blockLimitUpBuy/DownSell/停牌数据缺失/板块涨跌停幅度 resolveLimitRules/滑点/费用印花税单向/资金整手/maxHoldingDays 时间退出）+ 4 KNOWN_GAP（K1 止损止盈待 P4-T1 定义策略规则、K2 除权复权 adjustment_factors 已 FULL 未接入、K3 一字板封死概率仅 legacy 有、K4 板块 limitRules 已实现但未注入生产引擎）。
- 验证：tsc exit 0；server/engine(63)+server/strategy(54)=117 测试全绿（新增 18 边界用例）。
- G3 整体：P3-T1 CODE_READY + P3-T2/T3 VALIDATED + P3-T4 CODE_READY；去 legacy 已闭环，真实数据端到端 + 4 项 KNOWN_GAP 留待后续 Phase。
- 关键认知：生产引擎退出仅 hold-while-selected + maxHoldingDays，**无止损/止盈**；legacy 的 riskManagedHold（stopLoss/trailing/strongHold）生产引擎不消费——这是 P4-T1 定义 FIRST_FORMAL_STRATEGY 时必须让用户填写的真实规则，不擅自发明。
- evidence：docs/quantRoadmap/evidence/G3/{P3-T2,P3-T3,P3-T4}/。更新 MASTER_TASK_TRACKING / MASTER_PRODUCT_ROADMAP / PRODUCT_GAP_MATRIX / ROADMAP §47。
- 下一步：PHASE 3（G3）编码任务全清，进入 PHASE 4（P4-T1 定义 FIRST_FORMAL_STRATEGY，**等待用户填写真实交易规则**）。

## 22:55 策略编辑器页面产品化（中文化 + 数据集下拉 + 规则细化）
用户要求细化策略编辑器：英文转中文、数据集可从「数据集构建」页选择、入场/退出/仓位/风险规则更细。
- `client/src/adapters/strategyAdapter.ts`：新增中文化 helper（ruleKindLabel/ruleOperatorLabel/ruleFieldLabel/RULE_FIELD_LABELS/EXECUTION_MODEL_LABELS/positionSizingDescription）+ RULE_PRESETS 预设库（入场 10 / 退出 7 / 风险 5，含中文 description）+ presetToRule。**注意**：DeclaredRule.operand 只允许 number|string|null（后端校验），布尔状态用字符串 "true"/"false"。
- `RuleEditor.tsx`：新增 `category` prop，header 加「＋ 添加预设条件」原生 select（按类别过滤）+「自定义条件」按钮；操作符/类型中文化；字段输入加 datalist 联想 + 已知字段中文名提示。
- `StrategyBasicInfo.tsx`：数据集版本改 `researchDataset.list` 下拉选择，选中后同步 datasetVersion + universeId(`research-dataset:<version>`) + universeDescription；标签全中文。
- `PositionSizingEditor.tsx`：改「仓位与资金规则」，新增初始资金、模式中文说明，最大持仓同步 backtestConfig.maxPositions。
- `StrategyEditor.tsx`：TEMPLATE_DOCUMENT 入场 5 / 退出 4 / 风险 4 规则（中文描述），版本化/生命周期表头中文。
- `RunConfigPanel.tsx`：执行模型中文标签；`StrategyJsonEditor.tsx`：code/path/message 表头中文。
- 验证：tsc --noEmit exit 0；strategyAdapter 9 单测全过。

## STEP STRATEGY-001 Strategy Definition 能力审计（22:50，audit-only 无代码修改）
- 用户下达 STEP STRATEGY-001：只审计「定义/保存/版本化/读取正式策略」的基础能力，禁止开发首板回踩策略、禁止进入 Backtest/参数搜索/WFO/OOS/Paper Trading。
- 产出 `docs/STEP_STRATEGY_001_DEFINITION_AUDIT_REPORT.md`（12 节）。**判定 PARTIAL**。
- 核心结论（真实代码/DB Schema 证据）：
  - ✅ 声明式定义层 CODE_READY：`server/research/strategySchema/`（C-15.1）StrategyDocument（§16 全字段）+ StrategyVersionRecord（§17 九项追溯）+ bump 闸门/compare/serialize canonical+sha256，28 单测。
  - ✅ 前端 FE-4 策略编辑器真实连通 validate/bump/compare 无状态端点（researchRouter.ts），但「保存/保存新版本/运行」按钮因后端端点未暴露而禁用。
  - ❌ 核心 GAP：无 Strategy Repository / Service / CRUD Router / `strategies`+`strategy_versions` 表——策略仅内存存在（persistence/db.ts 只有 Experiment/Run/SweepBatch 三套 Repository）。
  - ⚠️ 硬编码策略：legacy「龙头候选」5 策略（leaderCandidateBaseline + leaderCandidates + leaderCandidateStrategyBacktest）+ `client/src/lib/firstBoard.ts` 首板过滤工具，未迁移到 StrategySchema。
  - 🟡 Dataset/Run 绑定 PARTIAL：research_runs 有 datasetId/datasetVersion/datasetFingerprint；但 research_experiments.strategyId/strategyVersion 是自由文本冗余列（无 strategies 表外键）。
- 全部 GAP DEFERRED 至 STRATEGY-002（策略持久化 + CRUD 闭环）。ROADMAP §47 append。

## DS-V2-FINAL Research Dataset 产品化与集成收口（23:10）
- 11 个 STEP 全完成（在既有 C-12.6 基础设施上，非零起点重设计）。
- 新增：`server/researchDataset/certify.ts`（认证决策树 CERTIFIED/CONDITIONAL/REJECTED）、`capability.ts`（10 维度能力矩阵，metadata-first 大表只取 MIN/MAX 不 COUNT）、`preview.ts`（先预览再构建）；`researchDatasetRouter` 增 capabilities/preview/certify/list；FE 三面板 + DatasetBuilder 组合。
- Research Run 强绑定：`research_runs` 增 datasetId/datasetVersion/datasetFingerprint（migration 0025 + 幂等脚本 migrate_add_research_run_dataset.ts 已执行）。
- 修三处真 bug：① builder 行排序（universe 成员序→`(tradeDate,securityId)` 升序）；② capability 全表 COUNT→MIN/MAX；③ E2E 落库 `db.execute({sql,values})`→`db.insert(researchRuns).values()`。
- 真实 E2E：`runResearchDatasetE2E.mts` 全链跑通，`--data-ready` 产出 **CERTIFIED researchSafe=true**，`datasetVersion=rd-1.0.0-1-fd1c487f2fe19e27`，Run `RUN-EXP-E2E-B9F42F-D4D1` 落库。
- gate 刷新：`research_ready_gate.json` 重生成 → `RESEARCH_READY=TRUE`（G2/G4 翻 PASS，G1/G3/G5 诚实 GAP）。
- 产出 4 份文档：`docs/researchDataset/{DATASET_SPECIFICATION,DATASET_CERTIFICATION_SPEC,DATASET_VERSIONING,RESEARCH_DATASET_V2_IMPLEMENTATION_REPORT}.md`。
- 保留设计决策：三态 PASS/FAIL/INCONCLUSIVE 不改；content-addressed 身份不加 V1/V2/V3；Industry 保持 CONDITIONAL。
- 回测路径三分类：FORMAL（强绑定）/ TECHNICAL PREVIEW R7（paramSearch/walkForward）/ LEGACY（engineAdapter 绕 C-12.6）。
- 验证：tsc 干净、researchDataset 57 + research 1104 全过。
- 更新：ROADMAP §44（覆盖）+ §47（append）+ MASTER_TASK_TRACKING §3 append。

## STEP STRATEGY-002 Strategy 持久化与 CRUD 闭环（23:15，完成）
- 用户下达 STEP STRATEGY-002：把 StrategyDefinition 从「内存声明式对象」升级为「可持久化/可读取/可版本化/可追溯的正式领域实体」。禁止开发首板识别/回踩/Entry/Exit/Backtest/参数搜索等。
- 交付（复用 STEP-001 纯函数 serializeStrategyDocument/deserializeStrategyDocument/cloneStrategyDocument/bumpStrategyVersion + StrategyVersionRecord，不重造 semver）：
  - **DB**：`strategies` + `strategy_versions` 两表（migration `drizzle/0026_strategy_persistence.sql` + 幂等脚本 `scripts/applyStrategyPersistence.mjs`，statementsExecuted=6、UNIQUE(strategyId,version) 兜底不可变）；strategy_versions 存 strategyDocumentJson+versionRecordJson+fingerprint+datasetVersion/universeId/codeVersion。
  - **Repository/Service**：`server/research/strategyPersistence/`（contract/inMemory/db/service/index），saveVersion 幂等三态 inserted/idempotent-skip/conflict（并发 ER_DUP_ENTRY 1062 兜底）；读取 deserialize 重算指纹篡改抛错；createVersion 走语义闸门（结构→major、参数/文本→minor）。
  - **Router**：`researchRouter.ts` 八 CRUD 端点 `research.strategy.{create,save,load,list,delete,createVersion,loadVersion,listVersions}`；`shared/researchContracts.ts` zod 契约。
  - **前端**：StrategyEditor/StrategyHeader 解除「保存/保存新版本」禁用态接真实 save/createVersion + 最小策略列表 Load。
- **关键修复（持久化硬前置）**：`server/db.ts#getDb` 原 `drizzle(process.env.DATABASE_URL)` 把 URL 里 `ssl={"rejectUnauthorized":true}` 误当 mysql2 SSL profile 名（Unknown SSL profile）+ URLSearchParams 丢 JSON 双引号 parse 失败 → 改「原始字符串按 `ssl={...}` 正则提取 + drizzle config 形式」，向后兼容。
- 验证：单测 strategyPersistence 12 + researchContracts 13 + strategySchema 28 全过；真实 TiDB E2E `scripts/stepStrategy002E2E.mts` PASS（重启往返 matchesOriginal=true + 版本往返 1.0.0→1.1.0 + 不可变 + 幂等）。
- 设计决策：datasetId/datasetFingerprint 不加 StrategyDocument（datasetVersion 内容寻址可派生，改动连锁破坏现有 document/指纹/单测），strategy_versions.datasetVersion 已满足追溯。
- DEFERRED：lifecycle 状态持久化（strategies.status 已建列但状态机不在此范围）+ Research Run 绑定。
- 产出 `docs/STEP_STRATEGY_002_PERSISTENCE_IMPLEMENTATION_REPORT.md`（16 节）。ROADMAP §47 append + TASK_TRACKING §3.11 新轨道 + §5 append。STEP-003（首板 Event 定义）将单独启动，未越级。

## Dataset 构建器 universe 过滤层（板块/ST/T日条件）（23:50）
- 需求：高级设置里可选「市场板块(主板/创业板/科创板/北交所) + 排除ST + T日条件」，T日条件按用户选「首板」。
- 后端：`types.ts` 增 BoardCategory/TDayCondition/UniverseFilter/NormalizedUniverseFilter，请求增 universeFilter；`validate.ts` 规范化+校验。
- `universe.ts`：resolveUniverseDefinition 叠加板块(classifyBoard)+ST(PIT st维度)过滤，excludedByReason 记 BOARD_EXCLUDED:<board>/ST_EXCLUDED。
- 新 `tDayFilter.ts`：limitUpRatioForRow(主板10%/ST5%/创业板科创板20%/北交所30%)+isRowLimitUp+matchesTDayCondition；涨停用 boardRules.limitUpPrice 口径一致。
- `builder.ts`：T日条件在装配后逐日过滤（首板=T日涨停且T-1未涨停，逐日滚动 prevLimitUp PIT安全）；「无T日条件」走快路径(装配前截断)，「有T日条件」全 universe 扫描再对结果限流。finalUniverseDefinition 重建 days。
- 契约：`researchContracts.ts` 增 universeFilterSchema + DatasetPreviewSummary.universeFilter；router build/preview/certify 透传。
- FE：`datasetAdapter` 增 boards/excludeSt/tDayCondition（默认首板，全选4板块=不过滤）；`DatasetConfigPanel` 高级设置增板块多选/排除ST/T日条件下拉；`DatasetPreviewPanel` 显示过滤注记。
- 验证：tsc 干净；researchDataset 65(新增 tDayFilter 6 + board/ST 2) + research 1116 + researchContracts 13 全绿。
- 已知：首板=全市场扫描(与既有 E2E 全 universe 构建同量级，~分钟级)，板块/ST 过滤走快路径很快；预览只计入板块/ST，T日条件仅完整构建生效。
- 未改：三态 PASS/FAIL/INCONCLUSIVE；content-addressed 身份；policy value shape 未加 filters（过滤记在 universeDefinition.rule/excludedByReason）。

## 首板回踩 = universeFilter 通用条件（STEP DATASET-003 收尾，00:20）
- 用户两次纠正架构：不要独立页面、不要独立 tab，要把「首板回踩」做成**通用规则**接入现有 DatasetBuilder 的 universeFilter 条件引擎。
- 删除了此前独立实现：`server/firstBoardPullback/` 全模块（detect/screen/db/build/exclusions）、router 的 `firstBoardPullback` procedure、`FirstBoardPullbackPanel` 组件、DatasetBuilder 的 pullback tab。
- 通用化落地：
  - 类型：`server/researchDataset/types.ts` 增 `PullbackTargetType`(limitPrice/t0Open/t0Low/ma5)+`PullbackScreenCondition`，`UniverseFilter`/`NormalizedUniverseFilter` 增 `pullback`；`shared/researchContracts.ts` 增 `pullbackScreenConditionSchema` + `universeFilterSchema.pullback`。
  - 纯函数：新 `server/researchDataset/pullback.ts`（screenSingleTarget/screenPullback「触及且不破」+computeMa5FromFacts/buildWindowBars/buildFirstBoardEvent/screenFirstBoardRow），迁移 screen 测试 + 新增 10 用例。
  - 编排：`builder.ts` 在 pullback 存在时有界预加载扩展窗口 facts（前 4 日 MA5 + 后 N 日观察窗口），装配后做回踩后处理，`PULLBACK_NOT_MATCHED`/`PULLBACK_INCOMPLETE` 记入 excludedByReason，rule 字符串追加回踩口径；validate 强制 pullback 需 tDayCondition=firstBoard。
  - FE：`datasetAdapter` 增 pullback 视图模型 + wire 映射 + PULLBACK_TARGET_LABELS；`DatasetConfigPanel` Universe 过滤区增「首板回踩筛选」勾选 + 多选目标位 + 容差 + 窗口。
- 验证：tsc 干净；researchDataset 75 单测全绿（pullback 10 新增）。
- 真实 DB E2E（`scripts/verifyFirstBoardPullback.mts`，走 buildResearchDataset）：94 首板事件识别 + 回踩筛选跑通；`gate=INCONCLUSIVE`（dataReady=false 冒烟口径）。
- 修复边界 bug：`builder.ts` forwardEnd 日历不足 N 日时退到「日历最后一天」而非 lastDate，避免贴边事件被误判 incomplete。
- 性能事实（真实 TiDB）：`liquidity_daily` 约 900 万行逐日查询 ~7-9s、静态加载 ~100s，`buildResearchDataset` 固有慢；非本次引入。窗口 08-10~08-21 完整 E2E 后台运行中。

## 23:53 Dataset 构建「RPC_ERROR」诊断（用户报构建失败）
- 用户报 DatasetBuilder 构建失败 RPC_ERROR「后端构建过程抛出异常，未能返回数据集摘要」。
- 三项排查结论：①后端服务正常（`tsx watch` dev server，pid 24696，监听 8080，HTTP 200，事件循环未阻塞）；②DB 连接正常（TiDB Cloud us-east-1 可达，27 表齐全，research_securities 5552 / status 15925 / CA 31641 / index_daily 7452 / stock_daily_prices 8.9M / liquidity_daily 9.0M，`SELECT 1`~0.2s）。
- 根因（与 00:20 记录一致，**已知固有慢，非回归**）：`server/researchDataset/db.ts#loadResearchDatasetStatic` 用 `Promise.all` 一次性全表 `SELECT *` 拉 7 张表（合计 ~8.7MB），跨境链路吞吐实测仅 ~50-70KB/s → 静态加载 ~100s；叠加逐日 `liquidity_daily`（900 万行）每日期 ~7-9s，5 日默认构建合计 ~2-3 分钟，同步 tRPC mutation 超时 → 前端归为 RPC_ERROR。
- 表大小实测：CA 4.75MB / status 1.43MB / securities 0.59MB / identifiers 0.62MB / industry 0.62MB / index_daily 0.65MB。
- 修复方向（未实施，待用户定）：①异步构建+轮询（>30s 操作唯一正确姿势）；②静态加载按窗口 SQL 下推（status/industry/CA 加 effective 区间、securities 加 listedDate/delistedDate，5 日窗口 8.7MB→几十 KB，但需验证 resolveHistoricalUniverse PIT 语义不变）；③静态数据本地/内存缓存（一次性拉取后 TTL 复用）。

- **收尾确认（00:35）**：全量 `tsc --noEmit` exit=0；`vite build` 生产构建 exit=0（2891 modules，产物 index.js 1.89MB/gzip 473KB，chunk>500KB 为既有无害告警）。首板回踩集成（universeFilter.pullback 通用条件）交付完成，真实数据 E2E 已产出 52 个候选。
